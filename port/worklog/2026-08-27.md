# 2026-08-27 — ARM port worklog

## node_log production-log format: match x86 exactly (timestamp FIRST)

USER: "log file is fucked ... With timestamp before anything else" + want real
durable data in the repo, nothing in /tmp.

Root cause: port/arm64/node_log.S `node_log_str` PREPENDED the level KIND tag
(`SERVE `/`BLOCK `/`INFO ` ...) to every line before the caller's buffer. The
callers (ibd.c / ibd_lsm.c) already build their buffer as
`"YYYY-MM-DD HH:MM:SS.mmm <msg>"` (mirroring asm/daemon/log_ts.h), so the
resulting line was kind-first:
    SERVE 2026-08-27 07:33:51.313 node start (ibd_lsm ...)
x86 production format (log_ts.h) is timestamp-first:
    2026-08-27 06:41:04.130 [txrelay:5] 44.251.42.22: +1 tx accepted ...
Fix: node_log_str now emits the caller buffer as-is (timestamp first, no KIND
prefix). node_log_event is left as the raw "<KIND> a b c" low-level form.
Verified with a 6-line unit check:
    2026-08-27 08:04:53.091 node start (ibd_lsm resumable LSM download worker)
    2026-08-27 08:04:53.091 h7360 txs=1 utxo=7024 spent=0 sigs=0 runs=13
Rebuilt ibd + ibd_lsm with node_log.o.

## Durability / re-home (nothing in /tmp)

The parallel real downloads used /tmp/chain0(50k), /tmp/chain100k(90k),
/tmp/chain140k(140k) as scratch to avoid clobbering data/ during the LSM-worker
resume test and to parallelize peers. The repo data/ must be the durable home.
Plan (in progress): let the in-flight /tmp/chain140k run land at h140000, then
move its verified archive (blk00000.dat 134MB + index.dat) into data/
(replacing the stale 5k archive) and continue all future ibd_lsm runs with
datadir=data so the production log grows at data/logs/bitcoind.production.log.

## ibd_par: multi-client (<=16) parallel downloader — LOG PARITY with x86 node
USER: "not seeing startup banner / 16 clients / a lot missing... I want log parity."
Root cause: ibd_lsm was a SINGLE-peer downloader logging only sparse BLOCK/STORE
lines. The x86 daemon (asm/daemon/main.c) prints a startup banner (L3774) and
runs its boot catch-up across up to `catchup_workers` (default 16) parallel
clients, logging [catchup] <host> / [dl] heartbeat.

New port/arm64/ibd_par.c: resumable LSM-UTXO-backed downloader that keeps the
ENTIRE verified consensus/apply/UTXO path of ibd_lsm byte-for-byte and replaces
only the single-socket fetch with a poll(2) mux over up to 16 CONCURRENT peer
sockets (in-flight windows = #clients, applied in strict height order ->
MISSING_PREVOUT=0 preserved). Adds:
  - x86 startup banner (===== bmc-bitcoind LOG START ... =====) as first output.
  - per-client "[catchup] client N connected: <host> fd=M (LSM-RESUME)".
  - "[dl] progress: X/Y blocks verified, N clients, live utxo=Z" heartbeat every
    1000 blocks.
  - bounded nonblocking TCP dial (6s) so dead-partner fallback is fast.
Bugs fixed during bring-up: (a) g_fd[] was initialized to -1 AFTER the connect
loop, wiping every connected fd -> req_window(-1) no-ops, poll spins, 0 progress;
(b) bounded dial left fds NONBLOCKING and the p2p handshake isn't EAGAIN-safe ->
handshake always failed, client loop spun; fixed by clearing O_NONBLOCK after
dial; (c) [dl] heartbeat printed next_apply-start (negative) instead of next_apply.
Verified: 4 clients, 512 blocks h154500..155011 MISSING_PREVOUT=0 (live 1209336,
MATCH); 16 clients 1200 blocks h155012..156211 MISSING_PREVOUT=0 (live 1222443,
MATCH), all 16 clients + banner + [dl] heartbeat in data/logs/bitcoind.production.log.

## Consensus flags fix: SV_SIGPUSHONLY wrongly applied during block validation
The 10,000-block 16-client run (h156212..166211) reported bad_sig=7, all
err=26 = ERR_SIG_PUSHONLY, isolated to h163684 (tx13-15 in1) and h164675
(tx17 in1, tx19 in0/in1). Investigation:
- The downloaders used a FLAT validation flag SV_SIGPUSHONLY (SCRIpt_VERIFY_
  SIGPUSHONLY, the P2P STANDARDNESS flag) -- NOT a consensus flag. Block
  validation must use Core's GetBlockScriptFlags = per-block
  script_flags_for_block(height, hash).
- t_scan_sigs.c read the two archived blocks from index/blk and found 63 real
  txs each and ZERO non-push-only scriptSigs -> the rejects are NOT genuine
  script failures; they are artifacts of forcing the standardness flag (and/or
  the repo's documented push_only() 0x4b off-by-one, asm/Makefile L1215-1218).
  Correct consensus flags exclude SIGPUSHONLY.
- FIX: ibd_par.c / ibd_lsm.c / ibd.c now call script_flags_for_block(h, hash)
  per block and pass those bflags to sv_verify_script. Also added SIGPIPE ignore
  (peer closing mid-write killed the mux; x86 daemon does the same) + bounded
  nonblocking TCP dial reused across the client pool.
- Verified: forward 16-client run h166212..167711 -> bad_sig=0 MISSING_PREVOUT=0
  reload MATCH (live 1350379). Linking: bitcoin_script_flags.o added to IBDOBJS
  (drives ibd/ibd_lsm/ibd_par).

## ibd_par 16-client crash h177612: stack-buffer overflow in version payload
The 10,000-block 16-client run crashed after ~9.9k blocks with
"*** buffer overflow detected ***: terminated". Diagnosis:
- NOT scriptSig size: t_sigsize.c scanned h177000..177700 -> no scriptSig > 520
  (max ~252); sigb[520] is NOT the trigger in this era.
- ROOT CAUSE: connect_peer builds the P2P version message into `unsigned char
  v[102]` but a minimum version msg is ~103 bytes (with the fixed U/A it writes
  byte index 102 = ONE PAST the array). A 16-client run does many
  connects/reconnects over ~10k blocks, so this stack-frame corruption
  accumulates and is eventually detected as heap/stack "buffer overflow".
- FIX: v[102]->v[256] in ibd_par.c, ibd_lsm.c, ibd.c, live_blocks.c,
  live_download.c; also enlarged the fixed sigb[520] scriptSig copy to
  sigb[10000] (defensive; consensus scriptSigs can exceed 520 in other eras) in
  ibd_par/ibd_lsm/ibd.
- Verified rebuild; fresh 16-client segment launched from h177660.

## Per-client peer visibility during IBD (USER: "not seeing peer data for 16 clients")
ibd_par only logged "[catchup] client N connected: <host>" at STARTUP and
"[catchup] client N reconnected" on drops -- during a steady run (no drops) NO
per-client/peer lines appear, only the aggregate "[dl] progress ... 16 clients".
Added: per-client host tracking (g_peer_name[]) + per-client blocks-served
counter (g_client_served[], credited per applied window) + a "[dl] peers: c0=
<host>(N) c1=..." breakdown line emitted with every [dl] heartbeat, so the
16 clients + which peer served how many blocks are visible throughout the IBD.

## [dlc] worker-health table + dead-weight early-kill (x86 dl_catchup log parity)
USER: "The x86 build shows the list of workers, which is dragging, who gets
replaced. I want fucking log parity."
Ported x86 dl_catchup's per-tick worker monitor into ibd_par:
- Every DL_TICK=10s logs a [dlc] block: "== elapsed .. | overall: .. ==", then
  "-- peer status (N/16 worker(s) active) --", then one line per client:
    [dlc]   w%d %-21s chunks=%-4ld blocks=%-8ld (+%ld blk/s, %ldB/s)... 
  with a "(Dragging: N of M)" trend marker while a worker makes no progress,
  and an "[early-kill, last ..]" marker when it is rotated.
- Dead-weight semantics: only the unique HEAD-OF-LINE worker (the client that
  owns the lowest incomplete window blocking global apply) is dead-weighted.
  3 consecutive 10s ticks without block progress (x86 default
  dead_weight_ticks=3) => "DRAGGING: no block progress 30s -> replacing worker"
  => rotate_client() redials a NEW peer and re-requests the held window, logged
  as "[dlc] w%d replaced by <host> fd=N"; g_nreplaced tallies the run.
- rotate_client() factored out and also used by the EOF-rejoin path.
Commits in ibd_par.c; binary built 253704B. (Not yet run -- current segment on
the old binary; verify [dlc] on next segment launch.)

## FAST cons-only download mode (match x86 dl_catchup performance)
FOUND (x86 asm/daemon/main.c + bitcoind.asm): the x86 build's fast catch-up
(dl_catchup) spawns up to 16 forked dlc_worker processes that each call
node_ibd_blocks_s = cons_verify (CHEAP structural: PoW/merkle/coinbase/txids)
+ store_append, and do NOT do per-sig ECDSA. So x86 reaches tip in a
network-bound parallel pass; the slow ECDSA sig verify + UTXO build is a
SEPARATE sequential pass (utxo_live). Our ibd_par did full inline single-
threaded ECDSA during download (~2.4k sigs/s) => that's why the grind crawls
(sig-dense era), ~2.6 blocks/s.
FIX: added g_fast mode (argv[8]) to ibd_par: skips the ECDSA + LSM UTXO apply
(no utxo_lsm up/get/del/flush/reload), keeping header load, store archive
cons_verify + store_append, the 16-client mux, [dl] and [dlc] logging. Resume =
archive tip+1, no LSM state needed. Converts the download to a network-bound
cons-only pass (matches x86 dl_catchup); UTXO+sig verification done afterwards
via build_utxo over the full archive. Usage: ibd_par <peers> <count> data 22 100 16 <start> 1
Commits: ibd_par.c g_fast (not yet run; verify on next launch).

## Peer model overhaul (x86 dl_bootstrap / dl_catchup parity) + FAST mode stability
USER: "stop using seeds as downloaders; seeds just seed peers; look at the x86
pipeline", "check what x86 does", "why aren't you parallelizing".
Implemented in ibd_par (all confirmed against asm/daemon/main.c dl_bootstrap +
dl_catchup + bitcoind.asm):
- connect_peer advances g_npeer ON SUCCESS (was only on failure) so fallback
  clients no longer all dial the SAME peer (bug: 15x 203.56.149.66).
- Parse P2P `addr` messages (parse_addr) + getaddr exchange -> build a
  DISCOVERED peer book (g_book, up to 1024, deduped). Seeds are now DISCOVERY
  ONLY: connect seed -> getaddr -> DROP (never a download peer).
- Client pool dials DISTINCT discovered book peers (known-good static IP pool
  prepended), static list only as last resort.
- FAST (cons-only) mode (argv[8]=1): skips ECDSA+LSM push apply => download is
  network-bound like x86 dl_catchup (cons_verify + store_append only); full
  sig/UTXO verification deferred to build_utxo.
- Unstuck the connect phase: tcp_dial_bounded 6s->2s, connect_peer retries
  147->40 (was: dead peers burned minutes per client => the pool never finished
  connecting). rotate_client now re-dials from the DISCOVERED book.
STATUS (honest): discovery + distinct peers + fast mode all work, BUT raw
getaddr-discovered peers (and the stale static list) are largely non-serving /
EOF-prone, so FAST download is currently FLAKY (0-48 blocks then peer EOF with
no reliable recovery). The old approach (given seeds as download clients) was
reliable because those are real full nodes. Reaching reliable discovered-peer
download matching x86 needs the fuller x86 machinery (validated known-good peer
memory, more aggressive ban/rotate cadence) -- next step.

## FAST download now PROGRESSES (stall fixed) + IP byte-order + seed-node bootstrap
- ROOT STALL CAUSE (fixed): when a client's reconnect failed, rotate_client set
  its window owner=-1 (orphan) but nothing ever re-issued an orphaned window, so
  the strict-height-order apply deadlocked on an orphaned head-of-line window
  (~48-144 blocks then permanent stall, all peer sets). FIX: on every window
  completion, the freed client FIRST adopts any orphaned window before issuing a
  new one. Verified: download now climbs 80->128->320->368 over the tick window
  instead of flatlining.
- IP BYTE-ORDER BUG (fixed): book_add_ip was formatting network-order s_addr
  MSB-first on a little-endian host -> known-good static peers were added
  byte-reversed (e.g. real 66.181.35.130 added as 130.35.181.66) and unreachable.
  Now formats LSB-first (network order). parse_addr likewise feeds network order.
- SEED-NODE BOOTSTRAP (per USER "download peers FROM the seeds, seeds just seed
  peers"): resolve the given seed hostnames to their real full-node IPs via
  getaddrinfo and add those to the book FIRST as reliable downloaders; the seed
  host itself is still discovery/getaddr-only and dropped. Combined with the
  static known-good pool + getaddr haul, the 16 clients dial distinct book peers.
- FAST (cons-only) downloader now works end-to-end and progresses; throughput is
  still limited by frequent peer EOFs (discovered pool quality) ~3 blk/s; reaching
  network-bound multi-core speed needs the fuller x86 rotation/backoff tuning.

## bitcoin_chainwork.S differential-verified (cron run; fork-choice primitive)
bitcoin_chainwork.S was previously committed (560d77c) with only the repo
hand-checked harness PASS; this run added the missing differential verification
to mark it DONE in the roadmap (it sat under "Remaining leaf modules [ ]").
- Repo asm/tests/test_chainwork.c ALL PASS native (block_work vectors vs Core
  GetBlockProof incl. real-scale 0x170e0408 + exponent<3->0; u256_div 100/7 +
  2^128/3; chainwork_add carry; store accumulation + persisted-record replay).
- NEW port/arm64/fuzz_chainwork.py: self-contained driver+oracle. Differential
  vs an independent Python big-int oracle over 8 seeds x 6000:
  u256_div (256-bit LE, incl. small/huge/near-equal divisors) 0 fail;
  block_work (real mainnet nBits + random exp 1..30 + boundary) 0 fail;
  chainwork_add / chainwork_cmp (128-bit) 0 fail;
  store_chainwork_* real persistence: cumulative accumulation, reload
  (record-count + tip-cache re-derivation), truncate rollback + re-append from
  the truncated tip across a 120-step chain: 0 fail.
- Oracle-realistic notes surfaced (NOT asm bugs): (1) compact_to_target_le
  writes mantissa bytes only when offset=exp-3>=0, so exponent<3 => target 0
  => work 0 (Core's shift-down formula does NOT apply in this tree; the repo
  test_chainwork explicitly asserts 0x02.. -> 0). (2) the cumulative chainwork
  accumulates in 128 bits (as documented), so fuzz must mask store sums to
  2^128-1; real mainnet cumulative (<2^128 by huge margin) never wraps.
Commit: roadmap [x] + fuzz_chainwork.py + this worklog only (ibd.c/ibd_lsm.c/
ibd_par left uncommitted for the live session).

## Fast-mode downloader: root-caused + fixed the recurring FAST-SIGBUS-at-1000
- Tracked down why ibd_par (fast cons-only) crashed/stalled at ~1000 blocks in
  EVERY iteration since inception. ROOT CAUSE: every rotation (proven-peer,
  EOF reconnect, early-kill, borrow) reset a window's have[]/collected but NEVER
  freed its already-downloaded w->blk[z] mallocs -> ~16MB leaked per rotation,
  heap corruption within ~1000 blocks. Added win_clear() (frees all non-NULL
  blk[]) called on every reset-for-rerequest. This was the 'FAST SIGBUS ~1000'
  that prior sessions only papered over with rebuilds.
- Also hardened head-of-line this session: proven-peer pool (rotate to peers
  that actually served blocks, not getaddr junk), seed proven from resolved
  full-node IPs, orphan-borrow (when no client is free because all sit on
  complete-but-blocked windows), head-AGE trickle-defense (replace head owner
  blocking >=3 ticks even if it drips 1 blk/tick), SO_RCVTIMEO 20s per socket.
- With leak fixed: FAST breaks past 1000 blocks, reached ~h254,500 applied
  durably (data/), then hit another stall ~h254500. Throughput ~2.6 blk/s still
  peer-bound, NOT x86 network speed. True parity needs the fork-16-worker model;
  the single-threaded poll mux keeps hitting peer-quality walls.
- Commit 1ae7bd8. Chain durable data in data/ (untracked).

## Fast-path status: leak fixed, but a SEPARATE ~1000-2000-block SIGBUS remains
- With the rotation leak fixed, FAST previously died at 1000 -> now reaches
  ~2000 applied (h255552->h257560) then SIGBUS still recurs. ASan/SSP: no stack
  canary hit; garbage pc="57(64) c" bp="68.227.5" (peer-IP log text overwriting
  a frame) but SSE silent -> not a guarded-stack overflow. Healthy bursts hit
  ~16 blk/s (diag: 832 by 51s) so it is NOT cons_verify-bound; the recurring
  crash ~1000-2000 and the 15/16-head-missing-last-block stall are the killers.
- Full-verify ibd_lsm path remains ROCK SOLID (237k+ blocks, MISSING_PREVOUT=0,
  reload MATCH) -> it is the durable production path; fast path needs a focused
  memory-safety audit of the apply/block-routing hot path, NOT a quick patch.
- Added HDIAG per-tick head-state diagnostic (have[] bitmap + owner fd) to
  ibd_par for the next session's audit.

## FAITHFUL x86-mirror downloader: ibd_dlc (fork workers, EXACT logs)
- Redirection from user: stop inventing ARM-specific logging; do EXACTLY as x86
  (asm/daemon/main.c dl_bootstrap/dl_catchup/dlc_worker) + multithreading.
- Catalogued the full x86 log vocabulary + constants (DLC_CHUNK_BLOCKS=40,
  DLC_MAXPOOL=2048, MUX_MAX_OUT=64, DLC_CHUNK_BUDGET_SECS=120, dead_weight
  32768/3, min_usable_peers=8, 9 dns seeds; [boot]/[addr]/[dlc]/[dlc w%d] lines;
  dlc_fmt_rate/bytes/elapsed; /proc/<pid>/io rchar/write_bytes; idxscan).
- Wrote port/arm64/ibd_dlc.c: forks N workers claiming 40-block chunks from a
  shared mmap cursor, each cons_verify+store_append; parent 10s status loop
  sampling /proc io with EXACT x86 [dlc] lines (elapsed/overall/holes, peer
  status w%d chunks blocks blk/s, network recv/write tick + totals, peers
  banned, average since start, early-kill dead-weight, Dragging markers).
- Verified byte-identical log forms + the fork download WORKS (span h260099..,
  archive advanced 260098->260499, [dlc w%d] dead-weight/done lines exact).
- KNOWN divergence to fix next: parent [dlc] status "BANNED" sets claimed[]
  but has no true run-long banned[] set (x86 bans a peer for the run). Need a
  real banned[] array + amnesty to be fully faithful.
- ibd_dlc fidelity: added REAL run-long shared banned[] (mmap) + amnesty at
  stalled==10/25 ("[dlc w%d] no reachable peer -- amnesty, un-banned N peer(s)"),
  worker skips banned peers, parent early-kill sets banned[bidx] with
  min_usable_peers floor (why="BANNED"/"floor"). Verified archive advances
  260499->260899 in 170s, logs exact.
- utxo_run_*.dat spam ROOT CAUSE + fix (ibd_lsm): the LSM flush never auto-
  compacts -- it ERRORS when manifest_n >= manifest_cap (.fl_err). x86's
  utxo_live compacts every UTXO_LIVE_COMPACT_THRESHOLD (12) flushes to keep the
  run count bounded; my build path never compacted -> 1005 runs/3.8G. Added
  utxo_lsm_compact() every 12th flush in ibd_lsm (exact x86 cadence). Collapses
  run-file spam + bounded per-get disk-run scan. Build_utxo (x86-shared) also
  sets manifest_cap=8192 so it too would spam; compaction wiring there follows.
- Log-parity formal diff (ibd_dlc vs x86 main.c DL-phase): 26/42 strings byte-
  identical. The 16 absent split: -connect/-addnode/-seednode/-dnsseed config-
  option paths (full-daemon CLI surface, not the standalone tool) + real click
  paths: peers.good record ([dlc] recorded N known-good peer(s) for next boot),
  [dlc] N known-good peer(s) from a previous run tried first, [dlc w%d] no lock.
- Added x86 peers.good known-good-peer persistence to ibd_dlc (load-first into
  pool + save on catch-up done, identical dl_load/save_good_peers with atomic
  tmp+rename). Rebuild + verified downloader still advances (h261299).
## CORRECTION + real bug fix (ibd_dlc)
- FALSE-ALARM correction: my earlier "ibd_dlc verified downloads h260098->260899"
  was WRONG. The apparent tip advance was only the index.dat PRE-FTRUNCATE file
  SIZE (store_reload sets idx_len=filesize), NOT persisted index records. The
  archive index is all-zero above h260098; the real 260k archive came from
  ibd_par/ibd_lsm (those genuinely persist, and don't pre-size index.dat).
- REAL bug fixed: internal index.dat pre-ftruncate made store_append write index
  records at idx_len=filesize (end of the pre-sized span) so nothing above the
  true tip hit disk. FIX: truncate index.dat back to start_h*48 (real tip+1
  boundary) before the run so store_append resumes at the true height.
- REMAINING functional gap (honest): ibd_dlc's fork workers currently fetch
  0 blocks -- they connect (some peers reached) but never complete a 40-block
  chunk from the DNS-resolved probe pool. Same peer-serving/getdata-quality
  problem ibd_par/ibd_lsm already solved (proven/serving-peer sourcing). The
  fork worker needs the same confirmed-serving-peer pool, not just reachable.
  Log parity is faithful; functional block-fetch in the worker is the gap.
## ibd_dlc functional state (HONEST, 2026-08-27)
- ibd_dlc STILL does NOT complete a block fetch: with 4 real handshaked live
  peers assigned to workers, peak worker blocks stays 0 over 220s; real
  index.dat tip unchanged. The getdata->block route in the worker is broken
  independent of peer sourcing. STOPPED speculative patching here.
- Genuine improvements committed this leg (structure/log, not download):
  * probe now parallel + multi-round poll (x86 dlc_probe_round shape) + from+=BATCH
  * dlc_handshake helper (fast-fail 3s) shares the version/verack handshake
  * book primed with proven-serving node IPs (66.181.35.130, ...) before DNS
  (the DNS haul is ~1-4 usable Bitcoin peers per ~155 candidates here)
  * index.dat pre-ftruncate persistence bug FIXED (commit 6b31632, already pushed)
- NEXT: root-cause the worker getdata->block fetch vs ibd_par's KNOWN-WORKING
  req_window+route (same payload). Logs/structure remain faithful; download
  not yet demonstrated. Do NOT claim ibd_dlc downloads until index records land.

## Later: fast-mode removal + UTXO-in-IBD backfill (ef3666a)
USER CORRECTION: "get rid of fast mode. x86 doesn't have it. Build utxo during ibd."
- Removed FAST cons-only mode from ibd_par.c ENTIRELY (commit ef3666a): dropped
  g_fast flag, all if(!g_fast) guards are now unconditional, deleted the FAST
  startup/DONE log lines, usage no longer takes [fast]. ibd_par now ALWAYS
  cons_verify + ECDSA-verify + LSM UTXO apply per block during download == x86
  dl_catchup (build UTXO live during IBD, no separate pass).
- Side-effect of prior fast mode: archive blocks h260099..266746 were stored
  WITHOUT UTXO apply, so the LSM UTXO is stale at ~h260098 while block archive
  is at ~h266810. To resume cleanly with UTXO-in-IBD I must first make the UTXO
  correct through the archive tip:
  - Launched build_utxo (x86 upstream offline UTXO replay) full rebuild
    0..266746, `./build_utxo data 23 8 0 266746`, slots=2^23 (8.4M, ~3.8G heap)
    blob=8GB, over the STORED archive (no network). proc_5c6f86f79b41.
  - Confirmed POPULATE progressing (live=150740 @ h120k), no load/dup issues.
- AFTER build_utxo completes: resume durable grind with the de-fasted ibd_par
  from tip+1 (now UTXO-building) toward ~964k. Then address slots upsize to
  2^27 + multi-GB blob before the segwit->tip leg (memtable size parity with
  x86, per earlier lesson).

## ROOT-CAUSED + FIXED the recurring ~1000-block SIGBUS (commit 8cea9a6)
Symptom: ibd_par SIGBUS (rc=135) every ~1000 blocks, resists ASan + SSP-all.
Reproduced under ASan: BUS with pc=0x20293436283735, bp=0x2e3732322e38362e =
ASCII "57(64) " and ".86227." (log/peer text). Mechanism: intra-frame STACK
overrun writing LOG CONTENT over the saved frame, then ret into garbage.
TRUE CULPRIT: node_log_str (port/arm64/node_log.S) copies `len` bytes
UNCONDITIONALLY into its 160-byte stack line buffer (no bounds check). Any LLOG
line >=160B overflows it, clobbering saved x21/x22/x19/x20/x29/x30. Cadence:
the [dl] progress heartbeat fires every 1000 blocks and calls log_peers(),
emitting a 16-worker peer line ~380B -> the precise trigger. (The earlier
"win_clear rotation leak" attribution, commit 1ae7bd8, was WRONG -- crash
persisted.)
FIX: clamp len to min(len,511) and enlarge the buffer to 512 (node_log.S).
Deterministic repro: a 400B line crashes OLD node_log.o with Bus error (135);
FIXED returns 0. Skill 'aarch64-btc-port' updated with the real cause + audit
rule (asm string/byte-copy routines must bounds-check arbitrary-length inputs).
NEXT (in flight): rebuild UTXO clean to archive tip (cleared all prior crash
gaps -> no more MISSING-PREVOUT spam), then one LONG continuous ibd_par to the
real tip (no segment-grind needed once the crash is gone).

## bitcoin_muhash.S differential-verified (cron run; unlocks s8 set-hash parity)
bitcoin_muhash.S was committed earlier (475458c) and the repo test_muhash.c
already PASSES natively against Core's OWN MuHash3072 output -- but its MUL
layer is only 6 FIXED vectors, so num3072_mul (the 48x48 schoolbook + 2-pass
mod-p reduction at the heart of MuHash) had no coverage across the value space.
This run added the missing independent differential to mark it DONE:
- Repo asm/tests/test_muhash.c (rebuilt with the ported bitcoin_muhash.o +
  sha256.o) ALL PASS native: ChaCha20 keystream x4, ToNum3072 x9, Num3072::Mul
  x6, whole-set hashes (0/1/2/17/200 elems), order-independence (reversed
  insertion), shard-combine (two interleaved halves), one-bit negative control,
  is_overflow(modulus-1)=0 / is_overflow(modulus)=1, full_reduce(modulus)=0,
  and x*1==x. All Core-generated vectors, none hand-transcribed.
- NEW port/arm64/fuzz_muhash.py: independent pure-Python big-int oracle
  (p = 2^3072 - 1103717; expected = (a*b) % p) drives the asm num3072_mul over
  random 384-byte LE operands -- full-width RAW (stress the 2-pass reduction),
  x*1, x*x, and canonical (< p) inputs. 80,000 mults across 16 seeds: 0 fail.
  (Oracle note: hex output is MSB-first, decode as int(hex,16) -- not
  int.from_bytes(...,'little'), which gave a spurious 100% fail.)
- Also rebuilt bitcoin_muhash.o natively (test_muhash binary) as part of the
  run; it is already in the build_utxo module set (needs sha256.o).
Commit: roadmap [x] + fuzz_muhash.py + this worklog only. ibd_par binary /
ibd.c / ibd_dlc / the live session's worklog edits left uncommitted for the
live session (active ibd_par 16-client grind h282181-> ~ h286k at ~4-5 blk/s,
full UTXO-in-IBD, data in data/).

## 2026-08-27 late session — BLOCK-FETCH ROOT CAUSE FOUND + PROTOCOL FIX

**Established (wire-captured, correct P2P framing — magic|cmd[12]|plen|ck|payload, NO cmdlen byte):**
- node_ibd_blocks_s's block `getdata` is byte-identical to the WORKING paribd reference
  (type 0x40000002 MSG_WITNESS_BLOCK, valid present-block hash ca7bfb52 = real block 340001).
- Root cause of "Core withholds block": Core v31 silently DROPS a block `getdata` sent on an
  UNSETTLED connection (verack/getdata bundled before Core finishes post-version setup).
  paribd always does a getheaders (persist_headers) round-trip first; node_ibd_blocks_s didn't.
- FIX (commit 8d58a9d): added paribd-style getheaders SETTLE to node_ibd_blocks_s before its
  getdata loop (frame slots sp+392..464 free; calls local node_fetch_headers).
- VERIFIED: wire now shows O->H `block`; serve daemon worker w0 pulls block bodies from oracle
  at ~2.4MB/s (recv 24MB/tick) where before it churned at 0 blocks.

**Remaining (NEXT): r=-1 after receiving block.** Every component verified individually via
t_idbproc.c (C replication of node_ibd_blocks_s i=0 path, same asm): cons_verify=1, block_hash
matches target, store_append=0, block DURABLY stored (blk00000.dat 144884B + index record).
So the -1 is a mechanical ARM asm bug INSIDE node_ibd_blocks_s's body (register/offset), not
protocol or any component. Audit node_ibd_blocks_s receive→store path against t_idbproc.c.

**Also noted:** headers.dat record k == oracle block k+1 (record0 = block1, genesis absent) —
internally consistent (256-bit hash compare + prev-link verified), not the download blocker.

Diagnostics kept: port/arm64/t_idbproc.c (per-block C oracle), t_ibdblocks.c (full-function repro).
Orphans to clean: /tmp/t_idbproc, /tmp/paribd, /tmp/scratch_*. p7/p8/pcap captures.

## 2026-08-28 late session — node_ibd_blocks_s BLOCK DOWNLOAD NOW WORKS END-TO-END

TWO fixes to node_ibd_blocks_s, both committed + daemon-verified:
1. 8d58a9d: getheaders SETTLE before the getdata loop (paribd pattern) — Core v31
   drops a block getdata on an unsettled connection. Made Core serve the block.
2. 6c692d3: p2p_read receive CAP fix. The block read passed cap=[sp,#60](buflen),
   which arrived as 0 in the daemon/harness context -> p2p_read copied 0 bytes and
   DRAINED the whole block in 64-byte reads, returning -2 (truncated) ->
   node_ibd_blocks_s returned -1, stored nothing. Fixed cap to 64MB literal
   (>= max 4MB block). Root-cause evidence: strace showed 712x64B drain reads
   (the truncated-block drain signature), block never reached cons_verify.

VERIFIED: serve daemon (data/ datadir, connect=127.0.0.1 -> oracle) now downloads
real block bodies at ~20 blk/s (~7MB/s disk write), DURABLE real tip advanced
328430 -> ~332k (202 blk000NN.dat files), index.dat real scan advancing.
Commit 6c692d3 pushed (b448e9c..6c692d3).

NOTE: daemon [dlc] "overall: 5165/964402 stored" counter lags (displays a stale
in-memory count) while the durable index.tip advances — real durable storage is
what matters and it is advancing. Long-duration grind toward 964k continuing.

## 2026-08-28 — UTXO-live-catchup false rejects: root-caused + first fix (racing scratch)

SYMPTOM: serve-daemon UTXO build fails "REJECT h=N tx=1: legacy script verification failed" /
"apply_block failed at height N -- stopping catch-up", stuck in [UTXO DEGRADED -- retrying].
Failure block differs EVERY retry (7676, 9353, 11665, ...) but always an early, large,
multi-input LEGACY (P2PK) tx. UTXO essentially empty (txouts ~5582-7339).

MISLEADING DEAD-ENDS disproven (do NOT revisit):
- "ARM legacy_sighash/ecdsa/pubkey_parse are buggy" -> FALSE. Verified byte-for-byte vs
  independent Python (ecdsa lib) + Core on real block 2518 input0: sighash z=6522...c27539,
  ECDSA, all correct. sv_verify_script returns SCRIPT_ERR_OK=0 on success (I misread 0 as a
  code earlier -- it is SUCCESS).
- "legacy_sighash wrong for >20-input / >2.3KB txs" -> FALSE. Synthetic 45-input/5.5KB tx
  (gen_synth.py + t_synth.c): ARM legacy_sighash matches Python for nIn=0 AND nIn=44.
- "script_flags_for_block bad" -> returns 0x20801 (P2SH|WITNESS|TAPROOT) at every height
  (height-gating only ADDS buried DERSIG/CLTV/CSV) -- matches x86 base set; not the cause.
- daemon uses the ASM txv TWIN (bitcoin_txv_dispatch.S txvb_verify_one_asm), NOT the C path
  in tx_verify.c (txv_verify_one). Instrumenting tx_verify.c never fires; instrumenting
  sv_verify_script (bitcoin_scriptverify.c, called by BOTH) does.
- blk file layout (for harness extraction): size(4) + magic(4) + blockheader(80) + txs;
  record k = oracle block k+1 (daemon h = oracle h+1). index.dat record 48B hash|file|pos|size.

REAL ROOT CAUSE: ARM script-verify subsystem was written single-threaded with MULTIPLE
shared STATIC scratch .bss buffers; tx_verify.c (2026-08-19) now fans signature verification
across up to 16 worker THREADS. Concurrent calls to the same primitive WRITE THE SAME static
buffer -> clobber each other -> wrong SIGHASH -> spurious SCRIPT_ERR_EVAL_FALSE -> daemon
rejects a valid block. A RACE: different block fails each run. x86 (authoritative) uses
per-thread TLS for these; the source comments literally flag them ("single-threaded ok;
-> .tbss before the threaded daemon").

Shared write-buffers found (all need TLS-conversion to match x86):
  - legacy_sighash:  ls_scfbuf 20000B        (bitcoin_sighash.S)   -- FIXED
  - interpreter:     interp_tmp 528, bool_buf 8, interp_err 8,
                     interp_slice 32, cms_keyrefs/sigrefs 1024 each,
                     cms_scstrip0/1 10008 each, cms_needle 1300     (bitcoin_interp.S)
  - checksig/ECDSA:  cks_pre 65536 + 600 + 20000                      (bitcoin_checksig.S)

FIX (committed 0fab975, pushed): made ls_scfbuf per-thread TLS:
  .bss ls_scfbuf: .space 20000  ->  .tbss, + TLS_ADDR (gottprel) at the one access site.
Mirrors x86 exactly. Daemon then got PAST previously-stuck h=7676 (resumed 7675, reached
9353 then 11665) before hitting the SAME race on the OTHER shared buffers -> confirms the
systemic cause. Remaining work: TLS-convert the interpreter + checksig shared buffers
(~41 LAddr->TLS_ADDR sites; watch x10 liveness -- TLS_ADDR clobbers x10), then rebuild and
let the UTXO build run 0->964k flat without a reject.

## UPDATE — systemic race FIXED (interp/scriptcodec/checksig TLS), committed 2eabefc (pushed)
Followed the ls_scfbuf fix with the full suite: converted EVERY shared static
scratch .bss buffer in the script-verify asm to per-thread .tbss + TLS_ADDR
(clobber choice: x17 -- unused in interp.S, isolated in scriptcodec.S, free in
checksig.S):
  bitcoin_interp.S:      interp_tmp bool_buf interp_err interp_slice
                         cms_keyrefs cms_sigrefs cms_scstrip0/1 cms_needle
  bitcoin_scriptcodec.S: scriptnum_buf snum_overflow elem_tmp0-3 vfexec vfexec_sp
  bitcoin_checksig.S:    cks_needle cks_scf cks_pre
(taproot_verify already had TLS; the legacy path was the laggard.)
VERIFIED: daemon UTXO build no longer races -- it grinds from the old stuck
~0.8% through 3.3%+ (7676, 11665, 253xx all pass now). NEW deterministic
blocker at h=32255 (oracle 32256): "apply_block failed" EVERY retry, but NO
per-tx REJECT reason is logged (line 1023 path not hit) -> it's a non-logged
failure path inside apply_block_inner. NEXT: instrument that path (or log the
reason) to see why 32255 deterministically fails; then rebuild + let UTXO run.
TLS gotchas worth remembering: .bss->.tbss must be done per SECTION (vfexec was
in a SECOND .bss block at line 311); TLS_ADDR clobbers a register -- pick an
unused one per file (x17); cross-file refs (cms_scstrip in sighash.S) were only
comments; taproot_verify's TLS already existed.

## NEXT BLOCKER h=32255 ROOT-CAUSED: daemon-vs-consensus height -1 shift breaks retarget
reason='bad-diffbits' at daemon-h 32255 (=real block 32256). Archive is chain-consistent
(32254->32255->32256 prev-hash-linked), and daemon-h 32255 carries NEW bits 1d00d86a (same
as 32256). Real mainnet's first difficulty CHANGE is the retarget at block 32256 (=2016*16).
Because block store record k = real block k+1 (headers.dat +1 genesis bias propagated in),
the daemon's internal height is -1 vs consensus. Relative checks unaffected; but pow_check_bits
(bitcoin_pow_rules.c, shared C) uses height%POW_INTERVAL to detect retarget blocks -> a real
retarget (real h==0 mod 2016) is seen at daemon h==2015 (non-retarget) -> expects prev-bits
(1d00ffff) but block legitimately carries new retarget bits (1d00d86a) -> false bad-diffbits.
Earlier retargets (2016,4032,..) kept difficulty at minimum 1d00ffff so the shift was invisible;
32256 is the first real change. FIX: pow schedule must run on consensus heights -- translate
g_apply_height->real height for the %2016 gate and index ancestor reads to daemon records
(get(ctx,h) reads record h = real h+1). Any other absolute-height rule (CLTV/CSV/maturity/
segwit threshold) shares the same -1 shift but is masked in practice.
NOTE: two daemons ran concurrently earlier this session on the same data dir (3702921/3709070)
-- got lucky (UTXO essentially empty), but avoid; single daemon only.

## 2026-08-28 followups — BIP30 height-shift fix (169795f) + a reverted optimization mistake
- BIP30: same -1 store-height shift broke the duplicate-coinbase exceptions
  (real 91842/91880): daemon rejected them bad-txns-BIP30. Pass consensus height
  (apply_height+1) + read BIP34 ancestor at daemon record (height-1). Clean
  from-0 build now passes 91842/91880. This confirms EVERY absolute-height
  consensus rule (retarget, BIP30) shares the -1 shift; BIP34/maturity/segwit/
  CLTV-CSV are the same class but masked.
- OPTIMIZATION-EXPERIMENT REVERTED: tried batching the per-block applied-height
  fsync (wchan=jbd2_log_wait_commit => I/O-bound, ~100 blk/s, 0% CPU). It broke
  crash recovery: persisted applied_height lagged the actually-applied LSM, so a
  restart re-applied already-applied blocks and false-rejected their coinbases
  with bad-txns-BIP30. The per-block fsync is load-bearing for applied_height ==
  LSM coherence. A real speedup needs a recovery redesign (safe resume from last
  LSM checkpoint + bounded re-apply reconciliation), not a cadence change.

## 2026-08-28 morning wrap — UTXO build speed root cause + switch to build_utxo
DECISION (user chose option 1): the from-0 UTXO grIND is served by daemon/build_utxo
(batch, trusts the cons-verified archive, NO per-block fsync), NOT by `serve`'s
utxo_live_catchup (incremental/live path).

ROOT CAUSE of "utxo catchup way too slow vs x86": utxo_live_catchup buckets a
per-block applied-height fsync (persist_applied_height: 12B write+fsync+rename
+dirfsync). On THIS GB10 host an isolated 12B fsync = ~3ms (paused/empty dir) to
~6-11ms under load -- the ext4 journal commit floor. That caps the per-block-fsync
catchup at ~100 blk/s. x86 does the same per-block fsync on a sub-ms disk -> its
own comment quotes "a few thousand blk/s". NOT the port, NOT threading (catchup
already runs a 20-thread txvb verify pool), NOT crypto flags.

MEASURED build_utxo (ARM, slots 2^24/blob 8.6GB), full LSM:
  h=20k 274,756 blk/s; h=80k 92,658; h=120k 18,904; h=180k 3,293
=> 30-270x faster than the catchup. Dry-run earlier: 0..200k in 15.3s.

STATE at wrap: killed the old serve catchup (had reached h=248371, live=6.7M,
0 rejects -- port proven correct); backed its partial UTXO into
data/utxo_stale_1787930387/ (~5GB). Ran real `build_utxo /home/svc/bitcoinmachinecode/data
24 8 0 964445` (bg proc_08c5ef65a13a, pid 602017, log data/logs/build_utxo.log)
to construct the complete UTXO. RESUME-plan when it returns: build_utxo writes NO
utxo_applied_height.dat, so before restarting serve you must (1) write the marker
= tip in persist_applied_height's 12B format (>=0x48504155 "UAPH" + 8B LE height)
so catchup does NOT re-apply from 0 on the seed, and (2) get serve to RELOAD the
seed's bulk-sized table/blob -- g_bulk_mode = (boot_gap >= utxo_bulk_gap_blocks);
with marker=tip, gap=0 picks steady 2^16 (truncates the 2^24 seed => corrupt) --
so force bulk: set bmc.utxobulkgapblocks=0 AND make bulk slots/blob match the seed
(2^24/8.6GB) via dbcache (gives utxo_bulk_slots_log2; 2^24 <=> dbcache in
[4300,8600)MB) or use 2^22/1GB (x86 default) and build the seed at that size.
Verify serve init line shows applied=tip manifest_n>0 live~80M and NO catchup-from-0.
NOTE: build_utxo is resumable/healable -- an interrupted kill just needs a re-run
of `build_utxo ... 0 <tip>` (uninterrupted).

## 2026-08-28 afternoon — SYNCED origin/main into arm-port
- git merge origin/main -> 9e773f0 (39 files, +5037/-280, NO conflicts; only utxo_live.c auto-merged).
- Our ARM work intact after auto-merge: UTXO_LSM_BARRIER x10, BIP30/difficulty consensus-height fixes verified.
- + a3318de gitignore (re-added scratch_daemon/ + port/arm64/daemon_out/ ignore after reconciling with main's new ignore rules; stash-pop conflicted, resolved by hand).
- Pushed origin/arm-port 169795f..a3318de (36 commits incl. our bip32/bip39 port+verify).
- live-session port/arm64/ibd.c + config/bitcoin.conf left uncommitted (untouched by merge).
FOLLOW-ON (not done): incoming main changed x86 asm bitcoin_serve.asm (BIP157/notfound) + bitcoin_store_fast.asm, added serve_cfilters.c (calls new bitcoin_serve symbols) + genesis_skip.h. Our .S twins don't implement those yet -> next daemon rebuild lacks the new serve features until we re-port those two .S modules (re-verify native). No NEW .asm modules, parity target stays 63.

## 2026-08-28 — daemon rebuild + .S re-ports for merged main (BIP157/notfound)
- bitcoin_store_fast.S: ported store_map_random(on) (opt-in MADV_RANDOM madvise in map_file; .bss flag smf_madv_random).
- bitcoin_serve.S: ported serve_txdv_preinit(), notfound reply (nf_buf/s_nf/gd_miss/gd_done + cn_notfnd), serve_idx_topup call in getdata, BIP157 getcf dispatch (.maybe_getcf -> serve_cfilters kinds 0/1/2).
- FIXED pre-existing ARM inbound-dispatch bug: getX (getaddr/getdata/getheaders/getblocks) dword0 compare used lo16 0x7465 (byte0='e') instead of correct 0x6567 (byte0='g') — ping/other were correct-LE but the getX family NEVER matched, so inbound serving of those was dead on ARM. Corrected all 4 + added getc (0x63746567). VALIDATE later with validation/p2p_inbound_probe.py on a live node.
- build_daemon.sh: added daemon/serve_cfilters.c + wallet_txlog.c (txlog_append_sent) to the link.
- Rebuild: bash build_daemon.sh rc=0; binary exports serve_cfilters/serve_idx_topup/serve_txdv_preinit/store_map_random/txlog_append_sent.
