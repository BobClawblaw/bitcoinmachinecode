# 2026-09-10 — osx: the daemon day (p2/p3 wave, regtest IBD green, mainnet IBD running)

## 2026-09-10 — mainnet IBD RUNNING on Apple Silicon (the phase goal)

- Fresh mainnet datadir, `connect=192.168.5.242:8332` (the x86 reference
  node), `listen=0 dnsseed=0`. The native bmcbitcoind on this Mac (M1 Max,
  macOS 26.6, clang 21) connected, handshaked (BIP324 v2), pulled ALL
  966,400 headers in ~29 min (headers.dat 108,236,800 bytes, ~5k hdr/s
  steady -- request/response pacing, not bandwidth), then the boot
  catch-up began storing real mainnet blocks from genesis:
  115,401/966,400 (11.9%) at elapsed 5:01, ~460 blk/s and climbing, zero
  consensus failures.
- Note: .242 is ITSELF mid-IBD right now (its own re-run, 636k/966k,
  10.4 MB/s), so the osx node pulls everything .242 has and follows its
  tail live -- a genuine genesis-to-tip mainnet IBD, peer-progress-bound
  at the top of the chain.
- BENCHMARKS_OSX numbers for the record: 966,400 headers in 29 min;
  early-block store rate ~460 blk/s single peer; the [dlc] recv/write
  bandwidth line prints 0.0B/s on macOS (it samples /proc/<pid>/io --
  Linux-only); cosmetic.

## 2026-09-10 — osx p2/p3 wave landed (bc38f9e8): daemon links and runs natively

- The whole syscall .S layer the daemon references, adapted from
  origin/arm-port by adapt_armport.py (ELF->Mach-O) + fix_syscalls.py
  (Linux->Darwin BSD nr): bitcoin_idxscan.S (19 svc), bitcoin_serve.S,
  bitcoind.S (handshake/IBD engine), bitcoin_mempool.S,
  bitcoin_interp.S + bitcoin_scriptcodec.S + bitcoin_script_flags.S
  (Mach-O TLV through C getters in tls_*.c -- cross-object @TLVPPAGE has
  no fixup kind from hand asm), bitcoin_sigops.S, bitcoin_cmpct.S,
  bitcoin_strip_witness.S, node_log.S. C shims: bmcshim.c
  (getrandom->arc4random_buf), darwin_stubs.c (malloc_info, robust
  mutexes), compat/sys/prctl.h (PR_GET_NAME->pthread_getname_np,
  PDEATHSIG->no-op with the getppid re-check kept),
  compat/malloc.h. build_daemon.sh compiles+links bmcbitcoind and
  bmc_wallet_cli natively. bmc_regtest_mine.py mines via
  getblocktemplate/submitblock.

## 2026-09-10 — the IBD-blocking bugs (found by the first regtest run)

- THREE raw-svc sites kept the Linux nr-in-x8 convention after the
  mechanical rewrite: idxscan_append_locked's TWO flock sites
  (x8=32 -> x16=131) and node_log_open's openat (w8=56 -> x16=463).
  Darwin executes whatever garbage sits in x16: nosys slots raise
  process-directed SIGSYS (the worker died on signal 12 at random PCs --
  si_code 0, interrupted PC in plain C code, because the bad syscall was
  on another thread), and real numbers fire the WRONG syscall (the first
  crash, signal 11, was the fd-close roulette). Fixed all three; the
  worker has been stable since.
- bitcoind.S node_make_version double-dereferenced _node_services
  (x86: `node_services dq 9` is a plain u64 that main.c ORs into). The
  extra `ldr` read address 0x809 (NODE_NETWORK|WITNESS|P2P_V2 as a
  POINTER) -> SIGSEGV in dl_catchup's probe handshake. macOS
  DiagnosticReports .ips gave the clean backtrace
  (node_make_version+36 <- node_handshake+44 <- dl_catchup) without any
  debugger: lldb attach AND batch launch are permission-gated on this
  macOS 26.6 build (also true under `script` PTYs). Single-load fix.
- cons_twin.c txid_of_span (the osx twin computes txids itself because
  x86 asm tx_txid hashes the wrong length for segwit txids): the first
  cut hardcoded the marker+flag offset (tx+6), eating two bytes of every
  LEGACY txid, and passed the txid-slot cap where the scratch byte cap
  belongs (67-byte tx > 64 slots -> refuse -> cons_verify 0).
  test_cons 14/14 after both fixes; txid == sha256d(tx) for legacy and
  the stripped form for segwit. Found by the gate before it could bite
  mainnet (blocks 1..479k are all-legacy coinbases).
- asm/daemon/main.c: SIGSYS/SEGV/ILL/BUS/FPE/ABRT handler in the
  download worker dumping siginfo + native backtrace to stderr --
  the diagnostic that made the syscall-roulette crashes legible.
- asm/daemon/ibd_pipeline.c: on IBD_FAIL_CONSENSUS dump the offending
  block bytes to /tmp/ibd_bad_<pid>_<ts>.bin with its block hash, for
  offline analysis (standalone cons_verify passed every block of the
  failing chunk, so the arriving bytes were the question).
- Process lesson (again): build_daemon.sh only recompiles stale objects;
  a manually-built twin object or a half-edited source can leave the
  daemon binary inconsistent with the tree. The first mainnet chunk run
  failed cons_verify (code -5) on every chunk with a binary that did not
  match the tree; a clean full rebuild (which briefly failed its own
  link on my undeclared `time()` -- one more reason the link step must
  be allowed to fail loudly) made chunks pass with the same inputs.
  Rule going forward: touch a port source -> rebuild ALL, then run.

## 2026-09-10 — regtest IBD end-to-end GREEN (the phase-3 gate)

- Node B (fresh datadir, `addnode=127.0.0.1:18444` under the [regtest]
  section -- Core's network-section semantics, the top-level addnode is
  ignored with a note) probed node A, confirmed it live, and pulled all
  113 blocks over the wire: 3 chunks, ~10s, archive committed in height
  order, UTXO applied 113/113, `getblockchaininfo` tip hash IDENTICAL to
  A's, initialblockdownload=false at tip.
- Node A (listener) needed listen=1 -- with listen=0 the serve-mode
  positional port is never bound and the peer probe fails
  ("0 confirmed-live"). submitblock/116 mined blocks across the day all
  went through idxscan_append_locked.
- Gates re-run on the touched modules: test_cons 14/14 native,
  bench_idxscan green against the live 113-record archive
  (1.18-2.09x over the C baseline).
