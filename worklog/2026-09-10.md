# 2026-09-10 — osx: the daemon day (p2/p3 wave, regtest IBD green, mainnet IBD running)

## 2026-09-10 — mainnet IBD RUNNING on Apple Silicon (the phase goal)

- Fresh mainnet datadir, `connect=192.168.5.242:8332` (the x86 reference
  node), `listen=0 dnsseed=0`. The native bmcbitcoind on this Mac (M1 Max,
  macOS 26.6, clang 21) connected, handshaked (BIP324 v2), pulled ALL
  966,400 headers in ~29 min (headers.dat 108,236,800 bytes, ~5k hdr/s
  steady -- request/response pacing, not bandwidth), then the boot
  catch-up began storing real mainnet blocks from genesis:
  115,401/966,400 (11.9%) at elapsed 5:01, ~460 blk/s and climbing, zero
  consensus failures.
- Note: .242 is ITSELF mid-IBD right now (its own re-run, 636k/966k,
  10.4 MB/s), so the osx node pulls everything .242 has and follows its
  tail live -- a genuine genesis-to-tip mainnet IBD, peer-progress-bound
  at the top of the chain.
- BENCHMARKS_OSX numbers for the record: 966,400 headers in 29 min;
  early-block store rate ~460 blk/s single peer; the [dlc] recv/write
  bandwidth line prints 0.0B/s on macOS (it samples /proc/<pid>/io --
  Linux-only); cosmetic.

## 2026-09-10 — osx p2/p3 wave landed (bc38f9e8): daemon links and runs natively

- The whole syscall .S layer the daemon references, adapted from
  origin/arm-port by adapt_armport.py (ELF->Mach-O) + fix_syscalls.py
  (Linux->Darwin BSD nr): bitcoin_idxscan.S (19 svc), bitcoin_serve.S,
  bitcoind.S (handshake/IBD engine), bitcoin_mempool.S,
  bitcoin_interp.S + bitcoin_scriptcodec.S + bitcoin_script_flags.S
  (Mach-O TLV through C getters in tls_*.c -- cross-object @TLVPPAGE has
  no fixup kind from hand asm), bitcoin_sigops.S, bitcoin_cmpct.S,
  bitcoin_strip_witness.S, node_log.S. C shims: bmcshim.c
  (getrandom->arc4random_buf), darwin_stubs.c (malloc_info, robust
  mutexes), compat/sys/prctl.h (PR_GET_NAME->pthread_getname_np,
  PDEATHSIG->no-op with the getppid re-check kept),
  compat/malloc.h. build_daemon.sh compiles+links bmcbitcoind and
  bmc_wallet_cli natively. bmc_regtest_mine.py mines via
  getblocktemplate/submitblock.

## 2026-09-10 — the IBD-blocking bugs (found by the first regtest run)

- THREE raw-svc sites kept the Linux nr-in-x8 convention after the
  mechanical rewrite: idxscan_append_locked's TWO flock sites
  (x8=32 -> x16=131) and node_log_open's openat (w8=56 -> x16=463).
  Darwin executes whatever garbage sits in x16: nosys slots raise
  process-directed SIGSYS (the worker died on signal 12 at random PCs --
  si_code 0, interrupted PC in plain C code, because the bad syscall was
  on another thread), and real numbers fire the WRONG syscall (the first
  crash, signal 11, was the fd-close roulette). Fixed all three; the
  worker has been stable since.
- bitcoind.S node_make_version double-dereferenced _node_services
  (x86: `node_services dq 9` is a plain u64 that main.c ORs into). The
  extra `ldr` read address 0x809 (NODE_NETWORK|WITNESS|P2P_V2 as a
  POINTER) -> SIGSEGV in dl_catchup's probe handshake. macOS
  DiagnosticReports .ips gave the clean backtrace
  (node_make_version+36 <- node_handshake+44 <- dl_catchup) without any
  debugger: lldb attach AND batch launch are permission-gated on this
  macOS 26.6 build (also true under `script` PTYs). Single-load fix.
- cons_twin.c txid_of_span (the osx twin computes txids itself because
  x86 asm tx_txid hashes the wrong length for segwit txids): the first
  cut hardcoded the marker+flag offset (tx+6), eating two bytes of every
  LEGACY txid, and passed the txid-slot cap where the scratch byte cap
  belongs (67-byte tx > 64 slots -> refuse -> cons_verify 0).
  test_cons 14/14 after both fixes; txid == sha256d(tx) for legacy and
  the stripped form for segwit. Found by the gate before it could bite
  mainnet (blocks 1..479k are all-legacy coinbases).
- asm/daemon/main.c: SIGSYS/SEGV/ILL/BUS/FPE/ABRT handler in the
  download worker dumping siginfo + native backtrace to stderr --
  the diagnostic that made the syscall-roulette crashes legible.
- asm/daemon/ibd_pipeline.c: on IBD_FAIL_CONSENSUS dump the offending
  block bytes to /tmp/ibd_bad_<pid>_<ts>.bin with its block hash, for
  offline analysis (standalone cons_verify passed every block of the
  failing chunk, so the arriving bytes were the question).
- Process lesson (again): build_daemon.sh only recompiles stale objects;
  a manually-built twin object or a half-edited source can leave the
  daemon binary inconsistent with the tree. The first mainnet chunk run
  failed cons_verify (code -5) on every chunk with a binary that did not
  match the tree; a clean full rebuild (which briefly failed its own
  link on my undeclared `time()` -- one more reason the link step must
  be allowed to fail loudly) made chunks pass with the same inputs.
  Rule going forward: touch a port source -> rebuild ALL, then run.

## 2026-09-10 — regtest IBD end-to-end GREEN (the phase-3 gate)

- Node B (fresh datadir, `addnode=127.0.0.1:18444` under the [regtest]
  section -- Core's network-section semantics, the top-level addnode is
  ignored with a note) probed node A, confirmed it live, and pulled all
  113 blocks over the wire: 3 chunks, ~10s, archive committed in height
  order, UTXO applied 113/113, `getblockchaininfo` tip hash IDENTICAL to
  A's, initialblockdownload=false at tip.
- Node A (listener) needed listen=1 -- with listen=0 the serve-mode
  positional port is never bound and the peer probe fails
  ("0 confirmed-live"). submitblock/116 mined blocks across the day all
  went through idxscan_append_locked.
- Gates re-run on the touched modules: test_cons 14/14 native,
  bench_idxscan green against the live 113-record archive
  (1.18-2.09x over the C baseline).

## 2026-09-11 (late) — the script-VM wave gets its gates; the real TLS bug

- Ran the upstream harnesses natively against the ported objects (the
  roadmap's DONE bar for the wave committed in bc38f9e8, which had landed
  with "gates = the daemon itself"). FIRST REAL GATES for interp +
  scriptcodec: test_nop1_regression SEGFAULTED immediately.
- Root cause (two layers, found with macOS .ips crash reports, an
  in-test siginfo+backtrace handler, and a 30-line isolated repro --
  lldb still permission-gated):
  1. The Mach-O TLV getters turned x86 TLS_ADDR (inline gottpoff+fs:0
     arithmetic, clobbers ONLY its destination) into a CALL that returns
     in x0 -- clobbering the caller's x0 argument (hnd_begin stored the
     getter's return where main_elems belonged) and, fatally, LR.
  2. THE RULE: on AArch64 `ret` jumps to x30; a leaf .S function whose
     body now contains a bl (TLS_ADDR) must save/restore x30 or its
     closing ret jumps to the last getter's return site. `sample` on a
     hung minimal repro showed the loop literally: asm_plain+4 ->
     asm_plain+4. x86 ret pops the stack; arm64 ret = br x30. The
     arm-port never hit this because ELF TLS access was inline too.
- Fix (0bac9e2b): TLS_ADDR now saves x30 + x0-x17, calls the getter, and
  delivers to \d by writing the result into \d's OWN save slot (offset
  i*8; the x30 pad sits below the pairs) so the pops restore everything
  except \d. First cut of the delivery used offsets 16+i*8 -- wrong by
  one pair slot; the isolated test's MISMATCH (0x120a8 = the caller's
  stale x2, a compiler constant, not stack junk) pinned it.
- Gates green natively on the ported objects after the fix:
  test_nop1_regression 9/9, test_checkmultisig_findanddelete,
  test_checkmultisig_zero_keys, test_checkmultisigverify_pop,
  test_sig_pushonly_75, test_cltv_csv 17/17, test_ir10_p2pkh_bounds 5/5,
  test_interp_legacy_spend 6/6, test_sigops, test_script_flags 13/13,
  test_mempool_wtxid 26/26, test_cmpct_recv 43/43 (needed net_twin for
  p2p_write; got a Darwin guard for the Linux cpu_set_t core pinning),
  test_private_broadcast, bench_idxscan, test_cons 14/14, hnd_begin/
  hnd_end round trip. consensus_shim builds. The wallet round trip via
  the live regtest node: getbalance 300.00000000 (six 50-BTC mining
  payouts tracked), getnewaddress derives, wallet store loads at boot.
- The mainnet node was RESTARTED onto the fixed binary (the running one
  predated the fix; its boot catch-up only stores blocks so it was safe
  so far, but the UTXO-connect handoff runs script verification on every
  input from genesis -- the old binary would have died there). Resumed
  clean at 440,320 stored, no consensus failures.
- Standing lesson: the wave landed with zero gates because "the daemon
  links" felt like evidence. It was not: three distinct bug classes
  (x8-nr syscalls, node_services deref, TLS_ADDR) were all invisible to
  the daemon's happy path and each would have fired mid-IBD. One module,
  one gate -- the roadmap bar exists because of days like this.

---

# DAILY WORKLOG — 2026-09-10 (x86-64 main line)

Continues worklog/2026-09-09.md (sessions 37-43: run 19, the leg lifecycle,
compact blocks, the divergence inventory). Run 19 still on the bench
(tag picker-dead-mark-2026-09-09, epoch 13:19:44Z 09-09); production on
snapshot p since 23:16:38Z 09-09, q staged, not restarted.

## Session 44 (23:45Z 09-09 →): the divergence list, top down

- [x] #154 `compaction-under-apply-2026-09-09` landed 23:5xZ 09-09 (gate
  MAKE_EXIT 0); snapshot q staged on the live link, production kept on p.
- [x] "next hole?" -> the checkpoint cadence (13-24% of block time). "start".
  Reading the cadence found the cause: "within 64 of the tip" is per block,
  and during the download the archive tip is the frontier the apply reaches
  every few passes (run 19: ckpt 72% on a lag-0 tick, 0% on a 105-block
  tick). And a second defect: the caught-up DOWNSHIFT to the steady-state
  memtable fires on any pass that reaches the archive tip, bounded or not,
  so the dbcache-sized memtable #153 gave a fresh sync would have been thrown
  away minutes in. Bulk mode now checkpoints every 1,024 blocks / 60 s, a
  bounded pass carries its batch, the post-loop second persist is gone, and
  only the unbounded drain downshifts. test_utxo_ckpt_batch extended (watched
  to fail 6 ways first); test_utxo_catchup_bounded's "checkpoint == applied
  after every call" assertion pinned the storm and now reads "trails by at
  most the batch". Gating as PR #155.
- [x] "do what core does with coinstats index and fold per block": the
  2026-09-06 bulk-mode deferral (index invalid until a walk at the
  downshift) predated the fold worker and outlived its reason; a fresh sync
  ended with no index, no history rows and a rebuild. Removed: one shape in
  every mode, the worker folds from block 0 (~40% of a core at the bench's
  apply rate), rows per height. test_coinstats_bulk_seed rewritten to the
  per-block contract (worker digest == walk, rows reach the tip, no seed
  walk). Built in a worktree while #155's gate ran. PR #156.
- [x] #156 `coinstats-fold-per-block-2026-09-10` landed; snapshot r staged.
- [x] "next hole?" -> download parallelism (run 19: 16 workers, 124 live
  peers, 11.3 MB/s; the apply idle at lag 0). "re catchup. what does core
  do?" -> every usable peer, 16 in flight each, a 1,024 window above the
  connected tip, assignment by completion, the window's tail the only judge
  (2 s stall, adaptive to 64 s). "Implement Cores shape": every live peer
  downloads up to bmc.catchupworkers (now "at most this many peers", default
  64), the window = 6x in flight (>= 4,096) anchored to the connected tip,
  dlc_stall_tick evicts the tail's holder (labelled), rotation and the rate
  floor only while a free peer exists, the worker-side duplicate help
  removed. test_dlc_rules + a clock-driven stall scenario in
  test_dialhelper. PR #157.
- [x] **Run 19 reached 800,000 at 12h05m** (01:24:55Z): level with Core's
  12h06m, 1h19m ahead of run 16's 13h24m -- on the small memtable, 16
  workers, the per-block checkpoint. ETA to the tip ~6h40m (about 08:05Z).
- [x] #157 `core-download-shape-2026-09-10` landed; snapshot s staged.
- [x] "next hole?" -> tip latency: production 5-20 s behind the Core oracle
  per block (UpdateTip vs our new-block line). "resolve those identified
  items to be in line with core behavior": a passive capture split one block
  (0.8 s RTT peer): 3 round trips of protocol + ~6 s of TCP slow start on a
  40 KB cmpctblock and an 800 KB blocktxn (half the block missing from our
  4,472-entry mempool), then up to 9 s waiting for the rotation's apply.
  Landed: the sweep recognises block invs / pushed headers / cmpctblock /
  blocktxn / block (block-relay-only legs swept too); sendheaders after the
  handshake; an announced leg goes next and legs are not polled for headers
  otherwise (30 s safety net); the three most recent block sources become
  high-bandwidth and their pushes are stored from the sweep via the
  submitblock evaluator; a store ends the rotation so the apply runs at
  once. test_tx_relay scenario 9; the regtest e2e with Core net logging shows
  six pushed compact blocks stored with no pass. New open row 5: mempool
  overlap. PR #159.
- [x] "do all": production restarted onto t 02:37:25Z. First block on t
  stored 27 s after the oracle: the worker sat in an inline top-up (four
  10 s connect timeouts). Every dial is a helper now (DH_MAX 4; a re-dial
  fills its slot when it lands). Row 5's measurement: a per-block
  [cmpct] overlap line with the classifier (never announced / announced
  not requested / requested no reply / orphan / rejected).
  blockfilterindex=1 added to production's conf (the index sat at 966,181
  untended); getblockstats below 965,826 answers with fees (undo complete
  since the 09-09 reindex). tcp_slow_start_after_idle=0 on the host (#160).
  bench-2026-09-10 tag on main for the next fresh run; a monitor on run 19's
  phase.log fires at TIP. PR #161.
- [x] Row 3 (legs through a handoff): legs_sweep_except factored and run
  inside dl_catchup's loop each pass; dlc_workers_for bounded by the span
  (a 40-block handoff forked 64 helpers). PR #162.
- [x] Row 4 (counter drift): the crashed child's deletes never reached the
  WAL; the ghost rollback restored coins still present and utxo_lsm_put of
  a run-resident key counted it as new (+2). undo_restore_cb now looks the
  coin up first (the delete side's gate from 09-03). test_utxo_crash_recovery_bulk
  (same source, -DCRASH_RECOVERY_BULK) watched to fail 153 with the gate off.
  Folded into PR #162 with row 3.
- [x] Row 2 (index self-heal): daemon/index_repair.{c,h} (the coinstats
  supervisor as a module), instances for the block filter index (absent or
  >144 behind) and the address history (base absent), ticked at the
  heartbeat; test_index_repair. Folded into PR #162.
- [x] #161 landed 03:19Z; production restarted onto u 03:19:31Z. Helper-
  dialed legs died within 1 s (v2 cipher state in the child), and every
  failure line read "not dialled again for 0 min" (the worker never had a
  dial memory: created by dl_catchup only). Rolled back to t 03:29:54Z.
  #162 (rows 2/3/4) landed 03:33Z. Fix: helpers dial v1 (open row 3: hand
  the v2 state over), the worker creates the dial memory. PR #163.
- [x] #163 landed 03:49Z; production onto v 03:49:49Z. Helper legs survive
  (v1), backoffs read "10 min" -- but the block-relay-only picker hogged the
  helpers on one refused host and the legs sat at 3/4: a helper budget
  (extra dials leave two free), the picker honours the dial memory and
  rotates, the child starts with a clean reason. PR #164. Block 966,299
  on t: announced pass at Core's second, 10.5 s fetch from one slow peer,
  apply 0.14 s after the store; the filter index adopted and synced.
- [x] #164 landed 04:21Z; production onto w 04:21:22Z: 15 block-relay-only
  dials to 15 hosts, no helper starvation, legs 6/8 at ten minutes, the
  60 s closes are cloud nodes' inbound eviction (dial memory now keeps
  them out). Block 966,302: announced at Core's second, full-block fetch
  11.5 s -- the sweep had been swallowing the peer's sendcmpct since
  #159, so compact blocks were off on new legs. PR #165.
- [x] #165 landed 04:49Z; production onto x 04:49:35Z. First blocks on x:
  966,304 announced 0.7 s after Core, compact via the pass, applied at 9.2 s
  (690 KB blocktxn); 966,305 announced 0.6 s after Core, the FIRST mainnet
  high-bandwidth push (136.62.60.173) stored at 3.9 s with a 460 KB
  blocktxn. Row 2's measurement: 87-98% of the fetched transactions were
  never announced to us -- coverage, not policy. Inventory row updated.
- [x] Row 3 (v2 on helper legs): bip324_t_export/import and bmc_v2_export/
  import; the helper child exports after the handshake, the parent imports
  before its first write; the v1 gate removed. test_v2transport: the
  handover scenario end to end. PR #167.
- [x] Row 2's code piece: txr_drain_pending requests what a leg announced
  and nothing fetched, up to 32 per poll within the 100 in flight;
  test_tx_relay scenario 10 watched to fail first. PR #168. Coverage
  (inbound peers) waits on the router: both 8332 and 8333 unreachable
  from outside; inboundrelaypercent=100 set in the conf.
- [x] Row 1 (leg service): a leg's pass runs in a helper child with a report
  back (verdict, tip, sendcmpct, rewind, the v2 session); the parent replays
  the bookkeeping once; four passes at once; the sweep skips a busy leg.
  PR #169. Only row 1's coverage half (inbound peers) remains open.
- [x] #168 and #169 landed; production onto y 05:58:51Z. y's first minutes:
  the current leg's ping tick ran after its pass helper took the socket
  (a stale v2 cipher's ping: the peer hung up, "EOF on the first read" on
  four legs in a minute), and a helper export could leave cipher-counted
  bytes unflushed. Fix: nothing touches a busy leg; flush before export.
  PR #170. (The rollback to x was blocked by the session's classifier;
  the operator can run it.)
- [x] #170 landed; production onto z 06:22:50Z -- still churning: the
  parent's tip announcement, the per-rotation tx inv and the self-address
  announce wrote to busy legs (a child holds the v2 cipher). Rolled back
  to x 06:31:57Z. Every parent write site now skips a busy leg. PR #171.
- [x] "why dialing twice?" (06:35Z on x): the block-relay-only picker and
  the top-up dialled the same first candidate in one rotation. One dial
  per host at a time (dh_inflight_host), in PR #171.
- Waiters: run 19's 800,000 mark (Core 12h06m, run 16 13h24m); production's
  first hour on p.
- [x] #171 landed; production onto aa 06:51:14Z -- still churning (3/9,
  "EOF on the first read" seconds after install, v1 and v2 alike, four
  dial helpers exhausted). Rolled back to x 07:02:01Z as promised.
  Reproduced on regtest against Core with -debug=net: the leg died every
  30-70 s and Core logged "Connection reset by peer"; strace: the parent's
  reorg probe (keyed on "the pass returned nothing", which it now always
  does) armed its 60 s alarm within a millisecond of the pass child's fork
  and read the same socket. Probe before the pass, on an idle leg whose
  last report was empty; no inline pass; DH_MAX 8. The e2e holds one leg
  through eight blocks with zero resets. PR #172.
  Also in #172: the child's stale [cmpct] overlap line (a static that was
  the parent's snapshot at fork) -- compared at the pass's entry now.
- [x] #172 landed 07:41Z; production onto ab 07:41:55Z. First eight minutes:
  peers 9/10 on every heartbeat, zero "EOF on the first read", zero pass
  crashes; the eight closes are the known classes (the 60 s crawlers, the
  0-3 s inbound-full evictions). Block 966,325 stored at Core's second via
  a compact block (92.9% from the mempool).
- [x] Run 19 finished: 965,703 (Core's finishing height) at 18h38m against
  Core's 21h10m (2h32m ahead; level to 800,000, 2h24m ahead at 900,000);
  its boot tip 966,211 at 18h39m, live at 966,327 with 8/9 legs, txouts
  165,199,143 = the oracle's. README marks table updated; muhash check
  against the oracle at the tip in progress; archive rsync to
  /mnt/10gbusb1/archive2/run19-20260909 at 96% (the lsm blob map follows
  in the final pass).
- [x] Run 19 muhash: the monitor's step failed (CLI read timeout) and printed
  an empty-equals-empty PASS; the running daemon's live-walk RPC gave a
  different hash at 966,327 with every aggregate equal. A full-field dump
  of both sets (165,192,821 coins: value, height, coinbase, script hash)
  was identical to production's. Stopped the bench at 966,331 (08:58Z) and
  measured offline: muhash 2b9f8bfc..7f85cc71 = the oracle's. The set is
  right; the live-walk RPC mid-apply on the 09-09 build is not. Final
  archive pass running; the fresh run on bench-2026-09-10 needs the SSD
  cleared (779 GB free, run 19 holds 973 GB) -- the operator's rm.
- Open from ab's first hour: three "ours/pass-session -- the pass could not
  export its v2 session" closes on fresh v2 legs (0-2 s old), and two
  sync-budget closes at where=4 after 14-31 min. Next batch.
- [x] ab's "pass-session" closes: the pass helper's 64 KB session blob could
  not hold a headers reply half received at report time, so the export was
  refused and a healthy fresh leg was closed. Blob 8 MB; the refusal reports
  the bytes needed; bmc_v2_pump_once for the test. test_v2transport:
  a handover mid-message (200 KB block, 64 KB refused, 1 MB carried);
  test_dialhelper pins the cap (watched to fail). PR #174.
- [x] #174 landed 09:20Z; production onto ac 09:20:48Z. First nine minutes:
  peers 10/12, zero closes of ours, zero pass-session, six v2 legs.
  Run 19's datadir is archived (file-by-file match) and moved aside to
  /mnt/2tbssd/bmc-bench-run19; the fresh run on bench-2026-09-10 (through
  #174) waits for the operator's rm of that directory (760 GB free, the
  harness wants 1 TB), then `/mnt/2tbssd/fresh_run.sh bench-2026-09-10`.
- [x] 11:55Z: the operator made /mnt/10gbusb1 the archive for past runs;
  run 19's datadir removed from the SSD (1.7 TB free), bench-logs and the
  run 2-16 evidence dirs copied to archive2. monitor_fixed.sh: muhash over
  HTTP with an hour's timeout, an empty value FAILS. Run 20 launched
  12:00:36Z on bench-2026-09-10 (24d74ec5); the boot line reads BULK
  2^25/6144 MB for the first time on a fresh sync. benchmarks/2026-09-10.
- [x] Run 20 (launched 12:00:36Z on bench-2026-09-10) STALLED at 18,561
  blocks. Root cause: dlc_stall_tick (#157) evicted the worker holding the
  window's oldest missing chunk without recording the peer, so dlc_pick_peer
  handed the same AWS listener chunk [18561,18600] fourteen times while the
  timeout backed off to its 64 s ceiling; all 64 workers parked behind it.
  The dead-weight eviction has banned its peer for the run since 09-06 --
  the stall rule now does too, under the same manual/usable-floor guards.
  test_dialhelper: the ban assertion watched to fail with the fix reverted,
  plus a floor case. Also removed the sample conf's bmc.cmpctrecv, which the
  daemon has not accepted since #148 (the operator spotted it). PR #177.
- [x] "why aren't we defaulting to 8 like Core?" -- because nobody chose 64:
  it came in with the par fix (43950d24) as DLC_WORKERS_HARD_MAX adopted as a
  default, and before that fix the harness ran par=8, so the count WAS 8.
  Core has no such setting (8 outbound full-relay, 16 in flight each, window
  1024, one thread). Measured first: the link is 2500 Mb/s, we sustain
  11 MB/s (~88 Mbit) on the wire with nothing on tun0 and no shaping, and
  16 workers (run 19) led 64 (run 21) at every early mark for the same
  11.2 MB/s. Also: Core is pinned at 8 by net.h:1124 whatever maxconnections
  says, so the harness's maxconnections=64 never handicapped it -- every
  published comparison ran bmc at 16-64 download peers against Core's 8.
  Default now 8, ceiling still 64. test_node_config watched to fail at 64.
  PR #178.
- [x] First RPC of our own: bmcgetdownloadinfo (the parallel download's
  worker->peer->chunk->rate map, window, tail, stall timeout, bans). Core has
  no counterpart -- 8 peers in one message-handler thread, no worker to
  report. Named with the bmc prefix on the same rule as the bmc.* keys.
  Added bps_recv to rpc_peer_t and a dl_* aggregate block to node_status_t,
  published by the catch-up parent on the dlpeers tick. Registration is
  three lines in rpc_node.c (cmd, NODE_METHODS, dispatch) plus a test.
  Watched to fail by unpublishing `window` -- NOT by deleting the dispatch
  line, which fails -Werror and leaves a stale binary that passes. PR #179.
- [x] Read bmcmonitor (/storage/bmcmonitor) and found two node-side issues.
  (1) The RPC I shipped in #179 was unreachable from it: its allowlist is
  default-deny over READ-SHAPED PREFIXES and "bmc..." matched none, so
  bmcgetdownloadinfo -> getbmcdownloadinfo (leading "get" passes the existing
  rule; widening the allowlist would pre-authorise a future bmcset*, which
  its own comment calls "a hole, not a guard"). (2) Its AGENTS.md records two
  builds answering getnettotals 0/0 + getpeerinfo [] vs 11.56 MB/s + 21 peers
  under the SAME subversion, with "the log banner remains the only build
  attestation on this box" -- so getnetworkinfo now carries bmc_build_commit
  and bmc_build_dirty from a generated build_gen.h (rewritten only when the
  value changes; only rpc_node.o depends on it). The 0/0 symptom is NOT on
  the current build (ae: getconnectioncount 9, getpeerinfo 9, real bytes) --
  it was build-dependent, which is why attestation is the durable fix. PR #180.
- [x] Operator's rule: our commands are ALWAYS prefaced bmc*. The #180 rename
  to getbmcdownloadinfo is reverted -- the accommodation belongs in the
  consumer, not in our naming. bmcmonitor now allows the marker PLUS a read
  verb (bmcget/bmclist/bmcestimate/bmcverify), names the mutating bmc shapes
  in its deny list, and still denies an unknown bmc* by default; a bare "bmc"
  prefix would have pre-authorised bmcsetban. bmcmonitor fdec42f, 369/369.
  Also there: README.md and AGENTS.md were root-owned so its own
  counts:fix died with EACCES; ownership restored. PR #181.
