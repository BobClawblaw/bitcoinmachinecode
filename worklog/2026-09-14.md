# DAILY WORKLOG — 2026-09-14

> **Backfilled 2026-09-16 from the commit record**, not written live. The daily
> file was missed on the day; PR numbers, commit subjects and landing order are
> from git and are reliable, but the session narrative and any reasoning that
> never reached a commit message are not recoverable and are not invented here.

Cluster mempool, staged — and the discovery that two of the seven planned stages
were already done. PRs #197–#210. The sweep was retired.

## Cluster mempool

- [x] `c1aa7a2a` stages 1–2: chunking and topological validity,
  exactly specified.
- [x] `0ecd887a` stage 3: a linearization, **and two holes the revert check
  found in its tests**.
- [x] `2761a908` discovery, and a 64-bound that reverted to nothing failing.
- [x] `1dc26f3f` stage 4: getmempoolcluster answers real clusters.
- [x] `cccae008` stage 5: post-linearization, **and proof it is not a no-op**.
- [x] `924025a6` one bound, Core's, everywhere — and why the algorithms are not
  merged yet.
- [x] `7dab8574` **the plan was corrected: two of its seven stages describe work
  already done.** Eviction and mining were already cluster-aware; the real
  problem was three implementations of one idea in one tree.
- [x] `b33cb056`/`314468b0`/`c1853542` gbt: characterisation tests for the
  selection paths nothing pinned, a varied-size fixture for the cluster-skip
  rule, then the mining path moved onto the one tested linearization with no
  behaviour change.
- [x] `82cf0489` eviction moved onto the same linearization — **and four linkage
  lessons** (symbols `nm` shows in the daemon, not what grep suggests).
- [x] `7d79835d` chunk denominator: measured, recorded, **deliberately not
  fixed** — and my claim about it corrected.

## The sweep, retired

- [x] `72054754` cap connections and arms — **the w=32 arm saturated the
  operator's LAN.**
- [x] `310d084e` **retired: it answers a question this project should not act
  on.**

## Other

- [x] `6e70af49` serve: rate-limit inbound connections per source address,
  before the fork.
- [x] `c503d4ef` psbt: a zero-input transaction is not a decode failure —
  `createpsbt [] {}` said "oom".
