# DAILY WORKLOG — 2026-09-12

> **Backfilled 2026-09-16 from the commit record**, not written live. The daily
> file was missed on the day; PR numbers, commit subjects and landing order are
> from git and are reliable, but the session narrative and any reasoning that
> never reached a commit message are not recoverable and are not invented here.

The run 22 scare resolved, run 23 passed, and RPC field parity became a gate.
PRs #183–#189.

## Run 22 was never wrong

- [x] `4e692c4b` **capstone: run 22's UTXO set was right; the check that failed
  it was reading a moving set.** The instrument, not the node. (This is the
  finding recorded in memory as "No coin-height bug".)
- [x] `389060eb` **run 23: PASS** — a second fresh sync whose UTXO set is
  identical to Core's.
- [x] `cc266040` bench register: the Tier 4 IBD runs, and the `ionice` handicap
  that was never a control.

## RPC parity stops being a claim and starts being a gate

- [x] `6651b4f3` no method is missing — the gap is **response shapes**.
- [x] `20cf0d93` a frozen capture of Core v31.1's response shapes now gates.
- [x] `77fa8e8d` a field-level differential, and the **30+ fields** it found
  missing.
- [x] `7263c37d` / `c6fcc22e` getpeerinfo: 38 of 38 against v31.1, one builder
  for every peer, no additive keys left.
- [x] `bba89d88` getmininginfo, getmempoolinfo, getblock, getpeerinfo match.
- [x] `a39d4d97` `getrawmempool true` carries the ancestor graph in one pass
  instead of n scans; `d6110c56` getmempoolcluster answers the singleton case.

## Ops

- [x] `3fa1ecd0` the Core oracle is a **systemd unit** now, not a process that
  happened to survive — manage with systemctl, never by hand.
- [x] `6b532902` the oracle section dropped from shipped ops docs: that rig is
  local, not something the repo ships.
