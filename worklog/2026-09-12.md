# DAILY WORKLOG — 2026-09-12

Bitcoin Machine Code — `bmc_osx` branch: fresh macOS/Apple Silicon port.
One file per day; newest top. Times local (CDT, UTC-05:00). Convention
inherited from x86: bullet per meaningful action, with evidence and outcome.

## Round 1 — merge main (131 commits), relink green, deploy home moved out of /tmp

- [x] Fetched main over HTTPS (SSH publickey still blocked; gh credential
      helper) — 131 new commits (#175–#189: RPC parity wave, runs 22/23,
      getmempoolcluster). Merged into `bmc_osx` (cce2f5d7).
- [x] Two add/add worklog conflicts (09-09, 09-10): both streams wrote the
      same day files. Kept both — osx journal first, x86 section second
      under an "(x86-64 main line)" heading.
- [x] Post-merge native build caught two semantic conflicts, fixed in
      045c8c07: SYS_ioprio_set (Linux-only) guarded in utxo_live.c;
      build_daemon.sh now generates build_gen.h (rpc_node.c includes it
      since the RPC parity wave).
- [x] The link's undefined-symbol list was longer than the first tail
      showed: dialmem_* (6), inflight_* (3), ir_configure/ir_tick,
      hdrtree_* (3), g_block_fetch_hook, g_cmpct_hook_fallback,
      node_start_height, node_listen_port_be. Fixed in 061bfeca: four C
      twins added to the build list (inflight, index_repair, dial_memory,
      hdr_tree — all plain C, no Darwin hazards), four data symbols added
      to port/osx/bitcoind.S (defaults = the x86 defaults). LINK OK,
      zero undefined.
- [x] Mainnet IBD had been STALLED since 09-11 23:13 (21.5h): the sole
      peer's connection died silently and the log showed "peers
      exhausted / chunk abandoned -> retry ring" every 2 min, no redial.
      The running binary predated the merge.
- [x] Relocated the deploy runtime out of /tmp (boot-time clearing plus
      the 3-day daily cleanup made a 431 GB datadir there one reboot away
      from loss): /Users/xian/bmc_osx_deploy/ now holds bmcbitcoind,
      bmc_mainnet (431 GB, instant same-volume rename), bmc_mainnet_conf,
      logs/ and start.sh/stop.sh (pgrep scoped to the deploy path, never
      by binary name). /tmp/bmc_mainnet(+_conf) left as symlinks. The dev
      repo stays at ~/bmc_osx/bitcoinmachinecode; builds are copied to
      the deploy dir.
- [x] Relaunched on the merged binary (boot9): SIGTERM the old tree clean
      (staged=0), boot green (0 FATAL), IBD resumed 777,681 -> 780,601 in
      the first 90 s, zero consensus failures. The config warning
      "connect=... is not a usable number" predates the merge (present in
      boot7) and the peer connects regardless — noted, not a regression.
