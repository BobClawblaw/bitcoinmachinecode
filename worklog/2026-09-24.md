# DAILY WORKLOG — 2026-09-24

Bitcoin Machine Code — `bmc_osx` branch: macOS/Apple Silicon port.
One file per day; newest top. Times local (CDT, UTC-05:00); log excerpts
keep the node's own UTC stamps.

## Round 5 (2026-09-25) — synced_headers on a quiet leg; the Mac pass had no fetch gate

- [x] **Core's initial getheaders from pprev (ad11ac5a).** Every new leg,
      with a recent tip, is asked for headers from the block below our best,
      so a synced peer answers with our tip at once -- where a leg that
      announced nothing used to read synced_headers -1. block_fetch_gate
      also records a header naming a block we have.
- [x] **The Mac's node_sync_multi never called the fetch gate (4500868c).**
      port/osx/bitcoind.S dates from 09-10 and missed x86's 09-09 sync
      changes: no "another leg holds it / already stored" check (every
      leg's pass fetched every block), and a refused duplicate (-2) failed
      the pass (where=10, a strike) where x86 ends it well. Both ported.
      test_sync_gate's gate half: 4 of 7 fail on the old object, 7/7 now;
      test_sync_dup fail -> pass.
- [x] **A background leg's getheaders was skipped (61eaa0f9).** Traced with
      a signet debug build: dl_tip_is_ibd() read the tip through the
      worker's store handle and failed closed right after a pass child
      appended, so legs installed then were never asked. leg_tip_recent()
      falls back a few heights, to node_serve_block, and to the last tip
      time read. Verified on signet with a block landing during boot (10 of
      10 legs read the tip) and on mainnet (8 of 9; the 9th got an empty
      reply). Deployed 08:16Z; rollback `bmcbitcoind.pre-61eaa0f9`.
- [ ] Open: why store_read_at failed for six heights at once in that
      window. Anything else reading the store then would fail too.
- [ ] Open: the rest of the Mac bitcoind.S drift from x86 -- no compact
      blocks inside the pass (test_cmpct_fallback 4 fails) and the version
      message's live fields (test_bitcoind 5 fails: timestamp not the wall
      clock, a constant nonce, stale start_height, addr_from port).
- [ ] Open: getpeerinfo reports block-relay-only background legs as
      `outbound-full-relay` (the dial log says "dialing as
      block-relay-only").

## Round 4 — synced_blocks, the build script's missing header deps, signet IBD green

- [x] **getpeerinfo synced_headers / synced_blocks (aada0228, shared -- for
      main).** Both were a constant -1 for every relay leg (rpc_node.c).
      rpc_peer_t gains `best_known_height` (appended). The worker raises it
      when a block the peer announced or sent is stored, and when a sync
      pass on its connection stores blocks; an announced block we lacked is
      resolved by the peer-table sweep once any path stores it.
      synced_blocks = that, capped at the connected tip; only a positive
      height counts (the presynced_headers rule). test_rpc_node +9 (6 fail
      on the old file). Deployed 00:03Z; after block 968,471, 10 of 11 legs
      read 968,471 for both.
- [ ] Open (docs/PARITY_RPC_FIELDS.md): a leg that has announced nothing
      since connecting stays -1. Core sends every new peer a getheaders
      from `m_best_header->pprev` (with a recent tip) so the reply carries
      one header, and reads the tip within seconds. The 11th leg had relayed
      transactions for 20 min without announcing a block. My first write-up
      said Core also reads -1 there; it doesn't, corrected.
- [x] **build_daemon.sh had no header dependencies (2cb7a418).** An object
      was rebuilt only when its own .c changed, so growing rpc_peer_t would
      have linked objects disagreeing on the shared peers[] stride. Any
      header newer than an object now rebuilds it (~15 s for all). Checked
      today's other header edits against the running binary: node_config.h
      only gained a prototype, and dlc_rules.h is included by main.c alone
      (rebuilt with it) -- nothing stale in production.
- [x] Mainnet redeployed on 2cb7a418 at 00:03Z (1 s stop; rollback
      `bmcbitcoind.pre-2cb7a418`). Tip 968,471+, IBD false. The heartbeat's
      occasional `sync_failing=1` (15 of ~540 lines) is one leg with a
      failed pass, dropped at three -- ordinary churn.
- [x] **Signet IBD green (p3's last item).** `~/bmc_signet/` (conf, start/stop
      scripted like the deploy, ports 3853x, listen=0, no indexes), same
      2cb7a418 binary. 39 DNS-seeded candidates, 30 live; 323,566 headers in
      23 s; all blocks from genesis in 21:51, applied to the tip in 25 min,
      **zero rejects**. Scripts skipped below assumevalid 293,175, evaluated
      above. Tip 323,567 hash 0000000eaf18..2de9 = mempool.space's. The apply
      was the bottleneck through ~255k-275k (33-47 ms/blk, LSM get/put) --
      the testnet4 perf gap again, not correctness. The node stays up
      following the signet tip.

## Round 3 — testnet4 settles ad4f0d9b; four bugs the testnet4 node found; production at the tip

- [x] **Merged main again (c002a62b):** 18 commits -- main's ports of this
      branch's fixes, and the mux-dial-gate batch. Resolved per the x86
      side's `worklog/2026-09-24-note-for-osx.md`: `tx_verify.c` ours whole,
      main's side in all five `main.c` dial-gate hunks, the Makefile `test:`
      line combined. test_mux_dial_gate fails 2 of 11 on the Mac ("not
      re-dialled at once"). That is the environment: `lo0` carries only
      127.0.0.1, so dials to .3/.4 hang to the 10 s connect bound and
      `drain()` gives up at 5 s. Its rate checks pass.
- [x] **The ad4f0d9b question, settled: revert it.** Static: nothing in the
      download worker verifies transactions concurrently with block
      connect. The one extra submitting thread (the mempool.dat reload)
      runs in the serve parent and hands transactions to the worker.
      Dynamic, on testnet4 from the network (`~/bmc_t4repro/`):
      - node B, current code with ad4f0d9b's `__thread` arenas made shared
        again and 182c0d87's condvar pool kept: synced to 153,882, with
        every script evaluated above assumevalid's 123,613. Zero rejects,
        through all three original failure heights (124,864 / 125,673 /
        126,361).
      - a chainstate replay with `assumevalid=0` rebuilt past 140,000, zero
        rejects.
      - node A (a4c0e2d3, the original post-TLS_ADDR binary) spun ~16 cores
        in sem_post/sem_wait -- 182c0d87's no-op semaphores, visible
        directly. It was stopped: it carries both suspected causes, so its
        failure could not tell them apart.
      The x86 session reproduced the crash on Linux (19f26cfc: ad4f0d9b
      segfaults at 124,864; main's copy applies all 18 blocks). The same
      replay then ran natively here, with fixtures pulled from node B over
      RPC: main's `tx_verify.c` applied all 18 in 3 of 3 runs, the old
      bmc_osx HEAD also applied them all, and ad4f0d9b segfaulted at
      124,864 in 2 of 2, right after 124,863 applied. Revert committed
      here as deab9fe2 (main's `tx_verify.c` whole; b4cb4e55's realloc
      re-resolve is redundant there, since `wit`/`witlen` are resolved from
      `wit_off` after the parse loop). 19 tx_verify gates pass natively
      with it. The six `*_diff` tests against x86 asm twins
      (`txv_parse_asm`, `tapagg_*_asm`, ...) can't run on a Mac: the twins
      were never ported, and the tests crash the same way before and after.
      Two gates were not run: test_ir5_sighash_cache needs `cpu_set_t`
      (Linux), and test_schnorr_thread_stress takes arguments. To rerun on a
      Mac: `worklog/2026-09-24-ad4f0d9b-repro/build_osx.sh` (tn4_replay.c
      takes `#define` renames under `__APPLE__`, since ld64 has no `--wrap`)
      and `fetch_tn4_rpc.py` (reads any cookie-auth node, a bmc node
      included).
- [x] **Four bugs the testnet4 node found:**
  - 56519b5b: the archive check hardcoded mainnet's frame magic, while
    NET-15 writers use the chain's own. It flagged valid committer blocks on
    testnet4/signet/regtest, and STO-11 blanked them (node B, 153,877-8).
    Mainnet is unaffected. test_archive_check 5c2.
  - bd1700d4: a hole below the archive tip was never re-fetched under
    `bmc.bootcatchup=0`. A boot repair now forces the catch-up, and the
    worker triggers the downloader when the apply sits at a hole. Live on
    B: the rebuild stopped at 153,876, the downloader rewrote
    [153877,153892] in 2 s, and the apply ran to the tip.
    test_parallel_trigger +7. (A first try looked dead only because B ran
    a binary built before the trigger existed -- the unit test compiles
    main.c itself; the daemon needs its own rebuild.)
  - **05c01822 (shared, x86 porting it):** a reorg forking ABOVE the
    applied height moved the apply forward without applying: the
    post-disconnect rewind advanced it to the fork, and the reconnect
    applied replacements onto the stale set. Node B, 23:01Z: applied
    153,876 -> 153,892 with 153,877..887 never applied. Now the rewind
    only moves down, and a replacement is applied only when it is next in
    turn; the catch-up connects the rest in order. New test_reorg case
    (HEAD: reorg -1, 5 of 11 coins; fixed: matches the independent model).
  - **a419e3e0: `chainwork_cmp` was `int` in the twin, `extern long`
    everywhere else.** On arm64, -1 read as 4294967295, so a LIGHTER chain
    compared as heavier. Nine store twins had the same mismatch
    (store_append's -1 read as a valid height). All now return long, like
    the x86. test_reorg passes on arm64 for the first time (19 failures ->
    0).
- [x] Production: reached the mainnet tip (968,463, IBD false) on
      418b9129 -- which still had the chainwork_cmp bug -- and was moved to
      a419e3e0 at once (backup `bmcbitcoind.pre-a419e3e0`, 1 s stop, boot
      clean). Earlier this round, 418b9129 (the mm cache fix) took the Mac
      from ~5 to ~11.5-14 blk/s.
- [x] **getpeerinfo showed 0 peers on the Mac (9e728d57), deployed 23:29Z.**
      Checking the tip: getpeerinfo, getconnectioncount and
      getnetworkinfo all answered 0, while the heartbeat said peers=11/11
      and blocks kept arriving. dl_publish_peer_table has treated a failing
      getsockopt(TCP_INFO) as a dead socket since run 26 (09-16), and
      port/osx/compat maps TCP_INFO to an unassigned option on purpose. So
      every live leg was retired on every sweep. On __APPLE__ the sweep now
      reads TCP_CONNECTION_INFO (as dh_tcp_state does) for liveness and
      tcpi_txbytes/rxbytes. After the deploy all three RPCs report 10
      outbound peers with real byte counts, matching peers=10/10. Rollback:
      `bmcbitcoind.pre-9e728d57`. (The running binary's build stamp reads
      e1d90861: the same change, built before a rebase onto 30980211
      renumbered it.)
- [x] Cleanup after the resync: `reindex_chainstate.done` and the three
      `*.pre-reindex` backups (index/headers/chainwork from the ~274k state)
      deleted. The four stale incident notes and `bmc.bootcatchup=0` (the
      compiled default since ad4e7cf1) removed from `bmc_mainnet_conf`;
      backup `bmc_mainnet_conf.pre-cleanup`. No behaviour change.
- [x] Node B stopped. `~/bmc_t4repro/` removed (~18 GB: node B's testnet4
      datadir, node A's remains, the `src_A`/`src_B` worktrees -- removed
      through git, so the repo is back to one checkout).
- [x] `~/bmc_osx_deploy/forensics-274443/` deleted (54 GB, the UTXO state
      at the 274443 halt; f9368f9e is proven in production). Deploy-home
      backups pruned to one rollback step: `bmcbitcoind.pre-a419e3e0` and
      `bmc_mainnet_conf.pre-cleanup`. Nine older `bmcbitcoind.pre-*`
      binaries (each rebuildable from its commit), `bmc_mainnet_conf.pre-f9368f9e`
      and the `*.sh.pre-pgrepfix` scripts removed.

## Round 2 — the download window's tail, the 10-minute stop, the bug report answered

- [x] **The window's tail was judged per CHUNK, not per block (0a11830e).**
      After the 13:56 restart the download sat at 823,243 for its last
      eleven minutes: the apply 4,096 behind (window anchored to the
      connected tip, so full), and three peers in a row holding chunk
      [823243,823282] dropped after 2, 4, 8 s with "completed 0 chunk(s)/0
      block(s)", each banned for the run; the last measured 855 KB/s.
      dlc_stall_tick timed the holder from when it became the tail, so the
      2-64 s timeout was a budget for ~60 MB of recent blocks. Core clears
      m_stalling_since on every block the peer delivers. The worker's
      progress hook now stamps `last_block_ms` in its stats slot and the
      tick restarts the clock from it (`dlc_stall_clock`, dlc_rules.h).
      test_dlc_rules +5 cases.
- [x] **The 10-minute stop (ff623431).** 18:56:25Z the serve parent
      forwarded SIGTERM; the download worker's loop went silent (no [dlc]
      status line; utxo_live applied 24 blocks in ten minutes) and noticed
      the signal at 19:06:46Z, 11 s after the parent gave up at 610 s and
      exited. No EOWNERDEAD, so not the mempool lock. The worker's gettxout
      service point (`txoq_service`, "never blocks") answered with a
      blocking send on a socketpair that Darwin sizes at 8 KB each way
      (Linux ~208 KB), and replies to queries the RPC side timed out on
      are never read. tests/test_txoq_shutdown reproduces the block within
      a few rounds -- and shows that shutdown() of the parent's end does
      NOT wake a Darwin sender (the first fix tried exactly that; the test
      caught it). Fix: 256 KB each way, SO_SNDLOWAT = the largest reply,
      and a request is read only when POLLOUT says its reply fits. A parent
      that stops reading gets no answers (gettxout refuses), the worker
      keeps connecting. **Fits the stop; not proven from the log** -- the
      14:34 stop of the old binary was clean (4 s), so the next slow stop
      gets a `sample` of the worker.
- [x] **Bug #1 of BUGS-2026-09-24 re-read: nothing for the download
      worker to do.** Undo is written at CONNECT, never downloaded. The
      rev files ended at ~274k because the apply halted at 274443, and the
      public tip then jumped to the stored 968k (fixed in b2f4b43c); the
      chainstate rebuild regenerated undo. getblockstats at 821,620
      answers. The report's proposed "dlc treats undo chunks as required"
      has no object; the [dlc] line already carries `applied=`/`lag=`.
- [x] Deployed ff623431 on the m5ultra (14:35; the previous binary kept
      as `bmcbitcoind.pre-ff623431`). Answers to the report:
      `~/bmc_osx_deploy/BUGS-2026-09-24.response.md`.
- [x] Darwin test notes: test_dlc_rules, test_dlc_stop_sigign and
      test_txoq_shutdown run natively (main.c-as-TU tests link against
      `port/osx/daemon_out/*.o` minus main.o). test_dlc_interleave and
      test_dlc_wire_bytes do NOT run on a Mac as written: TCP_QUICKACK is
      Linux-only, and their fixture binds extra 127.0.0.x addresses, which
      Darwin refuses without an `ifconfig lo0 alias` (root). HEAD and the
      fix fail identically at `bind`.
- [x] **Merged #297 from the x86 side (a1c3df00), deployed 15:54.**
      txv_worker_thread wrote `g_txv_results` by name, and that name has
      been `static __thread` since ad4f0d9b, so each worker thread wrote
      through its own NULL instance: SIGSEGV on any tx with
      TXV_PARALLEL_MIN (8)+ inputs verified threaded. Every relayed 8+-input
      tx after IBD would have hit it, and the near-empty IBD mempool is
      probably why the node never did. Also, the t1 tap pool argument was
      the `tapagg_t*` passed as the `bytepool_t*`. Verified on Darwin:
      test_tx_verify_parallel and test_taproot_parallel_arena SIGSEGV
      (rc=139) on d5211c26 and pass with the PR. These are linked through an
      archive of `daemon_out` objects so the tests' own stubs win, plus
      `-undefined dynamic_lookup`.
- [x] **The mm cache thrashed on a slot collision (343bc1a7), deployed
      16:09. The Mac went from ~5 to ~11.5 blk/s.** Asked why x86 AMD does
      ~7 blk/s to the Mac's ~6, I sampled the apply thread for 10 s:
      ~5,300 of ~10,000 samples were munmap/open/mmap/close under
      lsm_run_lookup_mm (via tx_verify_block_connect_all and
      undo_capture_and_del). The per-thread mapping cache was
      direct-mapped (slot = run_no % 64), and the 15 GB base run 9040 shared
      slot 16 with a fresh run 9232. A lookup checks the newer run first and
      then finds an old coin in the base, so every lookup remapped both.
      Fully associative with LRU now; 64 slots hold every live run.
      tests/test_lsm_mm_collide: HEAD 2,001 maps for 2,000 lookups, fixed
      2. test_lsm_mmap_diff still passes. Post-fix sample: no munmap, open
      or mmap left; the time goes to lsm_run_lookup_mm (the real search),
      WAL writev and axt_undo_cb_fn. Rate 876,148 -> 886,179 in 14.5 min.
      x86 confirms asm/utxo_lsm_mm.c has the same collision (~16 us per
      remap on Linux; both x86 datadirs have a 13 GB base run with a slot
      coming due) and is porting the fix to main.
- [x] **dbg_txv_parse's two unused locals removed (8b0da7b0)**, so
      tx_verify.c builds under -Werror. Raised in #297 and in the x86
      side's note (`worklog/2026-09-24-note-for-osx.md`, e06bedf2), which
      also covers the resolution plan for the next merge of main
      (mux-dial-gate): keep ours for tx_verify.c, take main's main.c per
      hunk, union the Makefile test: line, and expect test_mux_dial_gate
      to need lo0 aliases on a Mac.
- [x] #297 asked whether ad4f0d9b (thread-local arenas) should be
      reverted -- settled in Round 3: yes (deab9fe2), shown on testnet4
      and on x86.
- [x] After the resync, the reindex marker, the `*.pre-reindex` files and
      the stale conf notes were removed (Round 3).
- [x] Stall rule under load: 14 evictions in ~2 h 20 min of window-full
      downloading after 0a11830e (before it, a ban every ~17 s), and the
      download reached the tip. Six of the evicted peers had delivered
      nothing at all.

## Round 1 — the m5ultra bug report: the h=274443 halt, undo, addrindex, the dial storm

The BlockYard monitor's report (`~/bmc_osx_deploy/BUGS-2026-09-24.md`,
four items) against the first full production IBD. Fixed in order of
blast radius; the UTXO set rebuilt from the archive on the fixed sorter
(reindex-chainstate) with `bmc.bootcatchup=0`.

- [x] **The h=274443 FATAL was the sorter, not the chain (f9368f9e).**
      utxo_lsm_twin's radix variant C read the digit at
      `A[eidx * 64 + byteoff]` with a u32 eidx, which wraps at 2^26: a
      70,992,972-descriptor flush scrambled its tie groups, and the run's
      first-match scan reported a live coin absent. x86 zero-extends. Two
      latent x86-parity fixes in the same sorter. tests/sort_repro3.c
      simulates the flush at full scale (HEAD: 94 violations at 71M;
      fixed: 0 at 71M/100M/150M, ASan clean).
- [x] **The worker that ignored SIGTERM for 2.5 h (95041ee1).** Under the
      worker's SIGCHLD = SIG_IGN a blocking waitpid on a live child sleeps
      until every child exits; dlc_stop_workers blocked on its first
      SIGKILLed helper. Bounded WNOHANG reaps everywhere in the worker.
      tests/test_dlc_stop_sigign.
- [x] **The dial storm (06f0bc47, 63afd44c).** Darwin ignores SO_SNDTIMEO
      on connect(), so "Interrupted system call" was main.c's 20 s budget
      alarm: connect is now non-blocking + a 10 s poll. And on Darwin an
      SCM_RIGHTS socket arrives DEAD when the sender exits before the
      receiver's recvmsg; the dial helper now holds its end until the
      worker has it (212 of 261 leg closes were that). tests/test_dialhelper 1b.
- [x] **A halt froze nothing (b2f4b43c).** With live tracking switched off
      the public tip fell back to the STORED tip: getblockcount spoke for
      ~694k never-connected blocks and getblockstats on them failed "Can't
      read undo data" -- the report's item 1. A halted node now stays at
      its applied height. Same commit: getblockstats utxo_size_inc parity
      (PER_UTXO_OVERHEAD 41; the two BIP30 coinbases).
- [x] **addrindex disabled itself for a closable gap (5042f624).** A
      journal behind the history base is superseded, set aside as
      `addrindex.tail.stale`; the fresh-journal path adopts the base.
- [x] **Compaction wrote record by record (77fb3208):** the twin never
      ported mac_out_write/flush -- ~300M syscalls per full compaction;
      the boot's pre-catchup compaction ran 10+ minutes in write().
- [x] **getblockstats honours its stats filter (c5bf4a6f)**, Core 29.4's
      order, dedupe and errors.
