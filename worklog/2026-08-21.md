# 2026-08-21

## Session: Stage D replay incidents #4/#5, docs audit, archive fixes, first Core measurements

The long-form narrative for incidents #4/#5 is in `LOG.md`'s 2026-08-21
entry; this is the terse action log. Production = `bmc-bitcoind.service`
doing Stage D's from-scratch full-verification replay throughout.

- [x] `fbeff60` (03:42): real `CHECKLOCKTIMEVERIFY`/`CHECKSEQUENCEVERIFY`
      (were no-op stubs) after the replay rejected real height 388431.
      Redeployed from scratch rather than trusting in-place recovery.
- [x] Host rebooted 5× between 11:16 and 12:13 (daemon not enabled on
      boot). On manual restart at 13:25 the replay rejected 363897
      ("missing/already-spent UTXO") reproducibly. Root cause `2fd4a14`
      (14:23): `applied_height` was persisted only at compaction/end-of-call,
      so durable state could run hours ahead of the checkpoint; now
      persisted per block. Regression test
      `tests/test_utxo_catchup_crash_resume.c` (crash-injection hook),
      proven failing pre-fix. UTXO state dropped and rebuilt from block 0
      (14:36) — the baseline run every 08-22 throughput number is measured
      against.
- [x] `677e50b`, `20d1969`: `PLAN_SCRIPT_VERIFY.md`/`LOG.md` brought
      current; every historical REJECT height mapped to its fix commit
      (202471 traced to `646c3cf` via git history — the pre-Stage-D
      manifest-cap deadlock, same bug as 202134).
- [x] `5e9f8bc`: a stale header comment in `secp256k1_scalar.asm` still
      described the abandoned double-and-add `sc_mul`; a perf scoping pass
      misdiagnosed the hot loop from it. Comment fixed; `sc_mul` had been a
      real schoolbook multiply since `5e39cc5`/`71985ca` (08-16).
- [x] `9269a86`: reorg/repair truncation on the non-monotonic archive via
      index-only truncation (`store_truncate_index_only` +
      `archive_truncate_safe`); `store_layout_monotonic`'s guard (added
      after a ~600 GB loss) now has a fallback instead of a dead end.
      `tests/test_archive_truncate_nonmonotonic.c`.
- [x] `a051f21`: whole-file-granular pruning on a non-monotonic archive
      (`archive_prune_file_granular`), Core's own approach. Pruned index
      records marked `data_size=0xFFFFFFFF`, not the all-zero hole sentinel
      — reusing the hole sentinel would have made `dlc_span` re-download
      every pruned block each boot. `store_get_at` gained the read-side
      check. `tests/test_prune_nonmonotonic.c` (34 asserts).
- [x] `5d8ae9c` `FEATURE_GAPS.md`: evidence-based survey vs Core. Headline:
      `rpc_dispatch` implemented 8 methods; taproot script-path verify
      existed but had zero callers; mining, PSBT, descriptor wallets,
      chain selection, light-client indexes absent.
- [x] `e789df8`: BIP341/342 script-path spending wired into consensus
      verify (`taproot_verify_input`: annex, control-block Merkle
      commitment, tapscript via the shared `script_eval`, OP_SUCCESSx,
      CHECKSIGADD, weight budget). Two dependency bugs on the way: an
      unbounded write in `tap_leaf_hash` for scripts >~288 B (4 MiB .bss
      now, bounds-checked), and `tapscript_checksig` ignoring the annex.
      9 independently generated vectors. Was fail-closed before (verified),
      so a liveness bug, not a security hole.
- [x] `da3e1a9` `PERF_SCOPE.md`: symbol-resolved `perf` of the live replay
      at ~430k (perf printed file offsets; `VA = off + 0x400000`). ~53 %
      secp256k1 field/scalar, ~31 % kernel file-read path — traced to
      `utxo_lsm_get`'s run scans, NOT the non-monotonic archive.
- [x] Running Bitcoin Core authorized as a development tool (from the
      source build, never the production install). `52e3280`: libsecp256k1
      `bench ecdsa_verify` on this CPU = **21.8 µs** vs ours 120.9 µs —
      the gap was 5.5×, not the ~2× assumed from public figures.
- [x] `0fc5d06` PERF_SCOPE 4.2: callgrind inventory — one `sc_inv` (450
      `sc_mul`) is 45 % of a verify; batching is the wrong first move
      (legacy inputs verify synchronously inside the interpreter); levers
      A (Core's projective `r·Z² == X` compare) and B (variable-time scalar
      inverse) scoped.
- [x] 23:20: scratch Core oracle started at `/storage/core-oracle`
      (P2P 8334, RPC 8335, `txindex`+`coinstatsindex`). Our `headers.dat`
      vs `getblockhash` at 14 heights: 0 mismatches — **after** noticing
      our record 0 was block 1 (see 08-22, genesis).
- [x] Forks launched for the evening's parallel work (RPC tranche,
      OP_CODESEPARATOR, A+B) — landed on 08-22.

**Status at end of day:** `main` = `0fc5d06`. Replay at ~439k on the old
code (this is the baseline). Stage D not DONE.
