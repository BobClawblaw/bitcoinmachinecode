# 2026-09-02

## 00:00–01:00 UTC — the lost tombstones: repro written, mechanism found, fix landed on branch test/lost-tombstone-repro
Store-level repro (bad LSM object = fix compiled out): lookups through a
bad run miss and READ TOMBSTONED COINS AS LIVE; the walk stays exact in
every sequence (flush/reject/compact/reload/post-flush dels). So the store
never lost a record. Daemon-level repro needed four fixture facts to bite:
(1) 86-byte records so the 1 MB drain lands mid-record (shape-dependent:
53-byte records never miss), (2) many txs per block so the flush lands
mid-block with spends after it, (3) spends of coins created a few blocks
earlier (in the fresh run), (4) undo capture ON (production default).
Then: resurrected_spent=11 of 31904, +10 at the first flush. Mechanism:
undo_capture_and_del = get THEN del; a lying get returned 0 and
live_on_input took 0 as "already absent" and skipped the spend. Block data
matches to the tx (539016: first lost tx 61, 0/170 before, 471/4691 after,
0/274 older-run). Fix: absent-at-apply = store inconsistency -> block
fails, rollback without lookups, HALT + main.c stops. Five tests green;
gate running.
01:06 UTC — DEPLOY ai (main fbbae91, code ac64d46): halt-on-absent-coin at
apply + lookup-free rollback live. Clean stop (0 s), RPC up 48 s, reload
EXACT (applied 965104 / live 165,695,164 == pre-stop), coinstats adopted,
muhash IDENTICAL to the oracle at 965104 (2bc5760c…74f8). Rollback = relink
bitcoind.live -> bitcoind.deploy-20260901ah + restart.

## 01:40–02:35 UTC — private broadcast (Core v30) implemented and proven
daemon/private_broadcast.{c,h} (queue + wire exchange), helper-child
connections in main.c (pb_rotation), dialer_pb_pick_network /
dialer_connect_private (transient I2P session, isolation creds),
tx_submit_private on the status channel, RPC_CTL_PB_ABORT + ctl_out,
getprivatebroadcastinfo / abortprivatebroadcast, config gating. Tests:
tests/test_private_broadcast (queue rules + fake-Core-peer wire test that
also asserts we echo nothing), validation/private_broadcast_regtest_e2e.sh
(24/24 vs Core v31.99: pynode/services-0/relaytxes-false peers, 3 distinct
SOCKS5 creds, received back, abort, refusals). Found on the way:
addnode=host:port dropped its port at three dial sites (fixed); Core
v31.99 holds a lone inbound-bucket tx announcement until bumped (harness
accommodates, documented). connect= pairing is a warning here, not Core's
error (documented divergence).

## 02:55–03:20 UTC — audit N4: wallet_store.c on the strong container
Branch fix/wallet-store-v3: BMCWAL v3 + BMCHDK sealed via wcrypt_seal/open
(BytesToKeySHA512AES 100k, AES-256-CBC), legacy v2/v1-blob read-only +
upgraded in place on a successful open. tests/test_wallet_store with genuine
legacy fixtures (generated by the pre-change code; the first attempt used a
stub KDF and was worthless -- regenerated with the real bip39 object).
05:45 UTC — DEPLOY aj (main ff673ec): private broadcast (off by default) +
wallet_store v3 + addnode port fixes. Clean stop (0 s), RPC up 70 s, reload
EXACT (applied 965124 / live 165,662,711 == pre-stop), coinstats adopted,
muhash IDENTICAL to the oracle at 965124 (aeed9ba5…766f);
getprivatebroadcastinfo refuses with Core's text while the option is off.
Rollback = relink bitcoind.live -> bitcoind.deploy-20260902ai + restart.
