# 2026-09-01

- [x] `-reindex` shipped: deploy 20260901a (`026816d`), live-proven on the real
      testnet4 archive (5 s, index identical, chainwork byte-identical,
      headers.dat repaired 149,955 -> 149,960). Frame magic is the STORE
      constant 0xd9b4bef9 on every chain -- the first attempt passed
      net_magic and read a whole archive as junk; the resync now searches the
      magic in 4 MiB chunks.
- [x] cjdroute is a systemd unit (`cjdroute.service`, `--nobg`, config on
      stdin); `bmc-bitcoind` ordered After=/Wants= it via a drop-in. The first
      start crash-looped (6 restarts) because the hand-run instance's `core`
      child, running as root, survived a user-side kill and held
      /tmp/cjdroute.sock; killed with sudo, socket removed, stable since.
- [ ] Dial-pool diversity (in gate): `dl_pool_from_book` reservoir-samples
      each reachable network and lays out the 64-slot pool with quotas
      (onion 12, ipv6 8, i2p 6, cjdns 3, rest clearnet), boot slots mostly
      clearnet (one onion at 3, one ipv6 at 6), the rest interleaved so the
      rotation reaches an anonymity peer within a few dials. Found and fixed
      a latent dialer bug on the way: `dialer_net_reachable()` did not
      lazily initialise the dialer, so any early caller saw every network
      but IPv4 as unreachable. tests/test_pool_diversity (18 checks).
- [x] INCIDENT 00:31 (deploy 20260901b, the diverse pool): worker wedged --
      heartbeat silent, 0 outbound peers, SIGTERM unanswered, backtrace
      `tcp_connect_ip <- outbound_connect <- serve_download_worker`. Rolled
      back to 20260901a within minutes. ROOT CAUSE (pre-existing; the 21:20
      wedge yesterday had the same shape): both TCP connect paths set only
      SO_RCVTIMEO, so a blackholed address holds the blocking connect() for
      the kernel's ~127 s SYN schedule at every dial site outside the SIGALRM
      budget. Proven on this kernel with a scratch probe (no timeout: >10 s
      and counting; SO_SNDTIMEO=3: EINPROGRESS after 3.1 s). Fix: SO_SNDTIMEO
      10 s before connect() in bitcoin_net.asm and net6.c. The diverse pool
      only exposed it faster (more unreachable targets).
- [x] ROOT CAUSE, deeper: the SO_RCVTIMEO added 2026-08-15 NEVER took effect.
      bitcoin_net.asm loaded the timeval pointer into RCX; the kernel takes a
      syscall's 4th argument in R10 (SYSCALL clobbers RCX), so every
      setsockopt returned EFAULT into an ignored result -- strace:
      `setsockopt(3, SOL_SOCKET, SO_RCVTIMEO_OLD, NULL, 16) = -1 EFAULT`.
      Reads on outbound sockets have had NO timeout since that date; connect()
      never had one. Fixed (R10) + SO_SNDTIMEO; probe: connect to a blackhole
      returns EINPROGRESS after 10.3 s. tests/test_net_timeouts pins it (a
      silent server; the read must give up in ~10 s). Only the two sites in
      the asm issue setsockopt; the listeners are C.
- [x] INCIDENT 00:51 (deploy 20260901c): worker wedged AGAIN 20 min after
      boot, this time in `fd_write_all` (a blocking write to an fd with no
      send bound); rolled back. The stop stalled because the PARENT was in
      the mempool.dat reload waiting 90 s per entry for acks from a worker
      that was already dead (backtrace mpd_import_one <- mempool_dump_read
      <- rpc_node_mempool_load), ignoring SIGTERM -- SIGKILL to finish.
      Fixes: fd_write_all polls POLLOUT (10 s) before every write, covering
      every fd (IPC pairs, legs, subscribers); the reload loop aborts on the
      shutdown flag and after two consecutive unanswered entries.
      test_net_timeouts now also proves a blocked write returns in ~10 s.
- [x] THE COMMON TRIGGER: the worker's tx-submission follow loop
      (`for(;;)`, exits only after 30 ms with no new submission). A mempool.dat
      reload submits thousands of entries back-to-back with retry passes, so
      the worker lived in that loop for MINUTES after every boot: no heartbeat,
      no leg polling, no block sync (tip frozen), and every blocking dial or
      write inside it looked like a wedge. All four "wedges" (21:20, 00:31,
      00:51, 01:10) began 1-4 min after a boot with a large mempool.dat.
      Fix: TXSUB_ROTATION_BUDGET 256 / TXSUB_ROTATION_MS 250 per rotation;
      the submitter tolerates rotation gaps. Together with the socket
      timeouts, the write bound and the shutdown-aware reload, this closes
      the family. Deploy d carries all of it under a heartbeat-guarded watch.
- [x] Deploy d (eed0dba, all wedge fixes + pool + walletdir) under a
      heartbeat guard: first heartbeat at uptime 1:00 WHILE the 5.6k-entry
      reload streamed (the follow-loop bound works); THREE OUTBOUND ONION LEGS
      connected ("filled outbound 4/5/6 = ...onion:8333") -- the anonymity
      outbound proof; clean SIGTERM stop ("saved 132 transaction(s)"). The
      guard then rolled back on a 174 s heartbeat gap: not a wedge -- the
      top-up loop filled three empty slots back-to-back, each onion dial
      ~40 s (rendezvous + handshake), inline in the worker. Fix: one fill and
      at most four failed dials per top-up pass; guard threshold 200 s.
- [x] Deploy e (03282cc): guarded watch OK -- 12 minutes of continuous
      heartbeats, 8/8 peers, onion legs filled one per pass, relay 1-2k tx/min.
      Found in that evidence: slots 6 and 7 held the SAME onion peer -- the
      leg-host buffer was 64 bytes, a v3 onion host:port is 67, so the
      top-up dedupe compared a truncated string. Widened to 128 (strncpy 127).
      Also DL_POOL_RESERVOIR 1024 -> 8192 so clearnet can fill the 2048-slot
      catch-up pool (it was ipv4 1024 / ipv6 1006).
- [x] Deploy f: one live leg in five minutes -> rolled back to e, then to a.
      Cause: uniform sampling over the WHOLE 17.8k-entry IPv4 book draws
      mostly dead gossip (odd ports :58333/:8332); the book has no tried/new
      distinction and gossip refreshes last_seen (histogram: 12k "seen" in
      3 days), so recency cannot separate once-connected peers from claims.
      The head of the book IS the migrated once-connected set (why the old
      pool worked). Rule now: clearnet candidates come from the first 4096
      entries; anonymity quotas unchanged. Also: this host has NO global IPv6
      route (only a ULA + the cjdns tun) -- dialer_net_reachable(IPV6) said
      yes from a bare socket() probe and every IPv6 candidate burned a 10 s
      connect timeout; a UDP connect() route probe now gates it (cjdns keeps
      the tun). test_pool_diversity adapts to the host's real reachability.
- [x] Deploy g (f0877de): guarded watch OK -- 12 min continuous heartbeats,
      8/8 peers within a minute, relay ~170 tx/min, pool ipv4 46 / onion 12 /
      i2p 6 / ipv6 0 (no global v6 route here). With a healthy clearnet head
      the legs fill clearnet-first, so no onion leg was needed this boot;
      onion legs appear on churn (as on d/e). OPEN: reserve one outbound slot
      for a reachable anonymity network (Core keeps >=1 per network) so the
      diversity is guaranteed rather than churn-dependent; and legs 1 and 2
      held the same IPv4 host (two book entries, different ports?) -- check.
- [x] Leg dedupe (deploy h): mux_next_peer rotated each leg's own pool pointer
      with no cross-leg check, so two legs could land on one host (g: legs 1
      and 2 on 108.245.166.132). It now skips any pool entry whose HOST another
      live leg holds (compared by IP, so one peer under two ports = one leg).
- [x] Parity 1/6 -- async dialer + reserved anonymity slots (deploy i):
      onion/i2p dials run in a forked helper (dh_start/dh_poll/
      dh_install_leg in main.c) that hands the connected socket back with
      SCM_RIGHTS plus the version payload and addrv2 flag; the worker polls
      it every rotation. Policy: at least one leg per reachable anonymity
      network, dialled in the background, allowed beyond MUX_WANT_OUT by 2
      (Core's extra network-specific outbound); mux_next_peer and the inline
      top-up never dial anonymity entries. A helper child never writes the
      address book. tests/test_dialhelper (18 checks, fake peer + blackhole).

## GLV + ecdsa dispatch + script_op_len + parity sweep (arm64, 2026-09-01)

- GLV landed: secp256k1_glv.S (split), secp256k1_glv_mul.S (point_scalar_mul_glv,
  disjoint frame map), wired into MODULES/DAEMONOBJS; ecdsa_verify now dispatches
  u2*Q through bmc_ecdsa_glv_enabled + point_scalar_mul_glv like x86, with the
  frozen pre-4.2 verifier ported as ecdsa_verify_ref.S (test-only).
  script_op_len exported from bitcoin_sighash.S.
  Commits 5ff7c60 4b1903f aee718a 5a80781 b24ba0f.
- parity_sweep.sh replaced the corrupted parity_all.sh (derives link lines from
  asm/Makefile: full make-var expansion incl. filter/patsubst/sort/wildcard,
  -I remap, .o->port/.c mapping, -l/-L/-Wl passed through verbatim; ARM
  symbol-home extras; undo_log_ref.o/addrbook.a/ecdsa_verify_ref.o special
  builds; ./daemon scratch symlink; runner args parsed). Commits c23dd35 272c999.
- Six real port bugs it caught (commits 2d09a54 9ed5ceb 77072df 369bc2a 17561e6
  + HARDENFLAGS 2561264):
  1. bitcoin_undo.S: "undo"/".dat" immediates byte-swapped -> files named
     doun_<h>at.d (strace: openat ENOENT).
  2. bitcoin_serve.S: five p2p command matchers mis-split LE ("bloc", "head",
     "ers\0", "lock", "ilt\0") -> block/getheaders/getblocks/getblocktxn/
     feefilter dispatch was dead code.
  3. bitcoin_idx.S: predated x86 f80d09e, still byte-reversed index.dat records
     (table keyed backwards; no getdata could hit).
  4. bitcoind.S: node_serve_block kept the store fd in x1 across the lseek svc
     (x1 = offset) -> read(8,..) EBADF, every block served as notfound.
  5. bitcoin_serve.S: violation reporting entirely missing (hook never called,
     no serve_inv_bounds, single-byte inv count); bitcoin_net.S p2p_read
     returned -1 instead of -3 for oversize announcements.
  6. build_daemon.sh lacked HARDENFLAGS -> no full RELRO.
- Sweep round 2 (358 asm/tests sources, native aarch64): 256 pass / 12 bench-ok /
  17 helpers built / 21 fail / 6 build-fail / 11 skip.
- OPEN items (next session, evidence captured):
  * verify_p2pkh port gap: ARM bitcoin_script.S has 2 of the x86 module's
    symbols; verify_p2pkh/der_parse_sig/be_to_limbs (~133 instrs) missing ->
    blocks test_p2pkh/test_send/test_txval/test_wrpc_{send,sign}/test_e2e_sighash.
  * Interpreter/store segfaults (empty stdout lost to buffering): test_dersig_
    encoding (stack_push copy loop, len=0xffffffff pushed from an element
    record via the toalt path), test_interp_legacy_spend (done+44 copy loop),
    test_archive_truncate_nonmonotonic (main+528 ldr x0,[x22] -- x22 clobbered
    by SOME asm callee; store_get_at audited clean), test_taproot_parity,
    test_utxo_lsm, test_utxo_setinfo, test_multisig_opcount (silent crashes).
    A faithful AArch64 port of scripts/abi_callee_saved_audit.py is the right
    systematic tool (my quick objdump heuristic is not sound enough to gate).
  * Serve getaddr: test_addrv2_serve reports no getaddr reply at all;
    test_keepup: pushed block not served byte-exact. Both post-date the serve
    fixes -- likely one shared root in serve_getaddr/the keepup loop.
  * test_chainparams: 4 failures around signet flag selection;
    test_script_flags: signet h=1 flags=0x20801 missing bits.
  * env-only: bench_checkblock/bench_hashidx/bench_idxscan/bench_taproot_block
    (production data files absent), test_net_timeouts (needs >600s),
    test_rpc_server/test_rpc_transport (daemon lifecycle; daemon symlink now
    provided, still failing -- needs a look), test_node_config ("0 of 0
    documented keys" -- input file resolution from scratch cwd).

## IBD smoke vs the live chain (arm64, 2026-09-01, step 6)

- Built ibd_lsm + ibd_par natively (link: port objects + bitcoin_p2p/bitcoind/
  scriptverify_drv + the scriptverify/witness_v0/txval_modern/taproot_sighash/
  segwit C twins + bitcoin_mempool.o + a 2-line mempool_resolve_confirmed_utxo
  stub -- tx_accept's resolver returns 0 when the daemon mempool was never
  initialized (g_ready=0), which is exactly this process's state; the daemon
  itself does NOT use the stub).
- headers.dat was stale (964806 vs archive 965008) -> "nothing to download";
  refreshed with live_download against the LAN oracle (192.168.5.69:8333):
  965064 headers paged from genesis, deepest hash
  00000000000000000001616b957ca32b4540534a09dfdadf6a64874c7a47a003.
  GOTCHA: live_download's default datadir is cwd-relative "data" -- run it from
  the datadir's PARENT or set BITCOIN_DATA_DIR.
- ibd_lsm reload on the production LSM (6-run manifest, 117454-entry WAL):
  ~61s, then resume EXACTLY at the archive tip (965008) -- resume-at-tip
  confirmed against a live peer.
- First bounded run hit "tx2/tx3 MALFORMED" on every block: ibd_lsm's tx_walk
  was legacy-only (no BIP144 marker/flag, no witness skip; then a witness
  parser that skipped the item counts and length varints). Fixed (8238256);
  the tool had only ever met the legacy-serialized generated test archive.
- Fixed run (h965015..965017, 3 real mainnet blocks incl. a 1.44MB / 4428-tx
  one): valid=3 bad_gate=0 -- cons_verify accepted all three natively; txs
  walked 15467, spent=1189 added=13992; LSM flush runs 7->8;
  PERSISTENCE CHECK reload_after_close live MATCH -- tombstone-resurrection
  proof on the production state. LSM live counter reads 241,190,804 at tip
  (self-consistent across close/reopen).
- CAVEAT: the two PRE-fix runs appended 965009..965014 to the archive but added
  zero LSM outputs ("added=0" in both logs) -- that window's outputs are missing
  from the persisted UTXO, so 316 spends of them report MISSING-PREVOUT (sigs
  skipped; 0 actual SIGFAILs). Backfill: lift the G_force_start clamp to tip+1
  (ibd_lsm.c:414) and re-apply 965009..965014 in order (puts+dels are
  idempotent-safe against the current state since those effects were never
  applied).
- ibd_par built but not exercised live this session (single-peer ibd_lsm path
  covered the resume/persistence assertions; ibd_par adds parallel clients +
  log parity, no new state machinery).

## Sweep follow-ups (arm64, 2026-09-01 evening)

- verify_p2pkh port landed (daeb44a): bitcoin_script.S (der_parse_sig,
  be_to_limbs, verify_p2pkh) unblocked test_p2pkh/test_txval/test_send/
  test_wrpc_send/test_wrpc_sign/test_e2e_sighash -- all pass.
- Sweep harness: wallet_cli + bitcoin_rpcd + bitcoin_cli special builds
  (each needed its own ARM link-line notes: sighash_all_ext + GLV pair for
  the post-dispatch ecdsa.o; addrbook.a via parity_out; bip32_ckdpub.c for
  bip32_xpub_parse; store/idx/tx/chainwork from RPCCHAIN). Scratch daemon/
  is a real dir now. THE relative-symlink bug: helper-overlay targets were
  relative and dangled from the /tmp scratch cwd -- all absolute now, which
  fixed the bip30 shim family too.
- serve getaddr rewritten to the x86 shape: serve_getaddr(fd,
  g_peer_wants_addrv2) over the v2 book (7b22e29) -- test_addrv2_serve pass.
- script_flags 4-way chain dispatch restored (f2877e2) -- test_chainparams +
  test_script_flags pass.
- test_rpc_server + test_rpc_transport pass (rpcd/cli special builds).
- Still open: test_keepup (pushed block not served; .do_block chain gate or
  serve-side store read), test_verify_p2sh + test_scriptverify_parity +
  the 7 SIGSEGVs (genuine P2SH spends return EVAL_FALSE through script_eval's
  CHECKSIG while direct sv_checksig_asm passes -- the deep interpreter
  session), LSM backfill 965009..965014, test_net_timeouts (>600s), env-only
  benches.

## LSM backfill 965009..965017 (late session, the hole closed)

- Shipped `ibd_backfill` (port/arm64/ibd_backfill.c + Makefile rule): offline
  re-apply of an ALREADY-ARCHIVED block window to the persisted LSM UTXO --
  store_get_at + pread, no network, no store_append. Runs in place over
  data/ (manifest + blob/table maps + WAL utxo.dat + run files). The
  G_force_start clamp lift in ibd_lsm became unnecessary; the clamp stays.
- TWO ibd_lsm defects fixed while bringing the tool up (both uncommitted from
  the previous session + one found today):
  1. Outputs were keyed by sha256d over the FULL serialization = the WTXID
     (BIP141) -- every segwit output it added could never have its own spends
     resolved. Added txid_of(): witness-stripped core (version + vin/vout +
     locktime) hashed, marker/flag/witness excluded. Validated end-to-end:
     computed txids rebuild each block's merkle root EXACTLY (all 9 window
     blocks checked in Python against the header's own merkle field).
  2. tx_out/tx_in (both ibd_lsm.c and the tool drafts) skipped the BIP144
     marker/flag: the marker read as ni=0 and the flag as vout count=1, so
     "vout 0" was 8 bytes of the first vin's prevhash (garbage value/script
     keyed at idx=0) and EVERY segwit input was skipped (no dels at all).
     Symptom in the first production attempt: val=7096155651606458626 sats,
     24 garbage PUT ERRs, added~=segwit-tx-count, missing where none were
     expected. One-line skip `if(o+2<=n && tx[o]==0x00 && tx[o+1]==0x01) o+=2;`
     in all four sites, mirroring tx_walk.
- Method: Python ground-truth parser (strict, witness-aware) over the window;
  per-tx offset tables matched the C tx_walk trace EXACTLY (3203/3203 for
  h965009) -- parse layer proven before touching state. LSM layer proven with
  standalone probes: put->flush->close->reload->get roundtrips at 2 keys and
  50000 keys (single flush) and 3x1000 keys (multi-flush) all verify values/
  heights/cb/slen. Archive bytes themselves verified intact (all 9 blocks
  parse to exactly their indexed sizes; merkle roots match).
- PRODUCTION RUN (ibd_backfill 965009 9 data): reload live=241203649 runs=8;
  added=94801 dup=49 spent=67995 missing=2667 del_err=0 bad=0, flushes=9
  (runs 8->17, utxo_run_000247..255.dat), PERSISTENCE reload_after_close
  live=241230455 MATCH. Log: data/logs/backfill_965009_965017.log. Spot
  check via /tmp-style lsmget probe: h965009 coinbase (raw txid
  417f52a9...06af7 idx 0) -> val=546 height=965009 cb=1 slen=23 script a914..
  -- matches the Python-parsed truth byte-for-byte. Sandbox rehearsal over a
  copy of the production state produced IDENTICAL numbers (deterministic).
- The window choice: 965009..965017, not just 965009..965014. The post-fix
  production run had keyed 965015..965017 by WTXID, so its intra-window
  spends (965016 spending a 965015 output) ALSO reported missing prevouts --
  re-applying the whole window in order resolves every one of them with real
  txids.
- Known residue, all documented and benign:
  * 13992 wtxid-keyed junk entries from the old post-fix run remain
    (unspendable garbage keys; a rebuild-from-assume-valid or a daemon-side
    reconciliation is the full cleanup -- unchanged, out of scope).
  * missing=2667 prevouts are PRE-EXISTING holes in the pre-tip LSM (sampled
    3: none are window txids; same class as the production run's
    MISSING_PREVOUT=316). Not caused by the backfill.
  * The aborted first attempt appended ~3244 garbage (txid,0) puts + 1807
    dels to the WAL (utxo.dat 9.7GB); the backfill re-puts the same keys with
    correct values in-place (the 49 put_dups ARE those re-puts), so the
    residue is shadowed. del never returns 0 for absent keys -> del_absent=0
    in the logs is a counting artifact, not lost deletes.
  * reload live reads 241203649 vs the manifest's total_live 241203607 (+42,
    WAL-tail recount artifact, count-only; next real flush rewrites it).
  * The backup taken before all of this: data/utxo_stale_1788296033/ (trio).
- IBDLSMOBJS now carries the GLV triple (secp256k1_glv_c/glv/glv_mul -- the
  ecdsa GLV dispatch made secp256k1_ecdsa.o reference them) and
  bitcoin_sighash_all_ext.o (sighash_all lives in the _ext half on ARM).
  ibd_lsm/ibd_par/ibd_backfill all link clean again.
