# 2026-09-01

- [x] `-reindex` shipped: deploy 20260901a (`026816d`), live-proven on the real
      testnet4 archive (5 s, index identical, chainwork byte-identical,
      headers.dat repaired 149,955 -> 149,960). Frame magic is the STORE
      constant 0xd9b4bef9 on every chain -- the first attempt passed
      net_magic and read a whole archive as junk; the resync now searches the
      magic in 4 MiB chunks.
- [x] cjdroute is a systemd unit (`cjdroute.service`, `--nobg`, config on
      stdin); `bmc-bitcoind` ordered After=/Wants= it via a drop-in. The first
      start crash-looped (6 restarts) because the hand-run instance's `core`
      child, running as root, survived a user-side kill and held
      /tmp/cjdroute.sock; killed with sudo, socket removed, stable since.
- [ ] Dial-pool diversity (in gate): `dl_pool_from_book` reservoir-samples
      each reachable network and lays out the 64-slot pool with quotas
      (onion 12, ipv6 8, i2p 6, cjdns 3, rest clearnet), boot slots mostly
      clearnet (one onion at 3, one ipv6 at 6), the rest interleaved so the
      rotation reaches an anonymity peer within a few dials. Found and fixed
      a latent dialer bug on the way: `dialer_net_reachable()` did not
      lazily initialise the dialer, so any early caller saw every network
      but IPv4 as unreachable. tests/test_pool_diversity (18 checks).
- [x] INCIDENT 00:31 (deploy 20260901b, the diverse pool): worker wedged --
      heartbeat silent, 0 outbound peers, SIGTERM unanswered, backtrace
      `tcp_connect_ip <- outbound_connect <- serve_download_worker`. Rolled
      back to 20260901a within minutes. ROOT CAUSE (pre-existing; the 21:20
      wedge yesterday had the same shape): both TCP connect paths set only
      SO_RCVTIMEO, so a blackholed address holds the blocking connect() for
      the kernel's ~127 s SYN schedule at every dial site outside the SIGALRM
      budget. Proven on this kernel with a scratch probe (no timeout: >10 s
      and counting; SO_SNDTIMEO=3: EINPROGRESS after 3.1 s). Fix: SO_SNDTIMEO
      10 s before connect() in bitcoin_net.asm and net6.c. The diverse pool
      only exposed it faster (more unreachable targets).
- [x] ROOT CAUSE, deeper: the SO_RCVTIMEO added 2026-08-15 NEVER took effect.
      bitcoin_net.asm loaded the timeval pointer into RCX; the kernel takes a
      syscall's 4th argument in R10 (SYSCALL clobbers RCX), so every
      setsockopt returned EFAULT into an ignored result -- strace:
      `setsockopt(3, SOL_SOCKET, SO_RCVTIMEO_OLD, NULL, 16) = -1 EFAULT`.
      Reads on outbound sockets have had NO timeout since that date; connect()
      never had one. Fixed (R10) + SO_SNDTIMEO; probe: connect to a blackhole
      returns EINPROGRESS after 10.3 s. tests/test_net_timeouts pins it (a
      silent server; the read must give up in ~10 s). Only the two sites in
      the asm issue setsockopt; the listeners are C.
- [x] INCIDENT 00:51 (deploy 20260901c): worker wedged AGAIN 20 min after
      boot, this time in `fd_write_all` (a blocking write to an fd with no
      send bound); rolled back. The stop stalled because the PARENT was in
      the mempool.dat reload waiting 90 s per entry for acks from a worker
      that was already dead (backtrace mpd_import_one <- mempool_dump_read
      <- rpc_node_mempool_load), ignoring SIGTERM -- SIGKILL to finish.
      Fixes: fd_write_all polls POLLOUT (10 s) before every write, covering
      every fd (IPC pairs, legs, subscribers); the reload loop aborts on the
      shutdown flag and after two consecutive unanswered entries.
      test_net_timeouts now also proves a blocked write returns in ~10 s.
- [x] THE COMMON TRIGGER: the worker's tx-submission follow loop
      (`for(;;)`, exits only after 30 ms with no new submission). A mempool.dat
      reload submits thousands of entries back-to-back with retry passes, so
      the worker lived in that loop for MINUTES after every boot: no heartbeat,
      no leg polling, no block sync (tip frozen), and every blocking dial or
      write inside it looked like a wedge. All four "wedges" (21:20, 00:31,
      00:51, 01:10) began 1-4 min after a boot with a large mempool.dat.
      Fix: TXSUB_ROTATION_BUDGET 256 / TXSUB_ROTATION_MS 250 per rotation;
      the submitter tolerates rotation gaps. Together with the socket
      timeouts, the write bound and the shutdown-aware reload, this closes
      the family. Deploy d carries all of it under a heartbeat-guarded watch.
- [x] Deploy d (eed0dba, all wedge fixes + pool + walletdir) under a
      heartbeat guard: first heartbeat at uptime 1:00 WHILE the 5.6k-entry
      reload streamed (the follow-loop bound works); THREE OUTBOUND ONION LEGS
      connected ("filled outbound 4/5/6 = ...onion:8333") -- the anonymity
      outbound proof; clean SIGTERM stop ("saved 132 transaction(s)"). The
      guard then rolled back on a 174 s heartbeat gap: not a wedge -- the
      top-up loop filled three empty slots back-to-back, each onion dial
      ~40 s (rendezvous + handshake), inline in the worker. Fix: one fill and
      at most four failed dials per top-up pass; guard threshold 200 s.
- [x] Deploy e (03282cc): guarded watch OK -- 12 minutes of continuous
      heartbeats, 8/8 peers, onion legs filled one per pass, relay 1-2k tx/min.
      Found in that evidence: slots 6 and 7 held the SAME onion peer -- the
      leg-host buffer was 64 bytes, a v3 onion host:port is 67, so the
      top-up dedupe compared a truncated string. Widened to 128 (strncpy 127).
      Also DL_POOL_RESERVOIR 1024 -> 8192 so clearnet can fill the 2048-slot
      catch-up pool (it was ipv4 1024 / ipv6 1006).
- [x] Deploy f: one live leg in five minutes -> rolled back to e, then to a.
      Cause: uniform sampling over the WHOLE 17.8k-entry IPv4 book draws
      mostly dead gossip (odd ports :58333/:8332); the book has no tried/new
      distinction and gossip refreshes last_seen (histogram: 12k "seen" in
      3 days), so recency cannot separate once-connected peers from claims.
      The head of the book IS the migrated once-connected set (why the old
      pool worked). Rule now: clearnet candidates come from the first 4096
      entries; anonymity quotas unchanged. Also: this host has NO global IPv6
      route (only a ULA + the cjdns tun) -- dialer_net_reachable(IPV6) said
      yes from a bare socket() probe and every IPv6 candidate burned a 10 s
      connect timeout; a UDP connect() route probe now gates it (cjdns keeps
      the tun). test_pool_diversity adapts to the host's real reachability.
- [x] Deploy g (f0877de): guarded watch OK -- 12 min continuous heartbeats,
      8/8 peers within a minute, relay ~170 tx/min, pool ipv4 46 / onion 12 /
      i2p 6 / ipv6 0 (no global v6 route here). With a healthy clearnet head
      the legs fill clearnet-first, so no onion leg was needed this boot;
      onion legs appear on churn (as on d/e). OPEN: reserve one outbound slot
      for a reachable anonymity network (Core keeps >=1 per network) so the
      diversity is guaranteed rather than churn-dependent; and legs 1 and 2
      held the same IPv4 host (two book entries, different ports?) -- check.
- [x] Leg dedupe (deploy h): mux_next_peer rotated each leg's own pool pointer
      with no cross-leg check, so two legs could land on one host (g: legs 1
      and 2 on 108.245.166.132). It now skips any pool entry whose HOST another
      live leg holds (compared by IP, so one peer under two ports = one leg).
- [x] Parity 1/6 -- async dialer + reserved anonymity slots (deploy i):
      onion/i2p dials run in a forked helper (dh_start/dh_poll/
      dh_install_leg in main.c) that hands the connected socket back with
      SCM_RIGHTS plus the version payload and addrv2 flag; the worker polls
      it every rotation. Policy: at least one leg per reachable anonymity
      network, dialled in the background, allowed beyond MUX_WANT_OUT by 2
      (Core's extra network-specific outbound); mux_next_peer and the inline
      top-up never dial anonymity entries. A helper child never writes the
      address book. tests/test_dialhelper (18 checks, fake peer + blackhole).

## GLV + ecdsa dispatch + script_op_len + parity sweep (arm64, 2026-09-01)

- GLV landed: secp256k1_glv.S (split), secp256k1_glv_mul.S (point_scalar_mul_glv,
  disjoint frame map), wired into MODULES/DAEMONOBJS; ecdsa_verify now dispatches
  u2*Q through bmc_ecdsa_glv_enabled + point_scalar_mul_glv like x86, with the
  frozen pre-4.2 verifier ported as ecdsa_verify_ref.S (test-only).
  script_op_len exported from bitcoin_sighash.S.
  Commits 5ff7c60 4b1903f aee718a 5a80781 b24ba0f.
- parity_sweep.sh replaced the corrupted parity_all.sh (derives link lines from
  asm/Makefile: full make-var expansion incl. filter/patsubst/sort/wildcard,
  -I remap, .o->port/.c mapping, -l/-L/-Wl passed through verbatim; ARM
  symbol-home extras; undo_log_ref.o/addrbook.a/ecdsa_verify_ref.o special
  builds; ./daemon scratch symlink; runner args parsed). Commits c23dd35 272c999.
- Six real port bugs it caught (commits 2d09a54 9ed5ceb 77072df 369bc2a 17561e6
  + HARDENFLAGS 2561264):
  1. bitcoin_undo.S: "undo"/".dat" immediates byte-swapped -> files named
     doun_<h>at.d (strace: openat ENOENT).
  2. bitcoin_serve.S: five p2p command matchers mis-split LE ("bloc", "head",
     "ers\0", "lock", "ilt\0") -> block/getheaders/getblocks/getblocktxn/
     feefilter dispatch was dead code.
  3. bitcoin_idx.S: predated x86 f80d09e, still byte-reversed index.dat records
     (table keyed backwards; no getdata could hit).
  4. bitcoind.S: node_serve_block kept the store fd in x1 across the lseek svc
     (x1 = offset) -> read(8,..) EBADF, every block served as notfound.
  5. bitcoin_serve.S: violation reporting entirely missing (hook never called,
     no serve_inv_bounds, single-byte inv count); bitcoin_net.S p2p_read
     returned -1 instead of -3 for oversize announcements.
  6. build_daemon.sh lacked HARDENFLAGS -> no full RELRO.
- Sweep round 2 (358 asm/tests sources, native aarch64): 256 pass / 12 bench-ok /
  17 helpers built / 21 fail / 6 build-fail / 11 skip.
- OPEN items (next session, evidence captured):
  * verify_p2pkh port gap: ARM bitcoin_script.S has 2 of the x86 module's
    symbols; verify_p2pkh/der_parse_sig/be_to_limbs (~133 instrs) missing ->
    blocks test_p2pkh/test_send/test_txval/test_wrpc_{send,sign}/test_e2e_sighash.
  * Interpreter/store segfaults (empty stdout lost to buffering): test_dersig_
    encoding (stack_push copy loop, len=0xffffffff pushed from an element
    record via the toalt path), test_interp_legacy_spend (done+44 copy loop),
    test_archive_truncate_nonmonotonic (main+528 ldr x0,[x22] -- x22 clobbered
    by SOME asm callee; store_get_at audited clean), test_taproot_parity,
    test_utxo_lsm, test_utxo_setinfo, test_multisig_opcount (silent crashes).
    A faithful AArch64 port of scripts/abi_callee_saved_audit.py is the right
    systematic tool (my quick objdump heuristic is not sound enough to gate).
  * Serve getaddr: test_addrv2_serve reports no getaddr reply at all;
    test_keepup: pushed block not served byte-exact. Both post-date the serve
    fixes -- likely one shared root in serve_getaddr/the keepup loop.
  * test_chainparams: 4 failures around signet flag selection;
    test_script_flags: signet h=1 flags=0x20801 missing bits.
  * env-only: bench_checkblock/bench_hashidx/bench_idxscan/bench_taproot_block
    (production data files absent), test_net_timeouts (needs >600s),
    test_rpc_server/test_rpc_transport (daemon lifecycle; daemon symlink now
    provided, still failing -- needs a look), test_node_config ("0 of 0
    documented keys" -- input file resolution from scratch cwd).

## IBD smoke vs the live chain (arm64, 2026-09-01, step 6)

- Built ibd_lsm + ibd_par natively (link: port objects + bitcoin_p2p/bitcoind/
  scriptverify_drv + the scriptverify/witness_v0/txval_modern/taproot_sighash/
  segwit C twins + bitcoin_mempool.o + a 2-line mempool_resolve_confirmed_utxo
  stub -- tx_accept's resolver returns 0 when the daemon mempool was never
  initialized (g_ready=0), which is exactly this process's state; the daemon
  itself does NOT use the stub).
- headers.dat was stale (964806 vs archive 965008) -> "nothing to download";
  refreshed with live_download against the LAN oracle (192.168.5.69:8333):
  965064 headers paged from genesis, deepest hash
  00000000000000000001616b957ca32b4540534a09dfdadf6a64874c7a47a003.
  GOTCHA: live_download's default datadir is cwd-relative "data" -- run it from
  the datadir's PARENT or set BITCOIN_DATA_DIR.
- ibd_lsm reload on the production LSM (6-run manifest, 117454-entry WAL):
  ~61s, then resume EXACTLY at the archive tip (965008) -- resume-at-tip
  confirmed against a live peer.
- First bounded run hit "tx2/tx3 MALFORMED" on every block: ibd_lsm's tx_walk
  was legacy-only (no BIP144 marker/flag, no witness skip; then a witness
  parser that skipped the item counts and length varints). Fixed (8238256);
  the tool had only ever met the legacy-serialized generated test archive.
- Fixed run (h965015..965017, 3 real mainnet blocks incl. a 1.44MB / 4428-tx
  one): valid=3 bad_gate=0 -- cons_verify accepted all three natively; txs
  walked 15467, spent=1189 added=13992; LSM flush runs 7->8;
  PERSISTENCE CHECK reload_after_close live MATCH -- tombstone-resurrection
  proof on the production state. LSM live counter reads 241,190,804 at tip
  (self-consistent across close/reopen).
- CAVEAT: the two PRE-fix runs appended 965009..965014 to the archive but added
  zero LSM outputs ("added=0" in both logs) -- that window's outputs are missing
  from the persisted UTXO, so 316 spends of them report MISSING-PREVOUT (sigs
  skipped; 0 actual SIGFAILs). Backfill: lift the G_force_start clamp to tip+1
  (ibd_lsm.c:414) and re-apply 965009..965014 in order (puts+dels are
  idempotent-safe against the current state since those effects were never
  applied).
- ibd_par built but not exercised live this session (single-peer ibd_lsm path
  covered the resume/persistence assertions; ibd_par adds parallel clients +
  log parity, no new state machinery).

## Sweep follow-ups (arm64, 2026-09-01 evening)

- verify_p2pkh port landed (daeb44a): bitcoin_script.S (der_parse_sig,
  be_to_limbs, verify_p2pkh) unblocked test_p2pkh/test_txval/test_send/
  test_wrpc_send/test_wrpc_sign/test_e2e_sighash -- all pass.
- Sweep harness: wallet_cli + bitcoin_rpcd + bitcoin_cli special builds
  (each needed its own ARM link-line notes: sighash_all_ext + GLV pair for
  the post-dispatch ecdsa.o; addrbook.a via parity_out; bip32_ckdpub.c for
  bip32_xpub_parse; store/idx/tx/chainwork from RPCCHAIN). Scratch daemon/
  is a real dir now. THE relative-symlink bug: helper-overlay targets were
  relative and dangled from the /tmp scratch cwd -- all absolute now, which
  fixed the bip30 shim family too.
- serve getaddr rewritten to the x86 shape: serve_getaddr(fd,
  g_peer_wants_addrv2) over the v2 book (7b22e29) -- test_addrv2_serve pass.
- script_flags 4-way chain dispatch restored (f2877e2) -- test_chainparams +
  test_script_flags pass.
- test_rpc_server + test_rpc_transport pass (rpcd/cli special builds).
- Still open: test_keepup (pushed block not served; .do_block chain gate or
  serve-side store read), test_verify_p2sh + test_scriptverify_parity +
  the 7 SIGSEGVs (genuine P2SH spends return EVAL_FALSE through script_eval's
  CHECKSIG while direct sv_checksig_asm passes -- the deep interpreter
  session), LSM backfill 965009..965014, test_net_timeouts (>600s), env-only
  benches.

## LSM backfill 965009..965017 (late session, the hole closed)

- Shipped `ibd_backfill` (port/arm64/ibd_backfill.c + Makefile rule): offline
  re-apply of an ALREADY-ARCHIVED block window to the persisted LSM UTXO --
  store_get_at + pread, no network, no store_append. Runs in place over
  data/ (manifest + blob/table maps + WAL utxo.dat + run files). The
  G_force_start clamp lift in ibd_lsm became unnecessary; the clamp stays.
- TWO ibd_lsm defects fixed while bringing the tool up (both uncommitted from
  the previous session + one found today):
  1. Outputs were keyed by sha256d over the FULL serialization = the WTXID
     (BIP141) -- every segwit output it added could never have its own spends
     resolved. Added txid_of(): witness-stripped core (version + vin/vout +
     locktime) hashed, marker/flag/witness excluded. Validated end-to-end:
     computed txids rebuild each block's merkle root EXACTLY (all 9 window
     blocks checked in Python against the header's own merkle field).
  2. tx_out/tx_in (both ibd_lsm.c and the tool drafts) skipped the BIP144
     marker/flag: the marker read as ni=0 and the flag as vout count=1, so
     "vout 0" was 8 bytes of the first vin's prevhash (garbage value/script
     keyed at idx=0) and EVERY segwit input was skipped (no dels at all).
     Symptom in the first production attempt: val=7096155651606458626 sats,
     24 garbage PUT ERRs, added~=segwit-tx-count, missing where none were
     expected. One-line skip `if(o+2<=n && tx[o]==0x00 && tx[o+1]==0x01) o+=2;`
     in all four sites, mirroring tx_walk.
- Method: Python ground-truth parser (strict, witness-aware) over the window;
  per-tx offset tables matched the C tx_walk trace EXACTLY (3203/3203 for
  h965009) -- parse layer proven before touching state. LSM layer proven with
  standalone probes: put->flush->close->reload->get roundtrips at 2 keys and
  50000 keys (single flush) and 3x1000 keys (multi-flush) all verify values/
  heights/cb/slen. Archive bytes themselves verified intact (all 9 blocks
  parse to exactly their indexed sizes; merkle roots match).
- PRODUCTION RUN (ibd_backfill 965009 9 data): reload live=241203649 runs=8;
  added=94801 dup=49 spent=67995 missing=2667 del_err=0 bad=0, flushes=9
  (runs 8->17, utxo_run_000247..255.dat), PERSISTENCE reload_after_close
  live=241230455 MATCH. Log: data/logs/backfill_965009_965017.log. Spot
  check via /tmp-style lsmget probe: h965009 coinbase (raw txid
  417f52a9...06af7 idx 0) -> val=546 height=965009 cb=1 slen=23 script a914..
  -- matches the Python-parsed truth byte-for-byte. Sandbox rehearsal over a
  copy of the production state produced IDENTICAL numbers (deterministic).
- The window choice: 965009..965017, not just 965009..965014. The post-fix
  production run had keyed 965015..965017 by WTXID, so its intra-window
  spends (965016 spending a 965015 output) ALSO reported missing prevouts --
  re-applying the whole window in order resolves every one of them with real
  txids.
- Known residue, all documented and benign:
  * 13992 wtxid-keyed junk entries from the old post-fix run remain
    (unspendable garbage keys; a rebuild-from-assume-valid or a daemon-side
    reconciliation is the full cleanup -- unchanged, out of scope).
  * missing=2667 prevouts are PRE-EXISTING holes in the pre-tip LSM (sampled
    3: none are window txids; same class as the production run's
    MISSING_PREVOUT=316). Not caused by the backfill.
  * The aborted first attempt appended ~3244 garbage (txid,0) puts + 1807
    dels to the WAL (utxo.dat 9.7GB); the backfill re-puts the same keys with
    correct values in-place (the 49 put_dups ARE those re-puts), so the
    residue is shadowed. del never returns 0 for absent keys -> del_absent=0
    in the logs is a counting artifact, not lost deletes.
  * reload live reads 241203649 vs the manifest's total_live 241203607 (+42,
    WAL-tail recount artifact, count-only; next real flush rewrites it).
  * The backup taken before all of this: data/utxo_stale_1788296033/ (trio).
- IBDLSMOBJS now carries the GLV triple (secp256k1_glv_c/glv/glv_mul -- the
  ecdsa GLV dispatch made secp256k1_ecdsa.o reference them) and
  bitcoin_sighash_all_ext.o (sighash_all lives in the _ext half on ARM).
  ibd_lsm/ibd_par/ibd_backfill all link clean again.

## Segfault cluster: five root causes fixed (same session, after the backfill)

- Built scripts/abi_callee_saved_audit_a64.py (AArch64 companion to the x86
  abi_callee_saved_audit.py): UNSAVED-CLOBBER (writes x19-x28/x29/x30/d8-d15
  with no save or no restore), CALLS-OFFENDER, SAVE-AREA-EXPOSED, --check.
  Parser lessons encoded: '#' is the immediate prefix (stripping it as a
  comment fabricated ~2500 offenders), local labels are '.name' style,
  numeric labels share lines with instructions, .macro bodies are templates
  not code, post-index loads keep the mem operand at ops[-2], stlxr writes
  its first operand. Full tree: clean after two fixes. The x86 tool's
  SAVE-AREA-ALIAS check is NOT yet ported -- it would have caught fix (1)
  mechanically; noted as follow-up.
- Fixed, each root-caused by gdb (watchpoints where backtraces were dead):
  1. mac_lsm_reload_impl frame 0x80 -> 0x200 (x86 parity): the WAL-replay
     loop stages 49-byte PUSH bodies at [x28,#0x60]; +0x80..0x91 overwrote
     the x27/x28 save slots, and the epilogue handed main a record VALUE
     (0x2a00000000 = a real 1803-BTC output) as its lst pointer. This was
     test_utxo_lsm's crash AND the +42 reload-count artifact's likely cousin
     (same overflow, counters land in the same slots).
  2. store_truncate_index_only saved only x29/x30 while using x19-x22 -- the
     "some asm callee clobbers main's x22" the worklog suspected;
     test_archive_truncate_nonmonotonic passes.
  3. interp_push_bool passed the RAW bool arg as stack_push's len instead of
     the normalized 0/1 (x86: "rcx = 0 or 1"): upper-bit garbage -> len=
     0xffffffff -> the copy loop walked 4GB. test_dersig_encoding no longer
     segfaults (now a functional failure: "tx re-hosted at h400000"
     case -- next session).
  4. .Lsh_parse_varint's fail path zeroed the cursor: truncated varint
     prefix -> txcur=NULL -> the sequence(4) copy dereferenced it. x86
     .pv_fail leaves the cursor as-is; port now matches.
     test_interp_legacy_spend no longer segfaults (functional fail left).
  5. __tb_cmp returned its result in x22 (unsaved callee-saved as a return
     channel); now x0, caller updated. test_taproot_parity passes.
- test_utxo_setinfo passes (was SIGSEGV).
- Remaining in the cluster, all FUNCTIONAL now (no segfaults):
  test_dersig_encoding rc=1 ("tx h152841 re-hosted at h400000" case),
  test_interp_legacy_spend rc=1 (2-of-3 multisig case), and
  test_multisig_opcount rc=1 (10x 0-of-20 multisig must exceed the 201-op
  budget via KEY COUNT and be rejected -- port still accepts; sigops
  counting gap). Sweep round 3 totals at the bottom of this file when done.

## Session close-out: sweep round 4 (all fixes in)

- Full sweep (323 plan rows): pass 253 / fail 32 / bench-ok 13 / built 18 /
  skip 12. Round 3 was 248/35 with a STALE verify_p2sh_shim; round 4 has the
  fresh shim + all session fixes: test_taproot, test_taproot_thread_stress,
  test_multisig_opcount back to pass (+3), plus the shim-driven others that
  un-froze (+2 pass, +2 bench-ok).
- STALE-SHIM PITFALL (new, cost an hour): parity_out/verify_p2sh_shim has no
  build rule and the sweep reuses it in place; any .S/.c change means
  shim-driven tests measure STALE code. build_shim.sh (committed) relinks it
  from build_daemon.sh's DAEMONOBJS + daemon C sources minus main.c. Rerun
  after touching anything the production verifier drives.
- multisig_opcount root cause: interp_checkmultisig never charged nKeys to
  the 201-op budget (x86 fixed this 2026-08-26; the ARM port predates it).
  Charged after the nKeys<=20 validation; CHECKMULTISIGVERIFY shares the
  handler. The gdb detour: the test drives a FORKED shim, so breakpoints
  need 'set follow-fork-mode child', and the shim must be fresh.
- Round-4 fail list (32) for the next session: 4 env-only benches
  (checkblock/hashidx/idxscan/taproot_block), test_keepup (known), and 27 to
  triage -- first establish the cd4919f baseline (worktree exists at
  /tmp/baseline_cd4919f; daemon build needs asm/version_gen.h copied in --
  generated, not in git). Suspects: several may be stale-shim artifacts that
  round 3 froze in (script/segwit/multisig/sighash cluster), others genuine
  (dersig_encoding's "tx re-hosted at h400000" case, interp_legacy_spend's
  2-of-3 multisig case, taproot_parallel_arena's 307 "p2wpkh signature
  invalid", the daemon-driven tx_relay/node_config/mempool group).

- [x] Parity 2/6 -- chunk eviction: TrimToSize evicts the worst linearization chunk whole (ancestor-set greedy, bumped chunks merged), floor = chunk feerate + incremental; test_mempool_chunks; gate chunk1 291/291 (deploy j): TrimToSize now evicts the worst
      linearization CHUNK across all clusters (Core v31 GetWorstMainChunk):
      clusters via parent/child adjacency, ancestor-set greedy linearization
      with bumped chunks merged, last chunk = worst; the floor is that chunk's
      feerate + incremental. tests/test_mempool_chunks: bumped parent judged
      by its chunk, chunk evicted whole, newcomer below the worst chunk
      refused, poor grandchild goes alone.

## Serve-process reload stall (found deploying j)
- [x] The serve process ignored SIGTERM for 2-3 min at every stop: the mempool.dat reload's shutdown-flag hook was installed only in the download worker; the serve ran the same reload inline at boot with no hook, so systemd's SIGTERM waited for two 90 s validation timeouts (184 s at 08:22; SIGKILLed by the deploy escalation at 08:38 -- no mempool.dat saved, pool lost). The inline reload also kept the serve loop (inbound accept, legs, shutdown check) from starting for the 10-20 min a 10k-entry dump took at the worker's 256/250 ms rotation budget. Fix: hook installed in the serve, reload on a thread, shutdown joins it before the dump; worker budget 2048/1000 ms. Structural pins in test_mempool_persist_wiring. Gate reload1 291/291 (deploy k).
- [x] Deploy i: 12-min guard OK (two background onion dials installed in <=7 s, no heartbeat gap). Deploy j (chunk eviction): 12-min guard OK.

## Parity 3/6 -- descriptor engine (gate desc2 292/292; deploy l)
- [x] `asm/descriptor.c`: Core's descriptor.cpp in C -- pk/pkh/wpkh/combo, multi/sortedmulti, sh/wsh, tr with script trees (multi_a/sortedmulti_a leaves, TapLeaf/TapBranch/tweak), rawtr, addr, raw; hex/x-only/WIF/xpub/xprv keys with origins, paths, ranges (hardened ranges from xprv); Core's context rules and limits (bare multisig 3, CHECKMULTISIG 20, 520-byte P2SH); public-form printing (x-only inside tr), checksum, DescriptorID.
- [x] Proven against Core: `tests/test_descriptor_vectors` runs all 43 non-miniscript/non-musig `Check()` vectors from `src/test/descriptor_tests.cpp` -- private and public forms expanded at every index byte-identical to Core's scriptPubKeys, public-form strings equal to Core's, descriptor ids equal (sha256 of pub#checksum, reversed), hardened-pub forms parse but refuse to expand, 12 parse-error rules. 251 checks.
- [x] rpc_chain.c: getdescriptorinfo (public form + its checksum, hasprivatekeys), deriveaddresses (every type with an address; xprv derivation), scantxoutset/scanblocks/getdescriptoractivity expand through the engine (combo scans all four forms); importdescriptors accepts pkh/wpkh/sh/wsh/tr/addr; wallet_scan.c matches P2WSH/P2TR by the program's first 20 bytes.
- [x] `descriptorprocesspsbt`: real -- the Signer with keys from the descriptors themselves (WIF or xprv-derived at the matching index; sh() redeemScript supplied), delegating to signrawtransactionwithkey; walletprocesspsbt refactored onto the same psbt_process path. Signs P2PKH/P2WPKH/P2SH-P2WPKH (the raw signer's forms); others left unsigned, complete=false. Tests in test_rpc_psbtfinal.
- [ ] Not done: createwalletdescriptor for other address types (the wallet is single-type by design; refusal unchanged and documented); miniscript; musig().
- Note: the boot reload of a 10k-entry mempool.dat still takes ~10 min (the submitter polls every 3 ms and the worker services it once per rotation); the serve loop no longer waits for it.
- [x] Gate desc1 failed on test_rpc_transport: linking secp256k1_taproot.o into RPCCHAIN added an 8.5 MB thread-local segment (the taproot asm keeps scratch in .tbss), leaving every pthread ~150 KB of stack. Fix: the engine has its own tagged hashes + bip32_xonly_tweak_add (plain C, no TLS); taproot.o stays out of the RPC libraries; descr_t (~45 KB) is heap-allocated in the RPC paths. Gate desc2 green.

## Parity 4/6 -- Erlay reconciliation rounds: FINDING, nothing to build
- Core v31.99 (the oracle source) implements only the sendtxrcncl negotiation: node/txreconciliation.cpp has PreRegisterPeer/RegisterPeer/ForgetPeer/IsPeerRegistered and net_processing.cpp has no reqrecon/sketch/reconcildiff. daemon/txrecon.c already matches that. Rounds would be beyond-Core work with no counterpart to test against (libminisketch.a is in the Core build tree if ever wanted). README's Erlay bullet now says so.

## Parity 5/6 -- inbound I2P (gate r56)
- [x] Acceptor thread in the serve process: dialer_i2p_accept() = SAM STREAM ACCEPT on the dialer's session (created before the worker fork; SAM lets any number of stream sockets reference it); each completed stream ({fd, caller's b32}) goes down a pipe the serve loop polls as an extra pfds slot and takes the ordinary inbound path (capacity, fork, handshake). listen=1 + i2pacceptincoming honoured. tests/test_i2p_inbound (fake SAM: session, first stream with identity + bytes, re-arm for the second). README/OPERATIONS/sample.conf updated. Live proof needs a remote I2P peer that can reach our leaseset (this i2pd reports itself firewalled).

## Parity 6/6 -- block templates from linearization chunks (gate r56)
- [x] getblocktemplate now fills the block with whole chunks in feerate order across clusters (Core v31 BlockAssembler over the cluster linearization): per cluster the ancestor-set greedy with chunk merging, a chunk that does not fit skips the rest of its cluster (later chunks depend on it). Test: sibling cluster {P2,C2,D2} + standalone S -- D2 alone (32.8 sat/vB) would have led S (26.2) under ancestor-package greedy; as a merged chunk (30.1) the cluster goes whole, S after it. README mining bullet updated.

## Follow-ups from the l/m watches (gate n1; deploy n)
- [x] Reserved-slot dialer re-dialled the same dead onion every 5 s (l watch): the chooser always took the first pool entry of that network. Now a rotating cursor per network (dh_reserved_pick), skipping hosts a live leg holds; test_dialhelper section 4.
- [x] `<b32>.b32.i2p:0` never parsed -- bmc_addr_from_string_port refused port 0 -- so leg_net_of() classed every I2P pool entry as IPv4: the reserved-slot chooser never saw an I2P candidate and the inline re-dial tried them as clearnet ("i2p peer needs its transport"). Port 0 is now accepted for I2P only (Core's convention); test_netaddr pins i2p:0 / ipv4:0 / onion:0.
- Deploys: l (descriptor engine) 12-min guard OK; l's stop of k was clean in <1 s with mempool saved 6202 (the serve-reload fix proven at a real stop); m (inbound I2P + chunk templates) live, "[i2p] accepting inbound streams on ...b32.i2p" logged at boot.
- [x] Deploy n: 12-min guard OK. FIRST OUTBOUND I2P LEG on mainnet: "[dial] czbt...b32.i2p:0: dialing in the background (i2p)" -> "filled outbound 7 = czbt...b32.i2p:0 (fd 31) proto=70016" 24 s later, alongside a background onion leg in slot 6 -- the port-0 parse was what had kept I2P candidates invisible.

## Signer round (gate o1; deploy o)
- [x] BIP340 Schnorr signing in C (`asm/bip340_sign.c`: sign, x-only pubkey, BIP341 tweaked signing key): all 19 rows of Core's bip340_test_vectors.csv (8 signatures byte-identical, 19 verify verdicts through the consensus schnorr_verify) -- tests/test_bip340_sign.
- [x] Raw signer (`signrawtransactionwithkey`, and so walletprocesspsbt/descriptorprocesspsbt): legacy P2SH multisig, P2WSH and P2SH-P2WSH (CHECKMULTISIG / single CHECKSIG witnessScript), P2TR key path for tr(KEY) with no tree (BIP341 sighash incl. ANYONECANPAY/SINGLE; SIGHASH_DEFAULT unless a type is asked for). Partial multisig -> complete=false "Missing signatures: have X of K". descriptorprocesspsbt supplies witnessScript (wsh, sh(wsh)) from the descriptor.
- [x] PROVEN against Core v31.99's script engine: validation/signer_core_diff.sh (scratch regtest bitcoind, signrawtransactionwithkey with an empty key set = VerifyScript) -> 11/11 (7 complete forms accepted, 4 partial/wrong-key correctly incomplete). The first run caught a real bug: witness items were emitted as script pushes (0x4c prefix for a 105-byte witnessScript), which Core refused as "TX decode failed"; the single-key and P2TR cases had passed because their items were < 0x4c. Fixed; KATs frozen in test_rpc_signraw.
- [x] Reload poll: SRT_POLL_US 3000 -> 500 (the 20-minute 10k-entry reload).

## INCIDENT 11:16-12:40 -- boot header sync accepted a genesis-first answer (deploy o boot)
Full report: docs/devlog/INCIDENT_2026-09-01_header_sync_genesis_answer.md. Summary: headers.dat lagged the archive by 12 blocks (the worker's leg sync never updated the mirror); the boot sync asked 93.115.26.6 from that stale point with a single-hash locator; the peer answered from genesis; node_ibd_headers appended 966,669 headers as heights 965018+; dlc_span extended index.dat to 1.93M records and 16 workers wrote 11,516 real early blocks (2.67 MB) under fake heights into the tail slack of blk00000..41. No real frame overlapped (proven), pverify [0,221022] + tail CHAIN VERIFIED. Stop hung (catch-up ignores SIGTERM) -> SIGKILL by PID. Fixes: linkage guard + rollback (2a98daa), header sanity cap, header mirror topped up from the archive at boot and after every stored block, boot self-heal trimming the derived files' junk tails (archive_trim_derived_tails -- this is how production repaired itself on the p boot), catch-up honours SIGTERM. Tests: test_dialhelper s5, test_archive_trim. Deploy p = o + these.

## Wallet output types (gate t1; deploy t)
- [x] createwalletdescriptor legacy / p2sh-segwit / bech32m: Core-shaped descriptors (pkh 44', sh(wpkh) 49', tr 86' over this wallet's m/<purpose>'/0'/0'/<i>/<chain> ordering), persisted as wallet.types; the type rides in the high nibble of a record's branch byte (no journal format change); the key window has one window per active type (pkh/wpkh key hash, sh script hash, tr Q prefix); getnewaddress(label, address_type) / getrawchangeaddress(address_type); listdescriptors/exportwatchonlywallet list every active type; wallet coins carry their own scriptPubKey (+redeemScript for sh(wpkh)); signrawtransactionwithwallet hands over every active type's keys (raw-signer key cap 64 -> 512 -- the cap silently dropped the 49'/86' keys). Tests: test_rpc_wallet_ops (activation, listing, addresses equal deriveaddresses of the listed descriptor, inactive type refused Core-style), test_rpc_signraw (the wallet signs its legacy / p2sh-segwit / bech32m coins).
- 12:41 status: deploy u live (C header fetch); UTXO set rebuilding from genesis (collateral of the t stop, see the report's "Collateral" section); do not restart until the replay reaches the tip. Committed, NOT yet deployed (v): 2fccb81 boot exits on shutdown after the catch-up; 6a4609d atomic UTXO checkpoint (utxo.idx.tmp + rename). Gate u1's single failure was a constant-time timing-ratio test under replay load; rerun after the replay.
- 12:52 deploy v (6a4609d: boot exits on a stop after the catch-up + atomic utxo.idx checkpoint), restarted with dbcache=32768 (bulk table 2^26 slots, 24 GB blob) to speed the replay: clean stop at height 340578 in 10 s, boot resumed at 340578 (manifest_n=9), header fetch "+2 from 81.221.129.62 (total 965036)". config/bitcoin.conf backup: bitcoin.conf.bak-20260901-dbcache.
- 13:10 assumevalid honoured, Core semantics: the chain's built-in block by default (Core v31.99 defaultAssumeValid: mainnet 938343, testnet4 123613, signet 293175), an explicit hash, or 0 = evaluate every script. Only tx_verify's script EVALUATION is switched off at/below that height (PoW, merkle, structure, UTXO checks unchanged; submitblock dry runs always evaluate). Motivation: perf profile of the UTXO replay = 55% secp256k1 arithmetic re-verifying signatures of blocks already verified once; README/OPERATIONS/sample.conf updated. Deploy w restarts the replay with it.
- 13:22 replay speed-ups, measured with perf on the live worker: (1) assumevalid honoured (deploy w/x): 55% of CPU was secp256k1 re-verifying signatures of blocks already verified once -> gone; the first cut gated only the per-tx path, the block-connect batch path (txvb_verify_one) needed the same gate (d34f59d). (2) coinstatsindex=0 for the replay (config, temporary): the per-block MuHash fold (chacha20 + num3072_mul, 48% after (1)) is waste when the index re-seeds from one 5.5-min walk at boot anyway. Rate at ~385k: ~30/s -> ~100/s. The hourly check-in re-enables coinstatsindex=1 (re-seed walk), runs the gate and the guard once the replay reaches the tip.
- 13:32 deploy y (b3d47a9): the memtable flush writes through a 1 MB buffer (was 3 write() syscalls per record; 265M syscalls for 60 GB; 100 s stall per flush). Replay resumed at 410514; ~80 blocks/s at 415k with assumevalid + coinstats off. Levers used today, in order of effect: assumevalid (2x), coinstats index off for the replay (1.5-2x), dbcache 32 GB (fewer flushes), buffered flush (removes the flush stalls). Remaining cost is the apply itself (LSM lookups, sha256, memcpy) -- single-threaded by design.

## 14:20 — deploy z: buffered-flush regression (sparse-index offsets) fixed

Deploy y's 1 MB write buffer in `mac_flush` (b3d47a9) left the sparse-index
sampling on `lseek(SEEK_CUR)`, which excludes buffered bytes: after the first
drain every sample pointed short of its record (often mid-record), lookups
through the new run scanned garbage and missed, and the replay hit
`REJECT … input references a missing/already-spent UTXO` on the first block
after EVERY flush — 13 times between 445239 and 520440 (~6 min apart), each
recovered by `recover: compact manifest_n=2 -> 1` (the compaction's sequential
rewrite has correct offsets, so the set was never wrong on disk — the replay
just lost ~30 s per flush and ran `[UTXO DEGRADED -- retrying]`).

Fix (bc098fd): sample = `lseek + mac_fl_fill` (the logical offset).
`tests/test_lsm_flush_sparse` flushes 40,000 records (156 strides, several
drains) once and looks every key up through the run via the asm and mmap
paths and after a reload: **11,968 misses on the deploy-y object, 0 now**. A
3,000-record version passed on BOTH objects — with no drain the lagging
samples all point at record 0 and the forward scan still finds everything,
which is why the existing suites (bloomsat compares the two lookup paths
against each other, not against the truth) never saw it. Lesson: a test for
a buffered path has to cross the buffer boundary.

Deployed as `bitcoind.deploy-20260901z` with a clean stop (17 s: "stopping
catch-up cleanly after height 539017") and start; replay resumed at 539018
(55.9%) on the new binary at 14:24.

## 14:58 — deploy aa: flush hot spots (perf-guided)

Two perf profiles of the replay worker on z. Flush phase (a 56-77 s pause
every ~5.5 min, ~20% of wall): `mac_copy_rec` 70% — a one-byte-at-a-time
64-byte descriptor copy called O(n log n) times by the merge sort;
`mac_bloom_h` 12% (36-iteration byte loop); sort inner 7%. Apply phase:
`lsm_run_lookup_mm` 10.5% (forward scan of up to 256 records after the
sparse search), sha256 6%, WAL `mac_wr_log`+vfs ~6%, memcmp/memcpy ~5%; one
core at 99.9%, nothing else contending.

Landed (f2016d4): `mac_copy_rec` = eight qword moves; `mac_bloom_h` fully
unrolled with the identical FNV sequence (existing runs' bloom bits
unchanged). LSM suites + test_lsm_flush_sparse pass. Deployed
`bitcoind.deploy-20260901aa` with a clean stop (4 s, after 618297) / start.

Left for after the tip: SPARSE_STRIDE 256 -> 64 (new runs only, ~7%),
per-block WAL write buffering (~6%; offsets must stay logical — see 14:20),
and a background double-buffered flush (~20%, design pass, not mid-replay).

## 15:25 — deploy ab: buffered WAL, per-block undo fd, SPARSE_STRIDE 64 (6ead9fb)

Second-flush data on aa: pauses 30 s (15:04), 85 s (15:12, right after a
boot with manifest_n=6 ≥ compact_at=4 — likely a compaction in the same
window), 51 s (15:20). Baseline on z was 56–77 s. So the copy/hash rewrite
roughly halves an ordinary flush but the pause is not a constant.

perf apply-phase hot spots taken on: (1) the WAL wrote one record per
write(2); (2) the undo file did open/write/write/close per spent input
(FOUR syscalls per input — more than the WAL itself); (3) the mmap run
lookup scanned up to 256 records after the sparse search.

- Store WAL is buffered (1 MB, process-wide, `wal_buf`); `log_len` is the
  LOGICAL length at all times (the 14:20 lesson). `utxo_store_wal_drain`
  exported; implicit drains before checkpoint, reload, close, at the top of
  `mac_flush` (which truncates the WAL), and when another store's fd wants
  the buffer. Crash semantics: only the undrained SUFFIX of the op stream
  is lost, i.e. a partially applied block, which the ghost guard already
  reverses: undo records are written immediately, a restore over a present
  coin is a put returning 0 (no-op, no coinstats add), and the
  created-outputs delete is get-guarded. Verified by probe (put over an
  existing key returns 0; new returns 1).
- Undo file stays open across the block (one writev per record);
  `undo_close_current()` at the block boundary; discard/prune close a
  cached fd before unlinking that height (a re-append after a discard must
  not write into the unlinked inode — pinned in test_undo_log).
- `apply_block_at` = inner apply + undo close + WAL drain on every exit;
  `persist_applied_height` drains first.
- `SPARSE_STRIDE` 256 → 64: readers use the header's `sparse_n`, old runs
  unchanged, compaction sizes its own buffer from the constant.
- New `tests/test_utxo_wal_buffer` (logical length, explicit/reload/
  checkpoint drains, fork+_exit suffix loss, self-drain past 1 MB). The
  crash-recovery, ghost-resume, catchup-crash-resume, checkpoint,
  live-count, reorg, undo-diff and LSM suites pass; abi/callee checks OK.
Deployed with a clean stop (1 s, after 651735) / start at 15:25.

## 15:32–15:45 — working-set cliff after ab's first flush (config fix, no code)

Right after run 000036 landed the apply thread dropped to 2–5 blocks/s in D
state (`folio_wait_bit_common`), reading ~155 MB/s. perf: write faults on
the FILE-BACKED memtable mapping (`utxo_lsm_table.map`/`utxo_lsm_blob.map`,
6.5 GB) and lookups through 10 mmap'd runs (30 GB) — the run pages no
longer fit in the page cache next to the worker's 18 GB of anonymous flush
scratch (2^26 slots from dbcache=32768: an 11.4 GB desc array + 2×2 GB).
Two amplifiers: bulk mode multiplies the compaction threshold by 4
(default 12 → 48 runs before a compaction), and I started the Core oracle
in the same minute (its LevelDB opens pulled GBs through the same cache;
it failed to bind anyway — see below).

Config (backup `bitcoin.conf.bak-20260901-compact`): `dbcache=16384`
(blob 24→12 GB; NOTE bulk_slots stays 2^26 so the scratch did not shrink)
and `bmc.utxocompactthreshold=2` (→ 8 in bulk). Clean restart 15:40:10
(1 s stop, ~400 MB WAL). Boot pre-catch-up compaction 10 → 1 run in
2.5 min (15:42:34–15:45:00); replay resumed 15:45:02 at 33–79 blocks/s,
~40/s once the 20 GB compaction output finished writing back.

Oracle: production `bitcoind.service` is ACTIVE (since 08-31 13:02,
`<core-checkout>/bin/bitcoind`, owns 0.0.0.0:8333) — the oracle must run
with `-port=8334` (its conf's sixteen `bind=127.0.0.x` lines follow -port;
an extra `-bind` duplicates one). Start it only when the replay is at the
tip; then `gettxoutsetinfo muhash <height>` vs ours.

## 16:30 — taproot PSBT + script-path signing (main checkout; parallel workstreams running)

The "remaining holes" program started: four agents in worktrees
(`feat/config-surface`, `feat/miniscript`, `feat/musig2`, `feat/feeest`),
each with a Core differential and a gate before it reports; nothing merged
yet. In the main checkout, on the raw signer / PSBT layer:

- PSBT (BIP371) taproot fields in/out; `decodepsbt` now names
  `taproot_*`, `partial_signatures`, `sighash`, `bip32_derivs`,
  `final_scriptwitness` as Core does (paths as `m/86h/...`).
- `signrawtransactionwithkey` signs P2TR script-path spends of `pk()` and
  `multi_a()` leaves (leaf + control block through an internal prevtx
  channel: `tapLeafScript`/`tapControlBlock`), key paths with a merkle root
  (`tapMerkleRoot`/`tapInternalKey`), and accepts `"DEFAULT"` as a sighash.
- `psbt_process` signs in rounds (key path, then leaf 0, 1, ...), keeps the
  best per-input result, re-assembles one tx, extracts `TAP_KEY_SIG` /
  `TAP_SCRIPT_SIG` partials, carries partials left by another signer, and
  the wallet derives leaf keys named by a bip32 origin under its own
  fingerprint (`tapBip32`). A partially signed multisig under finalize=true
  is now left untouched (complete=false) instead of getting bogus final
  fields.
- Evidence: `validation/signer_core_diff.sh` 18/18 (7 new taproot cases,
  Core's VerifyScript as judge, negatives included);
  `tests/test_rpc_psbt_taproot` 17/17; psbtfinal 50/50, signraw 39/39.
- Also: `addhdkey` was already real (docs said "one left"); the
  catch-up tick now prints blk/s (instantaneous + average) and a
  DD:HH:MM:SS ETA (deploy ac, 16:05).

## 16:35 — txospenderindex (Core v30 `-txospenderindex`) landed in main

`daemon/txosp_format.h` (28-byte records: 12-byte spent-txid prefix, vout,
spender height/offset/len; sparse every 256), `daemon/build_txospender_index`
(two-pass external sort, header last, like build_tx_index), `daemon/txosp_tail.c`
(unsorted append-only tail, boot backfill, reorg watermark, folds into a
rebuilt base), a verifying reader in `rpc_chain.c`
(`rpc_chain_txospender_lookup`: the spending tx is read from the archive and
must carry the full outpoint), `gettxspendingprevout` with Core's `options`
(`mempool_only`, `return_spending_tx`), `spendingtxid`/`spendingtx`/`blockhash`
and Core's exact error when the index is absent, `getindexinfo` entry. Weak
symbols keep the mempool-only test binaries linking. Test:
`tests/test_txospender_index` (runs the real builder on a synthetic archive;
28 checks). The mainnet base build (~35 GB, one archive pass) is a post-tip
operator step — see OPERATIONS.md.

## MuSig2 workstream (branch feat/musig2, worktree)

- `asm/musig2.c/.h`: BIP327 over the node's EC kernels; `tests/test_musig2`
  runs libsecp256k1's copy of the BIP vectors, every group incl. failures
  (57/57), aggregated sigs re-verified by the consensus `schnorr_verify`.
- `asm/rpc_commands.c` (PSBT area only): BIP373 fields, Core's `SignMuSig2`
  rounds for P2TR key-path aggregates (direct / synthetic-xpub-derived /
  taproot-tweaked), in-process nonce sessions erased on use, `decodepsbt`
  shapes, `finalizepsbt` tap_key_sig → witness. `tests/test_musig2_psbt`:
  a 2-of-2 session end to end with the signature checked over an
  independently computed BIP341 sighash (20/20).
- `validation/musig_core_diff.py` + `musig_shell.c`: Core-judged 3-of-3
  sessions (two Core wallets + this node), five patterns, 110/110. Two
  harness lessons: master Core defaults to PSBT **v2** (ask
  `psbt_version=0`), and `getdescriptorinfo`'s `descriptor` strips private
  keys (append its `checksum` to the original text instead).
- Not done: `musig()` descriptor parsing (spec in musig2.h, descriptor.c is
  the miniscript workstream's), leaf-script aggregates.


## config-surface workstream (branch feat/config-surface, 795d47e+)

Every one of Core v31.99's 181 `-help-debug` options classified against every
parse site: 133 implemented, 48 accepted without effect (each named with its
reason at start-up via the `k_noeffect` table), 0 unrecognised. ~40 options
gained real behaviour today (user agent on the wire, template budget/fee
floor/version, IBD age, wallet address/change types, fee floors and Core's
fallbackfee error, RBF/broadcast defaults, partial-spend avoidance, wallet
autoload, walletnotify, RPC pool/queue/timeout/whitelist/cookie mode,
includeconf, log prefix flags, shrinkdebugfile, BIP157 serving gate,
inbound relay share, relay/forcerelay permissions, signet seeds, cluster
limits). Full table in docs/FEATURE_GAPS.md; operator notes in
docs/OPERATIONS.md; every key in config/bitcoin.sample.conf.

Evidence: tests/test_node_config (+14), tests/test_rpc_whitelist (new),
tests/test_netperm (relay perms), and validation/config_surface_core_diff.sh
-- the same option on a scratch regtest Core and here, compared on the
observable it changes: 18/18. Behaviour change to flag before deploy:
`peerblockfilters` follows Core's default of 0 -- the live node must set
`peerblockfilters=1` to keep serving BIP157 (and now advertises
NODE_COMPACT_FILTERS when it does). Partial: whitelistrelay/forcerelay bits
are set but not enforced; inboundrelaypercent counts all inbound peers.

## feat/feeest — fee estimation is Core's estimator (CBlockPolicyEstimator port)

`estimatesmartfee` answered Core's contract over an accepted-feerate EMA;
`estimaterawfee` did not exist. `asm/daemon/fee_estimator.c` is now a C port
of v31.99's `policy/fees/block_policy_estimator.cpp`: three TxConfirmStats
horizons (12×1 .962 / 24×2 .9952 / 42×24 .99931), the 100…1e7 ×1.05 buckets
(+INF), NewTx/Record/UpdateMovingAverages/EstimateMedianVal/removeTx in the
same arithmetic order, the 60/85/95 % smart ladder + conservative,
estimateRawFee, MaxUsableEstimate, `fee_estimates.dat` (hourly + shutdown
flush with FlushUnconfirmed; 60 h max age). One MAP_SHARED region beside the
mempool (allocated pre-fork in mempool_cfg.c, `mp_ext_feeest`) under the
mempool's process-shared lock: the download worker feeds admission
(`txacc_fee_note` after every successful `mpool_policy_add`, valid = tip
within 3 h && no in-mempool parents && not a package) and block connection
(`tx_accept_block_connect_h`: begin(height) → the policy's confirmed hook
books mined txs → end), the forget callback books every other removal
(eviction/RBF/expiry/conflict) as a failure for the periods it outlived, the
RPC process reads. Weak no-op stubs in tx_accept.c / mempool_cfg.c keep the
~300 test binaries linking; `fee_hooks.c` carries the strong ones.

Evidence: `tests/test_fee_estimator` (Core's policyestimator_tests scenario
line by line — same expectations at blocks 3/200/250/265/266/665 — plus the
bucket lower_bound, tracked-map churn, removal-as-failure at the right
period, smart "blocks" semantics incl. the fresh-node 0, file round trip and
corrupt/stale file handling); `tests/test_rpc_node` (RPC shapes, Core's
error strings, per-horizon estimaterawfee, the 1e+99 INF bound);
`validation/feeest_core_diff.sh`: scratch regtest Core + this node over P2P,
60 rounds × 8 txs on a fixed feerate/confirmation schedule mined with
`generateblock`, estimatesmartfee (economical + conservative) and
estimaterawfee for targets 1/2/3/6/12/24/144 at rounds 10/20/40/60 —
**90/90 byte-identical to Core** (the first run's only mismatch, endrange
`0` vs Core's `1e+99`, was a cast of the INF bound through unsigned long
long in the JSON rounding — fixed). Not carried over: `-acceptstalefeeestimates`
(cut-off fixed at 60 h) and the best-header−1 half of IsCurrentForFeeEstimation
(the block-connect site has no header count; the 3 h tip-age rule stands in).
Two harness lessons: Core's inbound relay trickle lags tens of seconds, so
the harness hands each tx to our node directly as well; and `pkill -f` on a
pattern that appears in your own shell's command line kills your own shell.

Rebased onto main `1f25666` (Makefile test list + PARITY_PLAN T5 resolved).
Gate on the rebased tree: `make -k test` MAKE_EXIT=0, `make gate-log-check`
exit 0 ("all 303 gated tests ran; no failure markers"). Core differential
re-run on the rebased daemon: PASS, 90/90 identical (estimatesmartfee
economical + conservative and estimaterawfee for targets 1/2/3/6/12/24/144
at rounds 10/20/40/60; at r60 e.g. target 6 → 0.00015 BTC/kvB both modes,
target 24 → economical 2.502e-05 vs conservative 6e-05, target 144 clamps
to blocks 29 on both nodes), plus the shutdown flush and restart seeding.

## 17:25 — config-surface and fee-estimator merged into main (5012390, 37a4fdb)

Both merged in a scratch worktree over current main and re-verified there
before the fast-forward: test_node_config, test_rpc_whitelist,
test_core_parity, test_netperm, test_fee_estimator, test_rpc_node,
test_lsm_pick_budget, the taproot/musig/txospender/psbtfinal suites;
`validation/config_surface_core_diff.sh` 18/18 (needs `daemon/wallet_cli`
built), `validation/feeest_core_diff.sh` PASS. Live config gained
`peerblockfilters=1` (Core's default is 0; we keep serving BIP157). The
`txospenderindex` config row moved from "not built" to "on when
txospender.dat exists". Deploy: with `ad` at the tip.
## Miniscript + musig() descriptors (feat/miniscript, not merged)

- `asm/miniscript.c/.h`: Core's miniscript in C, no recursion (flat node pool,
  explicit stacks). `tests/test_miniscript`: all 97 Core vectors in both
  contexts + programmatic cases + TestSatisfy against our own interpreter
  with a mock checksig: 44,921 checks. The one engine bug the vectors caught:
  a type with no base letter must sanitize to "invalid" (Core's
  SanitizeType); everything else matched first time.
- `descriptor.c`: wsh(miniscript) and tr() miniscript leaves, Core's sanity
  messages verbatim; fixed a latent tap_leaf_hash 3-byte prefix buffer (any
  leaf >= 253 bytes overflowed it); script buffers to the heap (a 999-key
  multi_a leaf is 34 KB); hex keys under tr() print as given (Core), only
  WIF keys x-only; "tr(): " prefix on a bad internal key; hybrid keys refused
  by name.
- Signer: `miniscript_sign.c` for P2WSH (srw_sign_wsh fallback) and
  tapscript leaves (srw_sign_tap_script fallback, partial sigs from the PSBT
  count as available); prevtx "preimages"/"pubkeys"; psbt_process forwards
  the PSBT's scripts and PSBT_IN_*_PREIMAGES; descriptorprocesspsbt
  forwards the descriptor's pubkeys (pk_h needs them); witness buffers
  grown from 256/1024/1400 to a 3600-byte witnessScript (finalizepsbt's
  256-byte cap included).
- `musig()` descriptors per BIP390 with Core's rules; Core SORTS
  participants before KeyAgg (musig2.h said written order -- wrong);
  descriptorprocesspsbt is the Updater (0x1a/0x16/0x17, per-participant
  derivations).
- Core differential `validation/miniscript_core_diff.py`: 436/436 (58
  descriptors both forms; 41 spends incl. 7 tapscript leaves, Core mines
  36, 5 negatives; 3 MuSig2 sessions with stripped fields). Harness lessons:
  master Core's createpsbt defaults to PSBT v2 (`psbt_version=0`); Core's
  `getdescriptorinfo.descriptor` carries its checksum; the in-process RPC
  shell must be put on regtest (`wallet_set_chain("bcrt",0x6f,0xc4)`) or
  addresses come out bc1; default `maxfeerate` rejects a 0.1 BTC fee.
- Rebased onto main 34d8113 (MuSig2 + taproot PSBT layer): objects that
  embed descr_t (rpc_chain.o, rpc_wallet_ops.o, rpc_signer.o) now list
  descriptor.h/miniscript.h/musig2.h as prerequisites -- a stale
  rpc_chain.o with the old struct layout segfaulted getdescriptorinfo.


## 17:42 — deploy ad (main 152a458): the whole day's program, brought forward by the second cliff

At 17:38 the replay hit the working-set cliff again (6 runs = 30.1 GB,
worker in D reading ~105 MB/s, 1-3 blk/s); the count rule (8 in bulk)
would not have compacted for two more flushes. The byte-budget rule was
already merged and the union gate on main was green (306 tests,
gate-log-check 0), so `ad` went out with a clean restart (WAL 0.21 GB):
byte-budget compaction (boot loop now honours it too), miniscript + musig()
descriptors + tapscript satisfier, MuSig2, taproot PSBT layer,
txospenderindex, config surface (peerblockfilters=1 set in the live conf),
Core fee estimator, ETA tick. No guarded watch during the replay (heartbeats
do not flow); the tip procedure runs it at the tip.

## PSBT v2 workstream (branch feat/psbt-v2)

BIP370 in `asm/rpc_commands.c` as a normalization layer: any PSBT becomes a
v0-shaped buffer (the unsigned tx synthesized from the v2 fields, required
locktimes folded per Core's ComputeTimeLock, v2 per-input/output keys kept in
their maps) and is emitted back in the caller's version with every map sorted
by key -- Core's order, so a v2 Core produced round-trips byte-identical.
createpsbt/converttopsbt/walletcreatefundedpsbt default to v2 (Core master)
and take psbt_version; decodepsbt gained the v2 fields, witness_utxo /
non_witness_utxo / fee, and Core's object shapes for redeem/witness/final
scripts (they were hex strings). combinepsbt is sequence-blind for v2 and
refuses mixed versions; joinpsbts refuses v2; Core's validation messages.
Evidence: tests/test_psbt_v2 35/35; validation/psbt_v2_core_diff.py 75/75
with full decodepsbt JSON identical to Core's. Existing suites kept:
tests/test_rpc_rawtx now asks for psbt_version 0 where it pins v0 bytes.

## desc-multipath-updater workstream (feat/desc-multipath-updater)

BIP389 multipath through the whole engine and RPC surface; the PSBT Updater
role from descriptors (psbt_update.c, inside psbt_process on the v0-shaped
buffer after the PSBT v2 merge); partial signatures both ways for multisig
and miniscript inputs. Two bugs only the Core differential caught: a 0-of-k
leaf attempt produced an empty "partial" that shadowed the real partial
from a later leaf round, and witness_utxo was not materialised for (nested)
segwit inputs the way Core's signer step does. Third bug caught by Core's
tree field: PSBT_OUT_TAP_TREE lists leaves right-most first.
validation/updater_core_diff.py 145/145; miniscript 436/436 and musig
110/110 re-run green on the rebased tree; signer_core_diff 18/18.

## relay-policy workstream (branch feat/relay-policy)

Core's relay policy made real over the wire: `blocksonly=1` (fRelay=0 in
every version we send -- outbound legs, feelers, inbound children --, a tx
or tx-inv from a peer without `relay` disconnects with Core's log words and
NO misbehaviour score, no feefilter, `localrelay:false`, the whitelistrelay/
maxmempool interactions with Core's lines); the permission set `noban relay
forcerelay mempool download addr in` (bloomfilter/out refused with reasons;
Core's implicit set for bare entries incl. mempool and download); the
`mempool` message answered (inv of the pool) only with the permission and
otherwise disconnected unless noban; `download` exempts the upload cap;
`inboundrelaypercent` counts only inbound peers that negotiated tx relay
and is decided AFTER the peer's version is read (new asm hook
`g_accept_version_hook` in node_accept_handshake); inbound peers now fetch
announced transactions (`inv` MSG_TX/MSG_WTX -> getdata MSG_WITNESS_TX) when
the policy allows, so `relay@` admits a peer's txs into the shared pool;
`getpeerinfo` itemizes inbound peers (shared table grown to 128 slots,
64..127 owned by serve children, dead-pid slots skipped) with Core's
`permissions` strings and a truthful `relaytxes` (parsed from the peer's
version fRelay, absent = true); `acceptstalefeeestimates`.
Evidence: tests/test_relay_policy (35 checks), test_netperm updated,
validation/relay_policy_core_diff.sh 24/24 (four runs against a scratch
regtest Core: Core's getpeerinfo relaytxes for the fRelay we sent in both
directions, its tx reaching or not reaching our mempool, identical
`permissions` strings for the same whitelist line on both nodes, a python
stranger probe for the disconnects and the mempool reply).
Not done: onward re-announcement of transactions accepted from inbound peers
(the forcerelay "gateway" half; inbound-accepted txs are not announced to
the outbound legs at all today -- pre-existing), Core's eviction model for
the inbound relay share (we apply it at handshake instead), `addr`
rate-limit semantics (no inbound addr ingestion to limit).

## 20:00–21:00 UTC — host freeze, relaunch, muhash gap root-caused
Box froze at 20:00:33 (hard reset 20:03): a set-diff command in the
muhash hunt used `(k[key])` in awk over a 165.7M-line dump — gawk built a
~62 GB hash with swap already 100 % full; no OOM-kill, pure thrash. Daemon
was healthy and starved. Relaunched `ag` + oracle (-port=8354); both at tip.
Mismatch reproduced at 965080: +2,596 txouts / +5,589.97458543 BTC. Redid
the set-diff correctly (byte-order sorts, gap-adjusted, 14 GB cap): exactly
2,596 extras, 0 missing, amount exact. Bisected with logged `live=` counts
vs Core's coinstatsindex: surplus born in today's rebuild between 340578
and 618297, where eight `FATAL apply_block → in-place recovery` rounds ran
(13:37–14:23, one per flush, b3d47a9's sparse-index offsets). Each round
lost the spends of the block applied just before the failure (471 at
539016, 491 at 428470, …). Full write-up:
docs/devlog/INCIDENT_2026-09-01_oom_and_resurrected_spends.md. Evidence in
/storage/bmc-diff-work. Store not yet repaired; recovery path still blind.
21:21–21:28 UTC — SURGICAL REPAIR APPLIED AND PROVEN. daemon/utxo_repair_del
(new, offline, RW reload): 2,596/2,596 keys present, sum 5,589.97458543 BTC,
deleted, WAL drained, 0 still present; live 165,723,924 → 165,721,328 at
applied 965085 == Core. coinstats.dat retired → re-seed walk 4m40s →
gettxoutsetinfo muhash at 965085 IDENTICAL to the oracle
(7b3938df…8c44, 165,721,328 txouts, 20,078,163.63377124 BTC). Report
committed 11a2bc1 + updated. Still open: gate utxo_live_recover on a full
manifest + walk-count check; regtest repro of the lost tombstones.
23:30 UTC — recovery path fixed: 874c1a8 (branch fix/recovery-honest,
ff-merged). Failure classification + utxo_live_recovery_applicable() gate
(store error AND full manifest only) + utxo_live_verify_after_recovery()
walk check with a sticky HALT; heartbeat marker; new
tests/test_utxo_recover_gate (its first run caught that a correct count
could un-halt -- fixed). Gate: first launch in the fresh worktree FAILED
link-check before objects existed and `make -k` then skipped the whole
test recipe (the 08-30 trap; the audit caught it); relaunch clean (310
tests), and again on main. Not deployed yet.
23:38 UTC — DEPLOY ah (main 549354f, binary built from 874c1a8): the gated
recovery path is live. Clean stop (1 s), RPC up 31 s, reload EXACT
(applied 965099 / live 165,705,470 == pre-stop), coinstats adopted at
965099, muhash IDENTICAL to the oracle at 965099 (c8c6c5c3…3d66). Rollback
= relink bitcoind.live -> bitcoind.deploy-20260901ag + restart.

## Merging main into arm-port (late session)

- origin/main moved 98 commits (b46223c..d87adba): utxo_live incident fixes
  (halt-on-absent-coin, gated catch-up recovery), LSM buffered flush +
  sparse-offset fix, checkpoint-via-rename, boot self-heal/header mirror,
  assumevalid, relay policy hooks, descriptor engine/PSBT updater/BIP340
  signer/miniscript/musig2, I2P, coinstats progress. Merged with two
  conflicts (.gitignore both-sides; worklog both-sides spliced).
- Ported into the ARM tree: bitcoind.S runtime UA + relay byte + accept hook;
  bitcoin_serve.S relay-policy gates (tx/inv/mempool/policy-log/feefilter +
  violation strings + the mempool dispatch arm); bitcoin_utxo_store.S
  utxo_store_wal_drain + drain-before-checkpoint/reload/close; build_daemon.sh
  links main's ten new C modules.
- TWO PERF-ONLY REVERTS, each after a real port attempt:
  * mac_wr_log buffered WAL (1 MB wal_buf): test_utxo_wal_buffer SIGSEGV --
    put's x19 became a record byte (0x8d) after the make-room drain path;
    four gdb rounds (watchpoints, entry-condition breakpoints) did not pin
    it. mac_wr_log back to direct writes; utxo_store_wal_drain kept (no-op
    with fill=0). COST: test_utxo_wal_buffer fails rc=1 by design -- it
    checks the crash-suffix semantics that only buffering provides.
  * mac_flush buffered record writer (1 MB, b3d47a9): test_lsm_flush_sparse
    spun in the append path. Real bug found on the way: the draft held the
    fd in x8 across bl mac_fl_drain -- mac_write_exact clobbers x8 (the x86
    uses callee-saved r12); fixed with an stp x20/x21 sandwich, and STILL
    spun -- the forked-compact interaction with the shared mac_fl_fill bss
    went deeper. Reverted to direct mac_write_exact; SEEK_CUR samples are
    already logical without a buffer (bc098fd's term is buffer-relative).
- gdb traps for the log: pkill -f with a pattern that appears anywhere in
  your own command line kills your own session (the [b]racket trick does not
  protect against OTHER occurrences); stale parity_out test binaries measure
  old code after source reverts (round-6 confusion); setsid nohup survives
  the tool's bash-session teardown, plain nohup & does not.
- Sweep round 7 (347 rows, post-merge): pass 262 / fail 37 / bench-ok 13 /
  built 18 / skip 14. vs round 4 (pre-merge 253/32): +94 rows from main's
  new tests, most pass (lsm_flush_sparse, lsm_lost_tombstones, utxo_lsm,
  mempool_chunks...). Round-7 fail list: the pre-existing script/segwit/
  daemon cluster + new: test_utxo_wal_buffer (buffered-WAL revert cost),
  test_txospender_index, test_rpc_whitelist, test_archive_trim,
  test_signet_block/verify. Next session: triage against the cd4919f
  baseline worktree; then port the two buffered writers with the r12
  discipline (fd in a callee-saved) and a fork story for mac_fl_fill.

## The CHECKSIG cluster root causes (same session, after the merge)

- Round 8 (347 rows): pass 287 / fail 12 / bench-ok 13 / built 18 / skip 14.
  Rounds 4-7's 36-fail script/segwit/sighash/daemon-verify cluster is GONE --
  two consensus bugs in the CHECKSIG path:
  1. der_parse_sig's hashtype out-param: 8-byte str x9 into the caller's
     unsigned int (x86: r9d, 4 bytes). The 4 overflow bytes smashed adjacent
     caller data -- r[0] in test_script, interpreter state in script_eval.
  2. interp_checksig returned the SIG_DER error via mov w0,#-1: 32-bit write,
     ZERO-EXTENDED x0 = +4294967295; script_eval's cmp x0,#-1 (x86: full
     rax=-1 vs cmp rax,-1) read it as success -> bool TRUE pushed -> every
     non-strict signature ACCEPTED post-activation (BIP66 bypass, the
     dangerous chain-split direction). Fixed to mov x0,#-1.
  Fixed by (1)+(2): test_verify_p2sh, test_scriptverify_parity (both
  long-standing), test_dersig_encoding (ALL PASS 63/63), test_script,
  test_scriptnum_bool, test_interp_legacy_spend, test_taproot_parallel_arena,
  test_segwit_coverage/real/sighash, test_p2wpkh_real, test_hashtype_e2e,
  test_checksig_diff, test_cltv_csv, test_checkmultisig_findanddelete/
  verify_pop, test_tapscript_scale, test_scriptverify_thread_stress,
  test_tx_accept_e2e, test_tx_submit, test_tx_verify_parallel,
  test_mempool_accept_modern, test_mempool_consensus_verify, and more.
- ROUND-8 REMAINING FAILS (12): 4 env-only benches (production data files);
  test_utxo_wal_buffer (by design until the buffered WAL is re-ported);
  test_keepup (pushed-block serving, long-standing); and 6 post-merge:
  test_archive_trim, test_mempool_persist_wiring, test_node_config,
  test_rpc_whitelist, test_txospender_index, + one more -- all rc=1
  functional, each needs its own session.
- DEBUGGING TRAP that cost an hour, twice: parity_out test binaries are
  built by the SWEEP, not the daemon build -- after changing a .S, a direct
  run of parity_out/test_X measures the PREVIOUS build. Always rerun the
  sweep filter (or rebuild the test) before concluding a fix did nothing.

## "fix remaining items" session: 12 fails -> 6, all functional tests green

- The serve loop's block dispatch never matched "block": .do_block's movz/
  movk built 0x636f6c6f ("oloc") instead of 0x636f6c62 ("bloc") -- the ASCII
  immediate pitfall again, with the CORRECT value in the adjacent comment.
  Every inbound block push was silently dropped (test_keepup's pushed block
  was never stored/served). Fixed; test_keepup ALL PASS (long-standing item).
- The sweep harness now replicates the x86 asm/ launch layout (tests'
  tt_src() reads getcwd at launch): tests run from $REPO/asm with the ARM
  binaries symlinked into asm/tests/ + asm/daemon/. The wallet_cli/
  bitcoin_cli/bitcoin_rpcd special builds derive their link lines from
  build_daemon.sh's lists EVERY sweep (the stale pre-merge rpcd had zero
  whitelist symbols). Fixed: test_archive_trim, test_mempool_persist_wiring,
  test_node_config, test_rpc_whitelist, test_txospender_index.
- ROUND 9 FINAL: pass 293 / fail 6 / bench-ok 13 / built 18 / skip 14 (347
  rows). The 6: bench_checkblock/hashidx/idxscan/taproot_block (env-only,
  production data files) + test_utxo_wal_buffer (needs the buffered WAL
  re-port -- the ONLY genuine fail, and it fails by design of the perf
  revert). EVERY functional test passes.

## Sync 2 with main (same session): private broadcast + wallet_store v3 + serve violations

- origin/main advanced 10 more commits (d87adba..ea6cb01): private broadcast
  (Core v30), wallet_store v3 strong container, misbehaviour scoring N3/N6/N7,
  third security audit, wallet_cli passphrase prompt. Merged clean (3d47d53);
  private_broadcast.c added to the ARM daemon link.
- test_serve_violation (new): the N3 misbehaviour class -- malformed tx/block
  payloads must SCORE through g_serve_violation_hook, not drop quietly.
  Ported: viol_txparse/viol_blkshort strings + the .do_tx min-length and
  tx_txid-parse gates + the .do_block 81-byte gate + .viol_report_next
  (distinct from the policy_log disconnect path). PASS.
- The sweep's special builds (wallet_cli/bitcoin_cli/bitcoin_rpcd) now derive
  from build_daemon.sh's lists every sweep, so main's new modules link
  automatically (the hand lists were ten modules behind and the rpcd measured
  pre-merge code -- zero whitelist symbols). Adding mempool_cfg.c +
  bitcoin_mempool_policy.c + bitcoin_mempool.o + node_config.c +
  txval_modern/mempool_compact/bip32_ckdpub.c + wallet_book.c for the
  fee_hooks/mempool/whitelist/book references.
- ROUND 11 FINAL (351 rows): pass 297 / fail 5 / bench-ok 13 / built 18 /
  skip 14. The 5 fails: 4 env-only benches + test_utxo_wal_buffer (buffered
  WAL re-port pending). ZERO genuine functional failures on the board.

## SESSION HANDOFF (2026-09-02 morning) -- workstate for the next session

LIVE SYSTEM: bmc-arm.service ACTIVE (systemd, Restart=always), tip tracking
the network (965201 at handoff), RPC 8331 (bitcoin:bitcoin), P2P 8332, UTXO
tracking DEGRADED (the only degraded subsystem; everything else works).

IN FLIGHT: the LSM rebuild -- build_utxo RUNNING (nohup, log /tmp/rebuild.log,
output data/rebuild/ -- symlinked archive, slots 2^23, blob 2GB, 362 runs
flushed at handoff). It replays the WHOLE archive (1.43B txs) into a FRESH
LSM; hours to complete. The live daemon is unaffected (degraded-UTXO soak
continues).

WHEN THE REBUILD COMPLETES (check: pgrep -f build_utxo; the log's final
"[build_utxo] final live=... runs=..." line):
  1. VERIFY: the fresh LSM's live count ~241.25M (clean -- no 13992 wtxid
     junk, no double-apply artifacts); re-run the window capture
     (ibd_backfill 965009 9 data/main -- it now logs EVERY missing prevout):
     missing MUST be 0 or explainable. If the rebuild reproduces holes ->
     build_utxo itself has a parse gap -> muhash diff vs main's recorded
     values at matching heights is the next checkpoint.
  2. MAINTENANCE WINDOW: systemctl stop bmc-arm (wait for inactive -- the
     clean LSM close takes up to 180s; the unit may show 'failed' if the
     budget expires -- harmless), swap data/main/utxo_lsm_table.map +
     utxo_lsm_blob.map + utxo_manifest.dat (+ any utxo_run_*.dat the rebuild
     wrote into data/rebuild/ -- move them ALL into data/main/) with the
     rebuild's files.
  3. WRITE data/main/utxo_applied_height.dat = the archive tip at the
     rebuild's end, in EXACTLY 12 bytes: magic 'UAPH' (0x48504155) + int64 LE
     height (python: struct.pack('<Iq', 0x48504155, H)). A 16-byte write
     shifts the height into the high half and the replay goes rogue.
  4. systemctl start bmc-arm -> the LSM reload -> utxo_live resumes at
     tip+1 -> DEGRADED clears ("[dl] heartbeat: ... " without the UTXO
     DEGRADED tag; txouts advances past 241.27M).
  5. Watch: journalctl -u bmc-arm | grep -E 'utxo|catchup' -- the first
     applied blocks confirm the fix; then the weeks-long soak proceeds.

THIS SESSION'S FIXES (all pushed through 96e0917 + the worklog commits):
  - der_parse_sig's 8-byte hashtype store (smashed caller data) ->
    test_verify_p2sh + test_scriptverify_parity + ~18 more tests green.
  - interp_checksig's zero-extended w0 -1 return (BIP66 bypass).
  - the serve loop's 'oloc'/'bloc' block-dispatch constant (test_keepup).
  - the sweep harness: asm/ launch layout + build_daemon.sh-derived special
    builds + build_shim.sh + the txospender builder.
  - test_serve_violation's N3 misbehaviour class (post-merge port).
  - ibd_backfill: repair-mode logging + deferred recount.
ROUND 11 FINAL: pass 297 / fail 5 / bench-ok 13 / built 18 / skip 14 (351
rows) -- the 5: 4 env-only benches + test_utxo_wal_buffer (needs the buffered
WAL re-port; fd in a CALLEE-SAVED reg like the x86's r12).
