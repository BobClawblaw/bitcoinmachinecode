# DAILY WORKLOG — 2026-09-13

> **Backfilled 2026-09-16 from the commit record**, not written live. The daily
> file was missed on the day; PR numbers, commit subjects and landing order are
> from git and are reliable, but the session narrative and any reasoning that
> never reached a commit message are not recoverable and are not invented here.

A day spent almost entirely on the instruments rather than the node. PRs
#190–#196.

## The measuring equipment was not trustworthy

- [x] `94a27ead` **sweep: `$!` captured the subshell, not the daemon — three of
  four arms never ran.** The headline defect of the day.
- [x] `49e804f8` the worker sweep measured nothing; two tree-wide checks added
  so it cannot recur.
- [x] `087371d6` / `c9c92cdb` **a differential that compared nothing is not a
  passing differential** — all **17** differentials now refuse to report success
  over an empty case set.
- [x] `63752b40` **the capstone's Core baseline PASSES, and the published report
  was wrong in both directions.**
- [x] `6e13350d` the instruments that measure this node now have tests of their
  own.
- [x] `db1b8cd8` why the exec bit kept coming back (`core.fileMode=false`, so
  `chmod +x` never reaches a commit — use `git update-index --chmod=+x`), and
  tested readers for the Core runner.
