# 2026-08-22

## Session (continuous from 08-21, user on autonomous overnight authorization from ~03:00): verify-path perf ×4.4, genesis off-by-one, SIGKILL/checkpoint window, GLV

Long-form incident narratives are in `LOG.md`'s 2026-08-22 entry; numbers
and profiles in `PERF_SCOPE.md`. This is the terse action log, in order.
Times UTC. Production = `bmc-bitcoind.service`.

### 00:52–01:28 — three landed branches, then the genesis discovery

- [x] `54cc988`: two pre-existing lost-carry bugs. `sc_mul`'s MULACC
      propagated carries only two limbs (lost `2^256 ≡ DELTA mod n`);
      `fe_mul`'s fold-2 dropped the carry out of limb 3 (lost `2^256 ≡ C`).
      Deterministic on structured inputs — on the OLD code `sc_inv(6)` is
      simply wrong vs `pow(6,-1,n)`, `sc_inv(n−k)` wrong for 4095/4096
      small k, `fe_inv(p−k)` for 1607/4096 — but ~2⁻⁶⁴ / ~2⁻¹⁹⁰ on random
      operands, so 200,000 random `a·inv(a)==1` trials pass on both old and
      new code. That is why every random differential missed them. Fail-
      closed (a wrong `s⁻¹` rejects a valid signature; cannot forge).
      `tests/test_mul_carry_regression.c` pins exact operand pairs.
- [x] `9e11146` PERF_SCOPE 4.2 A+B: projective x-compare (drops `fe_inv`,
      incl. the `r+n` branch that a real signature reaches with p≈2⁻¹²⁷, so
      it is tested at function level with a constructed point) + `sc_inv_var`
      (binary xgcd, variable-time, dispatched ONLY at `ecdsa_verify`'s
      public `s⁻¹`; Fermat `sc_inv` untouched for signing).
      `tests/test_ecdsa_inverse.c`: 1,001,023 inverse cases, 6,008 compare
      cases, **113,315 `ecdsa_verify` new-vs-frozen-reference cases**
      (`tests/ecdsa_verify_ref.asm`), 0 mismatches. 115.4 → 55.7 µs
      (**2.05×**); gap to libsecp256k1 5.2× → 2.6×.
- [x] `b2ccb2d`: BIP342 `codesep_pos` tracked in `script_eval`
      (opcode position of the last EXECUTED `OP_CODESEPARATOR`, tapscript
      only; Core `interpreter.cpp:443/444/1065`), carried via a third
      `interp_slice` field. The old byte-level `0xab` refuse scan — which
      also rejected any tapscript whose *push data* contained `0xab` — is
      gone. 18 vectors incl. four "wrong/none position → REJECT" controls.
- [x] `090a109`: 9 blockchain-query RPCs in `asm/rpc_chain.c`
      (`getblockcount`, `getbestblockhash`, `getblockhash`,
      `getblockheader`, `getblock` v0/1/2, `getblockchaininfo`,
      `getrawtransaction` with blockhash, `uptime`, `stop`), Core-shaped
      for direct diffing against the oracle. 124 checks. Methods needing
      live peer/mempool state stay `-32601`, documented.
- [x] **Genesis was not in the archive.** Found while wiring RPC, confirmed
      against the oracle across the whole archive: `index.dat` record N is
      real height N+1. `apply_block_at` passes the record index to
      `script_flags_for_block`, so DERSIG/CLTV/CSV/NULLDUMMY each missed
      their own activation block (linked the real flags object: bits 0x4 /
      0x200 / 0x400 / 0x10 absent at 363725 / 388381 / 419328 / 481824).
      False-ACCEPT direction; structurally invisible to the replay (looser
      rules accept a superset). Re-downloading cannot fix it — the P2P
      "from the beginning" locator is the all-zero hash and peers answer
      from block 1 (`bitcoind.asm:1247`). `5f36dee`: genesis injected from
      its constant (285 B appended to `blk03663.dat`; `index.dat` and
      `headers.dat` shifted one record; `chainwork.dat` dropped and rebuilt
      — 963,447 records). Metadata backup
      `/storage/bmc-archive-backup-20260822/`. Re-verified vs Core: index
      hash, body, header record and `getblockhash` agree at 12 heights
      incl. 0. `apply_block_inner` skips genesis's coinbase (matched by
      HASH — a `height==0` guard broke two synthetic-chain tests).
- [x] 01:28: UTXO state dropped, replay restarted from block 0 with A+B
      deployed (the previous run was invalidated by the height bug).

### 02:19–02:40 — mmap, the scare, and the wrong conclusion

- [x] `e199952` PERF_SCOPE 4.1: `mac_run_lookup` was reading the ENTIRE
      bloom (4 MiB at bulk scale) per lookup per run to test 3 bits, plus
      open/lseek/read/close. `asm/utxo_lsm_mm.c`: per-thread cache of
      read-only mmaps, gen-tagged + epoch-invalidated on init/reload; asm
      path kept as fallback (`BMC_LSM_MMAP=0`). 781,882 → 94,918 syscalls
      on identical work (−88 %), syscall time −84 %.
- [x] 02:29 deployed. 02:32 first resume: `REJECT h=318148 ... missing/
      already-spent UTXO`. Attributed to mmap; kill-switch drop-in set;
      **02:33:49 UTXO state dropped for a from-scratch run 45 s after the
      REJECT — the reproducer was destroyed.** Mistake; gate adopted:
      snapshot before any drop. (02:37 restart resumed instead of starting
      fresh; dropped again and started clean 02:40:13 with the page cache
      dropped, mmap OFF. This is the run still going.)

### 02:47–03:40 — investigation: mmap exonerated, real cause found

- [x] Self-review of `utxo_lsm_mm.c` against the assembly ruled out
      offsets (u64), record advance, key order (`mac_cmp_key` bswap ==
      `memcmp`), bloom hash/seeds/bit index. Found the coverage gap:
      `test_lsm_mmap_diff.c` used `fill_threshold=1`, so no run ever had a
      sparse index (`SPARSE_STRIDE`=256) and the C binary search had never
      executed.
- [x] Fork: sparse/compaction/reload/bloom-saturated (3.5M-record, 290 MB)
      differentials + 180k-lookup soak — 0 mismatches; could not
      reproduce. New `tests/diff_real_run` (read-only differ against a
      real run file). Run against the four live production runs (18M
      records each, bloom at the 4 MiB cap, `sparse_n`≈70k): **80,000 keys,
      0 mismatches.** `b41f711` (tests merged 03:13 local / 04:1x).
- [x] **Root cause from `journalctl`:** every `systemctl stop` during
      catch-up ended in `State 'final-sigterm' timed out. Killing ...
      SIGKILL` at 90 s (21:24:39, 01:16:00, 02:30:44, 02:37:18, 02:40:12).
      `utxo_live_catchup`'s loop never read `g_shutdown_requested`. The
      02:30:44 kill hit the OLD binary mid-block; the new process reloaded
      state one block ahead of its checkpoint and — correctly — reported
      318148's inputs spent. The `2fd4a14` comment's "re-apply is safe"
      predates verify-before-apply. mmap innocent.
- [x] `879554b`: `test_scalarmul_ct` measured one ~0.1 ms wall-clock sample
      at ±5 % (4 runs of unchanged main: 1.001/1.177/0.996/0.743). Now
      `CLOCK_THREAD_CPUTIME_ID`, min-of-40 interleaved, ±3 %: 70/70 under
      load. The old window **could not detect an injected 4 % leak**; the
      new one fails it 3/3. Trap found: seven tracked test binaries get a
      fresh mtime after `git checkout --` and `make` runs the STALE one.
- [x] `98f1670`: PERF_SCOPE 4.3 GLV+wNAF scoped implementation-ready;
      constants verified numerically incl. `λ·G == (β·Gx, Gy)`.
- [x] `f2faf3b`: catch-up polls the shutdown flag per block (pointer
      registered by the worker; no `main.c` extern), finishes the block,
      persists, starts no compaction. `tests/test_utxo_catchup_shutdown.c`:
      returned 2 ms after the flag.
- [x] `96b555e`: boot-time rollback of a partially-applied block —
      `undo_<applied+1>.dat` exists ⇒ block began and never checkpointed;
      restore captured prevouts (`undo_replay_tolerant`, torn trailing
      record = EOF), delete created outputs, discard. Three crash-injection
      scenarios incl. flush-mid-block: set byte-identical to a
      never-crashed reference; negative control reproduces the production
      REJECT verbatim. Also fixed `del_created_on_output` deleting
      unconditionally (live-count drift).

### 04:08–04:35 — deploys, each measured

- [x] 04:08: `time systemctl stop` = **90.23 s, SIGKILL** (old binary,
      the last one). Checkpoint 343086; `undo_343087.dat` present ⇒ the
      kill landed mid-block. Snapshot `/storage/bmc-utxo-snapshot-20260822-0408/`
      (9.2 GB) taken first. mmap drop-in removed; `20-stop-timeout.conf`
      `TimeoutStopSec=900`.
- [x] 04:11:46 boot: `RECOVERY: rolled back partially-applied block 343087
      ... 147 prevout(s) restored`, zero rejects — the fix proved itself on
      real data at first deploy. Rate 13.0 → 24.2 blk/s at ~345k
      (mmap off → on).
- [x] **vs the 08-21 14:37 baseline, identical span 343087→363086: 2,579 s
      → 587 s, 7.8 → 34.1 blk/s = 4.39×.** The baseline was *falling*
      through this range (15 → 7.8) as bloom copies grew with the UTXO
      set; mmap-off segments at 300–340k had collapsed to 1.04–1.08×.
- [x] GLV branch: `5023fdf` Z=0-operand guards in `point_add`/
      `point_add_mixed` (latent; unreachable via `point_scalar_mul`),
      `19948e3` `sc_mul_512`+`sc_split_lambda`, `e53485b` `glv_wnaf`,
      `2389534` `point_scalar_mul_glv` (globalz), `da3e683` wired behind
      `BMC_ECDSA_GLV`. 102,056 projective point cases, 113,315-case verify
      campaign unchanged with GLV on and off, 30,240 switch cases.
- [x] `3b00f63`: suite caught `test_scriptverify_parity` segfaulting with
      GLV on — `glv_wnaf` (C) entered with `rsp ≡ 8 mod 16` from the
      all-asm `interp_checkmultisig → sv_checksig → ecdsa_verify` chain;
      gcc's `movaps` spill faulted. Direct harnesses were aligned by luck.
      `and rsp, -16` after the frame; `127ae2b` Makefile deps. Full suite
      `SUITE_EXIT=0`.
- [x] `860630c`: the seven tracked test binaries untracked.
- [x] 04:32: `time systemctl stop` = **10.05 s, no SIGKILL**
      (`shutdown requested -- stopping catch-up cleanly after height
      378844`); no partial block. 04:33 GLV deployed, clean boot.
      Bench (loaded box, same moment): GLV 44.1 µs, off 68.9, libsecp256k1
      26.8 → **1.56× on verify, gap 1.65×** (quiet box earlier: 38.7 vs
      23.5). End-to-end: **no change** — 72.5k vs 71.4k inputs/s normalized
      by `getblockstats` input counts.
- [x] Self-time profile post-GLV (12 s, symbol-resolved): kernel **5 %**
      (was 31–38 %), crypto 74 %, `fe_mul` alone **56 %**; `point_scalar_mul`
      0.00 % (GLV is live, no fallback); `mac_read_exact2` 0.00 % (mmap
      serving). Crypto is the only wall left; within crypto it is the
      4×64 field multiply.

**Status at end of session:** `main` = `860630c` + docs; everything above
deployed; replay from block 0 (02:40) past ~386k, zero rejects since the
recovery; oracle at ~552k. Snapshot and archive backup retained.

## 06:13 -> 06:51 -- Incident #10: witness-stripped archive

- 06:13 replay REJECT 481824 tx 562 "p2wpkh needs exactly 2 witness items";
  oracle says 2. index.dat data_size == Core `strippedsize` for every
  post-segwit height checked. Root cause `bitcoin_p2p.asm:103` MSG_BLOCK.
- Daemon stopped cleanly (38.8 s, no SIGKILL); checkpoint 481823, no partial
  block. UTXO state is valid below segwit -> resume, not restart.
- `31eac9a` MSG_WITNESS_BLOCK in `p2p_getdata_block` (+ test_p2p bytes).
- `fe3addb` server-side: mask the BIP144 flag before dispatch (test_serve
  caught it: we could not serve blocks to a witness-requesting peer at all);
  inbound-inv keep-up now requests MSG_WITNESS_BLOCK (tip blocks would have
  kept arriving stripped).
- `191df6c` BIP141 witness-commitment validation (`daemon/block_witness.c`),
  37 checks incl. the exact stripped 481824 bytes -> `bad-witness-nonce-size`.
- index.dat/chainwork.dat truncated to 481824 records (backups kept);
  359.8 GB of stripped bodies orphaned in blk files.
- Oracle moved to loopback 8333 (`paribd.c:58` hardcodes the port;
  `node_config.c:127` rejects others), `listen=1 bind=127.0.0.x`,
  `maxconnections=48`. `bitcoin-cli stop` took 4 min (dbcache flush) -- and
  `pgrep -f` matched my own shell twice; use `pgrep -x bitcoind -a`.
- Node: `connect=127.0.0.1..16`, `stopatheight=630000`. 06:50 start: first
  re-fetched block 481824 = 989,323 B == Core size. One peer = one worker at
  ~14 MB/s -> sixteen loopback aliases for sixteen workers.

## 06:56 -> 10:08 -- Incident #11, a session crash, and the 16-worker re-fetch

- 06:56 replay resumed at 481823 on witness-complete data: `481824 tx 562
  p2wpkh signature invalid`. Data byte-identical to Core -> verifier bug.
- Fork: `validation/bip143_ref.py` (anchored on BIP143's worked example) shows
  the real sig verifies only with scriptCode `76a914<h160>88ac`; ours used the
  22-byte program. `b3800f0` fixes verifier + generator + test helper.
- ~07:25 the Claude session died (no system trace). Resumed 09:56. The fix
  was committed in its worktree; its suites had failed on
  `test_tx_verify_parallel` (fixture signed with the old assumption) and,
  transiently, `test_outbound_mux` (passes on rerun).
- `b6c92fa`: `validation/gen_multi_p2wpkh.py` added; fixture regenerated;
  cross-checked both ways (new fixture fails old verifier, old fixture fails
  new). Full suite green; merged; daemon up 10:07:51.
- Re-fetch with `connect=127.0.0.1..16` (oracle binds 16 loopback addrs):
  all 16 workers active, ~79 MB/s average. NOTE: `[dlc] overall: N/M stored`
  is the real progress; `index.dat` record count is the index EXTENT (holes
  behind the out-of-order writer), not blocks on disk.
- Oracle ops lessons: `bitcoin-cli stop` leaves threads for minutes; wait for
  `ps -eo pid,args | grep "[b]uild/bin/bitcoind"` to be empty before starting
  a new instance or it fails "Cannot obtain a lock". `pgrep -f` matches the
  calling shell.

## 10:32 -> 11:22 -- Incident #12: nested segwit + general P2WSH

- 10:32 download leg to 641000 done (25 min, 16 workers); replay resumed;
  **481824 PASSED** (first real P2WPKH). 481825 tx 1668 rejected: P2SH-P2WPKH.
- Oracle census 481825..484000 then 484000..762875 (every 3000th): the
  P2WSH zoo (15 m-of-n pairs + HTLC scripts from 508k). Fork re-scoped to
  general witness-v0 via script_eval.
- `11f7aa9`: `bitcoin_witness_v0.c`; nested segwit; three bugs beneath
  (CHECKMULTISIG FindAndDelete under WITNESS_V0; legacy input sighash in a
  mixed tx; vector NULLDUMMY/sig-order). 9 real spends pinned
  (`tests/test_segwit_real.c`, `validation/fetch_segwit_real.py`). Suite green.
- 11:22 deployed; `stopatheight=781264` (oracle 783k). Download leg then
  replay from 481825.

## 11:48 -> 12:28 -- Incident #13: 481827 segfault

- 11:48 download leg to 781264 done (26 min). Replay: **481825 + 481826
  PASSED** (first real P2SH-P2WPKH). 11:49:39 worker segfault on 481827
  (dmesg: fault==sp in ld.so). Parent sat idle 13 min -- fixed (exits).
- First diagnosis: 12.0 MB static TLS (`readelf`) on pool threads; glibc
  default stack under `LimitSTACK=infinity` measured = **2 MB**. Real bug,
  fixed (64 MB explicit stacks via `bmc_thread.h`; TLS -> 4.4 MB).
- Proximate cause: `segwit_v0_sighash` 4096-byte midstate buffers vs a
  500-input tx (18,000 B). Same in taproot `agg_hashes`. Bounded heap
  buffers now. Regression: real 481827 through the real pool threads,
  4,855 prevouts (`validation/fetch_block_prevouts.py`). Suite green.
- `9445268` merged; daemon restarted 12:27:59; replay from 481827.

## 12:38 -> 13:46 -- Incident #14: dangling witness program

- `7533c98` bounded the remaining fixed sighash serialization buffers
  (follow-on to #13's 4096-byte overrun).
- Replay verified 481824..482565, then **rejected 482566 tx 1499**, a plain
  1-in/2-out native P2WPKH: "p2wpkh signature invalid". Cause: `in->wprog =
  spk + 2` pointed INTO the per-input scratch `g_txv_in[i].spk`, which
  Phase 1 refills for later inputs before Phase 2 verifies. Wrong bytes
  behind the pointer by verify time -> wrong BIP143 program/scriptCode.
  Worked for 742 blocks purely by luck of the clobbering values.
- Fix `d4d7d7c`: stable `wprog_off` recomputed at verify time for the
  native case (P2SH-wrapped points into stable tx bytes, kept);
  `sv_classify_segwit` was also passed a stale local `spk`.
  `test_txvb_wprog_stable.c` reproduces; 482566:1499 pinned real.

## 14:33 -> 15:17 -- Log hygiene, the census, and incident #15

- `4101587` unmistakable LOG START banner per launch; `fee7228` one-write
  log lines (timestamps could split mid-line under concurrent writers).
- `3a84241` **`CHAIN_AHEAD_CENSUS.md`** -- surveyed the un-replayed chain
  against verifier limits to find walls BEFORE hitting them. Predicted one
  hard wall (tapscript, ~775k-826k) + ~15 untested-but-handled shapes.
  That prediction paid off as #16 the same afternoon.
- **Incident #15** `d1a2259`: 498787:2420 (17-item P2SH-P2WSH witness)
  rejected by an 8-item parse cap that no consensus rule justifies. Items
  moved to a growable pool addressed by per-input OFFSET (the `spk_off`
  discipline, applied pre-emptively from #14); cap now 1004. Fixtures
  498787:2420 + 750500 (21 items, deepest in a 499k-780k census) + a
  synthetic 1005 reject. Also fixed `test:` recipe prereqs that had let
  two tests never build while the suite exited 127 with "0 failures".

## 15:32 -> 16:35 -- Two deploys; the 498787 wall cleared; incident #16

- 15:32 restart on `d1a2259`; `stopatheight` 841572 -> 880000 (oracle at
  881112). Download leg to 880000, then **replay resumed 15:43 from 498786
  and cleared 498787** -- the wall is gone. Confirmed later at 518786+.
- **Incident #16** `4dc5941`: real tapscript CSV fixture (h806500, txid
  e5dd172b...47d7) rejected. **My inline diagnosis (zeroed script-eval
  context) was WRONG and the fork's differential test overturned it** --
  that bug is real but too-permissive. The actual wall:
  `OP_CHECKSIGVERIFY` routed to `.bad_opcode` under SIGVERSION_TAPSCRIPT.
  BIP342 keeps CHECKSIG/CHECKSIGVERIFY, disabling only CHECKMULTISIG.
  Both fixed. A stale test assertion that encoded the bug was corrected.
  I re-proved fail-then-pass myself before merging.
- `8f08ee0` catch-up progress heartbeat: fires on `>=30s wall-clock OR
  absolute h%20000` (was session-relative `applied%20000`, which went
  silent for 20k blocks after every resume and masked the wall clearing).
- 16:35 restart deploying both; clean 10 s stop, resumed from 537,616.

## 17:39 -- Incident #17 (telemetry) deployed; ceiling raised to 938000

- **Incident #17** `72ee5f6`: `live_utxo=-2610837`. Reload re-derived
  `total_live` from the memtable generation only (~3.95M vs a true ~55M),
  and unconditional tombstone decrements drove it negative. Now persisted
  runs-only in a versioned manifest (`"UMN2"`, cf. MAGIC_RUN->RUN2),
  restored as base + WAL-tail net, with a one-time `mac_lsm_recount` for
  old-format manifests. Hot path unchanged; display clamped at 0 as a
  backstop, not as the fix. Verified fail-then-pass personally (old code:
  5 vs 205, then -55).
- Snapshot `/storage/bmc-utxo-snapshot-20260822-0408` deleted (9.2 GB): it
  held checkpoint **343,086** (pre-mmap deploy, NOT ~378k -- that was the
  later GLV stop, which never had its own snapshot), and the replay was
  227k blocks past it.
- 17:39 restart: clean 4.2 s stop at checkpoint 575,833; oracle had reached
  938,514 so `stopatheight` 880000 -> **938000**; download leg re-armed on
  span [880001,938000] (58,000 heights, ~86 GB) across 16 loopback workers.
- **Phase ordering, worth knowing before every future cap raise: the replay
  WAITS for the download leg to finish -- they do not interleave.** Proven
  twice (15:32 boot: workers `[done]` 15:43:02, catch-up resumed 15:43:31).
  So raising the cap buys headroom at the cost of a full download pause.
  Do it with a large replay backlog in hand, not near the ceiling.

## RESUME HERE

0. **Archive re-fetch continues** (blocks >= 481824 from the oracle via
   `connect=127.0.0.x`). `stopatheight` is at **938000** as of 17:39 (oracle
   was 938,514). Keep raising it as the oracle advances; when the archive
   reaches the real tip remove `connect=` and `stopatheight=` and restart.
   **Do NOT commit those two operational lines** -- `config/bitcoin.conf` is
   tracked and deliberately dirty. Remember each raise pauses the replay for
   the whole download leg (they do not interleave), so raise it while there
   is still a big replay backlog rather than just before the ceiling.
   Watch for `bad-witness-*` / `unexpected-witness` rejects -- they would
   mean a bad block from the oracle, which should not happen.

1. Let the replay run to tip (963,446). Watch the four activation heights
   (363725 passed; 388381, 419328, 481824) and 709632 (taproot). Monitor
   is armed for REJECT/FATAL/RECOVERY.
2. Next perf lever, the only one left: field representation — `fe_mul` is
   56 % of all cycles at ~290 Ir/call vs libsecp256k1's 5×52 lazy-reduction
   multiply. `PERF_SCOPE.md` §4.3 "what remains". Then the G-side comb
   (~8 µs).
3. Stage D's real acceptance test: `gettxoutsetinfo` parity with the
   oracle (`coinstatsindex` is on). Needs a UTXO-set hash on our side
   (`hash_serialized_3`/MuHash) — not implemented (`FEATURE_GAPS.md`).
4. Core full-verification IBD benchmark (`-assumevalid=0 -stopatheight`)
   in a second scratch datadir once our replay reaches tip — the only
   like-for-like end-to-end comparison.
5. ~~Delete `/storage/bmc-utxo-snapshot-20260822-0408`~~ — **done 17:40**
   (9.2 GB freed; it held checkpoint 343,086 and the replay was 227k past).
   `/storage/bmc-archive-backup-20260822/` stays: it guards the
   witness-truncate of `index.dat`/`chainwork.dat`.

6. ~~`test_keepup` fails 2/2~~ — **FIXED, incident #18, `b18114b`.** It was
   not a serve/serialization regression at all (my `fe3addb` attribution was
   wrong and is disproven by direct test). `node_serve_loop` violated the
   SysV 16-byte stack alignment, so the serve child took a SIGSEGV inside
   glibc's `vsnprintf` the moment a peer pushed a block. Those two failures
   were also **masking 34 tests that never ran** — `make -k test` went from
   103/137 executed to **137/137, zero failures**.

7. Pending fixture follow-ups noted by the forks: a genuine tapscript-CLTV
   fixture (the current one is honestly relabelled — it is PUSH32
   OP_CHECKSIG), an inscription-scale (>10 KB) tapscript witness, and a
   design note that `tap_leaf_hash` caps leaves at 4 MB.
