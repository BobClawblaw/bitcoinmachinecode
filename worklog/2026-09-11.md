# DAILY WORKLOG — 2026-09-11

> **Backfilled 2026-09-16 from the commit record**, not written live. The daily
> file was missed on the day; PR numbers, commit subjects and landing order are
> from git and are reliable, but the session narrative and any reasoning that
> never reached a commit message are not recoverable and are not invented here.

Run 22 on the bench. No PRs landed this day; the work is reports and harness.

## Benchmark reporting and the run 22 question

- [x] `cc4139d9` the IBD benchmark against **Core v31.1** published, in markdown
  and bbcode (`docs/reports/2026-09-11-ibd-vs-core.md`). Run 23's datadir was
  retained for it and the report survives that datadir's deletion on 09-16.
- [x] `42bb35b7` **run 22: a clean sync that produced the wrong UTXO set**, plus
  a harness that can say which block. Opened the question closed on 09-12.
- [x] `5742f69c` occupancy: idle is the time before the first byte, not the time
  inside the read — a measurement definition that was wrong.
- [x] `cb339dba` download occupancy reported: the number that decides whether
  more peers would help. Landed as #182 (merge dated 09-12).
- [x] `8bc638f9` bench runner resets `src` to the REMOTE ref, not a stale local
  branch.
- [x] `381b38f9` release note on activating a staged build, and why the live
  symlink was not moved.
