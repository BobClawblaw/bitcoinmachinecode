# DAILY WORKLOG — 2026-09-23

Bitcoin Machine Code — `bmc_osx` branch: macOS/Apple Silicon port.
One file per day; newest top. Times local (CDT, UTC-05:00).

## Round 1 — merge main (245 commits, #190–#292), Darwin guards, serve/store re-ports, gates green

- [x] Fetched main: 245 new commits since the 09-12 merge (cce2f5d7 brought
      main to #189; main has since landed the index-runs/trails wave, the
      departure journal, tx handoff to the worker, zmq `sequence`, cluster
      mempool completion, stop-waits-for-worker, run 27/28 harness work,
      RPC parity rounds, three new harness suites). Merged into `bmc_osx`.
- [x] Conflicts were three: `asm/daemon/node_config.c` and two add/add
      worklogs (09-12, 09-14 — both streams wrote the same day files again;
      kept both, x86 section under "(x86-64 main line)" as in the 09-12
      merge). node_config.c: the branch's DMN-9 follow-up (eager parse,
      deferred note/report, e75d6e11) and main's `IV` lazy-memoised parse
      (0cf715b3) fix the same false-warning bug; main's version supersedes
      — took main's file whole. test_node_config green on Darwin: 95
      documented keys, "a bad numeric value still warns, exactly once",
      string-valued keys quiet.
- [x] Six new daemon sources added to build_daemon.sh (index_runs,
      index_trail, mempool_journal, tx_handoff, mempool_cluster;
      merge_index_runs excluded — it is the standalone bmc_merge_index_runs
      tool with its own main, not a daemon object).
- [x] **Five Darwin guards, one per file, all the established
      `#ifdef __APPLE__` + degrade-comment shape:**
  - `daemon/notify.c` SYS_close_range: guarded; the manual close loop is
    the whole path on Darwin.
  - `daemon/tx_handoff.c` robust mutexes: macOS has none (MEM-20's compat
    stub refuses them the same way). The ring mutex arms plain;
    EOWNERDEAD recovery compiled out. **Accepted degrade:** a crash
    mid-hold (two memcpys, two cooperating processes) would stall the
    survivor's next txho_push. Documented in the guard.
  - `daemon/zmq_pub.c` eventfd → non-blocking pipe pair (osx port note in
    place; drain 64 B per wake, EAGAIN still means "already pending").
    test_zmq_queue ALL PASS natively.
  - `rpc_chain.c` posix_fadvise → fcntl F_RDADVISE (the same hint).
  - `daemon/main.c` the stop-wait lock-holder subsystem: no /proc, no
    kcmp on Darwin. Rebuilt on libproc: scan = KERN_PROC_ALL +
    PROC_PIDLISTFDS + PROC_PIDFDVNODEPATHINFO matched by dev+ino (the
    inode-alone conservative fallback main's own comment blesses);
    proc_comm via proc_pidpath; is_my_child via PROC_PIDTBSDINFO ppid
    (the /proc version silently no-op'd at stop — every serve child would
    have missed its SIGTERM); /proc/self/exe → _NSGetExecutablePath (both
    call sites); proc_exit_pending is 0 (no thread table to walk; our own
    worker is still tracked portably via waitpid + kill(0)).
- [x] **Re-ports of the four x86 .asm modules main changed:**
  - `bitcoin_net.asm` (+46): the g_p2p_read_hook receive wrapper — ported
    into net_twin.c (p2p_read fires the hook on 1/-2 with the ANNOUNCED
    plen, v1 and v2 paths alike; contract per the asm comments).
  - `bitcoin_serve.asm`: relay now calls tx_accept_serve_tx(mp, txid, tx,
    txlen, tx_dv_ok) — the worker validates against the LIVE UTXO set —
    and node_announce_tip's inv hash goes on the wire in INTERNAL order
    (the x86 09-19 fix: reversal named a block nobody has). Both ported
    into bitcoin_serve.S.
  - `bitcoin_store.asm` (+159): pos_file_no header field (+44) with the
    invariant "every writer of +32 also writes +44"; unshared
    store_append's drop-fd + frontier walk + conditional pos retake
    (trust pos only when pos_file_no == cur_file_no — the reinit-reclaim
    case stays); shared append's frontier walk inside the flock; reload/
    init/truncate_to maintain +44. Ported into store_twin.c; store_fast
    is read-only layers, unaffected.
  - `bitcoin_idx.asm`: the probe-budget fix — already correct in the osx
    twin (that is where the bug was found, 6fed3a71); nothing to port.
- [x] **One pre-existing twin bug the new main tests caught:**
    store_get_at's meta[2] (file_no) took a 4-byte memcpy into a u64 —
    the high half carried stack garbage. test_append_unshared_frontier
    (new on main) failed 4 file-no assertions with 2^32+k; masked, all
    green. Also ran its neighbours: test_store ALL PASS.
- [x] LINK OK zero undefined: daemon (bmcbitcoind) and wallet CLI both
      green against the merged tree.
- [x] Gates run natively on Darwin: test_stop_waits_for_worker 35/35
      END-TO-END (real regtest daemon, 4 cycles — the libproc holder scan
      correctly names-or-clears holders and never counts the stopping
      parent), test_tx_handoff 13/13, test_node_config (DMN-9 + sections +
      zmqpubsequence), test_append_unshared_frontier, test_store,
      test_p2p_msgsize (pins the restructured p2p_read), test_net,
      test_zmq_queue. **One suite adapted:** test_tx_handoff section 6
      (producer dies holding the lock) is skipped on __APPLE__ with the
      rationale in place — it pins robust-mutex recovery, which macOS
      cannot provide; skipped, not weakened, until the ring grows a
      Darwin death-safe lock.
- [x] Differential vector suites (d*.c vs .242) untouched by this merge —
      no consensus-path twin changed semantics except p2p_read's hook
      wrapper, which is add-only (unset hook = the old routine, tail-jump
      semantics preserved; test_p2p_msgsize green both sides' contracts).

## Round 2 — production deploy on m5ultra (192.168.5.178); full IBD under way; blockyard wired

- [x] **A second Mac joins the fleet.** the m5ultra (192.168.5.178,
      256 GB RAM, 36 cores, ~12 TB free) is neither the osx deploy box nor
      the .242 x86 reference — it gets its own production node from the
      merged branch (7d5cabe4). Deploy home `~/bmc_osx_deploy/`:
      bmcbitcoind + the five index helpers beside it (the daemon execs the
      builders from the exe dir), `bmc_mainnet_conf`, `bmc_mainnet/`,
      `logs/`, `start.sh`/`stop.sh` (pgrep scoped to the deploy path;
      stop signals the recorded parent only and waits — the stop-wait
      machinery then handles the worker and the lock).
- [x] **Ports moved for this host:** Bitcoin-Qt holds 127.0.0.1:8332 and
      *:8333 here, so `port=9333 rpcport=9332`, cookie auth, loopback
      RPC. `listen=0 dnsseed=1`: the x86 reference node currently REFUSES
      P2P on 8333 (connection refused — nothing listening), so the
      compiled-in mainnet DNS seeds are the bootstrap. `dbcache=65536`
      (64 GB of 256), `txindex=1 txospenderindex=1 addrindex=1`
      (deploy-20260919a parity + the live addr index), five zmq topics on
      127.0.0.1:28332-28336.
- [x] **The five index helpers built natively** (the daemon's trailing
      builders and merge tool exec them during the sync): bmc_merge_index
      _runs (plain C), bmc_build_tx_index / _txospender_index /
      _coinstats_hist / _addr_hist, each linking the px_* twin objects
      (store_twin/store_fast_twin/bitcoin_tx.S/bitcoin_hash.S/sha256.S/
      utxo_lsm_twin/...). build_coinstats_hist's mem_available() read
      /proc/meminfo — sysctl hw.usermem under __APPLE__ (the utxo_live.c
      precedent). All five print usage and run on Darwin.
- [x] **IBD green and FAST:** 8 DNS seeds -> 143 peers, 116 confirmed
      live, ranked by a 2000-header sample; archive phase pulled at
      135-170 MB/s aggregate (~968k blocks). **Restart-resume verified in
      production:** SIGTERM mid-archive (stop.sh, 11 s clean — the
      stop-wait path on the libproc holder scan), binary swap, restart —
      the store resumed from the durable archive at 63%, zero re-download.
- [x] **The [dlc] dashed line's recv/write were dead on Darwin** (user
      caught it watching the log): both came from Linux /proc/<pid>/io
      (rchar, write_bytes); without /proc they printed 0.0B/s while
      600k+ blocks landed. Fix 081e760c: write side =
      proc_pid_rusage ri_diskio_byteswritten; network side = the helpers'
      own published wire_recv (dl_wire_note's plen+24, Core's rule) —
      P2P-only, arguably what the line always claimed; rotation clamped.
      First minutes on the new binary: recv 144 MB/s, write 147 MB/s.
- [x] **blockyard wired to the node** (BobClawblaw/blockyard cloned to
      ~/github/blockyard): config/local.json -> rpcUrl 127.0.0.1:9332.
      bmc's datadir is FLAT (blk*.dat + index.dat), blockyard expects
      Core's <datadir>/blocks, so it gets a Core-view dir:
      bmc_mainnet.coreview/{blocks -> ../bmc_mainnet/main, .cookie ->
      ../bmc_mainnet/main/.cookie} — its credential fallback #3 and its
      address-index reader both resolve. Its own address index builds
      into blockyard_addrindex/ (124+ GB, paced by the node's RPC).
      Blockyard started before the node's RPC existed: its circuit
      breaker says "retry in 13s" until boot phase complete — expected,
      it closes itself.
