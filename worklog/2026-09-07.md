# DAILY WORKLOG — 2026-09-07

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR landed as a `--no-ff` merge after a green
`make -j8 test` gate (0 failures); one gate at a time.

## Session 10 (15:05Z →): run 10's cascade; the download made monotonic

- [x] Operator: "why do we have 584 holes already". Connect stuck at
  45,160 with lag 0 for minutes; 6 chunks ABANDONED, then **21,676** by
  minute 11. 21,669 were "peers exhausted": the per-worker `stalled`
  counter was never reset per chunk and the break preceded the sleep, so
  after 40 empty rounds a worker abandoned every chunk instantly. The
  40 empty rounds came from the morning's picker bar offering dead peers
  as "untried" forever (ema 0). Run 10 stopped at 16 min; logs in
  `/mnt/2tbssd/run10-cascade`.
- [x] Operator: "We need to make sure we write out monotonically too,
  like Core does". Built: a 1024-block window above the first hole
  (Core's BLOCK_DOWNLOAD_WINDOW) that workers do not claim past; a
  4096-entry shared retry ring for abandoned chunks, popped before any
  new claim; a 120 s "help" rule so a dead worker cannot deadlock the
  window; the parent publishes the first hole each tick; `stalled` reset
  per chunk; dead peers marked 1.0 for the picker; nine reason codes from
  the chunk fetcher, logged by the worker.
- [x] `test_dialhelper` +13, `test_ibd_pipeline` asserts the codes;
  **watched to fail** with the window stubbed (1 failure).
- [x] Gate green (0 failures); 8 audits exit 0. Landed `--no-ff`, tag
  `monotonic-download-2026-09-07`. Run 11 started on it (PR #77).
- [x] **Run 11: monotonic but throttled** -- 0 holes, lag 0, 32,241 blocks
  at 5:17 (run 9: 86k), 512 window waits. Operator: "it still seems very
  inefficient" / "Can we not have workers running ahead, that
  consolidate monotonically?" Cause 1: the anchor was one 10 s tick
  stale; a blocked worker now rescans the index itself. Cause 2: window
  1024 vs 640 in flight (1.6x; Core's is 6-8x) and help after 120 s
  (Core kicks a staller at 2 s): now 4096 and 2 s with a single-helper
  CAS slot. Stopped run 11 at 8 min (`/mnt/2tbssd/run11-stale-anchor`).
- [x] Scratch node on the new build: **101,591 at 5:52** (run 9 no-window:
  86,266), holes ~300 (bounded), lag ~800, 31 helps, 140 waits, 0
  abandons, 289 rotations over 59 peers.
- [x] Gate green (13,310 lines, 0 failures); 8 audits exit 0. Landed
  `--no-ff`, tag `download-window-2026-09-07`. Run 12 started on it.
- [x] Operator: "242 holes" -- the in-flight set inside the 4096 window,
  bounded, with the connect consolidating right behind (not run 10's
  stuck hole). But run 12's line said **263 MB/s average**: the blocked
  worker's own index rescans landed in its rchar, the counter every
  verdict is built from -> inflated median, rotation on nearly every
  chunk. Fix: the worker raises a flag; the parent (whose counters judge
  nobody) rescans within 200 ms and publishes. Run 12 stopped at 10 min
  (`/mnt/2tbssd/run12-rchar`); scratch node on the fix for the proof.
- [x] Scratch on the fix: **114,625 at 5:53** (best yet; run 9 86k),
  avg 476 KB/s (sane), median 74 KB/s, 213 rotations over 72 peers, 4
  abandons all retried below the connect line. Gate green (13,310
  lines, 0 failures); 8 audits exit 0. Landed `--no-ff`, tag
  `anchor-in-parent-2026-09-07`. Run 13 started on it.

## Session 9 (14:50Z →): run 9 stopped, run 10 started on today's main

- [x] Run 9 projection at 13h27m: 798,154 blocks, last hour 15,860 (Core's
  tail: ~18,500/h), 1,377 flat-budget kills -> finish ~24 h vs Core's 21.
  Operator: "stop run 9 and start run 10". Stopped at 13h28m / 798,543
  blocks, 0 rejects, 0 guard trips, 10.8 MB/s average; logs kept in
  `/mnt/2tbssd/run9-pipelined`. Run 9 is the record of what the flat
  chunk budget cost on the tail.
- [x] **Run 10 started 14:55:43Z on `a659d8b`** (stall clock, boundary
  rotation + picker bar, ETA, header keep, bmc names; dbcache=8192,
  `bmc.bootcatchup=0`). 112 live peers ranked in 40 s, best 1,221 KB/s.
  Genesis seeded; 0 rejects at start.

## Session 8 (13:55Z →): headers ahead of the archive are kept ("Fix")

- [x] `archive_trim_derived_tails` cut `headers.dat` to the archive tip
  on every boot (the 09-01 self-heal). Now: records past the tip are
  kept while each hashes to its own record and links to the one below;
  cut at the first break. `test_archive_trim` +6 checks, **watched to
  fail** on the old rule (4 failures).
- [x] Live: scratch datadir restart -- "816,167 linked record(s) ahead of
  the archive tip (headers-first): kept"; first download line at 90 s
  (previous restarts ~600 s, refetching 71 MB of headers).
- [ ] Found on the same boot, not fixed: the index chain-walk stops at the
  first HOLE and trims every stored record above it (566 blocks here);
  with the pipelined download a restart always has chunks in flight.
  Safe rule: keep a record above a hole when the linked header chain has
  the same hash at that height. Also: the index is pre-sized to the
  header count and the boot cuts 815k empty records back (harmless).
- [x] Gate green (13,305 lines, 0 failures); 8 audits exit 0. Landed
  `--no-ff`, tag `keep-headers-ahead-2026-09-07`. Scratch node stopped.

## Session 7 (12:20Z →): boundary rotation + ETA, before run 10

- [x] Estimate for run 10 vs run 9 from run 9's last 4 h: 20% of worker
  slot-time at 0 B/s (the kill/reconnect cycle), 7% at 191-400 KB/s, pool
  median ~800 KB/s; link headroom (peak 14.4 vs avg 10.9 MB/s). Gain from
  PR #68 ≈ +10-15% on the tail, BUT a delivering-but-slow peer would now
  hold its slot for the run (floor = 32 KB/s at the tail). Operator:
  "build it before run 10 - Also, add in ETA information in DD:HH:MM:SS".
- [x] `dlc_rotate_after_chunk`: after a CLEAN chunk, a worker under half
  the pool median (own /proc io over wall time; chunks < 2 s not judged)
  drops the peer and takes the fastest unclaimed one; nothing discarded;
  the line prints rate, median, bar. Parent publishes the median into the
  shared stats each tick. `eta DD:HH:MM:SS` on the status line from a
  61-tick (10 min) block-rate ring; `--:--:--:--` before a rate exists.
- [x] `test_dialhelper` +12 checks; **watched to fail** with the verdict
  stubbed to keep (2 failures). Two `-Werror=format-truncation` rounds on
  the formatter (bounded memcpy in the end).
- [x] Scratch node `/mnt/2tbssd/rottest` (port 8472, shares the link with
  run 9). First finding: the header phase looked hung for 35 min -- it
  was not: 125 KB/s through the ~71 MB held region below
  -minimumchainwork, silent. Added a progress line every 50 held pages.
- [x] Second finding: **ping-pong** -- 269 rotations among the same 16
  peers in 5 min, mean slot rate flat at 5.6 KB/s: the picker ranks by
  EMA and untried peers have none. `dlc_pick_peer` now takes the bar
  (fastest at/above bar → untried → best known → rotation); 5 checks,
  watched to fail with the bar ignored. After: 28 distinct peers of 153
  in 6 min, mean slot rate 16-22 KB/s on the same stretch; at 5:53 the
  starved scratch node had 103,593 blocks (run 9 alone: 86,266).
- [x] Third: one peer re-picked 17x because the tick-EMA lagged the
  verdict; the rotating worker now writes its chunk rate into the EMA.
- [x] ETA field live: `--:--:--:--` on the first line, then values.
- [ ] Open: `headers.dat` is trimmed to the archive tip on boot, so a
  restart mid-sync refetches every header (71 MB). Separate fix.
- [x] Gates 1-4 green (13,472 / 13,283 / 13,288 / 13,288 lines, 0
  failures each); 8 audits exit 0. Final build on the scratch node: 14
  rotations, 14 distinct peers, none re-picked, 30 distinct peers held,
  mean slot rate 35 → 170 KB/s over 5 min. Landed `--no-ff`, tag
  `boundary-rotation-eta-2026-09-07`. Scratch node stopped.

## Session 6 (10:05Z →): today's main deployed to production

- [x] Operator: "deploy new binary and continue benching". Built
  `bmcbitcoind` from `75e2192c` (= main: pipelined download, interleaved
  connect, peer ranking, relative floor, stall clock, bmc names);
  snapshot `bmcbitcoind.deploy-20260907a`, `bmcbitcoind.live` → it;
  `systemctl restart bmcbitcoind` (23 s). Banner now
  `===== bmcbitcoind  LOG START: 10:06:28`; pid written to
  `data/main/bmcbitcoind.pid`; 119 live peers ranked; at tip 965,915
  with 0 holes by 10:08:36; 0 errors, 0 evictions, 0 stalls in the log.
  Rollback: `ln -sfn bmcbitcoind.deploy-20260905a bmcbitcoind.live`.
- [x] Run 9 left running (the pipelining measurement); run 10 measures
  the stall clock.

## Session 5 (09:55Z →): the production unit renamed on the box

- [x] Operator: "you can sudo su. Fix it". Same 09-05a snapshot copied to
  `bmcbitcoind.deploy-20260905a` (cmp identical), `bmcbitcoind.live` →
  it; `bmcbitcoind.service` + drop-ins written, old unit stopped (1 s),
  disabled, removed; new unit enabled + active at 09:57:34Z. Node caught
  its 40 missed blocks within a minute; 0 errors in the log. Code
  unchanged by design; a deploy from today's main is a separate step.
- [x] Docs-only follow-up (this entry, the release-note addendum, the
  logrotate conf comment); no gate needed, no code touched.

## Session 4 (09:40Z →): everything we ship starts with `bmc`

- [x] Operator: "we should really preface stuff with bmcbitcoind ...
  Our executables should start with bmc so as not to clash with Core
  filenames." Applied: daemon `bmcbitcoind`, pid `bmcbitcoind.pid`,
  standalone RPC daemon `bmc_rpcd` (source file unchanged), unit
  convention `bmcbitcoind.service`, deploy `bmcbitcoind.live`. 43 files
  carried the morning's name; all rewritten. Old artifact names stay
  ignored. `bmc_cli` already conformed. Maintenance tools and
  `wallet_cli` left (no Core name in them) -- see the release note.
- [x] Scripts that address the unit fall back to the box's current
  `bmc-bitcoind` until the operator renames it; the exact commands are in
  `docs/releases/2026-09-07-bmc-prefix.md`. The production unit was not
  touched.
- [x] Built `bmcbitcoind` + `bmc_rpcd`; `test_core_parity` (pid pin),
  `test_elf_hardening`, `test_rpc_server`, `test_rpc_whitelist` pass on
  the new paths. Harness + memory updated.
- [x] Gate green (13,460 lines, 0 failures, the expected segfault); 8
  audits exit 0. Landed `--no-ff`, tag `bmc-prefix-2026-09-07`.

## Session 3 (09:10Z →): name confirmed; the pid file follows it

- [x] Operator asked to "make sure bitcoindmc is the name"; two spellings
  had come up, so asked: **bmcbitcoind** stands (PR #69). Pid file:
  **follow the daemon name** -- the one discussed divergence from Core's
  config defaults.
- [x] `pid` default `bitcoind.pid` → `bmcbitcoind.pid` (node_config.c,
  sample conf, main.c comment, the 09-06 defaults audit row).
  `test_core_parity` pins it; **watched to fail** against the old default
  (1 failure), then passes. Key semantics unchanged.
- [x] Gate green (13,460 lines, 0 failures, the expected segfault); 8
  audits exit 0. Landed `--no-ff`, tag `pidfile-bmcbitcoind-2026-09-07`.

## Session 2 (08:30Z →): the daemon is `bmcbitcoind`

- [x] Operator: "we should really rename our bmc client from bitcoind to
  bmcbitcoind". Scope measured first: 135 references, of which the live
  ones are the Makefile (11), 6 tests, 20 validation scripts, 6 scripts,
  4 current docs, the harness (2 lines). Historical docs untouched.
- [x] `asm/daemon/bitcoind` → `asm/daemon/bmcbitcoind`; banner and
  already-running message; `status.sh` matches both names; deploy
  convention `bmcbitcoind.live`. Kept: `bitcoind.asm` (a module), pid file
  `bitcoind.pid` (Core's default for a Core key), the box's
  `bmc-bitcoind.service` (runs a dated snapshot, unaffected), test names.
- [x] `test_elf_hardening` passes on the new path. `test_outbound_mux` and
  `test_redial` fail 3 each — both QUARANTINED since 2026-09-04 (stale
  fixture), built but not run by the gate; not a rename regression.
- [x] Harness: `install_bmc.sh` and `run_bmc_bench.sh` patched via rename
  (safe under the running run 9, which uses its own src copy).
- [x] **Gate green** (13,453 lines, MAKE_EXIT=0, 0 failures, the one
  expected segfault); 8 audits exit 0. Caught before push: `git add -A`
  had swept the ten un-ignored `bitcoind.deploy-*` snapshots (31 MB each)
  into the commit after the .gitignore rename -- amended out, both names
  ignored. Landed `--no-ff`, tag `rename-bmcbitcoind-2026-09-07`.

## Session 1 (01:20Z →): run 9 on the pipelined build; the chunk budget was an absolute bar

- [x] **Run 8 stopped at 87 min** (361,068 blocks, 1 eviction, 0 rejects;
  logs kept in `/mnt/2tbssd/run8-peerrank`). Monitor killed first, then
  the daemon by pid + cwd, then reinstall.
- [x] **Run 9 started 01:27Z on `bf4ecd8`** (= `origin/main`: peer ranking,
  relative floor, pipelined download; dbcache=8192, `bmc.bootcatchup=0`).
  123 live peers ranked in 40 s, best 1,910 KB/s. At 5 min 53 s: **86,266
  blocks** (run 8: 41,974 at 5:19; run 7: ~16,000), 0 evictions, 0 rejects,
  0 guard trips, lag 250. At 17 min: 172,277, lag 0.
- [x] **Core's number for the comparison**: 21 h 3 m (v31.1, 2026-09-05,
  `par=8 maxconnections=64` on the command line, the handicap removed
  from the harness on 09-06; not rerun by decision). Core's curve: 658k at
  7.6 h, 800k at 12.1 h, 900k at 18.6 h, tip at 21.1 h; its last 165k
  blocks ran at ~10 MB/s, so the tail is near the link for both nodes.
- [x] **Run 9 at 7 h 0 m: 622,007 blocks**, 10.7 MB/s average, peak 14.4,
  lag oscillating 0–5k, disk 1.5 TB free. On pace with Core's 658k at
  7.6 h. But **432 evictions**, ~75/hour, steady since hour 1.
- [x] **Root cause**: 429 of the 432 were `DLC_CHUNK_BUDGET_SECS`, the
  flat 120 s budget for a whole 40-block chunk ("requires ~467 KB/s
  sustained to survive", per its own comment) — rule 11 again, in seconds.
  Evicted peers by measured rate: 2 under the 32 KB/s floor, 236 at
  32–200 KB/s, 194 over 200 KB/s (one at 424 KB/s after 1,560 blocks).
  ~10.7 GB of partial chunks discarded. Only 3 were the real verdict.
- [x] **Fix on `batch/2026-09-07-chunk-stall-budget`** (`50d29582`): the
  fetcher's new `ibd_pipeline_set_progress` hook fires per arriving block;
  the worker re-arms `alarm()` from it, so the budget is a stall clock.
  Handler records the signal; log says "stalled: no block for 120s" vs
  "dead weight". `test_ibd_pipeline` 16 → 21 checks; **watched to fail**
  (hook calls removed: 4 of 5 fail). Daemon builds `-Wall -Werror` clean.
  Rule 11 gets a second-instance paragraph; release note
  `docs/releases/2026-09-07-chunk-stall-budget.md`.
- [x] **Gate green**: `make -j8 test` 13,319 lines, MAKE_EXIT=0, 0 failures
  (one expected test_rpc_signer segfault); all 8 static audits exit 0.
  Landed as a `--no-ff` merge, tag `chunk-stall-budget-2026-09-07`.
- [ ] Run 9 is NOT restarted for this: it is the clean pipelining
  measurement. The stall clock is measured on run 10.
