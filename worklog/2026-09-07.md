# DAILY WORKLOG — 2026-09-07

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR landed as a `--no-ff` merge after a green
`make -j8 test` gate (0 failures); one gate at a time.

## Session 3 (09:10Z →): name confirmed; the pid file follows it

- [x] Operator asked to "make sure bitcoindmc is the name"; two spellings
  had come up, so asked: **bitcoinmcd** stands (PR #69). Pid file:
  **follow the daemon name** -- the one discussed divergence from Core's
  config defaults.
- [x] `pid` default `bitcoind.pid` → `bitcoinmcd.pid` (node_config.c,
  sample conf, main.c comment, the 09-06 defaults audit row).
  `test_core_parity` pins it; **watched to fail** against the old default
  (1 failure), then passes. Key semantics unchanged.
- [x] Gate green (13,460 lines, 0 failures, the expected segfault); 8
  audits exit 0. Landed `--no-ff`, tag `pidfile-bitcoinmcd-2026-09-07`.

## Session 2 (08:30Z →): the daemon is `bitcoinmcd`

- [x] Operator: "we should really rename our bmc client from bitcoind to
  bitcoinmcd". Scope measured first: 135 references, of which the live
  ones are the Makefile (11), 6 tests, 20 validation scripts, 6 scripts,
  4 current docs, the harness (2 lines). Historical docs untouched.
- [x] `asm/daemon/bitcoind` → `asm/daemon/bitcoinmcd`; banner and
  already-running message; `status.sh` matches both names; deploy
  convention `bitcoinmcd.live`. Kept: `bitcoind.asm` (a module), pid file
  `bitcoind.pid` (Core's default for a Core key), the box's
  `bmc-bitcoind.service` (runs a dated snapshot, unaffected), test names.
- [x] `test_elf_hardening` passes on the new path. `test_outbound_mux` and
  `test_redial` fail 3 each — both QUARANTINED since 2026-09-04 (stale
  fixture), built but not run by the gate; not a rename regression.
- [x] Harness: `install_bmc.sh` and `run_bmc_bench.sh` patched via rename
  (safe under the running run 9, which uses its own src copy).
- [x] **Gate green** (13,453 lines, MAKE_EXIT=0, 0 failures, the one
  expected segfault); 8 audits exit 0. Caught before push: `git add -A`
  had swept the ten un-ignored `bitcoind.deploy-*` snapshots (31 MB each)
  into the commit after the .gitignore rename -- amended out, both names
  ignored. Landed `--no-ff`, tag `rename-bitcoinmcd-2026-09-07`.

## Session 1b (01:40 → 02:55Z): ARM port — merge the 45, port, sweep round 32, deploy arm-12

- [x] **Merged `origin/main` (45 commits, PRs #61–#67)** — the pipelined download
  landed with its bump-pointer fix, ban-list persistence in Core's `banlist.json`
  format, peer ranking by measured throughput, the shared-append stress test, and the
  rule-11/config-defaults docs. `d11ebf5b`. **Zero `.asm` files changed upstream**, so
  there was nothing to hand-port from the diff — the port work was the two new daemon
  C files (`ibd_pipeline.c`, `banlist.c`) into `build_daemon.sh`'s list, and letting the
  sweep pick the new rows out of `asm/Makefile` on its own. It did: `test_ibd_pipeline`,
  `test_banlist_persist` and `test_shared_stress` all pass natively, `bench_ibd_fetch`
  runs, so the pipelined downloader and the archive-guard retry are covered here on the
  day they landed x86-side.
- [x] **Two of the merge's conflicts were decisions, not text.**
  (1) `peer_has_witness` — arm-port carried a *local* workaround (e94c37b9) accepting any
  peer regardless of `NODE_WITNESS`; upstream went the other way on purpose (1d543d08).
  Took upstream's policy — and the deploy then measured it: the filter fires on **real**
  peers here (`services=0xc05`, 3 drops in 15 min), so the workaround's claim was never
  only about sim fixtures. Legs 4-5/8 where the old build held 5-6; the tip still
  advances, so it stands, but it is a live recommendation to upstream (see the deploy
  record). (2) The second hunk would have quietly cost more: upstream put its
  no-witness short-circuit exactly where its `tcp_connect_ip` call was, and arm-port had
  replaced *that same call* with the bounded non-blocking connect (4e411d2, the
  archive-gap fix deployed as arm-7). Taking either side whole loses one of them; merged
  to check the witness bit before the socket and keep the bounded connect after it.
- [x] **Sweep round 32 GREEN — the first on the merged tree:** pass 370 / fail 4 /
  bench-ok 15 / skip 10 / compared 389 of 432 plan rows, the 4 fails exactly the standing
  env-only bench rows. Build-fails went 7 → 8: upstream's `7781987c` added a
  `tests/test_par_threads` **rule** without committing its **source** (and without adding
  it to the `test:` run list, which is why x86's own gate never notices). Upstream's to
  fix; on ARM it is a visible build-fail row, not a port gap.
- [x] **DEPLOYED arm-12** at 02:37Z — full record in `docs/devlog/DEPLOYMENT_HISTORY.md`.
  Candidate md5 `cfe86ed4`, rollback `bitcoind.pre-arm12-20260907` (`36250f97`). Built to
  a temp path first so nothing ever wrote a file the running node had mapped (the
  05:25Z `Text file busy` lesson), stopped, waited for the **worker**, swapped, started.
  Boot: 136 confirmed-live peers, UTXO reload in ~4 s (`applied_height=965871
  manifest_n=3 live=165321036`), headers already current, tip advanced under the new
  binary (965871 → 965872), and 15 minutes in: connections 5, progress 1, mempool
  admitting, **0 invalid, 0 policy**. This is the first ARM deploy that changed consensus
  behaviour: it closes two false-accept gaps (IR-3's unread `hard_fail`, IR-1's ignored
  STACK_SIZE) and two memory-safety holes (the taproot ctx overrun, the unbounded
  `vfexec_push`).
- [x] My own tooling trap, recorded because it wasted a "the stop failed" scare:
  `pgrep -cf "daemon_out/bitcoind"` matches *the shell running the pgrep*, whose command
  line contains the pattern. It reported 2 surviving processes after a clean stop.
  Bracket a character (`bitcoi[n]d`) and it correctly reads 0.
- Note: the boot archive check still reports its one standing problem — non-monotonic
  block layout, first break at height 328432, so truncation and pruning refuse to run.
  Identical on the previous boot; pre-existing, unrelated to this deploy, still open.
## Session 1 (01:20Z →): run 9 on the pipelined build; the chunk budget was an absolute bar

- [x] **Run 8 stopped at 87 min** (361,068 blocks, 1 eviction, 0 rejects;
  logs kept in `/mnt/2tbssd/run8-peerrank`). Monitor killed first, then
  the daemon by pid + cwd, then reinstall.
- [x] **Run 9 started 01:27Z on `bf4ecd8`** (= `origin/main`: peer ranking,
  relative floor, pipelined download; dbcache=8192, `bmc.bootcatchup=0`).
  123 live peers ranked in 40 s, best 1,910 KB/s. At 5 min 53 s: **86,266
  blocks** (run 8: 41,974 at 5:19; run 7: ~16,000), 0 evictions, 0 rejects,
  0 guard trips, lag 250. At 17 min: 172,277, lag 0.
- [x] **Core's number for the comparison**: 21 h 3 m (v31.1, 2026-09-05,
  `par=8 maxconnections=64` on the command line, the handicap removed
  from the harness on 09-06; not rerun by decision). Core's curve: 658k at
  7.6 h, 800k at 12.1 h, 900k at 18.6 h, tip at 21.1 h; its last 165k
  blocks ran at ~10 MB/s, so the tail is near the link for both nodes.
- [x] **Run 9 at 7 h 0 m: 622,007 blocks**, 10.7 MB/s average, peak 14.4,
  lag oscillating 0–5k, disk 1.5 TB free. On pace with Core's 658k at
  7.6 h. But **432 evictions**, ~75/hour, steady since hour 1.
- [x] **Root cause**: 429 of the 432 were `DLC_CHUNK_BUDGET_SECS`, the
  flat 120 s budget for a whole 40-block chunk ("requires ~467 KB/s
  sustained to survive", per its own comment) — rule 11 again, in seconds.
  Evicted peers by measured rate: 2 under the 32 KB/s floor, 236 at
  32–200 KB/s, 194 over 200 KB/s (one at 424 KB/s after 1,560 blocks).
  ~10.7 GB of partial chunks discarded. Only 3 were the real verdict.
- [x] **Fix on `batch/2026-09-07-chunk-stall-budget`** (`50d29582`): the
  fetcher's new `ibd_pipeline_set_progress` hook fires per arriving block;
  the worker re-arms `alarm()` from it, so the budget is a stall clock.
  Handler records the signal; log says "stalled: no block for 120s" vs
  "dead weight". `test_ibd_pipeline` 16 → 21 checks; **watched to fail**
  (hook calls removed: 4 of 5 fail). Daemon builds `-Wall -Werror` clean.
  Rule 11 gets a second-instance paragraph; release note
  `docs/releases/2026-09-07-chunk-stall-budget.md`.
- [x] **Gate green**: `make -j8 test` 13,319 lines, MAKE_EXIT=0, 0 failures
  (one expected test_rpc_signer segfault); all 8 static audits exit 0.
  Landed as a `--no-ff` merge, tag `chunk-stall-budget-2026-09-07`.
- [ ] Run 9 is NOT restarted for this: it is the clean pipelining
  measurement. The stall clock is measured on run 10.
