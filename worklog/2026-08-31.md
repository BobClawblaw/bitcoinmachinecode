# 2026-08-31

Production: `bmc-bitcoind.service`, deploys b → q today, every one on a green
286-suite gate (`make -k test` + `make gate-log-check`, exit checked directly).

- [x] Catch-up performance program deployed (b); 26-minute RPC-dark boot fixed
      (mempool.dat reload before RPC start → RPC first, retry passes for
      out-of-order dumps; RPC up in 12–44 s) (c).
- [x] Orphan/relay pipeline rebuilt after "why so many rejects": relay floor
      1 → 0.1 sat/vB (Core v30), notfound clears the request ring, 60 s request
      TTL, header-sync drain no longer eats getdata replies (sync deferral while
      replies pending), orphanage 256 → 2048 slots / 8 MB, per-peer request
      tracking with cross-peer retry (want-table). Resolution 5.5 % → 56 %.
- [x] Signet: genesis coinbase was in the UTXO set (+50 BTC, one output) —
      `genesis_skip.h` lacked signet's hash; fixed, datadir repaired, muhash
      IDENTICAL to the scratch Core oracle at h=320157. Mainnet muhash
      IDENTICAL to Core v31.99 at h=964914.
- [x] Core v31 policy round: legacy sigops 2500, sigop cost 16000,
      bytespersigop 20, P2WSH witness standardness, cluster limits 64/101 kvB.
- [x] Datadir/log reorganisation: `data/<chain>/` for every chain including
      main; `logs/<chain>/`; 2 M configurable log rotation
      (`config/logrotate-bmc.conf`, `bmc-logrotate.timer`). testnet4/regtest
      chain data moved under `data/`; per-chain confs in `config/` (gitignored).
- [x] Console logs renamed `logs/<chain>/bitcoin.<chain>.log`.
- [x] Signet archive re-laid in height order; confirming boot clean; 19 G backup
      deleted; signet daemon stopped (binary restaged).
- [x] Relay/policy tail (`6b1d07c`): per-peer notfound memory (10 min),
      taproot witness standardness now Core-verbatim (annex REJECTED — the old
      "conservative accept" was a real divergence), cluster measured post-RBF-
      eviction. Trap: `okv()` double-evaluates its condition.
- [x] Mainnet archive re-layout, hot, 1.1 T → 713 G (964,924 blocks, 5,733
      files, readback 0 bad); swapped in the deploy-m window; boot re-fetched
      the 18-block tail; getblockhash at 5 heights identical to Core; old
      archive deleted (/storage 83 % → 54 %).
- [x] BIP324 confirmed complete and live (7 outbound v2 sessions); stale notes
      corrected (addhdkey done, bfilter adopted at tip, rpcpassword rotated to
      cookie-only).
- [x] Anonymity networks phases 2–4 wired to production: onion=/torcontrol=/
      i2psam=/cjdnsreachable=; onion service
      `r7tbtb6…auyd.onion:8333`, i2p `37bwoizd…ieca.b32.i2p`, cjdns fc00::/8.
      `getnetworkinfo` fixed (`06cad5b`, deploy n): real reachability +
      localaddresses (it hardcoded onion/i2p/cjdns unreachable).
- [x] Incident: deploy-m boot's worker wedged in a socket wait after ~4 min,
      SIGTERM stop hung → SIGKILL; WAL recovery clean (gap=1). Not recurred over
      four later clean stops. Lesson: kill-by-cwd resolves symlinks; a stale
      daemon keeps the port and answers probes with the wrong state.
- [x] Quiet-log series: reject metronome removed (`69675cf`, o), ZMQ ring
      16 → 64 + overrun 1/min (`5fd0782`, p), per-leg accept line → 1/min
      summary (`3991c92`, q). ~60 lines/min → ~17 lines/min of real events.
- [x] testnet4 wallet checked (empty at h=149,959); passfile moved out of the
      datadir (`config/bmcwallet.testnet4.pass`, walletpassfile= wired).
- [ ] Outbound dials never reach onion/i2p: the dial pool is the first 64
      dialable book entries in file order (17 k IPv4 first). Fix = sample
      across networks. Open.
- [ ] cjdroute has no systemd unit (hand-started). Open.
- [ ] ZMQ 28332/28333 have no subscriber connected (external). Informational.
