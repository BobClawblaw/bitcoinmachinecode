# 2026-08-20

## Session 1 (continued from `worklog/2026-08-19.md` Session 4): TXV_SPK_CAP fix, byte-pool redesign, persistent worker pool

Continuation of the same overnight session, calendar rollover only.
Production untouched until the confirmed redeploys noted below.

- [x] `e5c8c08`: found `TXV_SPK_CAP` (the flat verify array's per-entry
      prevout-script buffer cap) was hardcoded to 252 based on a wrong
      inherited assumption (that it was coupled to the taproot aggregate's
      1-byte-length encoding). Raised to 10000, matching
      `bitcoin_interp.asm`'s real `MAX_SCRIPT_SIZE` consensus cap, after a
      real "prevout script too large" rejection on real chain data proved
      the old cap too small.
- [x] Immediately made the arena-reuse fix's page-fault-storm symptom WORSE,
      not better: the flat per-entry array now inlined a 10000-byte buffer
      per entry regardless of the entry's real (almost always tiny) script
      size -- a ~25x memory-bloat regression in the persistent arena's
      growth phase. `c4a644b`: replaced the inline buffer with a packed
      byte-pool bump allocator (`bytepool_t`/`bytepool_alloc`), sized to the
      block's REAL total prevout-script bytes, not entries*TXV_SPK_CAP. This
      is the change that introduced Incident #2 below (not caught by the
      full test suite passing -- no existing test happened to trigger a
      mid-loop pool reallocation with enough legacy inputs in one block to
      expose it).
- [x] `950cd9c`: `strace -c -f -p <pid>` on the live daemon found a ~840
      `clone3`/sec storm (matched by `mmap`/`munmap`/`mprotect`) from
      per-block `pthread_create`/`pthread_join` in the new block-wide
      dispatch -- dwarfing the crypto work being parallelized at bulk-mode
      block rates. Replaced with a persistent, semaphore-parked worker pool
      started lazily and never torn down.
- [x] Full `make test` green after each step; each live-daemon restart
      confirmed with the user via `AskUserQuestion` first.

## Session 2: real production incident #1 -- LSM compaction inverted its manifest scan order

The live daemon (`bmc-bitcoind.service`), replaying the real archive with
the fixes above deployed, hit a genuine rejection at real mainnet height
184390:
```
REJECT h=184390 tx=1: legacy script verification failed
FATAL: apply_block failed at height 184390 -- stopping catch-up
[dl] utxo_live_catchup FAILED at height 184389 -- attempting in-place recovery
recover: compact manifest_n=2 -> 1 (result=1)
```
User instruction: "Investigate it now -- find the actual script and scope a
fix", then "Root-cause the flush/compact bug now" once the auto-recovery
compaction was found to be a blind, undiagnosed response
(`daemon/main.c` calls `utxo_live_recover()` unconditionally on ANY
`utxo_live_catchup` failure -- it cannot distinguish "manifest full" from
"genuine consensus rejection"; still true after this fix, worth treating any
future FATAL+recovery pair with suspicion over).

- [x] Root-caused by reading `bitcoin_utxo_lsm.asm` directly (not guessing):
      `utxo_lsm_get`'s disk-run scan walks the manifest array HIGHEST index
      down to 0 (highest = newest). `mac_flush` always appends correctly at
      the true array end. `utxo_lsm_compact` inverted this for the
      merged/oldest run after a partial compaction -- shifted survivors down
      to `[0,K)` FIRST, then appended the merged (oldest) entry AFTER them
      at the highest index, exactly backwards from the scan order. A stale,
      already-deleted key could resolve as live again post-compaction.
- [x] Fix (`e12dcbb`, `asm/bitcoin_utxo_lsm.asm`): write the merged entry
      first at index 0, shift survivors starting at `dst=1` instead of
      `dst=0`.
- [x] New regression test `tests/test_compact_manifest_order.c`: forces a
      partial compaction with a deleted key's tombstone landing in the
      merged (oldest) batch alongside a genuinely-live survivor. Caught a
      bug in the TEST ITSELF first (the initial `del` didn't trigger its own
      flush, so the tombstone never reached the buggy disk-run-scan code
      path at all -- fixed by adding a "trigger" dummy PUT to force the
      flush). Verified via `git stash` that the corrected test FAILS against
      the pre-fix code with the exact predicted symptom (key resolves live
      with its old value) and PASSES with the fix.
- [x] Full `make -k test` green; worktree -> merge `--ff-only` -> rebuild +
      retest on `main` -> push, standard workflow.
- [x] User approved (`AskUserQuestion`) a clean `archive_drop_utxo_state()`
      rebuild before redeploying, since the bug could have left latent
      corruption in the on-disk manifest from earlier compactions -- the
      block archive itself was untouched. Redeployed on the fresh state.

## Session 3: real production incident #2 -- dangling pointer into the byte pool

Redeployed daemon hit the SAME height-184390 rejection again on a
completely fresh, post-fix UTXO state -- proving incident #1, while real,
was not the sole cause of this symptom. Investigated on my own initiative
(no new explicit user request), consistent with the standing root-cause
mandate above -- stopped the daemon and moved to isolated reproduction.

- [x] Built a read-only reproduction: symlinked the real `blk*.dat`/
      `index.dat` files (read-only) into a throwaway scratchpad directory,
      ran the SAME `utxo_live_init`/`utxo_live_catchup` the real daemon
      uses against a fresh UTXO state there -- never touching the live
      daemon's own data.
- [x] Three rounds of targeted debug instrumentation (temporary, on `main`,
      reverted after use -- never committed) localized the bug precisely: a
      `[DEBUG-RESOLVE]`/`[DEBUG-RESOLVE2]` double-read at Phase 1 resolve
      time proved the prevout scriptPubKey was resolved CORRECTLY and
      stably; a `[DEBUG]` print at verify time (`txvb_verify_one`) proved
      the SAME input's script bytes were CORRUPTED by the time verification
      ran -- divergence isolated to inside/around `bytepool_alloc`.
- [x] Root cause: `bytepool_alloc` (introduced hours earlier, see Session 1
      above) returned a RAW POINTER into its own `realloc()`-backed buffer.
      A later input's allocation in the SAME block's Phase 1 resolve loop
      could trigger a `realloc()` that relocated the buffer, silently
      dangling every pointer already handed to EARLIER inputs in that loop.
      Non-deterministic across process runs (whether `realloc` relocates
      depends on that process's own heap layout), matching why it reproduced
      inconsistently between attempts.
- [x] Fix (`4ec089c`, `daemon/tx_verify.c`): `txvb_in_t.spk` -> `spk_off`, a
      stable byte OFFSET into `g_spk_pool` (now file-scope so both
      `txvb_verify_one` and the taproot pass can resolve it), converted to
      an address only after Phase 1 has finished growing the pool for that
      block. An offset survives relocation; a raw pointer captured
      mid-growth does not.
- [x] New regression test `tests/test_tx_verify_bytepool_realloc.c`: mines
      10 blocks each with a single ~9000-byte legacy coinbase output script
      (via chained data-pushes ending in OP_1, staying under both
      `TXV_SPK_CAP`=10000 and the interpreter's 520-byte-per-push /
      201-non-push-op limits), then spends all 10 in one block -- ~90000
      bytes total, comfortably past the pool's 65536-byte initial capacity,
      forcing `bytepool_alloc`'s `realloc()` to fire mid-loop after several
      earlier inputs are already resolved. Verified via `git stash` that it
      reproduces the EXACT production failure signature
      (`REJECT h=130 tx=1: legacy script verification failed`) against the
      pre-fix code and passes cleanly with the fix.
- [x] Full `make -k test`: 87/87 test binaries green on both the fix
      worktree and `main` after merge (`--ff-only`); pushed.
- [x] Before redeploying, found the on-disk UTXO state in an inconsistent
      condition unrelated to either bug: `utxo_applied_height.dat` was
      missing (an earlier SIGKILL during debugging) while undo logs past
      height ~203000 remained on disk -- resuming as-is risked silently
      double-applying blocks against a UTXO set that already reflected
      them. User approved (`AskUserQuestion`) another clean
      `archive_drop_utxo_state()` rebuild; confirmed with the user
      (`AskUserQuestion`) before the actual restart.
- [x] Redeployed `bmc-bitcoind.service` on the fresh state. Confirmed live
      past height 184390 (reached ~200000 cleanly within minutes, no
      rejections) during a full from-scratch archive replay that continues
      unattended overnight, per explicit user instruction ("reindexing from
      the start is fine as we're still in active development" -- fix and
      redeploy autonomously if anything comes up).

## Session 4: real production incident #3 -- OP_SIZE register-width bug + OP_SHA1 entirely unimplemented

User went to bed with standing authorization: "If you notice any problems
overnight, just fix them, and redeploy the server if you need to.
Reindexing from the start is fine as we're still in active development."
The redeployed daemon (Session 3's fix) hit a NEW, deterministic (not
intermittent -- repeated identically on every retry, unlike incident #2)
rejection at real mainnet height 251683:
```
REJECT h=251683 tx=9: legacy script verification failed
```
Investigated and fixed autonomously per the standing authorization above,
no new user message.

- [x] Stopped the daemon (retrying uselessly every 5-20s in a DEGRADED
      loop) and reproduced in the same isolated read-only symlinked-archive
      setup as incident #2. Decoded the failing tx's scriptPubKey by hand
      (`827651a0698faaa9a8a7a687`): `OP_SIZE OP_DUP OP_1 OP_GREATERTHAN
      OP_VERIFY OP_NEGATE OP_HASH256 OP_HASH160 OP_SHA256 OP_SHA1
      OP_RIPEMD160 OP_EQUAL` -- a real, well-known "hash puzzle" style
      scriptPubKey whose target is fully computable from public info (not a
      signature check), legitimately spendable by anyone. A first attempt
      at hand-decoding the opcode bytes was WRONG (misremembered several
      opcode values from memory) -- caught and corrected by re-deriving the
      table from real Bitcoin opcode definitions rather than continuing on
      the wrong premise.
- [x] Built a fast standalone `sv_verify_script`/`script_eval` repro (direct
      hex-byte harness, no archive replay needed) once the exact failing
      script/scriptSig bytes were known -- turned a multi-minute-per-attempt
      full-archive replay into a sub-second iteration loop for the rest of
      the investigation. Binary-searched by scriptPubKey prefix length to
      localize the failure to `OP_GREATERTHAN`, then wrote a stack-dump
      harness (calls `script_eval` directly, inspects the raw stack after
      each partial run) to see the ACTUAL runtime stack rather than
      continuing to hand-trace.
- [x] **Bug 1**: `OP_SIZE` read the top stack element's length via `mov rdx,
      [r13]` -- a 64-bit load -- but that field is a `uint32`
      (`ELEM_LEN_OFF`) immediately followed by the element's own data
      bytes, so the load pulled in 4 bytes of DATA as garbage high bits of
      the pushed "size" number. Confirmed via grep this was the ONE
      exception among ~20 similar length-field reads elsewhere in
      `bitcoin_interp.asm` (all correctly use a 32-bit register). The
      existing `{"5182",...}` OP_SIZE test vector never caught it because
      it never decodes the pushed size back as a number, and its specific
      data happened to have all-zero trailing padding that hid the
      corruption. Fixed: `mov edx, [r13]` (`asm/bitcoin_interp.asm`).
- [x] **Bug 2**: `OP_SHA1` was entirely unimplemented -- a real, always-
      defined Script opcode (not policy-gated), just never built. No SHA-1
      existed anywhere in this codebase. Implemented `asm/sha1.asm` from
      scratch (scalar-only, correctness-first -- OP_SHA1 is rare on real
      chain data, unlike the hot-path SHA-256/RIPEMD-160), mirroring
      `sha256.asm`'s structure/API. Verified against FIPS 180-4 vectors
      (empty, "abc", the 2-block NIST vector, 1M 'a's) plus
      55/56/63/64/65/119/120-byte padding-boundary cases against
      independently-computed Python `hashlib` references: 11/11. Wired into
      `bitcoin_interp.asm`'s `.op_crypto` dispatch; `sha1.o` added to every
      Makefile object list that already links `bitcoin_interp.o` (OBJS,
      INTERPOBJS, REORGOBJS, DAEMONOBJS, TAPSIGHASHOBJS, and
      `test_interp_legacy_spend`'s direct list).
- [x] New `tests/test_interp.c` vectors: a targeted OP_SIZE case (4-byte
      nonzero-data element, SIZE then a numeric consumer that actually
      decodes the pushed value -- unlike the old vector); canonical
      SHA1("abc"); and the EXACT real height-251683 tx=9 scriptSig+
      scriptPubKey bytes run end to end through the real interpreter path.
      Verified via `git stash` that all three fail against the pre-fix code
      (the real vector fails with the identical production signature) and
      pass with the fix.
- [x] Full `make -k test`: 87/87 green on both the fix worktree and `main`
      after merge (`--ff-only`); pushed (`8caa5ac`).
- [x] Found the on-disk UTXO state in the SAME inconsistent shape as
      incident #2's redeploy (`utxo_applied_height.dat` missing, undo logs
      present up to exactly height 251682 -- one less than the failure
      height, consistent with a clean apply followed by an interrupted
      degraded-retry loop). Dropped and rebuilt fresh
      (`archive_drop_utxo_state()`) and redeployed WITHOUT asking first --
      squarely covered by the standing "fix and redeploy autonomously,
      reindexing from scratch is fine" authorization above, unlike
      incidents #1/#2 earlier the same night which each got an explicit
      `AskUserQuestion` confirmation (the user was awake and available
      then; this one happened after they'd gone to bed).

**Status at end of session:** all three incidents' fixes on `main` and
pushed (`e12dcbb`, `4ec089c`, `8caa5ac`). Daemon running, replaying cleanly
toward chain tip 963183, confirmed past both height 184390 and height
251683. `PLAN_SCRIPT_VERIFY.md` Stage D is now IN PROGRESS (wired + fast
enough to run + three real bugs found and fixed by the live replay itself,
exactly the kind of thing Stage D's "any single rejection ... needs
explaining before this ships" bar exists to catch). Not yet DONE: the
replay hasn't reached chain tip. Monitoring continues unattended.
