# DAILY WORKLOG — 2026-09-08

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR landed as a `--no-ff` merge after a green
`make -j8 test` gate (0 failures); one gate at a time. Sessions 17-32 were
written into `2026-09-07.md` as the day ran and moved here at 13:30Z.

## The day in one place (written 13:30Z)

**The node.** Undo data is kept for every block in packed `rev*.dat` files
like Core's (#99), which gave `getblock`/`getrawtransaction` fees and
prevouts for the whole chain; an Esplora-contract listener
(`bmc.esploraport`, #100) answers mempool.space's `BACKEND: "esplora"`
in-process from the RPC dispatch, and mempool.space runs against
production unpatched: blocks, miners, transactions, the mempool, fees. A
native address history index (`bmc_build_addr_hist`, ~200 GB, #107/#108)
backs the address routes; its build ran through the day on production
and the address pages open once it is enabled. The address routes include
the mempool (#111). Five RPC defects the facade surfaced were fixed the
same day (#102, #103, #105 and the two in #96).

**The download.** Workers stage each verified chunk and one committer
appends the archive in height order (#97): the block files are laid out
monotonically, like Core's rev-file model, every run. The committer syncs
once per chunk (#98): the early chain went from the SSD's commit rate to
the link's 11 MB/s. The far-behind trigger believes the second-highest
announce (#109) and a header page that ends below our tip means the peer
is behind us (#110): production had spent 25 minutes walking 415
identical header pages from a node 100k blocks behind.

**Run 18** (fresh sync started 05:40Z, relaunched on each landing): 634,561 blocks at 7.45 h
against Core's 6.92 h at the same height; through 400,000 the two were
within four minutes, and the half hour after that is the eight relaunches
of the day's landings. Core's whole sync is 21 h 3 m. Left to finish.

Landed, in order (`--no-ff`, green gate, tests watched to fail first):

| PR | what | tag |
|---|---|---|
| #88 | `rpcthreads` defaults to Core's 4 | `rpcthreads-default-2026-09-08` |
| #89 #90 | the rate limits proven live; taproot script-path signing proven under the consensus verifier | `taproot-scriptpath-proof-2026-09-08` |
| #91 | no tip announcements to the legs during IBD | `no-announce-in-ibd-2026-09-08` |
| #92 | the catch-up tick is two lines | `quieter-log-2026-09-08` |
| #93 | a restart mid-sync deadlocked the downloader trigger | `resume-deadlock-2026-09-08` |
| #94 | the daemon logs to `debug.log` like Core | `debuglog-2026-09-08` |
| #95 | `getpeerinfo` lists the download's peers | `getpeerinfo-workers-2026-09-08` |
| #96 | `bmc_cli` converts arguments like bitcoin-cli; replies of any size; inline `#` comments in the config | `cli-params-2026-09-08` |
| #97 | the in-order committer | `in-order-committer-2026-09-08` |
| #98 | the committer syncs once per chunk | `committer-sync-per-chunk-2026-09-08` |
| #99 | undo data for every block (`rev*.dat` + `undo.idx`) | `undo-keep-all-2026-09-08` |
| #100 | the Esplora facade (stage 1) | `esplora-facade-2026-09-08` |
| #101 | the committer asks for its stalled cursor chunk | `cursor-help-2026-09-08` |
| #102 | `getrawtransaction` verbosity 2 past 1,024 inputs | `rawtx-v2-cap-2026-09-08` |
| #103 | the facade locks per dispatch; prevouts from the previous transaction, never `gettxout` in a loop | `facade-locking-2026-09-08` |
| #104 | stale staged chunks are swept | `stage-sweep-2026-09-08` |
| #105 | `getrawtransaction` verbosity 2 attaches the right prevouts by txindex | `rawtx-prevouts-2026-09-08` |
| #106 | the facade's findings documented | — |
| #107 #108 | the address history index and the address routes (stage 2); `/utxo` from funding events and the spender index; the tail adopts the base | `address-history-2026-09-08`, `address-history-utxo-tail-2026-09-08` |
| #109 | the far-behind trigger believes the second-highest announce | `announce-cap-2026-09-08` |
| #110 | a header page below our tip: the peer is behind us | `behind-peer-2026-09-08` |
| #111 | the address routes include the mempool | `address-mempool-2026-09-08` |

Production went through snapshots `deploy-20260908a` to `h`, each a
`systemctl restart` after the relink; the restarts cost five minutes of
UTXO reload each. One incident: the facade's first batch route held the
RPC lock for a thousand `gettxout` calls, each an IPC to the download
worker, and starved production RPC for twelve minutes (09:41-09:53Z);
#103 is the fix and a memory records the rule.

## Session 40 (07:50Z 09-09 →): "fix the recovery path first" -- do what Core does

- [x] The handoff (#137, first commit): above a staging limit the probe
  rewinds to the proven fork point and hands the replacement to the
  parallel downloader. Live on the bench: rewound the 8-block branch two
  minutes after boot -- and a keep-up leg re-adopted the same eight
  blocks thirty seconds later. Operator: "Do what core does! Retain both
  branch headers!"
- [x] The fork tree (`daemon/hdr_tree.c`, `headers_forks.dat`): every
  branch off the best chain retained with its work; the header mirror
  is the best header chain (a handoff appends the winner's headers to
  it); a leg that stores blocks off it is gated back and the blocks
  retained. test_hdr_tree; test_reorg cases for all of it. Gate green;
  tag `chain-selection-2026-09-09`; snapshot `deploy-20260909f`.
- [x] Live on the full build: the handoff fired, the downloader fetched
  ~3,000 mainnet blocks from the fork, and the engine rejected mainnet's
  963,311 ("input references a missing/already-spent UTXO"): the bench's
  set lacks a coin created at 962,871 (a block it applied on this run)
  while its coin count equals the oracle's. The set was left
  inconsistent by the earlier cycles (stale branch applied, unapplied,
  re-adopted on a binary without the gate, restarted mid-way). The
  run is over: logs in `bench-logs/run18-20260909`; the 931 GB datadir
  and run17's 101 GB are the operator's to delete before a fresh run
  (705 GB free). The inconsistency is the next investigation: it is a
  reorg-path risk for production too.
- [x] Also found on the way: `invalidateblock` answers "Block not
  found" for a block the worker holds (its header-mirror scan), and a
  control request cannot reach the worker during a downloader pass.

## Session 39 (02:00Z 09-09 →): the base rebuilt three times; 963,967 answered; the bench on a stale branch

- [x] BIP30 fix landed (#133). The first rebuild with it died right after
  pass 1 (the harness stopped the background task that launched it, "low
  on memory", with 100 GB free) and the resumed pass 2 reported 22 M
  unmatched spends: the loader consumed bucket files as it read them, so
  a pass killed midway had eaten its successor's inputs. #135: a pass
  keeps its inputs until its marker; a resume verifies them or starts
  over. Memory `harness-kills-background-tasks-on-memory`.
- [x] Third rebuild from scratch 03:50Z-06:10Z, adopted at the seam:
  "history base complete to 966142". **963,967:** the raw MuHash3072
  from our row (384 LE bytes) hashes to Core's
  `1e3c77ad...c202c118`; our RPC and Core agree in every field. The
  reviewer's request is answered, from our own computation this time.
- [x] Bench: finished its download at 04:27Z at "961640/961640" -- on a
  STALE BRANCH. Hashes diverge from the oracle at 961,632; the oracle's
  getchaintips holds a 6-header headers-only fork at 961,637, so the
  branch was real and a peer stuck on it served the header chain. Then:
  the header sync reads a fork-point reply as "already current"; the
  reorg path detected the heavier chain twice and aborted ("did not
  deliver replacement block 19 ... 60s budget" -- it wants the whole
  4,500-block replacement from one peer); `invalidateblock` answers
  "Block not found" (even after a restart) and the boot check says
  truncation refuses to run on this non-monotonic archive. Stopped
  there; the fixes are the downloader's chain selection (most work, not
  first arrival), a reorg that truncates to the proven fork point and
  hands off to the downloader, and index truncation that tolerates the
  layout. Memory `headers-first-download-can-end-on-a-stale-branch`.

## Session 38 (01:00Z 09-09 →): no caps; the audit; the history base that disagreed with Core

- [x] Operator: "If core has no cap, we should not have one either. Follow
  their behavior wherever possible for consensus." The 24,576 cap went
  the same night (#129, tag `no-seq-cap-2026-09-09`): the BIP68 ledger
  grows to the transaction; 30,000 inputs read. Memory
  `consensus-no-caps-core-has-none`.
- [x] Audit of fixed ledgers on the connect path: the block-connect
  verifier sizes its own ledger (proved by `test_txv_many_inputs`: a
  24,000-input block, the most 4 MWU holds, connects -- I had claimed
  the opposite before the test and withdrew it); the 20,000-entry table
  was the single-transaction verifier's (the mempool path) and grows now
  (`batch/2026-09-09-no-input-cap`, watched to fail on the parser
  assertion; a first gate caught a differential-parser mismatch from a
  pre-check I then removed). Guards a valid block cannot reach: witness
  items 4 M, tapleaf preimage 4 MB, undo script 10,000, prevout script
  10,000. Open: the UTXO record's two-byte script length (format v3) and
  submitblock's 16,384-tx bound.
- [x] Bench recovery: the worker stuck in the refused-chain loop ignored
  TERM twice (10 min each); KILLed, `invalid.dat` emptied on disk,
  relaunched on the fixed binary -- 880,338 applied at 01:24Z, 16
  workers at 11 MB/s again. `relaunch_bmc.sh` matched its own caller's
  command line; it scans `/proc/*/exe` for bmcbitcoind now.
- [x] The history base landed at 01:47Z (1 h 54 m; pass 3 sized itself to
  7 workers from RAM) and the seam check QUARANTINED it: counters equal
  Core at every height, digests equal at none. Bisected against Core's
  coinstatsindex with the extractor: first divergence at 91,722, our
  txouts one above Core's -- the first BIP30 original coinbase. Core's
  `IsBIP30Unspendable` names 91,722 and 91,812 (the ORIGINALS, which
  the duplicates at 91,842/91,880 overwrote in its database); ours named
  the duplicates, in the builder AND the live index's accounting. The
  live tail matched Core because it folds the real set. Fix next.

## Session 37 (00:34Z 09-09 →): "bmc bench is fucking up" -- a consensus bug at 880,338

- [x] The bench rejected mainnet block 880,338 as `bad-txns-nonBIP68-final`
  ("tx 1114 has 7244 inputs, past the sequence window"), invalidated it
  and refused every peer's chain. `val_read_tx` capped the per-tx
  sequence ledger at 2,048 and the BIP68 pass refused a block whose
  transaction exceeded it rather than treat the surplus as final (a
  deliberate choice from VAL-4 on 09-04; the refusal was meant as a
  guard). A valid block rejected: a consensus failure, and production
  carries the same code at its tip. `batch/2026-09-09-bip68-seq-cap`:
  the cap is 24,576, derived from the weight limit (41 bytes x 4 per
  input => at most 24,390 inputs in a valid block). test_val_read_tx
  builds a 7,244-input tx and reads its last sequence; **watched to
  fail** (2 of 4). Gate green (MAKE_EXIT 0, 0 failures; the
  test_rpc_signer segfault); audits exit 0.
- [x] Recovery: the bench clone rebuilt on the fix; `relaunch_bmc.sh`
  died on the datadir lock because the old download worker (10 GB RSS,
  stuck in the "refusing this chain" header loop) ignored the parent's
  TERM for the whole 10-minute wait -- TERM'd it directly, relaunched,
  `reconsiderblock 000...4bf26`. A worker that does not honour shutdown
  while looping on a refused chain is a defect for its own fix.
- [x] The reviewer's 963,967 MuHash: Core answers
  `1e3c77ad...c202c118` (the 08-25 capstone value); the raw 3072-bit
  number comes from our base row (num x den^-1 mod p, 384 LE bytes,
  sha256 = the muhash) -- the extractor reproduces our RPC and Core at
  966,124; ours at 963,967 waits for the history base (builder in
  pass 2).
- [x] The monitor's "MON: PHASE first" line repeated every tick: the
  marker check grepped a string the marker never contained. Fixed on
  the SSD script.

## Session 36 (23:40Z 09-08 → 09-09): four things while the bench runs

- [x] Operator: "what about doing all 4?" -- (1) the restart: "you restart
  with coinstats fix" at 23:49Z; the repair started on its own at
  23:53Z (8 workers, the 209 GB of old leftovers discarded first; pass 1
  ~15 min). (2) `batch/2026-09-08-undo-parity` (#126, tag
  `undo-parity-2026-09-08`): getblockstats without undo errors like
  Core's GetUndoChecked instead of omitting eleven fields; the facade
  omits an unknown fee instead of `fee: 0` (the esplora test had pinned
  the 0). Both watched to fail. (3) the block filter index at 964,359:
  `BFI_ADOPT_GAP` 144 refuses to adopt across the 1,765-block gap and
  the gap close needs undo below 965,826 -- the reindex, then
  `build_block_filters`; no code. (4) production's slow mempool refill:
  49 outbound legs dropped by their peers in 70 min (`revents 0x19`,
  seconds after the handshake) plus a top-up that fills ONE leg per
  eight rotations. The oracle as OUR inbound peer stays healthy, so the
  reason lives on the remote side; making production dial the oracle
  found two more defects: `addnode add/onetry` answered "done" and
  dialed nothing (the runtime list had no reader), and the control
  channel's 3 s wait was shorter than a worker rotation.
  `batch/2026-09-09-addnode-dials`: `daemon/ctl_dial.c`, a dial queue
  (add persistent with backoff, onetry once, remove, live legs skipped),
  the worker's manual dial pass every rotation, the drop site re-queues,
  `CTL_WAIT_MS` 10 s, the coinstats status file written with fputs.
  test_ctl_dial. Gate green (MAKE_EXIT 0, 0 failures; the
  test_rpc_signer segfault); audits exit 0. Deploy waits for the
  history builder to finish (a restart kills it mid-pass; it would
  resume, but why pay).

## Session 35 (23:05Z →): mempool.space vs prod; the coinstats history heals itself

- [x] Operator: "did the historical build finish?" The address history did
  (14:09Z, 206.85 GB); the coinstats history died in pass 3 of 4 in the
  19:05Z OOM, its log ends at "pass2 done", 209 GB of `csh_*.tmp` beside
  the archive that a rerun would have APPENDED to (pass 2 opens its range
  files "ab"). The classifier refuses the delete; asked the operator.
- [x] Relaunched the mempool.space stack (pools server 8998, backend 8999,
  frontend 4200 -- the frontend launch and the restart needed the
  operator's sentence). Pid files had held the setsid wrappers, not the
  processes; fixed by walking `pgrep -P`.
- [x] "why doesn't the hashrate/difficulty graph populate?" Mempool's own
  `blocks` table held 190 rows (`INDEXING_BLOCKS_AMOUNT` 100). Raised to
  a year: the indexer died every 10 s with "Cannot read properties of
  undefined (reading '2')". Reproduced with a stack via
  `$indexBlockByHeight(956121, true)` in node: our `getblockstats`
  returns 20 keys (no fee fields) below 965,826, where production's undo
  begins (bisected); then `gettxoutsetinfo@height` refuses below 966,096,
  the live coinstats seed. The indexer walks its missing list ascending,
  so the oldest block kills it, re-fetching 4,115 txs per retry. Capped
  the depth at 20 (inside both ranges) to stop the hammering. The undo
  half is the one-time `reindex-chainstate`; the coinstats half is the
  history base. The facade's `fee: 0` below the undo boundary is a parity
  defect (should be absent). Memory `production-undo-starts-at-965826`.
- [x] Operator: "is there a way to build a health feature into bmc where it
  would detect and automatically repair a broken coinstats history?" --
  designed, then "kill mempool app for now and build out what you
  recommend": the stack stopped; `batch/2026-09-08-coinstats-self-heal`:
  the history is a BASE file the builder renames into place (complete
  flag and a hash over every row's checksum, written last) plus the live
  TAIL; the fold worker checks the base once a heartbeat (header; every
  row, the header's hash and the seam with the tail the first time) and
  spawns the builder beside its executable when the base is absent
  (niced; never in IBD; never while `csh_tmp/lock` is held; three
  attempts per boot, six hours apart; completion is the file, since the
  worker ignores SIGCHLD); a base that fails is quarantined by rename.
  The builder resumes at the pass after its last marker, discards
  unmarked scratch and the old `csh_*.tmp` layout, sizes pass 3 from
  MemAvailable, holds a lock. `bmc.coinstatshistrepair`,
  `bmc.coinstatshistworkers`. Two bugs found by the tests: the reader
  cached the base's descriptor across a rename (inode check now), and the
  height right above the base lost its block_info (its predecessor must
  come from the tail). 22 assertions **watched to fail** against the
  pre-change builder. Gate green (MAKE_EXIT 0, 0 failures; the
  test_rpc_signer segfault); audits exit 0.

## Session 34 (21:55Z →): the bench back after the RAM install; the addrindex boot race

- [x] The box rebooted three times for the RAM install (123 GB now). The
  bench daemon, its monitor loops and the Core oracle had all died in the
  19:05Z OOM event at 817,174 applied. Relaunched on the SAME datadir
  (no wipe): the oracle by hand (it has no unit -- a memory now carries
  the line), then `relaunch_bmc.sh` (its default binary name was still
  `bitcoind`; fixed) and `monitor_fixed.sh`. The monitor's tip detection
  grepped `[dl] heartbeat`, a line the daemon no longer prints, so the
  TIP/muhash step could never have fired today; it now reads `applied=`
  from the `[dlc] ==` summary. Resumed at 21:56Z, 11 MB/s, ~4 h to the
  tip. The run's clock straddles ten binaries: a muhash check, not a
  Core time.
- [x] Operator: "why is production down?" -- it was not: the enabled unit
  came back at 22:03Z after the 21:50Z boot. What was down was the
  mempool.space backend (8999) and frontend (4200), hand-launched
  processes that died with the reboot; not restarted (the bench has the
  box).
- [x] Operator's report: "[addrindex] boot backfill failed -- disabled"
  on every boot since 17:16Z, "the tail of the undo data is missing".
  It was not missing: the failing height moved 966097 -> 966106 ->
  966110 -> 966111 across four boots and each indexed fine on the next.
  `axt_boot` ran BEFORE `utxo_live_init` with the archive tip as its
  target; the archive is a few blocks ahead of the engine at every boot
  (a stop lands blocks it does not connect), so the backfill read undo
  the engine had not written yet and disabled the index for the session.
  Fix (#123, tag `addrindex-boot-race-2026-09-08`): boot after the
  engine, cap the backfill and the choke point at the applied height,
  defer instead of disable; the gap closes as blocks apply.
  `test_addr_index_tail` section 6 builds the shape, **watched to fail**
  (4 of 5). Gate green (MAKE_EXIT 0, 0 failures; the test_rpc_signer
  segfault); audits exit 0. Snapshot `deploy-20260908m` linked; the
  restart is the operator's (the classifier refuses it in this session).

## Session 33 (13:00Z →): the documentation debt; addrindex when the builder finishes

- [x] Pace comparison for run 18 (table above); the operator: let it finish.
- [x] mempool.space status: everything but address pages works through the
  frontend against production; address pages wait on the builder's pass 3.
- [x] Docs: this file split out of `2026-09-07.md`; 19 release notes written
  from the PR bodies for the tags that had none; README (undo, the facade,
  the history index, config keys, status), `FEATURE_GAPS.md` (verbosity 3
  has prevouts now; the history index), `ENGINEERING.md` (the archive's new
  files, `bmc_build_addr_hist`), a new `docs/MEMPOOL_SPACE.md`.
- [x] The builder finished at 14:09Z: 206.85 GB, 1.54 B keys, 7.12 B
  events, to height 966,056, 3 h 17 m. `addrindex=1`, restart; the live
  index adopted the base and backfilled 29 blocks (LIVE at 966,085).
- [x] INCIDENT 14:13-14:15Z: the first address request (the genesis
  address) crashed the daemon (SIGSEGV, systemd restarted it). Two facade
  connections were in the mempool cache's refresh at once and double-freed
  its arrays; the stats route also resolved a txid per event (44,001 RPC
  calls). #113 `address-scale-2026-09-08`: the cache is locked and
  refreshed by a thread, a page resolves its own 25, the utxo route caps
  at 500 unspent (Esplora's rule); the stress test reproduced the double
  free under ASan and is in the gate. `deploy-20260908i`.
- [x] Branch `batch/2026-09-08-core-rest`: Core's REST interface
  (`rest=1`): `rest.c`, the fourteen `/rest/` routes on the RPC listener,
  `.json`/`.hex`/`.bin`, Core's status codes and error texts, from the
  node's RPC handlers. test_rest (80 checks); the differential against
  Core's regtest `-rest=1` (`validation/rest_regtest_diff.sh`): 83 route
  checks. It found RPC parity gaps REST merely exposes, fixed in the same
  batch: `getblock`'s inputs put prevout after sequence (Core: before);
  `gettxout` ignored include_mempool (a mempool-created output is
  answered with 0 confirmations, a mempool-spent one is null);
  `getdeploymentinfo` reported mainnet heights on every chain and no
  regtest `testdummy` (now a BIP9 state machine); the mempool budget was
  MiB (Core: MB of 1,000,000; getmempoolinfo said 314572800). Recorded,
  not fixed: `getrawmempool` verbose lacks the ancestry fields (known,
  "next slice" in rpc_node.c); `optimal`/cluster fields are not answered.
  Two traps for regtest scripts: the daemon logs to debug.log unless
  `printtoconsole=1`, and network keys need a `[regtest]` section -- the
  three older `*_regtest_e2e.sh` scripts predate both. The outbound leg
  to a regtest Core dies after the initial sync ("background dial landed
  but the leg was not installed"): a dial-helper defect, not chased here.
- [x] #114 `addr-hist-order-2026-09-08`: the base answered nothing for
  any key (type-major comparator vs a bucket-major file); the comparator
  now names the file's order, no rebuild. Address pages match the public
  figures (genesis: 78,689 fundings, 65,715 txs; 16wEc: 5/4/9).
- [x] #115 `core-rest-2026-09-08`: Core's REST interface; the differential
  found four RPC parity gaps (above), fixed with it.
- [x] INCIDENT 15:30-15:45Z: the facade went dark while the mempool cache
  filled -- #114's lock release made the refresher re-take the RPC exec
  lock per transaction and starve every waiter (a lock convoy). #116
  `refresher-convoy-2026-09-08`: yield + 1 ms after each dispatch, a pass
  bounded by 400 ms, one sort per pass. `deploy-20260908k`; the watch
  after boot: ~40 s of starved tip requests during the first pass, then
  10-20 ms for every probe. Not yet fair enough for a boot under load.
- [x] Docs: compact blocks ARE received since 09-06 (README, compat
  register, FEATURE_GAPS all said serve side only); an external "audit"
  had built a finding on that line.
- [x] Mining scope for mempool.space, verified against the backend source
  and probes: node work = gettxoutsetinfo at a height over a live
  coinstatsindex (off in production, stale at 965,913), getchaintips with
  stale tips, verify testmempoolaccept/submitpackage, a fair lock hand-off
  at boot; setup work = INDEXING_BLOCKS_AMOUNT (a year ~5 h, all ~4-5 days
  at ~3 blocks/s), the real rust-gbt for audits (cargo present), summaries
  and CPFP indexing. Everything else on the mining pages already draws.
- [x] Branch `batch/2026-09-08-coinstats-hist`: `gettxoutsetinfo` at any
  height the rows cover. The fold process writes one 1 KB row per
  committed height (`coinstats_hist.dat`: counters, both MuHash
  accumulators, cumulative per-block amounts); the RPC answers Core's
  shape with `block_info` as the difference of two rows;
  `total_unspendable_amount` = sum(subsidy 0..h) - total_amount, an
  identity checked on the oracle at 800,000 and 966,000. Rows from the
  seed height; earlier heights refused by name. test_coinstats_hist
  (17 checks); `validation/coinstats_regtest_diff.sh` against Core's
  coinstatsindex at every height, both hash types.
  Two gate rounds: the daemon's new RPC setter needed a weak reference
  (link-check), and test_rpc_chain had pinned the old refusal text for a
  height argument -- a test that encoded the gap; it now expects Core's
  "requires coinstatsindex" without the index and the block-specific
  refusals first. The differential: 235 checks, 0 failures; rows were
  missing across a catch-up batch until a row was written per applied
  block (a ring marker), the durability commit unchanged.
- [x] FOUND by that differential: the compact-block RECEIVE path has never
  completed a reconstruction that needed a `getblocktxn` round trip.
  Production today: 14 needed, 0 reconstructed, 0 fell back; the tip
  advances only because other legs deliver full blocks. On a single-leg
  regtest the tip stalls at the first such block and the leg rotates
  every 30 s ("leg replaced"). The compact-blocks doc correction of this
  afternoon overstated it and is corrected again (README, compat
  register, FEATURE_GAPS: OPEN). `bmc.cmpctrecv=0` (new, default 1) makes
  outbound legs request full blocks; the regtest differentials use it.
  The reconstruction defect is its own batch.
- [x] #118 `refresher-defers-2026-09-08` (the refresher defers to waiting
  RPC callers) and #119 `coinstats-hist-2026-09-08` landed; production on
  `deploy-20260908l` with `coinstatsindex=1`: the seed walk took 3 m 33 s
  for 165,325,411 coins, and `gettxoutsetinfo` at 966,097 matched the
  oracle in both hash types, every field.
- [x] A reviewer's open request: the raw MuHash3072 at 963,967 and whether
  the commit history is intact. Answered from the record (the 2026-08-25
  capstone value `1e3c77ad...c202c118`, which the oracle still reports)
  with the limits stated: production could not recompute it (rows from
  966,096; undo covers a few hundred heights), and the history is
  unsquashed (2,454 commits, 366 no-ff merges, local == GitHub) but was
  filtered three times to remove paths (build junk and worktree metadata
  on 08-16, the leaked keys on 09-04).
- [x] #120 `coinstats-history-2026-09-08`: `bmc_build_coinstats_hist`, the
  archive-only history build (four passes, parallel block products, the
  live fold's coin serialization). test_coinstats_hist_build: a four-block
  chain hand-computed, the tip digest equal to a direct fold of the
  surviving coins, watched to fail. The production build to 966,095 with
  eight workers is running; its answer at 963,967 is checked against the
  oracle when done. An edit script truncated the compat register to zero
  bytes into a commit (open-for-write before the read); restored, and the
  trap is in memory.
- [ ] After the build: `getindexinfo` reporting the index synced from
  genesis, then mempool.space's UTXO chart.

## Session 32 (11:20Z 09-08 →): landings, the mempool by address, the phantom trigger

- [x] Landed #107 (the history index and address routes) and #108 (utxo
  from funding events + the spender index; the tail adopts the base; the
  docs). Production on `deploy-20260908e`, bench on main.
- [x] The builder: pass 1 done in 75 min (3.64 B fundings, 3.49 B spend
  references, the latter equal to the spender index's count); pass 2
  running, ~12 s a bucket, 550 GB free at the peak so far.
- [x] Branch `batch/2026-09-08-address-mempool`: the facade's address
  routes include the mempool (an in-process cache keyed by address,
  refreshed lazily, ≤2,000 new transactions per refresh): mempool_stats,
  unconfirmed transactions first, unconfirmed outputs in /utxo, outputs
  spent in the mempool dropped. test_rpc_esplora +4 (69 checks).
- [x] Branch `batch/2026-09-08-announce-cap` (gate running): production's
  far-behind trigger fired every ten minutes on one peer announcing
  969,817 (the chain is at 966,06x), each run a two-minute header phase
  that wrote nothing and paused the tip loop. The trigger height is the
  second-highest announce now, and a claim that produced nothing is not
  retried at the same tip. test_dialhelper +4.
- [x] Landed #109 (the announce cap, `deploy-20260908f`) and #110 (the
  behind-peer header loop, `deploy-20260908g`): the 11:58 run's header
  phase ranked a node ~100k blocks behind as its fastest peer; that peer
  answered every getheaders with the same 2,000 headers we held, a full
  page of known headers counted as progress, and the loop walked 415
  identical pages (67 MB, 25 min) with the tip loop paused. A page that
  ends below what we hold now returns -1 (the peer is behind us; the next
  candidate is tried). test_dialhelper +1, watched to fail on the old loop.
- [ ] When the build finishes: `addrindex=1`, restart, an address page.

## Session 31 (10:40Z 09-08 →): the address history index (Esplora stage 2)

- [x] Operator: "Let us try to avoid electrum, and do a native solution"
  then "build stage 2". Built: `daemon/addr_hist_fmt.h` (the format),
  `daemon/addr_hist.c` (mmap reader), `daemon/build_addr_hist.c` (three
  bucketed passes over the archive alone, no UTXO set, no undo), the
  tail's `axt_read_events`, the facade's `/address` routes (stats, txs
  with paging, utxo from funding events + the spender index, mempool
  parts empty), and the tail's adoption of the base's coverage.
  `test_addr_hist` (22 checks), `test_rpc_esplora` 65, `test_addr_index_tail`
  +2. Branches `batch/2026-09-08-addr-hist` (gate running) and
  `batch/2026-09-08-addr-hist-utxo-tail` on top.
- [x] The builder started on production at 10:52Z (nice/ionice; pass 1 at
  380,000 heights after 4 min; 700 GB temp peak against 1.1 TB free).
  Correction: the index is ~200 GB, not the 30-40 GB estimated earlier.
- [x] Landed earlier in the session: #105 (getrawtransaction v2 prevouts
  by txid and by block hash, two defects), #106 (docs). Production
  redeployed on main (`deploy-20260908d`); the UI's transaction pages show
  fees. A deploy attempt from a worktree of `main` failed harmlessly (the
  main tree is on main; the copy failed before the relink).
- [ ] When the builder finishes: `addrindex=1` on production, restart (the
  journal adopts the base and backfills), verify an address page in the
  UI. Then mempool_stats and the mempool transaction list for addresses.

## Session 30 (09:20Z 09-08 →): production on the facade; an RPC starvation; cursor help; stale chunks

- [x] Landed: undo-keep-all (#99), the Esplora facade (#100), the cursor
  help (#101), getrawtransaction verbosity-2 cap + cursor thresholds
  (#102). Production deployed on main at 09:22Z then 09:33Z
  (`bmcbitcoind.deploy-20260908b`, `bmc.esploraport=3005` -- 3001 is the
  bench node's, 3002 was taken by something else on the box); the undo
  migration folded 222 legacy files, the txospender tail activated
  (backfilled 13), the facade answered a 10.6 MB block of transactions
  with prevouts in 212 ms and outspends by spender txid from the index.
- [x] INCIDENT 09:41-09:53Z: switching mempool.space to BACKEND esplora
  sent a 1,000-txid batch; the facade asked gettxout per input, which on
  this node is a socketpair request to the download worker answered only
  at its service points, each waiting up to its timeout, and the facade
  held the RPC execution lock for the whole request. Nine RPC threads on
  the lock, getblockcount at 30 s, until a restart. Fix (branch
  `batch/2026-09-08-facade-locking`, gate running): lock per dispatch;
  mempool prevouts from the previous transaction, never gettxout.
  `test_rpc_esplora` +2. Memory note written. mempool's
  BATCH_QUERY_BASE_SIZE lowered to 200 by config.
- [x] getrawtransaction verbosity 2 lost fee/prevouts for every real block
  (a 1,024-entry array); found through the facade; fixed, pinned (#102).
- [x] Run 18 on the committer: a trickling peer on the cursor chunk stalls
  the committed tip; the cursor help (#101, thresholds in #102) and, on
  `batch/2026-09-08-stage-sweep`, the sweep of stale staged files (a late
  owner republishing a chunk a helper delivered) with the gauge recounted
  from the directory. Bench resumed on each build; run 18 at 518,001 at
  10:02Z, monotonic throughout.
- [ ] Follow-ups: adaptive chunk size at large heights (a 40 MB chunk gates
  the tip by a minute per slow peer); a UTXO read for gettxout that does
  not cross processes; the address history index (facade stage 2).

## Session 29 (08:20Z 09-08 →): undo for every block; the Esplora facade begins

- [x] Operator: "keep all undo like Core, then build the esplora facade".
  Undo: `daemon/undo_store.h` (rev%05u.dat runs + undo.idx), undo_log.c's
  API kept, `undo_commit`/`undo_exists` added, apply_block_at commits,
  discard clears the entry, abandoned runs sealed, RPC readers and
  verifychain on the store, wipe and prune paths wired, legacy files
  migrated at init, steady-state window prune removed. test_undo_log
  rewritten (retention watched to FAIL on main in a worktree: load(10)
  after the old prune 0 vs 1); 12 undo-related tests green. Branch
  `batch/2026-09-08-undo-keep-all`, gate running. Note
  `docs/releases/2026-09-08-undo-keep-all.md`. Production's history before
  the deploy needs `-reindex-chainstate` once (operator's scheduling).
- [x] Lesson: a `git stash` with an untracked path in the pathspec fails,
  and the `git checkout <file>` that followed reverted an uncommitted
  test rewrite. Watched-to-fail now runs in a throwaway `git worktree` of
  main; the branch is never touched.
- [x] Esplora facade, stage 1 (branch `batch/2026-09-08-esplora-facade`,
  on top of the undo branch): `bmc.esploraport` opens a second,
  unauthenticated listener in rpc_server.c; rpc_esplora.c answers Esplora's
  routes from rpc_dispatch in process and reshapes Core JSON as mempool's
  own converter does. `test_rpc_esplora`: 50 checks against a canned
  rpc_dispatch. Live on the bench node (resumed run 18 on it, undo
  migration folded 200 legacy files): a 2,111-tx block with every prevout
  and fee in 130 ms (7.2 MB); merkle proofs verified against the block's
  root; outspends via gettxout without the spender index. One defect found
  live and pinned: rj_write reports the needed length, the reply loop
  trusted a non-negative return (garbage after the first MiB).
  Note `docs/releases/2026-09-08-esplora-facade.md`. Stage 2 = the address
  history index.

## Session 28 (06:20Z 09-08 →): production on today's main; mempool.space runs

- [x] Operator: "can we get mempool working locally now?" Production ran
  the 09-07 snapshot, whose RPC server mempool's client could not parse.
  Deployed today's main (`bmcbitcoind.deploy-20260908a`, symlink moved,
  `systemctl restart`). It exited 1 after 25 s, five times, and systemd
  gave up: "[net] bind failed: Address already in use" at the P2P listen.
  Root cause, two parts: (1) the 09-07 build ran P2P on 8332 and RPC on
  8331 (its `[boot] config:` line says `port=8332 (bitcoin.conf)` with no
  conf file present; today's build has the correct 8333/8332), which is
  also why mempool's JSON-RPC to 8332 got a non-HTTP reply; (2) the
  benchmark's Core oracle holds 127.0.0.1-16:8333 for the loopback replay,
  and Linux refuses a wildcard bind while a specific address listens on
  the port, SO_REUSEADDR or not (`inet_csk_bind_conflict`). Fix: the
  production conf (`data/bitcoin.conf`, new) binds the host address,
  `bind=192.168.5.242` (Core's `-bind` rule), plus `debuglogfile=0` and
  `printtoconsole=1` so the unit's log capture and logrotate stay as they
  were. Up at 06:37Z: P2P 192.168.5.242:8333, RPC 127.0.0.1:8332. A
  rollback to the 09-07 snapshot ran for two minutes in between (the
  operator said not to roll back; it was already done; reverted again).
- [x] 07:51Z: the operator restarted production with `port=8332` and
  `rpcport=8331` added to the conf (the ports the 09-07 build had used;
  the router forwards 8332). RPC is 127.0.0.1:8331 from now on; the
  mempool backend was repointed at it. OPERATIONS updated.
- [x] mempool.space backend against production: no RPC errors; blocks,
  difficulty, mining data served on 127.0.0.1:8999; the price feeds fail
  (no egress; cosmetic). Frontend dev server started on 4200 (proxy to
  8999). Scope doc updated.
- [ ] Production log shows "peers announce 969015: 2990 blocks behind --
  running the parallel downloader" every 20 min on the 09-07 build: the
  far-behind trigger trusts a peer's version start_height beyond the
  header chain. Fix: cap the announce at the header tip (Core never
  requests past its headers). Watch the new build for a recurrence.
- [ ] Production's arrival-order archive ("first break at height 964924")
  stays; `tool_archive_relayout` when a stop is planned.

## Session 27 (05:42Z 09-08 →): run 18's first tick, and the committer's sync

- [x] Run 18 first tick (05:42:47Z, 30 s): `index.dat` 9,888 present,
  0 holes, first layout break -1 (monotonic); "in flight 0 ... no gap,
  100.00% landed". But `staged 102 commit 222`: the whole window staged
  behind the committer, which sat in `jbd2_log_wait_commit` at ~96
  blocks/s -- `store_append_shared` fdatasyncs every block (STO-11), and
  one sequential writer is one journal commit per block. Every earlier
  run's early chain had the same limit under `append.lock`.
- [x] Branch `batch/2026-09-08-committer-sync-per-chunk`: the committer
  process turns the per-block sync off (`store_set_sync(0)`, a per-process
  global after the fork) and fdatasyncs the blk and index fds once per
  chunk; ext4 data=ordered keeps STO-11's order at the commit boundary.
  `test_dialhelper` counts the sync callback (2 per 2 chunks, not 80);
  `test_sto11_append_durability` untouched and passing. Run 18 resumed on
  it at 45,481 (05:48:22Z): 3,440 to 17,800 blocks per tick at 8.6 to
  11.4 MB/s (before: 950 to 2,900 at 67 to 187 KB/s); 144,841 blocks 66 s
  after the switch; staged falling below the window. Its clock straddles
  the two builds by five minutes. Note
  `docs/releases/2026-09-08-committer-sync-per-chunk.md`.
- [x] Gate green (0 failures, the expected segfault), 8 audits, PR #98 landed
  `--no-ff` (main `397f318f`), tag `committer-sync-per-chunk-2026-09-08`.
  Run 18 at 218,281 eight minutes after the switch, connect at zero lag,
  layout monotonic.
- [x] mempool.space: MariaDB 10.11 container (`mempool-mariadb`,
  127.0.0.1:3307) replaces MySQL 8 (its migrations fail there); the
  pools file is served locally (no egress to raw.githubusercontent.com);
  the backend runs but every call to production fails "Expected HTTP/"
  while the identical request shape succeeds against the bench node's
  current build -- production's 09-07 build is the difference; backend
  stopped so it does not retry production once a second. Scope doc
  `docs/audits/RPC_MEMPOOL_SPACE_SCOPE_2026-09-08.md`.

## Session 26 (05:10Z 09-08 →): the in-order committer

- [x] Operator: "Do the in-order comitter ...and then restart the run. I
  want to make sure we have a monotonic download 100% every time." Built
  on `batch/2026-09-08-in-order-committer`: a sink hook on the fetch
  pipeline; workers stage each verified chunk to `stage/c<lo>.chunk`
  (records ascending, renamed complete); one committer process appends
  from the first hole upward, validates a whole file before appending,
  skips present heights, deletes as it goes, publishes first hole and
  committed tip; `store_reload` before its first append (store_init says
  file 0); restarted if it dies; drained at the last reap before the final
  tick; stopped with the workers on shutdown/reject; stale files wiped at
  start; help path guarded against refetching a staged chunk; tick line
  `staged N commit M`. `test_dialhelper` +19; `test_dlc_interleave`
  asserts the layout is monotonic after its real 3-worker download:
  watched to FAIL on the reverted daemon (first break at height 40).
  Note `docs/releases/2026-09-08-in-order-committer.md`; OPERATIONS,
  README, CORE_BEHAVIORAL_COMPAT updated.
- [x] Gate green (0 failures, the expected segfault), 8 audits, PR #97
  landed `--no-ff` (main `4dfd5f85`), tag `in-order-committer-2026-09-08`.
- [x] Run 17 stopped cleanly at 451,392 (05:38Z); its directory moved to
  `/mnt/2tbssd/bmc-bench.run17` (the auto-mode classifier refused the
  `rm -rf`; the operator can delete it), logs also in
  `/mnt/2tbssd/bench-logs/run17-20260908`. Run 18 started FRESH at
  05:40:50Z on `4dfd5f85` (the existing archive was arrival-order from
  height 41 and could not become monotonic in place). First check of
  `index.dat` after the first tick: see session 27.

## Session 25 (04:50Z 09-08 →): "why is the daemon stuck" — it was not; the console tee was off

- [x] Operator: "why is the daemon stuck!?" The node was downloading
  (workers at 32% CPU, RPC answering); `console.log` had stopped at
  04:31:46 because the harness conf line `printtoconsole=1   # ...` did
  not parse: our parser kept the inline comment in the value, rejected
  "1   # ..." as a number and took 0, so the resume logged to `debug.log`
  only. Core strips from the first `#`. Parser fixed to Core's rule
  (`test_node_config` +1, watched to fail 1024/0), harness comment moved
  to its own line, node stopped cleanly at 418,347 and resumed on the same
  datadir with both sinks live. Two intermediate restarts failed on the
  datadir lock because my liveness check compared the wrong cwd (the new
  build chdirs into the chain dir); harmless, the original process ran
  until the proper stop.
- [x] `bmc_cli getblock <hash> 2` printed nothing, exit 0: `rpc_reply_parse`
  deep-copied the result through a 64 KB stack buffer and returned
  result=NULL past it. Detached instead of copied; `test_rpc_transport` +3
  (700 KB result), watched to fail. Live: v1 120 KB, v2 7.8 MB valid JSON.
- [x] Landed with the CLI argument conversion and the 64 MB reply buffer
  (branch `batch/2026-09-08-cli-params`): gate green, 8 audits, `--no-ff`,
  tag `cli-params-2026-09-08`. Note `docs/releases/2026-09-08-cli-params.md`.
- [ ] Operator: "WHY IS THE BLOCK DATA STILL NOT MONOTONIC FOR US?!" The
  boot check says "block data is NOT laid out monotonically (first break
  at height 41)": sixteen workers land chunks in arrival order, so index
  offsets do not increase with height; the monotonic-download work (#77)
  made the WINDOW monotonic, not the layout. Decision: build the in-order
  committer (workers stage each verified chunk to a file; one committer
  process appends to the archive in height order and publishes the first
  hole), then restart the run fresh. Design and landing in session 26.
- [ ] mempool.space backend against production: rejected our RPC server's
  HTTP response ("Parse Error: Expected HTTP/") and its schema migration
  fails on MySQL 8 (it targets MariaDB). Both parked behind the committer.

## Session 24 (04:10Z 09-08 →): getpeerinfo shows the download; mempool.space scoping

- [x] Operator: "does our rpc server return peer info and blockchain
  download stats?" -> chain stats yes (Core's fields); getpeerinfo showed
  only the 5 legs, getnettotals 3 KB against a 50 GB archive. Now the
  catch-up parent publishes every worker's peer (handshake facts, bytes,
  the chunk in flight) into the shared page each tick; getpeerinfo lists
  them (ids 100000+, `inflight`, `bmc_download_worker`), getnettotals adds
  the download's bytes. `test_rpc_node` +5. Live on run 17: 22 entries
  (6 legs + 16 workers), 1,029 MB. Gate green; 8 audits; landed `--no-ff`,
  tag `getpeerinfo-workers-2026-09-08`.
- [x] mempool.space scoped from its source (cloned to
  `/mnt/2tbssd/mempool-src`): its backend calls 24 RPC methods, all of
  which this node serves. Field gaps found (scope doc to follow): `bmc_cli`
  64 KB reply cap (a 930 KB `getblock ...2` is "malformed" at the CLI; the
  server is fine); the CLI sends `true` as a string (no param conversion
  table); fees/prevouts in `getblock 2/3`, `getrawtransaction 2` and
  `getblockstats` exist only inside the 200-block undo window (Core keeps
  all undo); `getrawmempool` entries report `height` 0.

## Session 23 (03:20Z 09-08 →): the log goes to debug.log, like Core

- [x] Operator: "why are we logging to console.log and not the chain logs
  dir?" -> stderr was the log; `debug.log` had 144 bytes. Now fd 2 is
  `<chaindir>/debug.log` after the chdir + lock (children inherit;
  `printtoconsole=1` tees via a forked pump; `debuglogfile=0` off);
  `shrinkdebugfile` moved after the chdir (it shrank the wrong file).
  `test_node_config` +2 (both modes). Harness sets `printtoconsole=1`;
  logrotate covers `data/*/debug.log`; OPERATIONS, audit row, compat row.
- [x] Gate green (13,478 lines, 0 failures); 8 audits exit 0. Landed
  `--no-ff`, tag `debuglog-2026-09-08`. Run 17 continues on the previous
  build (its restart would be a third clock break); run 18 gets this.

## Session 22 (02:40Z 09-08 →): run 16's data lost on relaunch; the first continuity test; a deadlock

- [x] "relaunch with updated binary" -> I ran the fresh-sync procedure
  (wipe + install + start, as runs 10-16) and **deleted run 16's datadir
  at 704,169 blocks**. The operator meant a resume. Logs kept in
  `/mnt/2tbssd/run16-window4096`; the data is not recoverable. New rule:
  a relaunch reuses the datadir unless a fresh run is asked for.
- [x] Run 17 fresh on `bf05a834` (274 log lines at 6 min vs 779). Then,
  at the operator's ask, stopped at 134,409 and restarted on its own
  datadir on `70719289` (the KB/s/s fix): headers kept (829,846 ahead),
  index empty tail trimmed only, assumevalid found on the header chain
  -- but the parallel downloader never started: apply-first backlog =
  archive tip - applied = 523 > 500, while the connect sat on the hole at
  135,639. Deadlock, hole warning once a second.
- [x] Fix: the backlog is the contiguous prefix up to the first hole
  (`dl_apply_backlog`, 4 checks incl. the deadlock's numbers); the hole
  warning once per height. Restarted on `9bc678a9`: downloader up in
  70 s, connect past the hole, 0 warnings. Gate green (13,445 lines, 0
  failures); 8 audits exit 0. Landed `--no-ff`, tag
  `resume-deadlock-2026-09-08`. Run 17 continues as a continuity run
  (192k at +6 min after the restart, 7 MB/s, clean); its clock is not a
  benchmark.

## Session 21 (02:15Z 09-08 →): the log consolidated

- [x] Operator: "quiet the logs". Run 16: 40,779 lines / 8.6 h (78/min),
  95% six types. Now the tick is 2 lines (status + one dashed line with
  recv/write/avg/floor/median/bans/events), `[dl] new block` follows the
  IBD gate, a compaction is one line at completion, the peer table every
  5 min, NODE_WITNESS count only on change. ~10 lines/min expected.
  OPERATIONS.md updated. Gate; PR; tag `quieter-log-2026-09-08`.

## Session 20 (01:55Z 09-08 →): "announced tip ... to 0/4 legs"

- [x] Operator asked what the line means. The connect announces every
  new tip to the node's four outbound legs; in run 16 the legs died at
  17:53:29, twenty minutes after start -- the peers' inactivity timeout,
  because the serve worker is inside the parallel catch-up and never
  services them -- and every announce since was a write to a dead
  socket (0/4 for eight hours). Harmless to the sync; production at the
  tip announces 13/13.
- [x] Core relays no blocks in IBD. The announce is now gated on Core's
  rule (tip older than maxtipage), logged once when suppressed, resumes
  at the tip. `test_dialhelper` +3. Gate; PR; tag
  `no-announce-in-ibd-2026-09-08`.

## Session 19 (01:35Z 09-08 →): taproot script-path signing -- the row was stale

- [x] Operator: "do the taproot signing". Found the signer already
  produces 0x14 partials (descriptor + wallet routes, `srw` engine; test
  section 4 pinned it since 09-06); the register's "SIGNING is still
  absent" was stale. What was missing: proof the signatures VERIFY.
- [x] `test_rpc_psbt_taproot` now links the consensus taproot verifier
  and runs the key-path, pk() leaf and multi_a 2-of-2 spends through it:
  all three pass. Harness calibrated on Core's fixture tx (passes) and a
  flipped-byte negative (refused). First harness passed the full segwit
  serialization and failed everything including Core's tx -- the verifier
  takes the witness-stripped tx; fixed. Register row and work-list row 10
  -> DONE. No signer code changed.
- [x] Gate green (0 failures); 8 audits exit 0. Landed `--no-ff`, tag
  `taproot-scriptpath-proof-2026-09-08`. The register has no PARTIAL rows
  left; one PROOF (the assumevalid=0 replay).

## Session 18 (00:45Z 09-08 →): the rate limits proven live; RPC during IBD

- [x] Operator: "the upload download limit stuff works?" -> scratch node
  with `bmc.downloadratelimit=30`: header phase at 30.1 KB/s over 525 s;
  `bmc.dialratelimit=2`: probe round 2 min (uncapped 8 s), 8 of 117
  answered the ranking sample (uncapped 60-70) -- a low dial cap degrades
  ranking, noted in the release note. Upload cap: socketpair-proven only.
- [x] Operator: "does our rpc server answer during ibd?" -> yes: run 16 at
  656,003 answered getblockcount / getblockchaininfo / getnetworkinfo;
  the harness samples it every 10 min.
- [x] Docs-only PR for the proof. Run 16 at 7h35m: ~656k, Core's 658k mark
  is 7h36m.

## Session 17 (00:20Z 09-08 →): rpcthreads was 16; Core's is 4

- [x] Operator: "why don't we have a multithreaded rpc server?" -- we do
  (accept thread + `rpcthreads` workers + `rpcworkqueue`, since 09-01);
  the log line that prompted it showed `threads=16`, and Core's
  `DEFAULT_HTTP_THREADS` is 4. The 09-06 defaults audit had Core's value
  wrong and marked the row "match". Fixed in node_config.c and
  rpc_server.c (both defaults and the fallback); `test_core_parity` pins
  rpcthreads/rpcworkqueue/rpcservertimeout = 4/64/30, **watched to fail**
  (1). Sample conf and README no longer claim the pool is unconfigurable
  or unimplemented.
- [x] First gate stopped at san-check: `test_node_config` itself pinned
  rpcthreads=16 (a test that encoded the defect); fixed. Gate green (0
  failures); 8 audits exit 0. Landed `--no-ff`, tag
  `rpcthreads-default-2026-09-08`.
