# DAILY WORKLOG — 2026-08-14

Bitcoin Machine Code (x86-64 NASM node). Each day gets its own file. Newest at
top per section. A line per meaningful action: what, evidence, result.

## Node completion (overnight, working the full remaining list)

### getblocks serving [DONE]
Added `getblocks` to `node_serve_loop` in `bitcoin_serve.asm`:
- Dispatch on dword0 "getb" + confirm cmd[4..7]="lock".
- Resolve the locator (pl+5) via idx_get -> from = fh+1; serve up to 500 inv
  entries (type=2 MSG_BLOCK, hash in LE wire order) via node_serve_block +
  block_hash (reversed). Canonical CompactSize count varint (0xFD+2 for >=253,
  compact single-byte otherwise), same discipline as getheaders.
- Findings along the way (all asm bugs, caught live):
  * NASM 64-bit addressing: no `symbol+reg+reg` and no `base-index(sub)` forms;
    use base registers (r10/r11/r9) and negated indices.
  * block_hash needs `extern` in bitcoin_serve.asm.
  * getblocks confirm check: cmd[4..7] is "lock" (0x6b636f6c), NOT "loc\0"
    (0x636f6c) -- the same off-by-one class as the earlier getheaders "head"
    bug; 'k' sits at cmd[7] so the dword must include it. Without this the
    confirm always failed and getblocks never served.
- VERIFIED LIVE (port 8358, server stayed ALIVE):
  * locator h=1 -> inv, n=500, lenmatch=True, type=2, first hash == height-2
    block (49444695...) exactly.
  * locator h=309000 (near tip) -> n=500, match=True.
  * server log "served 500 block(s)" per connection.
- make test still 33/33.

### Also cleaned up
- gitignored the live-test helper binaries (gh_client, live_serve_check,
  test_gh_real, test_nsb_range) so the tree stays clean.

## Prior today (already in this file's earlier sections / done earlier)
... (getdata+getheaders+fd-leak+memcpy_len-watchpoint work) ...

## Status at handoff
- git clean except SHA-NI experimental files (untracked, not part of node work).
- Downloader healthy near tip.
- Node progress through this session: getdata [DONE], getheaders [DONE],
  getblocks [DONE], mempool + tx relay (getdata MSG_TX + inbound tx) [DONE],
  multi-peer concurrent serving [DONE]. Leaner: inv-announce/proactive relay and
  reorg hardening remain; full UTXO/sig/fee validation is the wallet's future
  job. Wallet deferred per directive.

## (overnight continuation) mempool, tx relay, multi-peer

### bitcoin_mempool.asm [DONE, commit cb36043]
txid->tx open-addressing hash store (relay storage; dedup, count+byte bounds).
Debugged: memcmp32 clobbering its rdx loop bound with dl (use r8b, preserve r8);
copy-length register; probe missing wraparound (table falsely 'full' at 19/64).

### tx relay through node_serve_loop [DONE, commit 0ca749b]
- getdata MSG_TX: look up hash in the mempool, serve the stored tx verbatim.
- new tx handler: BIP141 txid via tx_txid, mpool_put it (accept + re-serve).
- VERIFIED LIVE: send tx -> getdata(MSG_TX) -> reply cmd='tx' len=64 byte-exact.
  (Earlier probe failure was a bad test client using a zero checksum, which
  p2p_read correctly rejected -- not a server bug.)

### multi-peer serve [DONE, commit 377b076]
fork a worker per accepted connection (shares copy-on-write archive+index+mempool,
runs single-connection node_serve_loop; parent keeps accepting; SIGCHLD auto-reap).
VERIFIED LIVE: 12 concurrent clients, 12/12 correct getdata+getheaders, no zombies.

### test_txtxid [DONE]
BIP141 txid == sha256d(raw) for a legacy tx passes. Suite now 34/34.

### Node server surface (all live-verified)
version/verack, ping->pong, getaddr->addr, getdata->block, getdata(MSG_TX)->tx,
getheaders->headers, getblocks->inv, inbound tx accepted, multi-peer concurrent.

