# 2026-08-24

## Session (continuous from 08-23): the C→asm conversion campaign — fourteen differential-proven twins, and the three bug classes the differentials caught

Long-form narratives in `LOG.md`'s 2026-08-24 entries. This is the terse
action log, in order. Times UTC. The parity rebuild started 08-23 20:34 runs
throughout; the box is shared with it, so no timed benchmark on this page is a
quiet-box number.

### Why this campaign exists

The project's claim is 100% AI-generated **machine code**, and `daemon/` plus
the script-verification layer were ~16,100 lines of C — a third of the
codebase. The honest description was "all consensus *primitives* in assembly,
orchestration in C". Every slice below closes part of that gap.

**The method, unchanged for every slice:** extract a testable seam in the C if
one is needed → write the asm twin → drive both over identical inputs and
compare *everything written*, not just return codes → benchmark → optimise to
parity → merge. **The C stays in the tree as the differential oracle and the
daemon keeps running it**; nothing here is deployed yet. The swap happens after
a replay soak, as its own change.

### 00:23–01:35 — phase 1, then slices 1–5

- [x] `8b21904` `bitcoin_undo.asm`: all 8 functions of `undo_log.c`, raw
      syscalls. Differential compares files byte-for-byte, every rc,
      cross-loaded record arrays, replay stream checksums, the strict/tolerant
      torn-tail split, and discard/prune survivors. Bench: append 0.73 vs
      0.78 µs (asm 0.94×), replay 0.995× — **syscall-bound; this is a purity
      change and was scoped as one.**
- [x] `648fb24` slice 1 `bitcoin_txv_parse.asm` (single-tx parse), `403ae22`
      slice 2 `bitcoin_txv_classify.asm` (per-input consensus classification),
      `f7109b7` slice 3 `bitcoin_segwit_classify.asm` (the BIP141
      fork-in-the-road), `a6ce775` slice 4 `txvb_parse_tx_asm` (**the parser
      the live daemon actually runs**), `ad59a49` slice 5 the arena twins.
- [x] `55c3307`: `callee-saved-check` flagged the first `rd_cs` (cursor in
      `rbx`, mutated as output — a custom convention). Rather than widen the
      allowlist and dilute "anything else here is a real finding", the helper
      was made **fully SysV**.
- [x] `8d28a86` parity pass: two twins measured **slower** than `gcc -O2` —
      segwit-heavy parse 1.165×, segwit classify ~1.08×. One cause both times:
      call/frame overhead around leaf logic the compiler inlines. Fixed with a
      NASM inline macro (`RDCS`) and a frameless fast path. Result: parse
      0.845/0.793, native classify 0.490, wrapped 0.998, legacy 0.516.
      **Every measured row now ≤ 1.0.**
- [x] Found while porting, **not fixed** (bug-for-bug fidelity keeps the
      differential meaningful): `txv_parse`'s scriptSig bound
      `(u64)(end-p) < sl+4` **wraps** for `sl` within 4 of 2⁶⁴. Core's
      compactsize reader rejects anything over `MAX_SIZE` before any bound
      check runs, so Core cannot reach it. Filed in `LOG.md` as the next
      Core-differential to chase.

### 01:57–03:22 — slices 6–12

- [x] `0ee9361` slice 6 `bitcoin_strip_witness.asm`; `c7309c3` slice 7
      `bitcoin_tapagg.asm`; `6992203` slice 8 dispatch + legacy view;
      `6d49287` slice 9 witness-v0 driver; `c3a914d` slice 10 legacy/P2SH
      driver; `255fdad` slice 12 `bitcoin_bip143.asm`.
- [x] Slice 6's differential compares the **entire output buffer** on cap
      rejections, pinning the partial bytes a mid-emit failure leaves behind.
      Slice 12's compares the **entire preimage buffer**, not just the digest —
      a digest-only comparison hides any bug that happens to cancel.
      **26,888 cases, 0 mismatches.**

### 02:53 — slice 11, and the bug that justifies the whole method

- [x] `b70356a` `bitcoin_checksig.asm`. It assembled, passed
      `callee-saved-check` (406 functions), and **matched the C on every
      rejection case**. It failed only the *accept* cases, and only in the
      final stack bytes — `sv_run_v`'s ok/err and the stack depth matched,
      because `OP_CHECKSIG` pushes false rather than failing the script.
- [x] Root cause: **`qy[4]` at `-0xf8` overlapped `zl[4]` at `-0xf0` by 24 of
      32 bytes**, so `pubkey_parse` clobbered the message hash and
      `ecdsa_verify` **rejected every valid signature**. Same class as incident
      #31, and again invisible to both static auditors: they check save-area
      aliasing and push/pop balance, not whether two of a function's *own*
      locals overlap.
- [x] Two rules recorded: **an accept case is not optional in a crypto
      differential** (every reject-only comparison is weaker than it looks),
      and **overlapping locals is a third question nothing in the tree asks
      statically** — filed as a candidate check, mechanically decidable from
      the frame-map comments this codebase already writes.

### 06:07–07:50 — slice 13, in two halves

- [x] `1c8fd5f` slice 13a `bitcoin_taproot_verify.asm`: annex detection, path
      classification, control-block rules, the merkle commitment at **every**
      leaf version, Core's `ExecuteWitnessScript` prologue **in its order**
      (OP_SUCCESSx scan before the stack limits), the BIP342 weight budget.
      27 cases including **real script-path accepts** built from a real
      internal key. Self-review caught the slice-11 class again *before*
      assembly — `leaf_hash[32]` sat on the arena pointers.
- [x] `62624c2` slice 13b `bitcoin_bip341.asm` (TapSighash builder). **Three
      iterations, three failures, all the same root cause in different
      costumes — a layout or ABI detail typed from memory instead of
      measured:**
      1. `tapctx_t` field **order** (hash_type sits after `n_in`, not later);
      2. `txview_t` **layout** — not the same as the other file's `swtx_t`
         despite looking similar (`int version`, no `inputs` field). The twin
         segfaulted; then the same wrong struct **in the test** drove the C
         oracle through garbage, so the second crash was in the *oracle*;
      3. the **SysV register/stack boundary** — `h_seq` is the 6th argument, so
         it arrives in `r9`; the twin read `[rbp+16]`, which is the 7th.
- [x] Diagnostic worth keeping: **ASAN clean + systematic wrong values + a
      segfault = a marshaling bug, not a memory bug** — count the argument
      list first. Final: **1,684 cases, 0 mismatches**, comparing aggregates,
      the located spk-at-n_in, the length, the entire preimage buffer, and the
      digest, across 13 hash_types (including the six invalid ones the
      2026-08-22 consensus fix must keep rejecting).
- [x] **Standing rule, applied without exception from here:** no struct
      offset, no field order, no argument position enters assembly until an
      `offsetof` probe has printed it or it has been counted against the SysV
      tables. Three for three today, all caught before the daemon linked
      anything.

### Housekeeping

- [x] **Mistake of my own:** `asm/tests/.gitignore` lists build products by
      individual *name*, so every test added during this campaign defaulted to
      **tracked**, and `git add -A` committed **~5.6 MB of test binaries**
      across the night's merges. All 20 names added to `.gitignore` and
      untracked. History not rewritten — not worth it at this size — but
      nothing further accumulates.

### Status at the time of writing

- Parity rebuild **96.7%** (931,490 / 963,775), **zero rejects** since it
  started. (The 483 `REJECT|FATAL` lines in the production log are all
  historical, latest 08-23 01:22 — I miscounted this once and corrected it.)
- Slices 1–13a merged and pushed. **13b is committed in its worktree and
  deliberately unmerged**: the full suite now exceeds the harness timeout
  because the box is loaded, and competing with the rebuild for a merge gate
  is the wrong trade with the parity proof minutes away.
- Two slices remain (worker pool, entry points), then the swap.

## Session (afternoon): the parity proof lands, then "runs live" turns out to be false and gets fixed

Times UTC. The morning's C→asm campaign is above; this is what happened once
the rebuild finished.

### 08:57 — the rebuild completes, and the parity proof is clean with nothing waved away

- [x] Rebuild done: genesis → 963,775, **zero rejects**. Heartbeat shows
      `txouts=165,733,680` (Core-shaped ~166M, was 419M before the write-time
      unspendable filter).
- [x] `diff_utxo_setinfo.py` at 963,775 vs the scratch oracle:
      **txouts, total_amount, bogosize and MuHash all identical, delta 0 on
      all four.** `filtered out as unspendable: 0` — the read-time filter
      found nothing to remove, so the set IS Core's, not a superset that
      compares equal through a filter. And no overrides: incident #29's
      coinbase-overwrite fix made the two 2010 duplicate heights natively
      correct, so the "modulo two height fields" caveat every prior MuHash
      proof carried is gone. `RESULT: IDENTICAL`.

### 12:20–14:23 — incident #33: the node was never actually following the chain

- [x] Noticed the tip frozen at 963,775 for **14h26m** while Core moved 80
      blocks ahead, with `peers=8/8` the whole time. Every advance the night
      before had come from a restart's catch-up path; I restarted constantly
      to deploy, so "8/8 + advancing tip" read as keep-up. It was the
      restarts. And `do_outbound_sync` returned SILENTLY on both "exchange
      failed" and "peer had nothing" — the one condition that mattered logged
      nothing.
- [x] `b36c567` root cause: `node_sync_multi` passed `cap=64` to
      `cons_verify`, whose 4th arg is the txid-scratch capacity IN
      TRANSACTIONS. Any block over 64 txs failed validation; tip blocks carry
      thousands. Isolated by driving node_sync_multi against the oracle:
      **ok=0 with cnt=51,727** — it downloaded, validated and STORED 51,727
      blocks, then returned failure, and the caller threw the work away.
      Core's net log named the boundary: a run of ~216-byte blocks, then one
      of 97,994 bytes, then nothing.
- [x] `9fea9ad` RESOLVED: fixed (scratch to .bss, cap 80,000), and verified
      on live data — followed two consecutive blocks with no restart,
      `sync ok=1 new=1`, including one with **tx=3923**. Added `sync_fail_code`
      (1–9) naming which `.fail` exit was taken. It looked unfixed for half an
      hour only because no block was mined in that window; waiting for a real
      block instead of concluding from a quiet interval is what settled it.

### 14:35 — incident #34: the same units error, three more times, two of them stack smashes

- [x] `5c2860f`: auditing every `cons_verify` call site found three more.
      `node_drain` reserved 1024 bytes and declared 256 txids (8192): any
      block >32 txs writes ~7 KB past it over saved regs and the return
      address, on peer-supplied data. `node_ibd_blocks` similar. `node_serve_
      loop` passed `(2000*81+8)` — a headers-page BYTE size — into the COUNT
      parameter, and its scratch was `hp_buf`, the live headers page. All
      three now use dedicated .bss scratches. Generalisation recorded: a
      parameter that is a COUNT while the caller owns the BUFFER is a units
      trap; the C callers all get it right structurally with
      `sizeof scratch / 32`, every asm caller wrote a literal.

### 15:04 — incident #35: `connect=` never took the node offline

- [x] `eb469a6`: the first real tier-3 `run-ours` dialled DNS seeds despite
      `connect=192.0.2.1`, appended 567 blocks past the truncated tip
      (invalidating the run by the harness's own post-condition), and the
      failing seed legs starved the rotation so it applied **zero blocks in 8
      minutes**. The seed fallback fired without consulting `connect_only`.
      `bench_tier3.sh` had DOCUMENTED this since 08-22 and worked around it
      rather than fixing it. Same shape as `max_outbound` being parsed and
      ignored: a setting honoured on the main path and bypassed on the
      fallback is worse than one that does not exist, because the log claims
      it was applied. Fixed; re-run stays offline, archive steady at 800,001.
- [x] `b4fb290`: the incident-#33 instrumentation was left too loud in
      production (a line per leg per rotation, including the normal at-tip
      no-op). Replaced with one heartbeat field `sync_failing=N`, and a leg
      that fails 3 passes is dropped so the rotation stops burning ~60 s on a
      `where=3` headers-drain timeout against a peer that will not answer.

### 14:56 → — Tier 3 running

- [x] `run-ours` (genesis → 800,000, full verification, no assumevalid, 8
      pinned cores, offline and bounded) is replaying. This is the end-to-end
      vs-Core number the project has never had. Core side (`-assumevalid=0`,
      same range) runs after, once `run-ours` frees disk. Result expected
      late tonight; every timed-run caveat stays with it.

### ~20:00 → — read-side Core-parity RPCs while Tier 3 runs (branch `rpcfeat`)

Kept building on an isolated worktree so nothing touches the timed run
(compiles pinned to cores 0–15; Tier 3 owns 16–23). All read-only, all
self-contained block data — the kind of parity work that needs no live-node
state and no architecture change.

- [x] `getdifficulty` — difficulty of the current tip's `nBits`. Verified live
      against the scratch oracle: ours and Core both return
      `125807076547197.5` exactly.
- [x] `gettxoutproof` / `verifytxoutproof` — BIP37 `CMerkleBlock` (partial
      merkle tree). The differential is the strong kind: **the proof bytes are
      byte-identical to Core's**, and each node verifies the other's proof and
      returns the matched txids in the same tree order. Checked against the
      oracle over block 100000 (single tx, a 2-of-4 subset, all 4 including the
      coinbase) and block 800000 (3,721 txs — coinbase + tx1000 + last). Both
      require the block hash, exactly as Core does with no txindex; txid-not-in
      -block returns Core's `-5`, a tampered proof returns `[]` on both.
- [x] `tests/test_txoutproof.c` — a no-oracle regression that locks the
      algorithm to a fixed vector: block 100000's four txids rebuild the known
      merkle root, and a single-leaf proof round-trips (build → serialise-free
      extract) back to that root for every leaf, plus the 1-tx and odd-width
      (3-tx, duplicated last node) shapes. Wired into `make test`. ALL PASS.

Not merged to main and the full suite not run — both wait for the Tier 3 gate.
Branch pushed to GitHub as the backup.

### ~20:30 → — `decodescript` (util RPC, byte-diffable against Core)

Continuing the read-side parity work on `rpcfeat` while the UTXO replay runs
(compiles still pinned to cores 0–15).

- [x] `decodescript <hex>` — a faithful port of Core's `rawtransaction.cpp`:
      `ScriptToUniv` (asm/type/address, no top-level hex) then the `p2sh` and
      `segwit` wrappers, gated by Core's exact `can_wrap` / `can_wrap_P2WSH`
      logic — the uncompressed-pubkey exclusion for PUBKEY/MULTISIG, the
      `OP_CHECKSIGADD` / `OP_SUCCESSx` / `HasValidOps` / `IsUnspendable`
      rejections, and P2SH-nesting of witness programs. Reuses the existing
      `script_asm`/`script_type`/`script_pubkey_json` + `hash160`/`sha256_full`.
- [x] `validation/decodescript_diff.py` — output differential vs the scratch
      oracle. **37/37 identical modulo `desc`**: every wrapper branch (each
      standard type, compressed vs uncompressed pubkey/multisig, nulldata,
      OP_CHECKSIGADD, witness programs, empty) plus 24 real scriptPubKeys
      pulled live from blocks 963900–963902. `desc` (inferred descriptor) is
      the sole documented omission — we have no descriptor engine.
- [x] `tests/test_rpc_chain.c` — 19 in-process decodescript assertions over the
      same branches (no archive needed; decodescript is pure). Confirmed one of
      my own test vectors was a mis-typed 68-byte "pubkey" before fixing it —
      the classifier was right (nonstandard), the literal was wrong.

Same gate as the rest of `rpcfeat`: not merged to main, full suite deferred to
the Tier 3 gate. Next pure-function util candidates: `validateaddress`,
`createmultisig`.

### ~20:40 → — `validateaddress`, and three field bugs it exposed

- [x] `validateaddress <address>` — decode/classify into Core's
      `DescribeAddress` shape (`isvalid`, canonical `address`, `scriptPubKey`,
      `isscript`, `iswitness`, `witness_version`, `witness_program`). The
      method already existed in `rpc_commands.c` (shared with `getaddressinfo`)
      but its JSON builder had **no field-level test**, and writing the
      differential found three bugs against Core:
        - **P2WSH `witness_program` was garbage** — it read the 20-byte `h160`
          buffer but copied 32 bytes, appending 12 bytes of adjacent stack. So
          every P2WSH address reported a wrong witness program. Fixed to use
          the 32-byte `prog32`.
        - **P2TR `isscript` was false** (Core: true — taproot is a script path).
        - a stray **`ischange`** field that Core's `validateaddress` doesn't
          emit (it belongs to `getaddressinfo`).
      Also: `isvalid` now false for a checksum-valid but unknown base58 version
      (matches `DecodeDestination`), and the address is echoed in Core's
      canonical form (bech32 lower-cased), not verbatim.
- [x] `validation/validateaddress_diff.py` — every type identical to the oracle
      (valid byte-for-byte; invalid on `isvalid`). `tests/test_rpc_chain.c`
      gains 16 in-process assertions incl. a P2WSH-witness_program regression
      and a "no ischange" guard.

Process note: the decodescript commit earlier this session (`a50a7b7`)
accidentally committed **only** its differential script — a `git add` with a
not-yet-existing pathspec aborted the whole staging, and I didn't check
`git status` before committing. A later `git checkout -- rpc_chain.c` then
discarded the uncommitted implementation. Caught when the decodescript tests
went red; re-applied and this commit carries the real implementation. Lesson
re-learned: verify what's staged before committing.

### ~20:55 → — `createmultisig` (util, m-of-n)

- [x] `createmultisig <n> <keys> [address_type]` — a faithful port of Core's
      `output_script.cpp` + `AddAndGetMultisigDestination`. Validates each
      pubkey on-curve through `pubkey_parse` (the ASM CPubKey::IsFullyValid),
      assembles the `OP_m <key>.. OP_n OP_CHECKMULTISIG` redeemScript with the
      correct count encoding (OP_1..OP_16, or a 1-byte push for 17..20), and
      derives the address for legacy (P2SH), p2sh-segwit (P2SH-P2WSH) or bech32
      (P2WSH). Uncompressed keys force legacy and, if a segwit type was asked
      for, add Core's exact warning; `bech32m` is refused verbatim.
- [x] `validation/createmultisig_diff.py` — **17/17 vs the oracle, modulo the
      omitted `descriptor`**: every output type, the 15/16/17-key count-encoding
      boundary, the uncompressed→forced-legacy+warning path, and every error
      code (required<1, not-enough-keys, >20 keys, bech32m, unknown type,
      invalid key, off-curve key). The off-curve case matching is what confirms
      `pubkey_parse` == Core's IsFullyValid. Valid on-curve test keys are
      generated in-harness (privkeys 1..N × G) so the many-key cases are real.
- [x] `tests/test_rpc_chain.c` — 9 in-process known-answer assertions (Core's
      help-example keys: the legacy/bech32/p2sh-segwit addresses + redeemScript,
      and the error paths). `descriptor` omission asserted explicitly.

Note on the harness: RPC errors return over HTTP as 500 + a JSON error body
(Bitcoin's own JSON-RPC convention). A first harness read 500 as a transport
failure; corrected to read the body, which is what bitcoin-cli does.

### ~21:15 → — descriptor checksum; createmultisig now byte-identical incl. descriptor

- [x] Implemented Core's descriptor checksum (`descriptor.cpp
      DescriptorChecksum` + `PolyMod`) exactly — the 8-char `#...` suffix over
      the descriptor input charset. Verified against three known Core outputs
      before wiring it in (`4djp057k`, `5wacx8g6`, `pue77965`).
- [x] `createmultisig` now emits the **`descriptor`** field —
      `sh(multi(m,keys))` / `wsh(...)` / `sh(wsh(...))` with the checksum. It's
      mechanical here (all keys are known from the request), so it needs the
      checksum but **not** a full descriptor engine. This **closes the last
      divergence**: `validation/createmultisig_diff.py` now compares the whole
      object (descriptor included) and is 17/17 byte-identical to the oracle.
- [x] `tests/test_rpc_chain.c` asserts the exact legacy descriptor string.

The checksum primitive is reusable for the broader `desc` field on
decodescript / scriptPubKey output, but those need `InferDescriptor` (a real
descriptor engine) to build the string — a larger, separate piece.
