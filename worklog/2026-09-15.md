# DAILY WORKLOG — 2026-09-15

> **Backfilled 2026-09-16 from the commit record**, not written live. The daily
> file was missed on the day; PR numbers, commit subjects and landing order are
> from git and are reliable, but the session narrative and any reasoning that
> never reached a commit message are not recoverable and are not invented here.

RPC response shapes and Core's argument-error vocabulary. PRs #211–#219.

## Defects found by comparing whole responses

- [x] `74c2813c` cli: **a 64-character hash made only of digits was sent as a
  JSON number.**
- [x] `2bce2783` 8 more methods covered, and an open defect in
  `signrawtransactionwithkey`.
- [x] `175601d7` **signrawtransactionwithkey: the segwit marker was read as the
  input count.**
- [x] `aa29405e` **wallet: seven methods reported "out of memory" when no wallet
  was loaded.**
- [x] `b1bc132f` signrawtransactionwithkey: keep the witness — and the
  scriptSig, and the locktime.

## Core's argument-error vocabulary

Core answers `-1` missing (with full help text) / `-3` wrong type / `-8` bad
value, and resolves the wallet BETWEEN the type and value stages. Measured
against a Core with genuinely no wallet rather than reasoned about.

- [x] `9b1cca20` (#216) the `errors` entries carry `witness` and `scriptSig`.
- [x] `aa38c497` (#217) the wallet-absent and argument-type error codes match
  Core: `-4` → `-18`, `-8` → `-3`.
- [x] `e244a1b9` (#218) Core's "Wrong type passed" wrapper at the remaining
  **nine** sites.
- [x] `72db7a76` (#219) **createrawtransaction ignored `version`, and Core
  reports EVERY bad position** — not just the lowest, which an earlier claim
  here got wrong from truncated probe output.
