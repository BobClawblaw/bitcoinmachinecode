# DAILY WORKLOG — 2026-09-06

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR landed as a `--no-ff` merge after a green
`make -j8 test` gate (0 failures); one gate at a time.

## Session 11 (21:14Z 09-06 → 01:30Z 09-07): the ARM port — the interpreter-review series closes

The arm-port queue was five red pinning tests plus one segfault, all attributed to
the IR-1..IR-17 interpreter-review series. All six are green, and the interesting
part is what the attribution had been hiding: the sweep line that read `IR-4 child
killed by signal 11 (alarm: quadratic scan)` was a SIGSEGV inside vfexec_push, and
`test_taproot_verify_diff segfaults, expected to clear with the IR series` was an
8-byte stack overrun that no IR item on the list covered.

- [x] **Round 31 GREEN: pass 367 / fail 4, compared 385 of 427 plan rows.** All four
  fails are the standing env-only bench rows (bench_checkblock and bench_taproot_block
  want Core's block413567.raw; bench_hashidx and bench_idxscan want ./index.dat in the
  scratch dir). The session opened at pass 362 / fail 8 with four real failures —
  **there is now no functional failure on the port.** bench-ok 14, skip 10,
  build-fail 7 (new, see below).
- [x] **IR-2** (406a1b6e) hashtype popped before the DER parse — all three ARM sites
  (bitcoin_checksig.S x2, bitcoin_multisig.S, bitcoin_script.S). a15a8a4c
- [x] **IR-6** (4efb0c4a) OP_ROLL rotates 4-byte handles: hnd_base/hnd_dirty/hnd_tab,
  hnd_begin/hnd_end, all nine stack primitives, script_eval's register/unregister. The
  pinning roll storm: 27.9 s -> **128 ms** against a 400 ms bound. Two things worth
  recording: (a) x86's hnd_addr/hnd_of are local `call`s, but AArch64 has no
  callee-saved scratch below x19, so they became inlined MACROS — a frameless `bl`
  writes x30 with no save area (the a64 auditor's UNSAVED-CLOBBER gate flagged 5) and
  would bolt a prologue back onto the primitives IR-6 exists to keep frameless; (b)
  verifying the port found the four position accessors had **lost their `ret`** when
  ELEM_PTR became HND_ADDR and were falling through into each other — stack_top_ptr
  subtracted a *second* position and at sp<2 indexed hnd_tab with a 32-bit -1
  (0xffffffff << 2), a SIGSEGV on the first OP_TUCK. A fall-through is invisible to
  the ABI auditor; only running the test caught it. 3711a746
- [x] **SCR-2's vfexec bound was never ported — a memory-safety gap that was on no
  queue.** x86 grew the condition stack 1 KiB -> 5 MiB and bounded vfexec_push (audit
  2026-09-03); the twin still had the 1024-byte buffer with an *unbounded* push, so the
  1025th nested OP_IF wrote over vfexec_sp and the rest of the TLS block — the
  attacker-controlled OOB write from P2P input the audit named, plus a false accept of
  an unbalanced script. Found by refusing the sweep's own explanation: SIGALRM is 14,
  and the message said 11. f8c9474f
- [x] **IR-4** (71b6de1f) all_true is one compare against a cached first-false
  position, not a scan before every opcode. ARM detail: NO_FALSE is stored as -1 but
  tested as `cmp xN,xzr; b.lt`, because cmp takes no negative 12-bit immediate —
  `cmp xN,#-1` does not assemble. Same commit as SCR-2.
- [x] **IR-1** (9952a1c4) 17 x86 push sites land as 12 ARM ones (the twin loops OP_3DUP
  and OP_2OVER into one body each), all routing to a new .stack_size_err. Same commit.
- [x] **IR-8** (fe3fb996) OP_CODESEPARATOR under CONST_SCRIPTCODE at BASE refused at
  .not_push, *before* the fExec gate, so it fires in an unexecuted branch. Same commit.
- [x] **IR-12/IR-13** (b7455a84) is_opsuccess_c zero-extends its C-int argument
  (`mov w0,w0` = x86's `mov eax, edi`); the LOW_S arm compares S against the group
  order N before N/2 — Core lax-parses, so S >= N is a ZERO signature, which is not
  high, and the answer is NULLFAIL(32) where this reported SIG_HIGH_S(27). Same commit.
- [x] **IR-3** (9df5e95a) — the rc=139 root cause, and never an IR-6/1/8 symptom.
  taproot_checksig_ctx is 104 bytes (hard_fail +96, hard_err +100) and the shared C
  hook writes both; bitcoin_taproot_verify.S reserved **96** with the script_state
  immediately below, so every tapscript CHECKSIG wrote 8 bytes past the ctx onto the
  state's main_elems. Caught with a watchpoint: the write came from
  taproot_checksig_fn+0x98 (`str w0,[x19,#96]`), elems became 0xffff00000001, and
  stack_push faulted writing through it. Second and worse: **nothing read hard_fail
  back**, so a hard failure (invalid non-empty sig, empty pubkey, weight budget) was a
  silent ACCEPT where Core fails the script — `OP_CHECKSIG OP_DROP OP_1` with an empty
  pubkey. L_CTX is 104 B now (L_ST/L_LEAFV move 8 down), both fields zeroed, and a
  truthy stack is rejected with the byte-identical `p2tr tapscript checksig invalid`.
  test_taproot_verify_diff: 30 cases, 0 mismatch. 9cffb4c5
- [x] **IR-10** (a7c16c7a) bounds on every cursor advance in verify_p2pkh — plus a
  deliberate non-port: x86's second half (parse_varint clobbering the caller's rcx)
  does not apply, because this file's .Lparse_varint documents clobbers-x2-x7-only and
  the walk lives in x21/x23/x24. That is why the ARM twin passed the 253-byte-scriptSig
  case *before* the fix and x86 did not. IR-17 needed no change either: bitcoin_sigops.S
  already labels script_sigops accurate=false and does `mov x2,#0` — the inverted
  header was never copied here. 37310ae3
- [x] **IR-7** (1138aed2) the driver stopped memsetting the 528,000-byte arena per call
  and both P2SH memcpys narrowed to sp x ELEM_SIZE live records — ~1.06 MB of dead
  traffic per legacy input on the connect path. Removing the clear rather than filling
  with 0xAA is the *stronger* form of x86's deadness proof: stale records now genuinely
  persist across calls, so any reader that wanted the zeroing meets old data. 228e8c5e
- [x] **arm-12 is buildable**: build_daemon.sh's link re-run against a temp output —
  rc=0, no undefined symbols. **NOT deployed**: the live node (two bitcoind processes
  off daemon_out/bitcoind, config bitcoin-arm-stability.conf, data ./data) is
  deliberately left on the round-29-green build and daemon_out/bitcoind still has its
  15:33 mtime. Deploying is a user call, not a port session's.
- [ ] **NEW open item — 7 sweep rows BUILD-FAIL, all one cause.** test_muhash,
  test_muhash_mul_diff, test_lsm_flush_sort_diff, test_utxo_probe_diff, bench_muhash,
  bench_lsm_flush_sort, bench_utxo_probe: undefined reference to num3072_cpu_has_adx /
  num3072_mul_force_path / num3072_mul_current_path — the 2026-09-06 x86 perf batch
  reached for BMI2/ADX + AVX-512 IFMA paths with no ARM twin, so those four
  *differentials* do not run here at all yet. Needs an ARM num3072 (or C path selectors).
- [ ] **New open item — bitcoind.S CC-2 is half-wired**: the hook table exists as data
  symbols, but the mux never CALLs them (no sendcmpct announcement after verack, no
  hook-selected getdata type). The x86 daemon has both.
- Note on process, for whoever delegates next: the first pass of this queue ran as a
  delegated long task with a 900 s budget per attempt. It produced IR-2 soundly (its
  own revert-control check was good work) and IR-6 *unverified*, three attempts over —
  it never got as far as running the test, and the half-ported IR-6 fell over on the
  missing `ret`s described above. The lesson is the one this port keeps relearning: an
  ARM twin that assembles is not a port; only the pinned test run is.
- Host hygiene: parity_out/test_interp_core_vectors (PID 203627) has been running for
  **4 days** under a shell from an older session's probe. It is not part of the sweep's
  current run and nobody owns it; it wants a kill.

## Session 10 (01:00Z 09-07 →): the pipelined download landed

- [x] **PR #66 merged** (`ibd-pipeline-landed-2026-09-07`): whole-chunk
  getdata, in-order hold with the bump-pointer fix, archive guard with
  cache-invalidating retry. Proof on the real network, this build, fresh
  datadir, sharing the box with run 8: contiguous through **83,868**, past
  all three heights where earlier pipelined runs died (44,863 / 48,585 /
  74,765); 810 records hand-verified, 0 mismatches; 0 false BIP30, 0 guard
  trips, 0 evictions; lag 239 under the download.
- Run 8 NOT restarted: at 75 min it is 320k+ blocks in at ~10 MB/s, already
  past the early chain where the pipelining pays most. It is measured on the
  next run.

## Session 9 (00:00Z 09-07 →): the pipelining bug found without a network

- [x] **`test_shared_stress` landed**: 16 interleaved writers, 200 B-3 MB
  bodies, re-appends, reload per chunk -- 64,000 appends, zero
  inconsistencies. The store is correct. My "writer race" root cause was
  WRONG (second correction).
- [x] **The real bug is in `ibd_pipeline.c`'s hold**: the drain moved the bump
  pointer back over parked blocks; equal-size blocks then overwrote each
  other exactly. Reproduced in a unit test (order 1,3,0,4,2): old fetcher
  writes one wrong body, fixed writes none. On the branch, NOT merged --
  needs a real pipelined sync past 48,585 / 74,765 / 44,863.
- [x] **Rule 11 written** (`ENGINEERING_RULES.md`): a threshold calibrated at
  one chain depth is wrong at every other. The operator's verdict on runs
  4-7 -- "a crime against good coding" -- with the three mistakes and the
  counter-example (evicting faster gave 4x fewer blocks).
- Run 8 at 31 min: 205,761 blocks, 6.6 MB/s, 1 eviction, lag 360.

## Session 8 (23:00 →): the peer pool

- [x] **`lacks NODE_WITNESS` dial spam** (PR #60): 91% of the log, 7,891
  lines in 9 min from 28 addresses, one dialled 819 times -- each a wasted
  socket and handshake. Remembered per run in a page SHARED across the 16
  forked helpers (a per-process set still repeated once per helper, measured
  exactly 16). 91% -> 5% of the log; 12,857 redials skipped in 5 min.
- [x] **Peers killed for being early in the chain** (PR #61, tag
  `peer-selection-2026-09-06`): the absolute 32 KB/s floor, calibrated for
  megabyte blocks, declared every healthy worker dead at height 50k where a
  block is 200 bytes and a serial fetch moves ~9 KB/s. Run 7: 655 evictions
  in 30 min, 181 of them peers that had served two full chunks; 1,227 by
  56 min. Floor is now min(absolute, pool median / 4), never below 512 B/s.
- [x] **Peers ranked by a measured throughput sample** (same PR): the probe
  only tested TCP connect, so workers took peers in DNS-seed order. Each
  live peer now gets one timed 2,000-header fetch; pool sorted fastest
  first. 119 ranked in 35 s, best 1,446 KB/s, median 219, 46 silent last.
- [x] **Tried and REJECTED: evict faster while untried peers remain.** First
  eviction in 112 s instead of 249 -- and 4x FEWER blocks. Churn costs a
  handshake and abandons partial chunks. Not landed; the measurement is in
  the commit message.
- [x] **Run 8 started 23:59Z on `e0bd0b8`.** At 5 min 19 s: **41,974 blocks
  vs run 7's ~16,000**, avg recv 100 KB/s vs 32, 0 evictions, 0 rejects.
- Runs 5, 6, 7 stopped this session; lesson recorded: stopping a run must
  kill `run_bmc_bench.sh` as well as `bitcoind`, or the orphaned monitor
  writes a false FAIL into the next run's phase.log (it did, at 23:09Z).
- `peers banned this run: 715 of 119` was ban EVENTS across amnesty, never
  decremented; the line now reports current bans and events separately.

## Session 7 (22:00 →): run 5, and the root cause corrected

- [x] **Run 4 stopped at 8h55m / 622,558 blocks** to test the pipelined
  download, which is the only change that moves the hours. Logs kept in
  `/mnt/2tbssd/run4-interleave`. Its connect held **lag 0** through the heavy
  chain, which is the interleave working.
- [x] **Run 5 (pipelined, 22:12Z): the download IS ~10x** -- 125,136 blocks
  in 10.6 min against run 4's 74,638 in 74 min -- and **zero false
  `bad-txns-BIP30`**, past both heights where earlier pipelined runs died.
  The archive guard works: nothing wrong is applied.
- [x] **But the connect WEDGED at 44,862** and the 16:00Z root cause was
  WRONG. Hand-parsing that archive: the index record at 44,863 correctly
  names block 44,863 (matches the oracle) and points at bytes that hash to
  block **44,888** -- another block from the same 40-block chunk. The archive
  is inconsistent ON DISK. It is a **writer race in the concurrent append**,
  not a stale reader, and dropping the reader's caches cured none of the 264
  trips. Corrected in `UTXO_INLINE_BUILD_PERF_SCOPE.md` with the evidence and
  a measurement (not a guess) as the next step.
- [x] Benchmark restarted on main so the night still produces a number.

## Session 6 (21:00 →): the open behavioural rows

- [x] **`-blockversion` scope** (PR #57): Core honours it only under
  `MineBlocksOnDemand()` == `fPowNoRetargeting` (regtest); we honoured it on
  every chain, so a mainnet operator could change what `getblocktemplate`
  hands out. Same predicate now; 3 checks in `test_rpc_chain`, watched to
  fail.
- [x] **Ban list survives a restart**: `daemon/banlist.c` writes Core's
  `banlist.json` (its four keys, its entry version, its `_warning_`) with
  tmp+fsync+rename, loads at boot BEFORE anything dials, sweeps expiries.
  Every restart used to forgive every ban. 17 checks, watched to fail
  (11 of 17).
- [x] **Taproot script-path PSBT finalization** — the GAP that was deferred
  for want of fixtures. Generated them from an ISOLATED Core regtest node
  (never the oracle): `tr(NUMS,{pk(A),pk(B)})` can only be spent through a
  leaf, which forces Core to emit `PSBT_IN_TAP_SCRIPT_SIG`. Our
  `finalizepsbt` now produces a transaction **byte-identical** to Core's.
  Leaf matched by tagged hash; deterministic choice when several leaves are
  signed (Core's own fixture has two). **Signing is still absent** — row is
  PARTIAL, not DONE.
- Register now: 1 PROOF (the replay), 1 PARTIAL (taproot script-path
  signing), 0 GAP.

## Session 5 (20:00 →): flags must match Core

- [x] **Core's benchmark leg was handicapped, by us, with no reason recorded.**
  `run_core_bench.sh` wrote `par=8` and `maxconnections=64` for Core (added
  2026-09-04; no justification in the script, the memo or the commit) against
  Core's defaults of `par=0` and `maxconnections=125`. So the **21.0 h figure
  we compare ourselves against was measured with Core on eight verification
  threads and half its connections**, a handicap on one side only. Both lines
  removed; `dbcache=8192` stays and is commented as the single deliberate
  deviation because BOTH sides get it. The perf scope and the benchmark memo
  now call 21.0 h a floor for Core, not a fair result.

- [x] **`-par` fixed to Core's semantics** (PR #52, tag
  `par-semantics-2026-09-06`). Core: `-par` is the TOTAL script-verification
  threads INCLUDING the calling thread (0 = every core, -n = leave n free,
  1 = single-threaded). Ours parsed it, printed it, then spent it on the
  DOWNLOAD chunk-worker count while both verify pools sized themselves from
  `sysconf()` and never read it — so `par=8` still verified on every core AND
  halved the download. Downloads are `bmc.catchupworkers` now. Nine checks in
  `test_tx_verify_parallel` assert the READER; watched to fail (7 of 9).
  **This was also the benchmark asymmetry**: the harness gives Core's leg
  `par=8` on a 32-core box while we used all of them, and no setting we
  exposed could have equalised it.
- [x] **Whole config surface audited by name**: all 140 keys against Core's
  full option list including hidden ones. Only `addrindex` and
  `walletpassfile` have no Core counterpart, plus three names Core retired
  (`limitancestorsize`, `limitdescendantsize`, `mempoolfullrbf`). Renaming
  breaks existing configs — left for a decision.
- [x] **Standing rule recorded**: a key named after a Core option must have
  Core's exact semantics; anything else is discussed first. User's words:
  "Our flags need to match core. Never do something different unless we
  discuss it."
- Two build mistakes, both caught by our own audits, neither by me: a new
  module had to join seventeen source lists and arrived twice in several
  links (`multiple definition`, gate died at 182 lines); and a test rule that
  linked objects it did not declare stopped `make` rebuilding it, so a
  control run passed when it should have failed. `prereq-check` and
  `link-check` named both.

## Session 4 (13:00 →): the download lever, and the bug it exposed

- [x] **Archive guard landed**: the connect refuses a body that is not the
  block the index records at that height (`utxo_live.c`). A racing read can
  no longer file another block's coins under a height and turn the NEXT
  height into a false `bad-txns-BIP30`. Stops the pass like a hole; the next
  pass retries; `utxo_live_archive_mismatches()` counts them.
  `test_dlc_interleave` gained a reverse-order peer mode
  (`BMC_TEST_PEER_REVERSE=1`) — every fixture peer in the suite answered
  getdata in request order, so no test ever delivered an out-of-order chunk
  to the real connect loop.
- [x] **ROOT CAUSE of the pipelining failure found (16:00Z): a stale
  READER, not a bad archive.** Ran the decisive experiment -- pipelined build
  + the archive guard -- and the guard fired 14 times at height 2,845 while
  the false `bad-txns-BIP30` never appeared. Hand-parsed `index.dat` and
  `blk00000.dat` at 2,843-2,847: every record matches its body and the
  oracle, so nothing on disk was ever wrong. `bitcoin_store_fast.asm` caches
  a read fd and an mmap window per blk file; `store_reload` refreshes the
  index but drops neither, so the connect re-read the same stale bytes every
  pass and never advanced. The serial build wrote slowly enough to go 494k
  blocks without tripping it. Half the fix is landed (the guard, PR #49);
  the other half -- drop the caches and re-read -- is written, unproven, and
  parked on `batch/2026-09-06-ibd-pipeline-v2`.
- [ ] **Pipelined download: 12x measured, NOT landed.** One getdata per
  40-block chunk instead of one per block: 75,425 blocks in 6 min against
  the serial build's 74,638 in 74 min, same peer pool, same afternoon.
  Landed as PR #47 and **reverted in PR #48** — both pipelined runs died on
  a false `bad-txns-BIP30` (heights 48,585 then 74,765) on blocks the oracle
  confirms are ordinary. Ruled out: a shifted archive, arrival-order storage
  (fixed and it still failed), a store ordering hole (STO-11 holds on both
  append paths), and the new reverse-order fixture (passes with the fault
  present and absent — 600 blocks over 3 peers is not enough concurrency).
  Full write-up and the next step in `UTXO_INLINE_BUILD_PERF_SCOPE.md`.
- [x] **Benchmark run 4 running** since 13:17Z on `c1a4473`, 0 rejects.
  Runs 1-3 were stopped: run 1 hit the genesis bug, run 2 was stopped to
  pick up the pipelining, run 3 died on the pipelining bug. About 8 h of
  wall clock spent on stops; the lesson is in the memory notes.
- [x] **CC-8 replay parked** at 744,367 blocks (77%) to give the box to the
  benchmark; resumes from its archive.

## Session 3 (04:00 →): the module branches landed; the interleave

- [x] **Benchmark RESTARTED 07:08Z on `ff5cc24`** after the fix: genesis
  seeded, slot 0 correct, 469 held header pages released, the parallel
  downloader running from height 0 with the connect interleaved
  (`applied=78 lag=2`), **0 rejects**. Expected ~20 h.
- [x] **CONSENSUS BUG found by the benchmark and fixed** (PR #39, `504d914`,
  tag `archive-genesis-seed-2026-09-06`): a fresh MAINNET datadir never
  seeded genesis at index 0, so the serial leg's first append (block 1, the
  first block a peer relays) landed in slot 0 and the archive was shifted by
  one for life — reading height h returned block h+1. It surfaced as a false
  `bad-txns-BIP30` four minutes into the fresh sync, and today's
  reject-not-halt change then persisted a BLOCK_FAILED_VALID mark on a valid
  block and stopped all 16 helpers. Diagnosed by printing the colliding
  coin's recorded height (h-1, the proof of the shift); oracle confirms the
  blocks are ordinary with unique, still-unspent coinbase outputs.
  `test_archive_seed` watched to fail first (7 of 9). Reproduced 3 runs of 4
  before the fix, 0 of 2 after. **The gate could not have caught it:** no
  test ever creates a fresh mainnet datadir.
- [x] **First benchmark attempt (05:50Z) ABANDONED** — it hit the above four
  minutes in and spent an hour on the serial fallback. Evidence kept in
  `/mnt/2tbssd/EVIDENCE-bip30-508-run1`.
- [x] **Full-sync benchmark vs Core STARTED 05:50Z** on `/mnt/2tbssd/bmc-bench`
  from a fresh clone of main `0151051`, 16 workers, 123 confirmed-live
  peers, 965,215 blocks to go. Expected ~20 h. Three things had to be
  fixed first: the harness built `daemon/bitcoin_cli`, renamed to
  `daemon/bmc_cli` on 2026-09-05 (`b89f468`) — 12 scripts on the bench SSD
  patched, the repo's own copies were already current; the harness wrote
  no `bmc.bootcatchup=0`, so the download would have run in the parent and
  never interleaved; and the SSD had 912 GB free against a ~1 TB archive,
  freed to 1.7 TB by deleting Core's finished benchmark chain data (its
  RESULT, phase/console/install logs and binaries kept in
  `core-bench/artifacts-2026-09-04/`; its 21.0 h number is in
  `benchmarks/2026-09-04-ssd-bmc-vs-core`).
  **Real-network proof of today's CC-5 fix, 05:53:58Z:** "chain from
  95.17.238.147:8333 crossed -minimumchainwork -- storing 469 held
  page(s)" — 938,000 headers held and released in order. On the four-page
  bound this fresh sync would have been abandoned outright.
- [x] **Replay restarted on the CC-5 fix (05:26Z) and again with
  `bmc.bootcatchup=0` (05:28Z).** PR #28 merged (`ab7087e`, tag
  `lowwork-hold-bound-2026-09-06`), gate green. Live proof of the fix:
  "crossed -minimumchainwork -- storing 284 held page(s)". The boot then
  ran the download in the parent ("connect deferred (no UTXO engine in
  this process)"), so the config now leaves it to the worker: from 05:32Z
  `applied=371745 lag=1` — download and UTXO connect interleaved, one
  block apart. Lesson from the first attempt: the parent exits in 2 s but
  the worker finishes its bounded step first; wait for the worker before
  swapping the binary (the early relaunch refused the datadir lock; nothing
  ran twice). `DEPLOYMENT_HISTORY.md` 2026-09-06 (05:25–05:33Z).
- [x] **CC-5 regression found on the replay and fixed** (`4c3e8fc`, branch
  `batch/2026-09-06-lowwork-hold-bound`): the low-work hold abandoned any
  header chain still below `-minimumchainwork` after FOUR pages — every
  honest sync below ~880k. On the restarted replay at 04:59:38Z: "4 full
  pages and still below -minimumchainwork -- abandoning", "archive already
  complete through 250913", "parallel downloader wrote 0 block(s)", then
  the serial leg (60 s re-dials, `sync_failing=1`). Hold now 1,000 pages /
  162 MB mapped lazily per fetch. `test_hdr_lowwork` 12 → 15 checks; the
  three new ones fail on the four-page module (watched), pass on the fix.
  test_ibd_headers, fakepeer_headers, test_bitcoind_sync, test_dlc_interleave
  green; 8 audits green; gate running.
- [x] **CC-8 replay restarted on the interleave build** at 04:55Z
  (`305c1b4`, `bitcoind.replay.prev-77b6c1f` kept): clean SIGTERM stop
  (parent forwarded, worker gone in 3 s; "catch-up done: 240320 new blocks
  written (10172.69s)"), same command, same datadir; resumed from the
  250,913-block archive and applied the 240k-block backlog at ~20k blk/s
  (early chain), ~500 blk/s by 200k. **Correction:** the first run's
  download was the parent's BOOT catch-up (its shutdown line: "shutdown
  requested during the catch-up -- exiting before the worker starts"), not
  the worker's far-behind run as claimed earlier; the restart went straight
  to the worker. To be restarted once more on the CC-5 fix.
- [x] **UTXO connect interleaved with the download (step 1)** —
  `batch/2026-09-06-utxo-interleave-land`: `utxo_live_catchup_bounded`
  (budget + stop-at-hole, one shared loop body) and `dl_catchup` connecting
  under an 8 s budget between reaps. Gate green (13,307 lines, 0 failures).
  `test_utxo_catchup_bounded` 7 failures with the budget forced to 0 → pass;
  `test_dlc_interleave` (real `dl_catchup`, 3 loopback fake peers, 600
  blocks, 3 helpers) 7 failures with the interleave off → pass; 583 blocks
  still missing while already connecting. **Caveat:** the boot-time
  catch-up runs in the parent before the UTXO writer exists and cannot
  interleave; a fresh-clone benchmark needs `bmc.bootcatchup=0`. Found, not
  fixed: no `TCP_NODELAY` on outbound sockets, `p2p_write` is two writes.
- [x] **Seven module branches landed as one merge** (PR #26, 04:33; tag
  `utxo-modules-2026-09-06`; `releases/2026-09-06-utxo-modules.md`).
  Measured on one pinned core: `num3072_mul` 944 → 603 ns (BMI2/ADX) → 301
  ns (AVX-512 IFMA), MuHash element 1640 → 1005 ns; flush descriptor sort
  541 → 91 ms at N=4M, run byte-identical (gated diff); compact-block
  reconstruction 10.03 → 1.45 ms/block; the coinstats fold leaves the bulk
  connect path (seed once at caught-up; forked worker + shared ring in
  steady state, RPC gated on the fold watermark). 13 cherry-picked commits,
  zero real edit conflicts; the keep-both resolution left EIGHT copies of
  the Makefile `test:` rule (make merges them, so it still passed) — caught
  by an 8x word count, collapsed to one union line before the gate.
- [x] **The public tip is the connected tip; a failed connect rejects, not
  halts** (PR #25, 04:14). `g_node_status->tip_height` was the STORED tip
  (main.c:5348, 8469); `[utxo_live] FATAL: apply_block failed` halted.
- [x] **Per-block cost timers on the connect loop** (PR #24, 04:01; scope
  step 0). Instrumentation found the two biggest terms: the MuHash fold per
  coin (~1.66 µs × ~6.4 B events ≈ 3 h) and every input looked up twice.
- [x] **Finding: the MuHash coinstats fold is on the bulk connect path**
  (PR #23, 03:39) — added to the perf scope with three levers.
- [x] **Scope: Core's in-RAM cache model on our memtable/runs** (PR #22,
  03:28; `audits/UTXO_CACHE_MODEL_SCOPE.md`). §4.2 (tiered compaction) was
  stale — 79d4c9c had landed it 2026-08-31; corrected today.
- [x] **Scope: build the UTXO set inline — the 3-hour gap decomposed**
  (PR #21, 03:20; `audits/UTXO_INLINE_BUILD_PERF_SCOPE.md`): 19.5 h
  download (peer-bound, both sides) then 4.5 h UTXO catch-up that ran AFTER
  it while the download worker sat in a 10 s sleep loop.
- [x] Devlog updated: `LOG.md` 2026-09-06 entry (ten findings in the order
  found), `PERF_SCOPE.md` §15, `BENCHMARKS.md`, `ASM_CONVERSION_CANDIDATES.md`
  status, README index rows.

## Session 2 (00:30 → 03:05): Core behavioral compatibility, the list resolved

- [x] **CC-2 compact block receive; CC-10 coin selection (knapsack, SRD,
  waste) + `invalidateblock`/`reconsiderblock`** (PR #20, 03:04; tag
  `core-compat-2026-09-06`; `releases/2026-09-06-core-compat.md`). CC-2's
  gate failed four times before it passed: cmpct_recv.c in an OBJS list
  that `clean` deletes; direct `call cmpct_*` from bitcoind.asm forced ~60
  test link sets to change (duplicate definitions) → hook pointers
  (`g_cmpct_hook_*`); an unconditional sendcmpct after verack desynced
  test_bitcoind_sync's fake peer → gated on the hook being installed.
- [x] **CC-4 block-relay-only legs + `anchors.dat` (Core's format byte for
  byte); CC-5 low-work header pages held until the chain proves its work**
  (PR #19, 02:18).
- [x] **CC-3 inbound eviction (`AttemptToEvictConnection`); a full table no
  longer serves unrecorded peers** (PR #18, 02:05).
- [x] **CC-1 tx relay to and from inbound peers** — shared announce ring,
  Poisson-timed, feefilter-honouring (PR #17, 01:49).
- [x] **CC-6 extra outbound on a stale tip; CC-7 `-peertimeout` (DMN-14)** —
  it was parsed and read by nothing (PR #16, 01:33).
- [x] **CC-8 full-verification replay started** 01:12Z from the Core oracle
  (`assumevalid=0`, `stopatheight=965598`, `bmc.peerminbps=1024`). Its leg
  to the live node blocked ten minutes in the boot header fetch (not
  reproduced; filed against the fetch); now syncs from the oracle.
- [x] **CC-9 BIP331 package relay: decided** — Core does not ship it;
  opportunistic 1p1c already matches.
- [x] **Scopes CC-1..CC-10** (PR #15, 01:09;
  `audits/CORE_COMPAT_SCOPES_2026-09-06.md`).
- [x] **Core behavioral compatibility register** (PR #14, 00:56;
  `CORE_BEHAVIORAL_COMPAT.md`, 121 rows; at close 81 DONE, 13 DECIDED, 2
  PARTIAL, 2 GAP, 2 PROOF).
- Observed on the host, not caused by this work: the bench node at
  `/mnt/2tbssd/bmc-bench` shut down cleanly at 01:50Z on a SIGTERM nobody
  here sent (`DEPLOYMENT_HISTORY.md` 2026-09-06).

## Session 1 (00:00 → 00:30): carry-over
- [x] Everything from 2026-09-05 confirmed pushed; `main` and `origin/main`
  equal; no local-only branches.
