# DAILY WORKLOG — 2026-09-05

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR below landed as a `--no-ff` merge after its own green
`make -j8 test` gate (0 failures) unless the line says otherwise.

## Session 2 (evening → night): live node restarts, a wrong diagnosis, a config defect

- [x] **Docs consistency review written to the audits** (PR #13, 22:25). The
  `/code-review` of the systemd docs PR found 7 confirmed inconsistencies
  (DR-1..DR-7) — one line "closed by decision" had left five documents
  contradicting it, including OPERATIONS.md publishing a reference unit with
  `LimitCORE=infinity` and no hardening. All seven fixed (PR #12, 22:14);
  the review itself is `docs/audits/DOCS_REVIEW_2026-09-05.md`.
- [x] **Systemd unit taken off the list by operator decision** (PR #11,
  22:02). "Ignore systemd. It local issue." Recorded as a decision, not a fix.
- [x] **Forum reply refreshed with the audit results** (PR #10, 21:58), then
  set aside on request ("Ignore forums post").
- [x] **Rolled back to 8332; CLI config precedence fixed** (PR #9, 21:56).
  `data/bitcoin.conf` deleted, node restarted, back at the tip
  (`headers == blocks`, `ibd false`, `NRestarts=0`). Defect found on the
  way: `cli_conf.c` resolved each key across BOTH config files while the
  daemon reads ONE, so a datadir conf setting `port` but not `rpcport` left
  the daemon on its default RPC port while the CLI read the repo file's.
  Now one file, the daemon's precedence; regression test watched to fail
  first.
- [x] **P2P moved to 8433 — and corrected** (PR #8, 21:23). The move was
  made to avoid a clash with Core that does not exist: Core binds sixteen
  specific loopback aliases on 8333, never 0.0.0.0. Two wrong claims
  corrected in the deploy record: the node had an inbound listener on
  0.0.0.0:8332 all along (`connections_in 0` means nobody dialled in, not
  "not listening"), and there was no clash. Cost: three restarts of a
  production node; every one clean. `DEPLOYMENT_HISTORY.md` 2026-09-05
  (later) and (night).
- [x] **Restart on the new build — deploy 2026-09-05a** (PR #7, 20:46): the
  audit-closure build; NET-10 migrated the live address book to v3 (46,274
  entries) on first boot; clean stop, back at the tip within a minute, zero
  FATAL/SEGV/HALTED lines.

## Session 1 (afternoon → evening): audits closed by ID

- [x] **Every audit finding closed, accepted or deferred with a reason**
  (PR #6, 20:37; `releases/2026-09-05-audits-closed.md`; tag
  `audit-2026-09-03-all-closed`). Superseded audits marked as such; the
  record is by finding ID, not narrative — a narrative had dropped STO-10.
- [x] **NET-10: the address book resists a flood** (PR #5, 20:35;
  `releases/2026-09-05-net10-addrbook.md`). No CRITICAL/HIGH/MEDIUM open.
- [x] **IR-6: OP_ROLL rotates handles, not records** (PR #4, 20:13). The
  interpreter review has nothing open; tags `interp-review-2026-09-05`,
  `-complete`.
- [x] **IR-5 sighash memo; MuHash re-verified and scripted; fresh clone
  passes link-check** (PR #3, 19:26; `releases/2026-09-05-interp-review.md`).
  IR-5's first perf control never failed (8-way parallel hid the recompute);
  re-pinned to one core, N sized for a 5x margin.
- [x] **Benchmark defect fixes CSI-1, TXOQ-1, SC1; CSI-2 documented as a
  limitation with a seam** (PR #2, 15:23; `BENCH_DEFECT_LEDGER_2026-09-04.md`).

## Lessons recorded today (memory + LOG)
- Read `ss -ltnp` and `config/bitcoin.conf` BEFORE changing a production
  node; both wrong claims were avoidable.
- A test is untrusted until the fix is reverted and it is watched to FAIL.
- Track audit closure by ID; re-derive "what is open" from the finding list.
