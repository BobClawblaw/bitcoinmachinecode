# 2026-09-05

## 05:45-07:25 UTC — catch-up COMPLETE (tip current, 0 invalid, mempool admitting); first-ever full UTXO parity check: txouts/total_amount/bogosize Core-EXACT, muhash DIFFERS (open); daemon's running tally drifted ~208k (open)

After the manifest-cap fix (below), catch-up ran 964001 → tip unbroken at
~0.8 blk/s with script evaluation live above assumevalid. Two port bugs
surfaced and were fixed on the way:

- **964092 tx 840 (5,226 inputs): false `bad-txns-nonBIP68-final`.** The BIP68
  pass refused ANY tx past VAL_SEQ_CAP (2048) inputs ("past the sequence
  window"); Core computes SequenceLocks over EVERY input, and the tx is in
  Core's chain. Fixed by streaming the sequences (val_seq_walk_init/next —
  input-section re-walk, no buffer, no cap) instead of refusing: **d72271fa**,
  test_val_read_tx.c extended with a synthetic 3,000-input case (39 checks /
  0 failures), ARM daemon rebuilt via build_daemon.sh (backup
  daemon_out/rollback/bitcoind.pre-bip68-oversize-20260905), deployed; the
  daemon applied 964092 on the next boot and resumed. A SIGSEGV during the
  restart churn was lock-race collateral, not the new binary (clean stop →
  start has been stable since).
- **(this one is a CHECK bug, not fixed yet) daemon's running UTXO tally
  drifted ~208k high** (boot prints live=165596215 at applied 965578 where
  the true count is 165,388,368+). It survives restart (the checkpoint
  trailer persists it) and trips gettxoutsetinfo's consistency guard:
  `inconsistent read (walk=165388368 lsm=165596215 ...) -- result discarded`.
  The WALK is right (see the parity below); the running counter is wrong —
  plausibly accumulated during the failed-apply/retry era. The offline
  utxo_setinfo's two INDEPENDENT derivations agree (`consistent: true`), so
  only the daemon's incremental counter needs re-anchoring.

**Catch-up completed 06:19 UTC**: `tip=965576` heartbeat clean (no DEGRADED),
then a normal post-catchup 13-run background compaction (2 runs now: the
13.3 GB base + 628 MB recent). Mempool admission recovered (+80/s, 0
invalid). applied_height persisted 965578 through a restart; tip tracks
965581 current. DEPLOYED build = the d72271fa binary (arm-11 equivalent —
not tagged, see below).

**First full UTXO parity check ever completed on this port** (all prior
attempts failed: the Umbrel oracle lacks coinstatsindex → rc -8 at-height
queries; two muhash parity runs died rc=124/139 on Aug 29). Offline
`utxo_setinfo data/main --muhash` (built for ARM, daemon stopped, quiesced
read) vs the Umbrel oracle `gettxoutsetinfo muhash`, BOTH at height 965578:

    ours: txouts=165388368  amount_sat=2007970425835732  bogosize=12955676140
          muhash=82622e2f2b1777d0eac435a9025d65559d8e0a75bd1ae3040aec754d69df0708
    core: txouts=165388368  amount=20079704.25835732     bogosize=12955676140
          muhash=774e43735df4f48e4dcde9d71ea9d955753b901ee46f2ad1f0870d075926a107

txouts, total_amount and bogosize are Core-EXACT. muhash DIFFERS. Verified
our per-entry image against Core v31's kernel/coinstats.cpp TxOutSer
(hash-then-n, fixed u32 (h<<1)|coinbase, i64 value, CompactSize+script) —
port/arm64/bitcoin_utxo_stats.S builds the identical byte string, and
muhash_insert itself is pinned by vectors generated from Core's own source.
So the delta is REAL SET CONTENT, invisible to count/sum/bogosize: almost
certainly a height or coinbase-byte difference on entries unspent since the
rebuild (every spent coin's script/value/height was verified during the
clean 1,576-block catch-up through full sighash/amount/BIP68 checks).

NEXT SESSION (priority order):
1. [CLOSED SAME SESSION 10:57 UTC — see the next section] Localize the
   muhash delta: found (BIP30 duplicate coinbases), fixed, repaired, parity
   GREEN.
2. Re-anchor the daemon's running tally (find where the drift enters:
   rollback/retry bookkeeping is the suspect; a flush that recomputes the
   count from the runs would re-anchor it) so gettxoutsetinfo answers
   in-RPC again.
3. Tag/deploy the running binary as arm-11 in the sweep (it was built
   ad-hoc via build_daemon.sh, not through parity_sweep.sh).

## 06:00-10:57 UTC — muhash delta FOUND and FIXED: build_utxo no-op'd on
# duplicate-outpoint puts (BIP30 coinbases kept the FIRST appearance's
# height); two coins repaired in place; FULL MUHASH PARITY GREEN
#
# ours  = core = 9b3acac657ce86a87736da7ee9b601e26c08e828660da103527232b2cbb33fd7
# at height 965598 (txouts 165,361,670, 20079766.75835718 BTC — all equal)

The localization plan wrote itself once count/total_amount/bogosize were
known Core-exact: the delta had to live in fields those cannot see —
height, coinbase byte, same-length script bytes, or outpoint keys — and
the sharpest probe target was the BIP30 duplicate-coinbase boundary,
where the builder's own tally said put_dup=2.

Ground truth from the oracle: the two duplicated txids each appear twice
(d5d27987… at 91812+91842, e3bf3d07… at 91722+91880), and Core's
chainstate holds the SECOND copy of each (heights 91842/91880 — a later
duplicate coinbase REPLACES the earlier coin, same value/script, new
nHeight). Our store held the FIRST copy of each (91812/91722). Exactly
two coins, height byte only — count/sum/bogosize untouched, which is why
only muhash could see it. put_dup=2 is precisely these two dropped puts.

Mechanism: utxo_store_put ALWAYS appends the WAL record (with height and
coinbase) and then calls the memtable utxo_put, which returns 0 on an
existing key WITHOUT updating; build_utxo's on_output treated r==0 as a
benign duplicate and moved on. The second put died in the memtable and
its WAL record was truncated at the next checkpoint.

Fix (a69e166a): on r==0, tombstone the old generation and insert fresh
(del+put — the exact sequence fuzz_lsm pins against the Python model), in
BOTH the serial on_output path and the -j pipeline's arena applier. The
-j edit initially read the script pointer after p += slen — caught in
self-review and rewritten with the pointer captured before advancing.
Proven on the bug itself: rebuilding 90000..92000 with the fixed binary
(serial AND -j 4) yields heights 91842/91880, matching Core's
confirmations.

Live-store repair (asm/daemon/repair_bip30_heights.c, new): with the
daemon stopped it reloads the store (2^24 sizing, no forced flush),
ASSERTS the stale state (get must show 91812/91722 and the 50 BTC
coinbase — refuses otherwise), tombstones each stale coin and re-puts it
with Core's height and the exact 67-byte P2PK script, verifies in-memory,
and leaves four WAL records for the daemon's normal replay. No third
rebuild of a 165M-entry store; the daemon picked the coins up on its next
boot and probes confirm 91842/91880 through BOTH read paths.

Closing proof — offline utxo_setinfo --muhash vs the Umbrel oracle, both
landing at height 965598:

    ours: muhash 9b3acac657ce86a87736da7ee9b601e26c08e828660da103527232b2cbb33fd7
          txouts 165361670  amount_sat 2007976675835718  consistent=true quiesced=true
    core: muhash 9b3acac657ce86a87736da7ee9b601e26c08e828660da103527232b2cbb33fd7
          txouts 165361670  amount 20079766.75835718

IDENTICAL. The rebuilt store is entry-for-entry Core's chainstate: count,
value, bogosize and the full MuHash3072 multiset digest. This is the
first time the port has ever completed a full-set parity check against a
real synced Core on mainnet.

Still open: the daemon's running tally (item 2 above) — the heartbeat
carries it (txouts=165574865 at tip 965599 where the true count differs
by the ~208k drift) and it still trips gettxoutsetinfo's in-RPC guard;
the offline tool is self-consistent and correct. Item 3 (arm-11 tag)
unmoved. Node restarted on the repaired store: tip current, mempool
admitting, 0 invalid.

Backups this session: /tmp/utxo_manifest_424.bak (pre-migrate manifest),
/compacted-store provenance = the v2 rebuild scratch (data/rebuild-scratch,
kept) + rollback-store-20260904 (the original damaged store).

## 02:30-05:30 UTC — DEGRADED root cause FOUND and FIXED: the daemon's 256-entry manifest cap silently zeroes the run table on any build_utxo-scale store; flush_wal_tail (2^24) drained the builder's WAL tail, build_migrate_compact shrank 424 runs -> 1; catch-up is RUNNING past 964001

Picked up mid-operation from the overnight session: it had swapped the v2
rebuilt store (build_utxo 0..964000, applied_height=964000) into data/main,
watched catch-up fail at **h=964001** (was 965496 on v1, 965018 on the
damaged store), then pivoted — stopped the daemon (21:07 CDT), fixed
`flush_wal_tail.c` (memtable 2^22 -> 2^24; a builder-scale WAL tail is
~11.26M records and 2^22's 3.1M fill threshold truncated the reload at -2),
rebuilt it and launched `flush_wal_tail data/main` at 02:38 UTC against the
705 MB builder WAL left by the swap. That run completed at 03:55 UTC:

    reload ok: manifest_n=423 replayed=11260225 total_live=165718352
    after forced flush: manifest_n=424 (was 423) log_len=0

`total_live` equals build_utxo's own final count exactly — the on-disk WAL
covered the whole unflushed window; the flush produced run 000423 (296 MB)
and an empty WAL. Committed as e89914bd.

**But the daemon STILL failed at 964001 after that** — with `live=165718351`
(off by ONE from the builder, not by the ~5.6M entries a truncated reload
would lose). So the coin-missing theory died and the lookup path came under
suspicion. Evidence chain:

- Built `utxo_probe_one` for ARM (source was x86-only; linked against
  bitcoin_utxo_lsm.o + utxo_lsm_mm.o + bitcoin_utxo_store.o + bitcoin_net.o;
  also added to port/arm64/.gitignore). Block 964001 tx1 via the daemon's
  own RPC spends `6b7fe28502c7d357...:1`; probe against data/main:
  `[get] FOUND value=304269214 height=963981` — and the Umbrel oracle
  (getrawtransaction through 192.168.5.69) confirms creation height 963981
  and value 3.04269214 BTC. **The store was right; the daemon could not see it.**
- The daemon's boot journal had been printing
  `init: orphan sweep skipped -- manifest file and memory disagree` and
  `manifest_n=0` with a 424-run manifest. Root cause in
  bitcoin_utxo_lsm.S `.rl_manifest_haveN`: the reload compares the manifest's
  entry count against the CALLER's manifest_cap and branches to
  `.rl_manifest_bad` when count > cap — which zeroes manifest_n,
  next_gen and next_run_no and returns success. The live daemon's cap is
  UTXO_LIVE_MANIFEST_CAP=256 (the "manifest 0/256" in the recovery message);
  the probe's own cap is 4096, which is why the probe loaded 424 runs and
  the daemon loaded ZERO. With zero runs registered every
  utxo_lsm_get misses — "input references a missing/already-spent UTXO" at
  the first spend of the first block. The UTX-2 comment's oversized-manifest
  remedy (the pre-catchup compaction loop) can never fire because it needs
  manifest_n >= 2 in memory; the >cap case dead-ends. This also explains the
  v1 boot's `manifest_n=0` print.

**Fix path (no daemon change needed):** `build_migrate_compact` (added as
d8e17559, built overnight for exactly this) compacts a finished build_utxo
replay down to a small manifest offline. Ran it on data/main (daemon
stopped; manifest backed up to /tmp):

    reload ok: manifest_n=424 replayed_wal_ops=0 (0.1s)
    pass 1: 424 -> 361 (183.2s) ... pass 7: 46 -> 1 (172.5s)
    DONE: manifest_n=1 total_elapsed=1480.5s

24.7 min, and as a bonus the merge collapsed ~200 GB of run history into a
single 13.3 GB live-entry run (dead/shadowed entries dropped). Post-migration
probe: `[get] FOUND` AND `[walk] FOUND` (height 963981, same value/spk) —
both read paths agree on the compacted store.

**Daemon rebooted 05:13 UTC:**

    [utxo_live] init ... slots=2^16 reload applied_height=964000 manifest_n=1 live=165718352
    [utxo_live] catchup progress: height=964001/965567 (99.8%)

**964001 applied** — the block that killed three store generations — and
catch-up is running ~0.6 blk/s with script evaluation resumed above
assumevalid, ETA ~47 min for the remaining ~1550 blocks. After it lands:
confirm DEGRADED clears, watch mempool admission recover, and compare
gettxoutsetinfo muhash against the Umbrel oracle at a matched tip as the
final parity proof of the rebuilt store.

Standing hazards worth a future session:
- The `.rl_manifest_bad` silent-zero is a landmine for every future
  build_utxo -> daemon handoff. Options: make reload fail loudly (UTX-2
  already treats r<0 as fatal), or size utxo_live's manifest from the file,
  or document + assert in the swap scripts (swap_rebuilt_store_v2.sh should
  refuse a manifest with >256 runs).
- The morning utxo_probe_one "NOT FOUND" on the live store was the same
  bug in miniature (its walk failed on the oversized WAL) — probe results
  are only trustworthy when the reload line reports manifest_n matching the
  manifest file.

Node restarted on the repaired store: tip current, mempool admitting, 0 invalid.
