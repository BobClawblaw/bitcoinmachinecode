# DAILY WORKLOG — 2026-09-16

Continues worklog/2026-09-10.md after a five-day gap (backfilled the same day;
see 2026-09-11 … 2026-09-15). Production on `deploy-20260916h`. Run 24 launched
on the bench at 14:42Z and running.

The argument-error vocabulary work (#216–#219) landed on **09-15** by the commit
record and is written up there, not here; this file starts at the first 09-16
commit, 01:07Z.

## Session: mempool.space's three gaps (→ 16:00Z)

The operator named three limitations of Core that mempool.space works around:
address indexing, API pagination caps, and the ephemeral mempool. Taken as a
work item, with BlockYard's database work drawn on where it applied.

- [x] **A departure journal** (#220): why a transaction left the pool, as a
  bounded ring of 152-byte records with sequence stamps at both ends for
  torn-record detection, `MAP_SHARED` across forks. `bmcgetmempooljournal`,
  `/tx/:txid/departure`, `/mempool/departures`. `docs/MEMPOOL_JOURNAL.md`.
- [x] **feerate in sat/kvB** (#221) — sat/vB integer truncation read **ZERO for
  188 of 200 real rows**. Caught by looking at the data, not the code.
- [x] **first_seen persistence** (#223) — a restart was re-aging the whole pool;
  arrival times now restored from mempool.dat, with vetting.
- [x] **`/fee-estimates`** (#224) and a refusal histogram on the mempool load.
- [x] **The arrival table had no lock** (#225, #226) — written by several
  processes. Open-addressing with atomic CAS slot claiming and
  insert-then-verify dedup. The duplicate race was NOT "a narrow window" as I
  first said: it produced **889 of 3,000**. The first concurrency test caught it
  once and then nothing across five runs *including with the fix removed* — a
  shared start barrier made it reliable.
- [x] **The histogram did not sum** (#227) — `16 refused` against a list of 23;
  it counted attempts, not entries, and two sites were uncounted.
- [x] **EVICTED/EXPIRED had never run outside the store** (#228); **MINED vs
  CONFLICTED driven by a real block** (#230); **wtxid recorded** (#229) — nearly
  filled it with the txid, which is right only for non-witness transactions.

## Session: the register, and three instruments that were wrong (→ 17:30Z)

Three of the day's defects were in **instruments**, not the node:

- [x] #231 row 1 remeasured: 40% → **95.8%**. The original figure was the
  instrument's.
- [x] #232 the compact-block log's height was a **guess** (`tip + 1`) and had
  made my own earlier analysis wrong. Re-keyed on tx count.
- [x] #234 every block hash printed `0000000000000000` — 78 identical lines. An
  8-byte display prefix of a mainnet hash is a constant. `log_hash_short` and
  `hash_short` widened from `out[17]` to `out[65]` in lockstep.
- [x] #233 `getaddressinfo` returned SUCCESS for an address it could not decode.
- [x] #222, #235 the register contradicted itself: row 3 was disproved on 09-12
  and still listed open; row 2 was never an open divergence (a category error).

## Session: BlockYard parity (→ 18:00Z)

- [x] #236 `bmcgetcapabilities` — a consumer could not tell this node from Core.
  Both sides: bmc advertises, BlockYard's `setup.js` detects and `check.js`
  probes. Pushed to the BlockYard project following its conventions.
- [x] Disk: deleted `bench-archive-20260916` (49 GB orphan), `sweep` (118 GB),
  `bmc-bench` (31 GB, run 23 — its report retained) and `core-bench` (832 GB) →
  **1.7 T free**. The Core oracle at `/storage/core-oracle` was NOT touched and
  is still at 967,291; `core-bench` was the standalone benchmark copy. A future
  vs-Core benchmark now needs a fresh Core sync, which was stated before
  deleting.

## Session: run 24, and the getblock incident (14:42Z →)

- [x] **Run 24 launched** 14:42Z, build `0d5f4044`, `/mnt/2tbssd/bmc-run24`,
  ports 8464/8463/3006. `addrindex=1` **before** the sync — the only way to get
  it; the node refuses to enable it later because undo below tip−200 is pruned.
  Binary pinned into the run directory so a later rebuild cannot disturb it.
  NOT a time comparison against run 23: four indexes where run 23 had one.
- [x] BlockYard's capability handshake works end to end against it; it skips its
  own 30-minute / 124 GB address index build because the node serves one. The
  three objections recorded in BlockYard's `server/config.js` against monitoring
  a bench node were **re-measured**: 8 concurrent calls at 1–2 ms each, 4 ms
  wall, against recorded figures of 18–32 s with 90 s timeouts.
- [x] `blockfilterindex` and the address history self-repair post-IBD via
  `ir_tick` (the row-2 design); `txindex` needs the one offline build the boot
  log names. Neither needs a resync — checked before concluding the launch was
  fine.
- [x] **INCIDENT: `getblock` refused every block above height 10,880 for the
  whole of IBD**, at every verbosity. Root cause: `idx_put`/`idx_get` held their
  linear-probe budget in **r8** across `call memcmp_exact`, which writes `r8b`.
  Full writeup in `docs/devlog/INCIDENT_2026-09-16_getblock_probe_budget.md`;
  fix in PR #237 (`c0b1bf56`). Three wrong turns on the way, all recorded —
  including a reproduction that passed with the fix reverted and so was never a
  valid pin. The deterministic pin is a full-table case in `test_idx`; every
  pre-existing case there sat below the load factor where the budget matters.
- [x] Gate `MAKE_EXIT=0`, no failures. Run 24 stopped cleanly at 393,520,
  relaunched on the fixed binary, **resumed — not re-synced** — and every height
  that previously failed now serves, including `getblock <tip> 3`.

## Open

- PR #237 is pushed and ready; the merge was refused by the tooling here, so it
  needs landing by hand.
- Run 24 continues. Row 4 de-duplication waits on hash-tagged data; row 5 peer
  selection is measurable only during this run.
- Two stale processes for the operator: pid 2143533 (regtest bitcoind, Sep 12),
  pid 2939901 (`tail -f`, Sep 11).
