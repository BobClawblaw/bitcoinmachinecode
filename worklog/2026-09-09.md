# DAILY WORKLOG — 2026-09-09

Bitcoin Machine Code — `bmc_osx` branch: fresh macOS/Apple Silicon port.
One file per day; newest top. Times local (CDT, UTC-05:00). Convention
inherited from x86: bullet per meaningful action, with evidence and outcome.

## Round 0 — branch setup and durable docs

- [x] Fresh clone of upstream `main` @1dac38ae into `~/bmc_osx/bitcoinmachinecode`
      (x86 tree left untouched; arm-port branch left on origin as reference).
- [x] Created branch `bmc_osx` at main's tip, pushed to origin
      (verified via `git ls-remote origin bmc_osx` -> 1dac38ae).
      `gh auth setup-git` run so https pushes use GH_TOKEN (BobClawblaw).
- [x] Declared the branch model: `main` = x86-64 development tree, merged
      into `bmc_osx` for new features only; `bmc_osx` never rebases.
- [x] Established the durable developer-memory convention on this branch,
      mirroring x86: `worklog/YYYY-MM-DD.md` (this file), long-form entries
      in `docs/devlog/LOG.md`, durable rules in `docs/ENGINEERING_RULES.md`,
      per-module status in `port/OSX_ROADMAP.md`.
- [x] Wrote `port/OSX_PORT.md` (branch & sync model) and
      `port/OSX_ROADMAP.md` (method + Darwin-vs-Linux syscall/ABI deltas).
- [x] Measured the syscall-rework weight per module from the arm-port .S
      files: utxo_lsm 66 svc, store 42, utxo_store 30, idxscan 19, undo 18,
      store_fast 17 — these are the heavy-lift modules for Darwin.
- [x] Verified local toolchain: Apple clang 21.0.0 on macOS 26.6.2 (M1 Max);
      arm64 native. First build loop target: a pure-compute module.

## Round 0 addendum (later same day)

- [x] Commit-identity hard rule recorded: gitignored
      `docs/PROJECT_LOCAL_RULES.md` (private, names the identity, never
      pushed) + public no-identity-details version in
      `port/OSX_ROADMAP.md`. Identity config set repo-local AND global
      (BobClawblaw / users.noreply.github.com). Pre-push gate: check
      author+committer of every new commit; any mismatch is amend +
      force-with-lease. Verified `git check-ignore` matches and the
      gitignore commit itself shows the correct author/committer.

## Round 0 addendum 2 — strategy locked

- [x] Port strategy written and merged as PR #130
      (https://github.com/BobClawblaw/bitcoinmachinecode/pull/130):
      port-don't-emulate; 5 phases; PR branches osx/pN-* -> bmc_osx.
      Measured: 238 raw syscalls across 15 asm files (utxo_lsm 65, store 51,
      utxo_store 31 top three); C layer POSIX-clean except prctl in
      coinstats_index.c + log_ts.h; no futex; no epoll/eventfd/io_uring.
- [x] OSX_STATE.md updated with the strategy snapshot.


## Round 0 addendum 3 — p0: sha256 ported and gated natively

- [x] Read asm/sha256.asm in full (827 lines): 7 public symbols, tri-state
      SHA-NI dispatcher (CRY-6 contract), scalar + SHA-NI bodies.
- [x] Wrote port/osx/sha256.S (Mach-O AArch64). Darwin deltas applied:
      _-prefixed symbols, adrp+@PAGEOFF, no .type/.size/.note.GNU-stack,
      x18/x28 forbidden (arm-port used w27/w28 temps -- moved to x9-x15),
      sp 16-byte aligned at every bl site (arm-port's lone x30 push was
      8-mod-16 at a call -- guardmalloc would kill it on macOS), probe via
      sysctlbyname hw.optional.arm.FEAT_SHA256 (ID_AA64ISAR0_EL1 MRS traps
      at EL0 on macOS), FIPS K-tables verbatim.
- [x] Assembles clean under Apple clang 21 (-arch arm64 -march=armv8.2-a+sha2).
- [x] Upstream test_cry6_sha_paths.c linked UNCHANGED against the Mach-O
      object: 7/7 ok (the "does NOT have SHA" label in its header line is a
      probe-order artifact -- force(0) re-probe semantics; dispatcher itself
      proven correct).
- [x] New native gate port/osx/tests/test_sha256_osx.c: 16 checks, 0
      failures -- KATs, padding-edge lengths (55/56/57/63/64/119/120),
      2x2000 random-length differential fuzz vs hashlib through BOTH bodies,
      5000 random state/block pairs scalar-vs-accelerator bit-identical,
      probe == sysctl, dispatcher re-probe contract. 0.6s wall.
- [x] Two harness bugs caught on the way (deadlocking popen stdin oracle ->
      file-based batched helper_sha256.py; python -c one-line compound while
      is a syntax error) + one test-contract fix (force(0) means re-probe on
      NEXT block call -- must drive a block before reading the tri-state).
      All recorded in OSX_ROADMAP.md pitfalls.
- [x] OSX_STATE/OSX_ROADMAP/worklog updated; sha256 marked DONE with evidence.


## Round 0 addendum 4 — p1 wave 1: sha512 + ripemd160

- [x] sha512.S: Mach-O port of asm/sha512.asm (CRY-4 stack W schedule,
      save-area semantics). Upstream test_sha512 unchanged: 4/4.
- [x] ripemd160: TWO asm attempts failed (1:1 unrolled translation;
      generated loop-form). Debug saga: frame-base ordering bug (mov x20,sp
      before sub sp -> stores into save area -> main-frame corruption,
      crash at exit), callee-saved table bases clobbering parked pointers,
      w19 temp zeroing parked x19. Ship-as-C-twin decision per rule #2:
      ripemd160_twin.c at clang -O2, same symbol. Gates: 23/23 +
      thread-stress 2.08M/0 mismatches. Pitfalls recorded in OSX_ROADMAP.
- [x] Committed directly to bmc_osx (f43fd0ee) + docs updated.


## Round 0 addendum 5 — sha1 as C twin (10/10)

- [x] sha1: two asm attempts diverged; C twin per the ripemd160 precedent.
      The twin's own first cut stored the 64-bit length field LE -- the
      gate caught it immediately (empty passes, content fails); BE fix,
      10/10 green. Committed 3ee6c5a5. Roadmap/state updated.


## Round 0 addendum 6 — bech32 (C twin, 53/53) + base32 vendored

- [x] bech32_twin.c: full codec incl. both audited security fixes
      (SER-1/WAL-1 90-char cap, SER-5/WAL-9 mixed-case refusal).
      Upstream test_bech32 unchanged: 53 PASS / 0 failures.
- [x] base32: upstream asm/base32.c is arch-neutral C -- vendored to
      port/osx/ and gated with upstream test_base32 (ALL PASS).
- [x] Committed to bmc_osx.


## Round 0 addendum 7 — secp256k1_fe C twin (40/40) + a debugging war story

- [x] fe_twin.c: add/sub/mul/sqr/inv. The x86 ADcx/ADox/MULX carry schedule
      has no AArch64 equivalent; reimplemented the algorithm (schoolbook
      4x4, fold by 2^256==C, one conditional subtract) as constant-time C.
- [x] Gates: upstream test_fe 40/40 (vectors + sqr==mul + a*inv(a)==1 per
      vector), test_fe_sqrt via crypto_fe_sqrt.c ALL PASS.
- [x] The gate caught two real bugs in my first reduce512 (both invisible
      to per-round static checks, found by differential testing against
      python big-int on random 512-bit inputs):
      (1) acc[5] weight typo: `<< 32` instead of `<< 64`;
      (2) the oh*C fold added lo at v[0]/hi at v[1] — correct weights are
      v[1]/v[2] (oh carries weight 2^64).
- [x] Also found and fixed the REAL inv bug: the exponent loop iterated
      bits LSB-first with the MSB-first square-and-multiply recurrence
      (result^2 * a^bit), computing a^(bit-reversed p-2). Fixed to iterate
      MSB-first. Symptom: inv correct for special inputs (1, p-1) but
      garbage for random ones.
- [~] test_fe_repr/test_fe_inline require the x86-only fe_ref/_inl
      differential objects; superseded by test_fe's vectors + the python
      big-int differential (5000-case) used during bring-up.

- bmc_osx session: merged origin/main (75a1f21e) into bmc_osx; bitcoin_hash ported
  natively (b006bba1) — test_pow_check 13/13 + 6,485-vector Python-oracle 0-fail +
  byte-identical vs x86 objects on the 9950X3D (192.168.5.242). Driver pair
  dvh.c/gen_bhash_vecs.py committed. x86 IBD on .242 confirmed COMPLETE (tip
  961639, progress 1.0, 760G). x86 bench refs captured: sha256 2.40GB/s, fe_mul
  5.54ns thru, ECDSA 47.1k/s/core, Schnorr 38.4k/s/core. Next: p1 wave delegation
  + osx bench_hash_core for the first cross-arch numbers.

## 2026-09-09 — osx wave backfill: point→ecdsa→chainwork→utxo→cons→headers (8b341e41..5b7f679f)

- Ten modules landed between the fe twin and net_twin without worklog
  entries; this section is the durable record (details also in
  OSX_ROADMAP.md / OSX_STATE.md).
- point_twin.c + point_ct_twin.c + g_comb_table_data.c: the secp256k1
  Jacobian point layer (windowed/comb/GLV scalar muls) and the constant-
  time layer (RCB Alg 7/9 adds, 256-round cmov ladder). Gates: test_point
  2/2 + test_point_inf native; 1200 + 1740-record cross-arch differentials
  byte-identical vs .242 (dpt.c + gen_dpt_vecs.py).
- ecdsa_twin.c + pubkey_schnorr_twin.c: ECDSA verify + pubkey parse +
  BIP340 schnorr_verify native, BIP340 csv fixture byte-identical; first
  cross-arch verify benches (BENCHMARKS_OSX.md: M1 Max 140us/273us vs x86
  21.5us/26.1us -- C twins vs hand-scheduled asm). Fixed sc_inv_var
  `ldp x25,x25` SIGILL in secp256k1_scalar.S.
- chainwork_twin.c + muhash_twin.c + utxo_stats_twin.c: chainwork.dat
  layer, num3072/MuHash3072, UTXO stats with x86-identical struct offsets.
  Gates: test_chainwork 0 failures, test_muhash green on Core-oracle
  vectors. asm/tests/test_tmpdir.h added so upstream harnesses run on
  Darwin (mkdtemp vs fixed tmp paths).
- utxo_twin.c + store_fast_twin.c (+ store_twin.c's first cut): in-memory
  UTXO set, fd-cache/mmap read layer. Gates: test_utxo 0 failures,
  test_store 0 failures, bench_store_read byte-exact.
- cons_twin.c + headers_twin.c: block consensus verifier and header-chain
  store. Gates: test_cons and test_headers ALL GREEN native (re-run green
  in the 2026-09-09 evening session to confirm the committed tree).
- With this wave: every module the IBD path and the block/UTXO storage
  stack needs is Darwin-covered except bitcoin_idxscan, bitcoin_undo and
  the script/sig family.

## 2026-09-09 — osx keys/addr/bip32/sighash: wallet derivation wave

- bitcoin_keys.S native (scalar_small_nonzero + scalar_to_pubkey CT-ladder
  path): test_keys 6/6 native; 647-record cross-arch differential
  byte-identical vs .242 -- the small_nonzero compare lattice walked
  first-differing-byte by position, plus n-1/n/n+1 and 600 random and
  curve-edge to_pubkey scalars (k=0/n/n+1 run both sides identically; the
  x86's k==n returns-1 quirk transcribed verbatim and noted in-file).
  Port bug caught by the gate: first N_BE draft was a placeholder table
  (caught before it could lie); limb counter w22 needed an explicit init.
- bitcoin_addr.S native (hash160 + base58check_encode): test_addr 5/5
  native after the gate caught the first cut parking the out cursor in
  x11 -- the digit-emit `ldrb w11` zeroed the parked x (the recurring
  w-temp-destroys-parked-x pitfall); cursor moved to callee-saved x22.
  56-record cross-arch differential byte-identical: hash160 over lengths
  0..1000 + the base58check paylen edges incl. the >78 and
  unsigned-negative refusal shapes (driver out window raised to the
  callers' real 128-byte contract; the 64-byte window tripped the stack
  canary on a legitimate 83-byte xprv string -- driver bug, not module).
- bitcoin_bip32.S native (master/ckd_priv/derive_path/fingerprint/
  extkey_serialize): all four upstream harnesses green native --
  test_bip32_chain's full vector-1 chain, test_bip32_extkey's BIP44/BIP84
  receive paths, test_bip32_ckdpub's 12 checks through the wallet_core
  link. Port bugs the gates caught: (1) the 33-byte pub temp sat above
  ckd_priv's 0xc0 local reservation and overran the save area (segfault;
  the x86 module's own documented FINDING class); (2) the (IL+kpar) mod n
  add/sub loops skipped byte 0 (post-decrement cbnz bound) -- the carry
  INTO byte 0 stayed in w9 and acted as a phantom 257th bit, triggering a
  spurious subtract (diagnosed against a Python oracle: the bad output was
  exactly sum + (2^256 - n)); (3) extkey_serialize's key/keylen arrive in
  x6/x7 under AAPCS64, not on the stack like x86 SysV's spilled args 7/8.
  990-record cross-arch differential byte-identical (dbip32.c +
  gen_dbip32_vecs.py).
- THE BIP32 DIFFERENTIAL DRIVER FOUND A REAL LATENT PORT BUG:
  bitcoin_hmac.S used x25 (tmp buffer base) without saving it -- x25 is
  callee-saved and the driver's main parks the GOT anchor there across
  bip32_master -> hmac_sha512; the next ldr [x25] segfaulted. Every prior
  caller (bip39, keys tests, harnesses) happened not to park x25 across
  the call. hmac now saves/restores x25,x26; test_hmac re-verified green.
- test_bip32_master.c itself had UB: sscanf("%2x", (unsigned*)&kg[i])
  writes 4 bytes into a 1-byte slot, spilling 3 into adjacent frame vars;
  under clang's arm64 layout they landed on kg[0..2] and the caller's
  c[0..2] AFTER the fill, so the harness reported correct outputs as
  failures. Fixed to the unsigned-temp pattern test_bip32_chain.c already
  used; green on both arches after the fix (.242 re-run).
- sighash_twin.c (bitcoin_sighash as C twin; __thread[20000] scfbuf for
  the x86 .tbss TLS): test_sighash green, test_legacy_sighash 500/500 Core
  sighash.json vectors, test_find_and_delete 23/23, test_sighash_oob
  rejection green -- all native, first build. 143 KB cross-arch
  differential byte-identical (dsighash.c + gen_dsighash_vecs.py: random
  txs x both builders x all hashtype classes, per-stage truncations,
  SINGLE out-of-range quirk, ACP shapes, PUSHDATA1/2/4 incl. truncated
  headers, all push length classes, find_and_delete needle shapes).
  Driver authoring bug caught before the diff: rd32() returning unsigned
  char truncated 1000 -> 232 and desynced the stream; op-4 layout also
  had to match the generator's interleaved len/data order.
- With this wave: seed -> master -> path -> xprv/xpub/address and the
  legacy sighash preimage builders are fully Darwin-covered. Next p1:
  bip143/bip341/bip342, taproot, then the script VM wave.

## 2026-09-09 — osx bip143 gate + bitcoin_script twin + schnorr debug cleanup

- bitcoin_bip143: NO PORT NEEDED -- bitcoin_segwit.c is arch-neutral
  production C (bitcoin_witness_v0.c / bitcoin_scriptverify.c call it; the
  asm is only the differential harness's perf twin).  Native gate
  test_bip143_osx.c: BIP143 published example digest, the real block-481824
  tx 562 sighash (32f2913c..) with the actual witness signature verified
  natively through ecdsa_twin under that sighash (a wrong sighash cannot
  verify -- this ties the port to the chain), and the swtx_parse contract.
  1,635-vector corpus dump byte-identical three ways: osx-C == x86-C ==
  x86-ASM (bitcoin_bip143.asm through a shim on .242).  ABI note recorded:
  the C takes the BARE scriptCode (writes the compactsize itself); the
  prefixed form is the Python oracle's convention.  Hand-transcribed
  fixture hex had two silent copy errors -- both regenerated
  programmatically from the JSON/BIP sources.
- bitcoin_script -> script_twin.c (C twin): tolerant der_parse_sig (Core
  parse_der_lax shape incl. long-form quirks), be_to_limbs, verify_p2pkh.
  test_script + test_p2pkh green native; 18.7 KB differential vs .242
  byte-identical; test_ir10_p2pkh_bounds deferred to the interp wave (needs
  bitcoin_interp/scriptcodec/sha1).  The differential caught a real twin
  bug invisible to the upstream short-form-only harnesses: der_long_len
  re-masked the first length BYTE as the count instead of the header's low
  7 bits -- long-form INTEGER parsing diverged only there.
- pubkey_schnorr_twin.c had committed bring-up debug inside schnorr_verify
  (two fe_inv + fprintf per call).  Removed; BIP340 19/19 re-verified;
  schnorr bench corrected 273.11 -> 79.92 us/verify (3.4x faster; ratio to
  x86 10.5x -> 3.1x; BENCHMARKS_OSX.md updated).  The first removal pass
  dropped the real point_scalar_mul_fixed(SG,sL) call along with the debug
  block -- caught because bench_schnorr refuses to time a fixture that does
  not verify.  Lesson: debug removal needs the same gate discipline as the
  port itself.
- secp256k1_taproot is the next port target (tagged_hash256/tweaked_pubkey
  unlock the bip341 corpus gate).

## 2026-09-09 — osx net_twin: bitcoin_net C twin landed (45fe22bb)

- port/osx/net_twin.c: all 12 public symbols (net_magic, g_v2_active,
  g_p2p_write_hook, g_v2_hook_write/read, fd_write_all, fd_read_full,
  fd_close, tcp_connect_ip, p2p_frame, p2p_write, p2p_read).
- Gates: upstream test_net 19/19 native; test_p2p_msgsize 14/14 native
  (0xFFFFFFFF -> -3 instantly, nothing drained; boundary 4,000,000 accepted;
  NET-11 checksum incl. empty payload); v2 hook smoke (read hook 5th arg is
  the plen_out POINTER; pacer fires on v1 AND v2 writes before dispatch);
  live tcp_connect_ip: .242:8332 connects, refused port returns -61
  (Darwin ECONNREFUSED, x86 keeps raw -errno too).
- 240-frame p2p_frame cross-arch differential: byte-identical vs x86 objects
  on .242 (drivers port/osx/tests/dnet.c + gen_dnet_vecs.py; cmd lens
  0..16, boundary payload lens 55/56/63/64/65/127/128/129, two 4MB frames).
- x86 contract notes for the record: p2p_read returns 1/0/-1/-2/-3; bad
  magic -> 0 (same as EOF); oversize -> -3 BEFORE payload/drain; checksum
  verified ONLY when announced<=cap, empty payload included; drain 64B
  chunks, short drain stops silently; *plen_out = announced AFTER drain.
  tcp_connect_ip returns RAW negative errno (Darwin 61 != Linux 111).
  p2p_write: pacer hook FIRST (v1+v2), then header/payload in TWO writes.
- Found the tree with net_twin.c half-edited (duplicate drain block, header
  comment swallowing the declarations, 3 stale .o.tmp) — rewrote clean.
- Cleaned stale build droppings: port/osx/*.o.tmp, stray asm/tests/muvals.h.

## 2026-09-09 — osx p2p_twin: bitcoin_p2p C twin landed (57588bdb)

- port/osx/p2p_twin.c: 6 pure-compute functions (getheaders/getdata_block/
  ping/headers_count/inv_count/inv_get), protocol version pinned 70016.
- Gates: upstream test_p2p 18/18 (incl. pacer-hook block exercising
  net_twin's p2p_write), test_p2p_inv 12/12, 130-record differential
  byte-identical vs x86 bitcoin_p2p.o on .242 (dp2p.c + gen_dp2p_vecs.py).
- Driver authoring bugs caught before the diff: ping records carry the
  nonce in a/b (not a body length); fd-varint headers shapes need 81007B
  body buffers; generator rec() signature churn. All fixed in-driver, none
  in the twin.
- IBD wire path (net + p2p + headers) now fully Darwin-covered.

## 2026-09-09 — osx addrmgr_twin: bitcoin_addrmgr C twin landed (2725d14b)

- port/osx/addrmgr_twin.c: 5 book ops + amr_put_csize + p2p_addr_v1/v2 +
  p2p_addr_count, byte-faithful to Core's serializer (the 2026-08-28
  IPv4-mapped + CompactSize-count fixes are pinned by test_addrmgr).
- Gates: upstream test_addrmgr 28/28 native; 73-record differential vs x86
  on .242 comparing BOTH the retval/payload stream and the peers.dat bytes
  written by the run (damr.c + gen_damr_vecs.py) -- first module gated on
  the persisted file bytes too, since the book is the module's real output.
- addr_gather_from lives in daemon/addr_ingest.c (upstream C), not this
  module -- test_addr_ingest is a live-network test, out of scope here.

## 2026-09-09 — osx idx_twin: bitcoin_idx C twin landed (6fed3a71) + x86 full-table spin BUG

- port/osx/idx_twin.c: idx_init/put/get/count + idx_build_from_file
  (buffered pread, wire-order hashes, hole skip). Layout-compatible.
- Gates: upstream test_idx 12/12 native (pow-prefix clustering guard 0.84s);
  868-record differential vs x86 byte-identical (raw table dumps included).
- REAL X86 BUG found by the differential: first vector set built a 100%-full
  1024-slot table (800 puts + 224 build inserts); x86 idx_put then spins
  forever -- memcmp_exact clobbers r8b, idx_put holds the probe budget in r8
  across the call, budget turns to garbage, `dec r8; jz .full` never fires.
  Diagnosed on .242 via sudo gdb attach (budget register 1115 > start 1024)
  + linked disassembly (r8b writes in memcmp_exact.l). Production masks it:
  1M-slot table vs 962k records never fills. Vectors regenerated with
  headroom (700 puts); x86 asm fix = move budget to a stack local, goes to
  main via TODO.md. Twin follows the documented contract (ret 2 = full).

## 2026-09-09 — osx store_twin: bitcoin_store C twin landed (a253ef93)

- port/osx/store_twin.c: 19 public symbols covering the whole block store:
  init (prune.dat restore), reload, append (rollover + STO-11 fdatasync),
  get_at (prune gate + sparse-prune 0xFFFFFFFF marker), get_tip,
  get_file_fd, set/get_sync, append_shared/nolock (flock, self-healing
  TRUE-end position), get_tip_hash, validates_prevhash, layout_monotonic,
  truncate_to (monotonic-safety gate, boundary ftruncate, later-file unlink),
  truncate_index_only, set_prune/prune (boundary-file in-place compaction).
- Gates: test_store 42/42 + test_truncate 54/54 native; 69-record cross-arch
  differential: retval stream AND the entire resulting file set (index.dat,
  blk*.dat, prune.dat tarballed and diffed) byte-identical vs .242.
- Differential gotcha: the store opens files by bare relative name in CWD, so
  a harness run inherits ANY stale prune.dat/index.dat -- first x86 run hit a
  leftover prune gate (-3 on get_at(0)); scratch cwd + fresh state fixed it.
- Twin bug found by the gate: u32 data_size must be masked into the u64
  out_meta[1] slot (x86 `mov eax` zero-extends; my memcpy left stack garbage
  in the high dword). Caught by test_store's size checks, fixed, re-green.

## 2026-09-09 — osx utxo_store_twin: bitcoin_utxo_store C twin landed (55c76deb)

- port/osx/utxo_store_twin.c: init/init_ro/put/del/get/count/sync/reload/
  wal_drain/close + utxo_store_clear. 1MB shared WAL buffer keyed by fd
  (drains on sync/reload/close/owner-switch/overflow), log_len = logical
  length, checkpoint via utxo.idx.tmp + rename, reload = snapshot + tail
  replay with the UTX-4 torn-tail truncate. utxo_struct_size added to
  utxo_twin.c (missing symbol the harness needed).
- Gates: upstream test_utxo_store, test_utxo_wal_buffer (42503-record
  self-drain), test_utxo_torn_tail all green native; 124-record differential
  byte-identical vs x86 (retval stream + utxo.dat/utxo.idx files).
- Two twin bugs the gates caught: (1) torn-tail handling needs a `torn` flag
  + the failing record's START offset -- gating on `consumed` misses
  unknown-op records (prefix already consumed) and truncating on the clean
  path ate the last good record; (2) utxo_get writes 8-byte unsigned longs
  through its out pointers -- a 4-byte out smashes its neighbor.
- Driver lesson: gcc -O2 stack-local out pointers corrupted the value slot
  through the upstream x86 asm (statics work); noted in duxst.c.

## 2026-09-09 — osx utxo_lsm_twin: bitcoin_utxo_lsm C twin landed (b65c12e1)

- port/osx/utxo_lsm_twin.c (~1000 lines C): the full LSM -- mac_flush
  (memtable+tombstone descriptors -> radix-or-merge sorted MAGIC_RUN3 run
  with bloom + sparse index, buffered writer, manifest tmp+rename+dirsync),
  utxo_lsm_get (memtable -> generation tombstone hash -> runs newest-index-
  first with the vendored mmap fast path + format-aware fallback), reload
  (manifest v1/v2, WAL tail rescan for tombstones/op_count, accurate
  total_live w/ the incident-45 recount rule), mac_lsm_recount (k-way merge,
  UTX-1 tie-by-manifest-index), utxo_lsm_compact_range (partial merges,
  keep_dels for interior batches, sparse offset via the buffered writer's
  logical tell), walk (visitor bridge: memtable walker's int-returning cb
  bridged from the void LSM visitor).
- asm/utxo_lsm_mm.c vendored byte-identical, DEFAULT OFF on __APPLE__: the
  upstream mmap fast path fails 8-thread concurrent gets under clang-21/
  arm64 (single-threaded mm is byte-correct; the twin fallback passes
  everything, concurrently included). Root cause unpinned; documented in
  the file. x86 passes the same test with the same file.
- Gates: test_utxo_lsm, test_lsm_flush_sparse, test_lsm_mmap_diff,
  test_lsm_sparse_diff, test_lsm_bloomsat, test_lsm_lost_tombstones all
  green native; 229-record cross-arch differential byte-identical (retvals
  + utxo.dat + utxo.idx + utxo_manifest.dat + every utxo_run_*.dat).
  test_lsm_count_drift deferred to p3 (needs the full REORGOBJS set).
- Port bugs the gates caught: walk's void-visitor bridged through a
  trampoline (a direct cast fed garbage into the memtable walker's int
  abort check); lsm_slot_t.key widened to 37 (the type byte sits at key[36]
  in the x86 slot layout).

---

# DAILY WORKLOG — 2026-09-09 (x86-64 main line)

Bitcoin Machine Code (x86-64 NASM node). One file per day; newest top.
Times UTC. Every PR landed as a `--no-ff` merge after a green
`make -j8 -k test` gate (0 failures; the expected test_rpc_signer
segfault); one gate at a time. Sessions 37-41 were written into
`2026-09-08.md` as the day ran and moved here at 16:15Z.

## The day in one place (written 16:15Z)

**Consensus.** The bench rejected a valid mainnet block, 880,338, on a
2,048-entry BIP68 sequence ledger (#128, then a derived 24,576 cap).
The operator's rule -- "If core has no cap, we should not have one
either. Follow their behavior wherever possible for consensus" -- took
every cap out: the ledger and the single-transaction verifier's input
table grow to the transaction (#129, #131); the connect path was proven
capless by a 24,000-input block test. BIP30's unspendable coinbases are
the originals, 91,722 and 91,812, as in Core (#133): the first history
base the self-repair built disagreed with Core's MuHash from 91,722 on,
bisected with the row extractor. The third rebuild was adopted at the
seam at 06:10Z and reproduced the reviewer's 963,967 MuHash
(`1e3c77ad...c202c118`) from our own computation.

**Chain selection like Core's (#137).** The bench had synced a real
8-block stale branch at 961,632 from a stuck peer and sat four hours.
Now: a reorg above the staging limit rewinds to the proven fork point
and hands the replacement to the parallel downloader; the header
mirror is the best header chain; every other branch is retained in a
fork tree (`headers_forks.dat`); a keep-up leg that stores blocks off
the best chain is gated back. Operator: "Do what core does! Retain
both branch headers!"

**The UTXO defect behind run 18 (#139).** The handoff worked and the
engine then rejected mainnet's 963,311 for a coin the set held. A run
file carried the key twice, PUSH then DEL: `utxo_lsm_del` appended a
tombstone on every call, and the merge stepped one record past a
duplicate. One record per key per run now; runs written before are
repaired by their next compaction. Found with `bmc_utxo_probe_one`
(get vs walk) and a one-run manifest; the `live=` count is a counter
and proved nothing.

**The fresh benchmark, run 19.** Started three times: the
second-highest announced height refused every honest peer when two
peers claimed 970,195 (#140: the median); the picker's dead mark
outranked untried peers under a fresh sync's unknown bar and two
workers looped 200 times on peers that closed on them (#141). Third
start 13:19:44Z on `picker-dead-mark-2026-09-09`, clean; 46% at 2h23m.
The two old datadirs (107 GB + 998 GB) were archived to
`/mnt/10gbusb1/archive2` (byte totals and file counts verified) and
deleted from the SSD on the operator's instruction.

**Production.** `reindex-chainstate=1` from 08:52Z to 15:57Z on
snapshot f: undo covers the whole chain now, so `getblockstats` has fees
at every height and mempool.space can index a year (52,560). The block
filter backfill runs to the tip; the mempool.space stack is relaunched
after it. Snapshot g (#139) is staged; production stays on f until the
operator restarts it.

**Discussion points and their resolutions.**
- *"Why isn't the production bmc chain monotonic and how do we fix
  it?"* Blocks landed out of height order before the 09-08 in-order
  committer (first break 964,924), and index-only truncation leaves
  dead bytes. Informational: the index is the truth, reads and
  consensus are unaffected, only in-place truncate/prune refuse and
  both have fallbacks. `tests/tool_archive_relayout` rewrites in height
  order (hours for 630 GB, swap with the daemon stopped); a fresh sync
  is monotonic on its own. Left as is for now.
- *"Why isn't the production branch doing compactions often during the
  rebuild?"* Bulk sizing at boot (2^22 slots, 1 GB blob, compact_at
  48): a full 48-run merge every ~45k blocks in a background child;
  lookups were 7% of the block time. By design.
- *"Nothing else to fix before starting a new benchmark run?"* The
  header-chain selection fix and the run 18 UTXO inconsistency, both
  done before the run; the second turned out to be the LSM, not the
  reorg code.
- *The reviewer's questions.* The rebuilt base was the self-heal
  path's own work after the BIP30 fix landed (#133), not a manual
  rebuild; the seam check compares the set's counters and MuHash at
  the tail's first height from two independent computations. Commit
  history: unsquashed, 2,502 commits, 385 merges, local == GitHub,
  filtered three times for paths. Stranger handshake: hand-verified
  today on production and the bench, and the repo's inbound probe
  answers every message as Core does (the `mempool` request closes the
  connection without NODE_BLOOM, as in Core).
- *"You suck for not catching this bug."* Fair: the picker's
  fresh-sync branch had no test with a dead mark, and the
  second-highest rule was a top-k statistic where a majority-robust
  one was needed. Both are pinned by tests that failed first.

| PR | what | tag |
|---|---|---|
| #128 | BIP68 ledger sized from the weight limit (superseded) | `bip68-seq-cap-2026-09-09` |
| #129 | no sequence cap | `no-seq-cap-2026-09-09` |
| #131 | no input cap in the single-transaction verifier | `no-input-cap-2026-09-09` |
| #127 | addnode dials; the control channel waits a rotation | `addnode-dials-2026-09-09` |
| #133 | BIP30 originals | `bip30-originals-2026-09-09` |
| #135 | the builder keeps a pass's inputs | `builder-keeps-inputs-2026-09-09` |
| #137 | chain selection: handoff, fork tree, leg gate | `chain-selection-2026-09-09` |
| #139 | one record per key per run; the announced-height rule | `one-record-per-key-2026-09-09` |
| #140 | the announced height is the median | `announced-median-2026-09-09` |
| #141 | the picker's dead mark never clears a bar | `picker-dead-mark-2026-09-09` |

## Session 43 (16:05Z 09-09 →): "outbound legs die within ~1 minute. Fix the cause, not the symptom."

- [x] Attribution first: two boot epochs in the log (23:49:50 09-08 and
  08:50:57 09-09, deploy-20260909f); the "16:00:49 process" is f's
  coinstats fold worker, a fork -- no restart happened. Both epochs
  behave alike per hour (33 connects, 12 peer drops, 100-200 connect
  timeouts); the reindex hours had no legs at all.
- [x] Passive evidence, no touch on the node: `sudo tcpdump` on port 8333
  (the bench and the oracle share the port; filtered to the hosts
  production dialled, 16:30-16:55Z): 49 legs opened, 22 closed by US
  (50-364 s), 19 by the peer (12 within 2 s, six at exactly 61 s
  having sent only version+verack), 8 open; pong latency 0/0/75 s.
  A Core-shaped Python client got the same 1 s refusals from the same
  addresses: not our version bytes (fixed nonce/timestamp/height=0 all
  the same, noted for their own batch).
- [x] Our closes: `do_outbound_sync` closed a leg after three failing
  passes with NO log line, and the streak lived in the slot, so a fresh
  leg's first failure was its third strike; the 60 s budget kills named
  the leg "?"; nothing remembered a refused/timed-out/early-dropped
  address, so the same eight flappers were dialled every rotation.
  "Operation now in progress" = tcp_connect_ip's blocking connect
  hitting its 10 s SO_SNDTIMEO (the operator's brief read it as a
  non-blocking connect in flight; it is not).
- [x] The batch: close reasons at every site (ours/<reason> vs theirs +
  unread messages drained and logged), `daemon/dial_memory.c` (shared
  table, 10 min doubling to 6 h, permanent for no-witness, cleared by a
  10-minute leg) at the re-dial, top-up and helper picks, streak reset
  per peer, re-dial on the next rotation, the budget keeps the only
  live leg, a between-pass sweep answers pings within one pass.
  `test_dial_memory` (new), `test_dialhelper` EINPROGRESS text watched
  to fail. Acceptance needs the operator's restart (forbidden while
  the bench runs): snapshot h staged, numbers to be measured then.
- [x] Production reached the network tip 16:14Z (keep-up at 5-7 blocks a
  minute); coinstats seeded, history base adopted to 966,142; the block
  filter backfill 964,360 -> tip running (8 threads, ~20 s a block in
  the heavy region).

- [x] #143 landed `--no-ff` 17:4xZ, tag `leg-lifecycle-2026-09-09`;
  snapshot h staged, no restart (operator: finish the bench first; the
  before-window is already held in bmcmonitor's series). The first gate
  failed test_sync_budget: my sole-leg rule had skipped the shutdown,
  and a trickling peer would never return -- a sole leg gets 4x the
  budget instead.
- [x] The small batch: the version message truthful (wall-clock
  timestamp, getrandom nonce, -port in both address fields, the tip as
  start_height); test_bitcoind's five pinned wrong values watched to
  fail; asm dry-run on a scratch copy caught the AL re-zero.
- [x] Run 19 at 4h12m: 529,361, clean; Core's nearest mark 634,561 at
  6h55m, run 18's 7h27m.

- [x] Operator 18:32Z: `bmc.cmpctrecv=0` in production's conf and a
  restart -- done 18:38:24Z on snapshot i (the LOG START for the
  after-window). "Why is it not 0 to begin with?" -- the switch was added
  09-08 as an opt-out for the differentials instead of flipping the
  default when the counter read zero; wrong call that day.
- [x] The compact-block defect pinned on regtest in 20 minutes with the new
  close labels: `where=8` (cons_verify) on every pass; the assembled
  bytes identical to Core's and valid standalone; the receiver traced --
  `getblocktxn sent`, never a `blocktxn`: the sync drain's 5-byte "block"
  compare swallowed `blocktxn` as a block. Six bytes now, five sites;
  `validation/cmpct_regtest_e2e.sh` fails on snapshot i and passes on the
  fix; 2 of 8 regtest blocks needed the round trip and reconstructed.
- [x] Production on i at 18:41Z: at the public tip, dial memory holding 18
  addresses with 15 skips in two minutes, connect timeouts named, refusals
  backing off 10 -> 20 min.

- [x] #145 `blocktxn-is-not-a-block-2026-09-09` landed; snapshot j staged
  (production stays on i with cmpctrecv=0 until j is deployed and the
  counter moves). Both validation runs had shared fixed ports and the
  second's verdict was empty: a port guard in the script, rerun alone,
  PASS 116/116 with 6 round trips.
- [x] First hour of labels: 8 x where=4 in 0.0 s (the peer's FIN, unseen
  by a POLLIN-only poll, retired as ours three rotations later), 7 x
  where=3 in 2.4 s (the leg kept its dial's 300 ms read timeout; the
  drain's 8 ticks were designed as 3 s). Second leg batch: POLLRDHUP,
  EOF-first-read = theirs with an immediate re-dial, 3 s ticks after the
  handshake, permanent entries answer -1. Gating.

- [x] #146 `leg-lifecycle-2-2026-09-09` landed 19:3xZ; snapshot k staged
  (i running). Production's first hour on i, 18:38-19:37Z: outbound
  budget FULL (peers=25/25, was 5-8 for a day), tip = public tip
  throughout, dial memory 53 addresses / 2,894 skips; 116 connects, 93
  closes at a median 47 s (63 streak-retired non-serving listeners, 28
  theirs, 2 budget). k carries the fixes for exactly those two classes.

- [x] Operator 19:42Z: production restarted onto k (LOG START 19:42:36Z).
  "Why do we need that key at all?" -> we don't; Core has the fallback we
  lacked. "add the fallback and remove the key": the sync drain
  re-requests a badly reconstructed compact block in full once
  (bitcoind.asm .have_block, two frame flags), counted on the [cmpct]
  line; bmc.cmpctrecv and its field/default/test/script line removed.
  test_cmpct_fallback (new): a corrupted prefilled coinbase from a fake
  peer, watched to fail with where=8, passes with the fallback. Gating.

- [x] #148 `cmpct-fallback-2026-09-09` landed; snapshot l. k's first 25
  min: 10 closes (was 93/h), tip = public, budget filling 9/9, memory 28
  addresses / 35 skips -- but five "where=3 in 2.4s" remained: the dial
  path re-applied its 300 ms bound after the handshake, past
  leg_settle_socket. Removed (PR #149, gating). Production restarted onto
  l at 20:11:40Z with the conf line removed; an hour's [cmpct] counters
  and leg stats due 21:11Z.
- [x] **Run 19 reached 634,561 at 6h52m: 3 min ahead of Core's 6h55m, 35
  min ahead of run 18.**

- [x] #149 `leg-read-tick-2026-09-09` landed; snapshot m staged (l running).
  Operator: "any other things we are doing differently from core?" ->
  `docs/CORE_DIVERGENCES.md`, seven open rows by measured payoff.
  "lets hit your recommendations for a start": pings every 2 min per leg
  with a 20-min timeout and the pong matched by nonce; one request per
  block across the legs (`daemon/inflight.c`, a fetch gate in the sync
  loop's header loop). test_inflight, test_sync_gate (watched to fail),
  test_dialhelper additions. Gating as PR #150.
- [x] Production's hour on l (20:11-21:09Z), compact blocks on: 6 new
  blocks, 4 reconstructed, 12 round trips, 0 fallbacks -- the counter
  moved; tip = public throughout; peers 13/15. The 20 "where=3 in 2.4s"
  closes are the tick fix (m), not yet deployed.

- [x] #150 `pings-inflight-2026-09-09` landed 21:3xZ; snapshot n staged
  (production on l). CORE_DIVERGENCES.md rows 1-2 closed; five open.
  Waiting on run 19's 800,000 mark (Core 12h06m, run 16 13h24m).

- [x] Operator 21:26Z: production restarted onto n (LOG START 21:26:12Z).
  Its first hour: 5 new blocks, 21 in-flight claims, 0 duplicate fetches
  avoided, peers 15/15, tip = public -- and six legs an hour closed with
  where=10: a duplicate another leg had just stored, counted as a strike.
  A refused duplicate ends the pass well now; the fetch gate also skips
  hashes the store holds. test_sync_dup (new, watched to fail; its own
  binary -- the hash index is process-global). Gating.
- [x] Operator: "is it likely today's improvements will make the next
  benchmark run faster?" / "nothing more we can optimize? We are at our
  peak bandwidth" -> no: the interface is 2.5 Gbit and the box receives
  11 MB/s, the sum of 16 peers at 250-600 KB/s; the apply path already
  lags in bursts (969 blocks in an hour, 300 ms/blk: get 35%, put 39%,
  ckpt 13-24%). The bench's engine chose steady-state sizing (2^16 slots,
  64 MB) because a fresh datadir boots with gap=1; dbcache=8192 maps to
  bulk 2^25 slots / 6 GB but the trigger (gap >= 50,000) never fires on
  a fresh sync. "Why aren't we using the dbcache like core does?" -- that.

- [x] #152 `duplicate-block-2026-09-09` landed; snapshot o staged.
  Operator: "fix the dbcache sizing for fresh syncs, and then update the
  core divergences": `utxo_live_sizing.h` -- an empty set is IBD, bulk
  regardless of gap; test_utxo_sizing (new), the fresh-dir assertion in
  test_utxo_catchup_bounded. Under bulk, test_utxo_crash_recovery's
  steady-state scenario shows a +2 live-count drift after a kill
  mid-block (keys identical): the scenario keeps its small memtable via
  utxo_live_test_force_sizing, the drift is CORE_DIVERGENCES row 9.
  The inventory now has the four initial-sync rows (dbcache -- closing,
  compaction under the apply, checkpoint cadence, the worker ceiling).

- [x] #153 `fresh-sync-dbcache-2026-09-09` landed (the first gate failed
  test_dlc_interleave's lag assertion under load: pinned to the small
  memtable); snapshot p; operator restarted production onto p 23:16:38Z.
  "what is the next divergence we should consider" -> compaction under
  the apply; "lets hit compaction under the apply then": run 19 merged
  3,382 times in ten hours (3.5 h of merge time, one every 10 s at 750k);
  a merge now waits while the apply trails the archive by 256+ blocks,
  up to twice the run-count threshold and never over the memory budget,
  and the child runs at nice 10 / best-effort I/O 7. test_compact_policy
  (new). Gating as PR #154.

## Session 42 (13:04Z 09-09 →): run 19's three starts; the reviewer answered; production at the tip

- [x] Operator 11:04Z: archive the old datadirs (no `/mnt/archive2`
  mount; the empty 9.1 TB USB disk chosen with the operator), delete
  the SSD copies once verified, start a fresh run. Done 11:47Z;
  `fresh_run.sh` (repo copy under `benchmarks/2026-09-09/scripts/`)
  clones at a tag, builds, writes the conf with `bmc.bootcatchup=0`,
  launches, hands off to `monitor_fixed.sh`.
- [x] Run 19a (11:47Z): two peers claimed 970,195; the second-highest
  rule refused eight honest peers. Median (#140). Operator chose a
  restart from scratch.
- [x] Run 19b (12:56Z): "bench is stuck" at block 560 -- chunk [561,600]
  failed 200 times on the same two peers; the dead mark outranked the
  untried under bar 0 (#141). Run 19c 13:19:44Z on the fix: announced
  966,211 (median of 93), chain accepted at once, 226k blocks in the
  first ten minutes, two chunks hit a third attempt and moved on.
- [x] The reviewer's follow-ups answered (see the day summary); the
  inbound probe against production ran to completion (its earlier
  silence was my two-minute budget).
- [x] Production replay caught up 15:57:34Z at 966,181; coinstats
  seeding from a full walk; block filter backfill 964,360 -> tip on 8
  threads (the tail adopts within 144 blocks). mempool.space depth set
  to 52,560; the backend on 8999 needs a restart to read it; pools
  server and frontend to start.
- [x] Docs: release-note index rows for every 09-08/09-09 note (13
  were missing), MEMPOOL_SPACE.md depth note, OPERATIONS probe recipe,
  this worklog.
- [ ] After the backfill: relaunch the mempool.space stack, verify the
  hashrate graph and `getindexinfo`; snapshot g for production when the
  operator restarts it; run 19 to the tip and the muhash check.

## Session 41 (08:55Z 09-09 →): the UTXO defect behind run 18; the announced-height rule

- [x] Operator: "why isn't the production bmc chain monotonic and how do
  we fix it?" -> blocks landed out of height order before the 09-08
  in-order committer (first break 964,924); informational, the index is
  the truth, only in-place truncate/prune refuse and both have fallbacks;
  `tests/tool_archive_relayout` rewrites in height order, best run after
  the reindex, or leave it.
- [x] Fix 1 for the fresh benchmark: the ranking children record every
  live peer's `start_height` (shared array, parsed by
  `rpc_peer_from_version` -- the "last 4 bytes" shortcut was wrong, the
  relay byte follows); `dlc_headers` refuses a chain ending more than
  `DLC_HDR_BEHIND_MAX`=144 below the pool's second-highest claim, rolls
  back and tries the next; takes the longest when every candidate falls
  short. `daemon/dlc_rules.h` + `test_dlc_rules` (**watched to fail**, 3).
- [x] Fix 2 became a real finding. Run 18's rejected 963,311 spends a coin
  created at 962,871; `bmc_utxo_probe_one` on the bench datadir: get
  NOT FOUND, walk FOUND. Symlinked run files + a hand-written one-entry
  manifest (`UMN2`) bisected it to run 64386, the compaction's output at
  08:36:43: the key twice, PUSH then DEL, the sparse entry on the DEL.
  A Python scan: 11,362 duplicated keys in that run, none in the six
  flush-written runs. Root causes: `utxo_lsm_del` appended a tombstone on
  every call (del/put/del in one generation = stale spend, unapply,
  replacement spend); the merge's advance loop advanced a matching slot
  by ONE record, so an input's duplicate was emitted again after a newer
  PUSH. Fixed both (the del probes the tombstone hash first; the advance
  loops in the compaction and the recount re-examine the slot).
  `test_utxo_lsm_dups` (new, **watched to fail**, 8, with hand-written
  URU3 runs); `test_lsm_flush_sort_diff` expected the duplicate
  tombstones (a test that encoded the defect) -- counts distinct keys now.
- [x] Production replay (reindex-chainstate on snapshot f) at 425k/966k
  (44%) at 09:23Z, avg 228 blk/s. Its fresh UTXO set never went through a
  reorg, so it has no duplicates; the bench datadir's are the operator's
  to delete before the fresh run.
- [x] Gate `make -j8 -k test`: MAKE_EXIT 0, 343 passes, the expected
  test_rpc_signer segfault only. PR #139 landed `--no-ff` at 09:33Z as
  `fcfe834d`, tag `one-record-per-key-2026-09-09`. Staged as
  `deploy-20260909g` (`.live` relinked; production stays on f until the
  operator restarts it -- the replay is at 46%).

- [x] Operator 11:04Z: "archive old data dirs to /mnt/archive2" (no such
  mount; chose the empty 9.1 TB USB disk, `/mnt/10gbusb1/archive2`) and
  "delete the benchmarks directories on the ssd, and start a new benchmark
  run once the backup has finished". rsync 107 GB + 998 GB in 1h40m at
  ~190 MB/s; byte totals and file counts matched; SSD copies deleted
  11:47Z (1.7 TB free); run 19 started 11:47:25Z from `fresh_run.sh`
  (clone at the tag, build, conf with bmc.bootcatchup=0, monitor_fixed).
- [x] Run 19's first header phase: TWO peers claimed 970,195 against a
  real tip of 966,200 and the second-highest rule refused every honest
  chain, a minute each. The announced height is the MEDIAN now
  (`test_dlc_rules` with run 19's claim set, watched to fail). Gate
  green (343, MAKE_EXIT 0); PR #140 landed `--no-ff` 12:5xZ as
  `ee783da2`, tag `announced-median-2026-09-09`. Operator chose a
  restart from scratch on the fix: run 19a's logs kept under
  `bench-logs/run19a-20260909`, datadir wiped, run 19 relaunched on the
  new tag.
- [x] Operator 13:04Z: "bench is stuck" -> run 19 at block 560 for 11 min:
  chunk [561,600] failed 200 times on the same two peers (socket closed on
  every getdata). The 09-07 dead mark (1.0) outranked every untried peer
  under a fresh sync's bar 0. `DLC_EMA_DEAD_MARK` never clears a bar now;
  test_dialhelper case watched to fail. Operator: "You suck for not
  catching this bug." -- fair: the fresh-sync branch of the picker had no
  test with a dead mark. Run 19b's logs kept; restart on the fix.
- [x] Operator: "why isn't the production branch doing compactions often
  during the rebuild" -> bulk sizing at boot (2^22 slots, 1 GB blob,
  compact_at=48): a full 48-run merge every ~45k blocks, 75-145 s in a
  background child; lookups were 7% of the block time.

## Session 40 (07:50Z 09-09 →): "fix the recovery path first" -- do what Core does

- [x] The handoff (#137, first commit): above a staging limit the probe
  rewinds to the proven fork point and hands the replacement to the
  parallel downloader. Live on the bench: rewound the 8-block branch two
  minutes after boot -- and a keep-up leg re-adopted the same eight
  blocks thirty seconds later. Operator: "Do what core does! Retain both
  branch headers!"
- [x] The fork tree (`daemon/hdr_tree.c`, `headers_forks.dat`): every
  branch off the best chain retained with its work; the header mirror
  is the best header chain (a handoff appends the winner's headers to
  it); a leg that stores blocks off it is gated back and the blocks
  retained. test_hdr_tree; test_reorg cases for all of it. Gate green;
  tag `chain-selection-2026-09-09`; snapshot `deploy-20260909f`.
- [x] Live on the full build: the handoff fired, the downloader fetched
  ~3,000 mainnet blocks from the fork, and the engine rejected mainnet's
  963,311 ("input references a missing/already-spent UTXO"): the bench's
  set lacks a coin created at 962,871 (a block it applied on this run)
  while its coin count equals the oracle's. The set was left
  inconsistent by the earlier cycles (stale branch applied, unapplied,
  re-adopted on a binary without the gate, restarted mid-way). The
  run is over: logs in `bench-logs/run18-20260909`; the 931 GB datadir
  and run17's 101 GB are the operator's to delete before a fresh run
  (705 GB free). The inconsistency is the next investigation: it is a
  reorg-path risk for production too.
- [x] Also found on the way: `invalidateblock` answers "Block not
  found" for a block the worker holds (its header-mirror scan), and a
  control request cannot reach the worker during a downloader pass.

## Session 39 (02:00Z 09-09 →): the base rebuilt three times; 963,967 answered; the bench on a stale branch

- [x] BIP30 fix landed (#133). The first rebuild with it died right after
  pass 1 (the harness stopped the background task that launched it, "low
  on memory", with 100 GB free) and the resumed pass 2 reported 22 M
  unmatched spends: the loader consumed bucket files as it read them, so
  a pass killed midway had eaten its successor's inputs. #135: a pass
  keeps its inputs until its marker; a resume verifies them or starts
  over. Memory `harness-kills-background-tasks-on-memory`.
- [x] Third rebuild from scratch 03:50Z-06:10Z, adopted at the seam:
  "history base complete to 966142". **963,967:** the raw MuHash3072
  from our row (384 LE bytes) hashes to Core's
  `1e3c77ad...c202c118`; our RPC and Core agree in every field. The
  reviewer's request is answered, from our own computation this time.
- [x] Bench: finished its download at 04:27Z at "961640/961640" -- on a
  STALE BRANCH. Hashes diverge from the oracle at 961,632; the oracle's
  getchaintips holds a 6-header headers-only fork at 961,637, so the
  branch was real and a peer stuck on it served the header chain. Then:
  the header sync reads a fork-point reply as "already current"; the
  reorg path detected the heavier chain twice and aborted ("did not
  deliver replacement block 19 ... 60s budget" -- it wants the whole
  4,500-block replacement from one peer); `invalidateblock` answers
  "Block not found" (even after a restart) and the boot check says
  truncation refuses to run on this non-monotonic archive. Stopped
  there; the fixes are the downloader's chain selection (most work, not
  first arrival), a reorg that truncates to the proven fork point and
  hands off to the downloader, and index truncation that tolerates the
  layout. Memory `headers-first-download-can-end-on-a-stale-branch`.

## Session 38 (01:00Z 09-09 →): no caps; the audit; the history base that disagreed with Core

- [x] Operator: "If core has no cap, we should not have one either. Follow
  their behavior wherever possible for consensus." The 24,576 cap went
  the same night (#129, tag `no-seq-cap-2026-09-09`): the BIP68 ledger
  grows to the transaction; 30,000 inputs read. Memory
  `consensus-no-caps-core-has-none`.
- [x] Audit of fixed ledgers on the connect path: the block-connect
  verifier sizes its own ledger (proved by `test_txv_many_inputs`: a
  24,000-input block, the most 4 MWU holds, connects -- I had claimed
  the opposite before the test and withdrew it); the 20,000-entry table
  was the single-transaction verifier's (the mempool path) and grows now
  (`batch/2026-09-09-no-input-cap`, watched to fail on the parser
  assertion; a first gate caught a differential-parser mismatch from a
  pre-check I then removed). Guards a valid block cannot reach: witness
  items 4 M, tapleaf preimage 4 MB, undo script 10,000, prevout script
  10,000. Open: the UTXO record's two-byte script length (format v3) and
  submitblock's 16,384-tx bound.
- [x] Bench recovery: the worker stuck in the refused-chain loop ignored
  TERM twice (10 min each); KILLed, `invalid.dat` emptied on disk,
  relaunched on the fixed binary -- 880,338 applied at 01:24Z, 16
  workers at 11 MB/s again. `relaunch_bmc.sh` matched its own caller's
  command line; it scans `/proc/*/exe` for bmcbitcoind now.
- [x] The history base landed at 01:47Z (1 h 54 m; pass 3 sized itself to
  7 workers from RAM) and the seam check QUARANTINED it: counters equal
  Core at every height, digests equal at none. Bisected against Core's
  coinstatsindex with the extractor: first divergence at 91,722, our
  txouts one above Core's -- the first BIP30 original coinbase. Core's
  `IsBIP30Unspendable` names 91,722 and 91,812 (the ORIGINALS, which
  the duplicates at 91,842/91,880 overwrote in its database); ours named
  the duplicates, in the builder AND the live index's accounting. The
  live tail matched Core because it folds the real set. Fix next.

## Session 37 (00:34Z 09-09 →): "bmc bench is fucking up" -- a consensus bug at 880,338

- [x] The bench rejected mainnet block 880,338 as `bad-txns-nonBIP68-final`
  ("tx 1114 has 7244 inputs, past the sequence window"), invalidated it
  and refused every peer's chain. `val_read_tx` capped the per-tx
  sequence ledger at 2,048 and the BIP68 pass refused a block whose
  transaction exceeded it rather than treat the surplus as final (a
  deliberate choice from VAL-4 on 09-04; the refusal was meant as a
  guard). A valid block rejected: a consensus failure, and production
  carries the same code at its tip. `batch/2026-09-09-bip68-seq-cap`:
  the cap is 24,576, derived from the weight limit (41 bytes x 4 per
  input => at most 24,390 inputs in a valid block). test_val_read_tx
  builds a 7,244-input tx and reads its last sequence; **watched to
  fail** (2 of 4). Gate green (MAKE_EXIT 0, 0 failures; the
  test_rpc_signer segfault); audits exit 0.
- [x] Recovery: the bench clone rebuilt on the fix; `relaunch_bmc.sh`
  died on the datadir lock because the old download worker (10 GB RSS,
  stuck in the "refusing this chain" header loop) ignored the parent's
  TERM for the whole 10-minute wait -- TERM'd it directly, relaunched,
  `reconsiderblock 000...4bf26`. A worker that does not honour shutdown
  while looping on a refused chain is a defect for its own fix.
- [x] The reviewer's 963,967 MuHash: Core answers
  `1e3c77ad...c202c118` (the 08-25 capstone value); the raw 3072-bit
  number comes from our base row (num x den^-1 mod p, 384 LE bytes,
  sha256 = the muhash) -- the extractor reproduces our RPC and Core at
  966,124; ours at 963,967 waits for the history base (builder in
  pass 2).
- [x] The monitor's "MON: PHASE first" line repeated every tick: the
  marker check grepped a string the marker never contained. Fixed on
  the SSD script.

